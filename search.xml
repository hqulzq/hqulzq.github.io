<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Java_Bug_总结</title>
    <url>/2025/01/22/Java-Bug-%E6%80%BB%E7%BB%93/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Bug总结</tag>
      </tags>
  </entry>
  <entry>
    <title>Java技术栈的网址与文档</title>
    <url>/2025/01/22/Java%E5%90%84%E4%B8%AA%E6%8A%80%E6%9C%AF%E6%A0%88%E7%9A%84%E5%AE%98%E6%96%B9%E7%BD%91%E5%9D%80/</url>
    <content><![CDATA[<h2 id="Java"><a href="#Java" class="headerlink" title="Java"></a>Java</h2><p><a href="https://hqulzq.github.io/pdf/Java.pdf">尚硅谷Java文档，预览请转移至Book栏</a></p>
<h2 id="Shell"><a href="#Shell" class="headerlink" title="Shell"></a>Shell</h2><p><a href="https://hqulzq.github.io/pdf/Shell.pdf">尚硅谷Shell文档，预览请转移至Book栏</a></p>
<span id="more"></span>

<h2 id="Spring全家桶中文官网"><a href="#Spring全家桶中文官网" class="headerlink" title="Spring全家桶中文官网"></a>Spring全家桶中文官网</h2><p><a href="https://springdoc.cn/docs/">spring 中文文档 - spring 中文网</a></p>
<h2 id="Spring"><a href="#Spring" class="headerlink" title="Spring"></a>Spring</h2><p>(<a href="https://docs.spring.io/spring-framework/reference/index.html">Spring Framework Documentation :: Spring Framework</a>)<br><a href="https://docs.spring.io/spring-boot/api/java/index.html">Overview (Spring Boot 3.4.1 API)</a></p>
<h2 id="Spring-Boot"><a href="#Spring-Boot" class="headerlink" title="Spring Boot"></a>Spring Boot</h2><p><a href="https://docs.spring.io/spring-boot/index.html">Spring Boot :: Spring Boot</a><br><a href="https://docs.spring.io/spring-boot/api/java/index.html">Overview (Spring Boot 3.4.1 API)</a></p>
<h2 id="Spring-Cloud"><a href="#Spring-Cloud" class="headerlink" title="Spring Cloud"></a>Spring Cloud</h2><p><a href="https://docs.spring.io/spring-cloud-release/reference/index.html">Spring Cloud Train Reference Documentation :: Spring Cloud Release</a><br><a href="https://www.yuque.com/leifengyang/sutong/oz4gbyh5maa0rmxu">SpringCloud - 快速通关</a></p>
<h2 id="Spring-Cloud-Alibaba"><a href="#Spring-Cloud-Alibaba" class="headerlink" title="Spring Cloud Alibaba"></a>Spring Cloud Alibaba</h2><p><a href="https://sca.aliyun.com/docs/2023/overview/what-is-sca/?spm=5176.29160081.0.0.74805c720rxoP0">Spring Cloud Alibaba 是什么-阿里云Spring Cloud Alibaba官网</a></p>
<h2 id="Mybatis"><a href="#Mybatis" class="headerlink" title="Mybatis"></a>Mybatis</h2><p><a href="https://mybatis.org/mybatis-3/zh_CN/index.html">Mybatis</a></p>
<h2 id="Mybatis-Plus"><a href="#Mybatis-Plus" class="headerlink" title="Mybatis-Plus"></a>Mybatis-Plus</h2><p><a href="https://baomidou.com/introduce/">Mybatis-Plus</a></p>
<h2 id="Docker"><a href="#Docker" class="headerlink" title="Docker"></a>Docker</h2><p><a href="https://www.yuque.com/leifengyang/sutong/au0lv3sv3eldsmn8">Docker - 快速通关 （3h）</a></p>
<h2 id="Linux"><a href="#Linux" class="headerlink" title="Linux"></a>Linux</h2><p><a href="https://hqulzq.github.io/pdf/Linux.pdf">Linux命令总结，预览请转移至Book栏</a></p>
<h2 id="Git"><a href="#Git" class="headerlink" title="Git"></a>Git</h2><p><a href="https://learngitbranching.js.org/">git练习网站</a><br>[git常用命令]<a href="https://www.cnblogs.com/jamiechoo/articles/18408791">https://www.cnblogs.com/jamiechoo/articles/18408791</a></p>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>技术总结</tag>
      </tags>
  </entry>
  <entry>
    <title>Java设计模式</title>
    <url>/2025/01/23/Java%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/</url>
    <content><![CDATA[]]></content>
  </entry>
  <entry>
    <title>Knife4j快速入门</title>
    <url>/2025/01/22/Knife4j%E5%BF%AB%E9%80%9F%E5%85%A5%E9%97%A8/</url>
    <content><![CDATA[<h1 id="Knife4j快速入门"><a href="#Knife4j快速入门" class="headerlink" title="Knife4j快速入门"></a>Knife4j快速入门</h1><h2 id="1-概述"><a href="#1-概述" class="headerlink" title="1 概述"></a>1 概述</h2><p>Knife4j是一个用于生成和展示API文档的工具，同时它还提供了在线调试的功能，下图是其工作界面。</p>
<p><img src="/2025/01/22/Knife4j%E5%BF%AB%E9%80%9F%E5%85%A5%E9%97%A8/Knife4j.png"></p>
<p><strong>了解</strong>：</p>
<ul>
<li>Knife4j有多个版本，最新版的Knife4j基于开源项目<code>springdoc-openapi</code>，这个开源项目的核心功能就是根据SpringBoot项目中的代码自动生成符合OpenAPI规范的接口信息。</li>
<li>OpenAPI规范定义接口文档的内容和格式，其前身是<code>Swagger</code>规范。<span id="more"></span></li>
</ul>
<h2 id="2-与SpringBoot集成"><a href="#2-与SpringBoot集成" class="headerlink" title="2 与SpringBoot集成"></a>2 与SpringBoot集成</h2><p>与SpringBoot的集成相对简单，具体操作如下</p>
<ol>
<li><p><strong>创建SpringBoot项目</strong></p>
</li>
<li><p><strong>引入Maven 依赖</strong></p>
<p>Knife4j的依赖如下</p>
</li>
</ol>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.github.xiaoymin<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>knife4j-openapi3-jakarta-spring-boot-starter<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>4.3.0<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>   项目完整的pom.xml文件如下</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version=<span class="string">&quot;1.0&quot;</span> encoding=<span class="string">&quot;UTF-8&quot;</span>?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">project</span> <span class="attr">xmlns</span>=<span class="string">&quot;http://maven.apache.org/POM/4.0.0&quot;</span> <span class="attr">xmlns:xsi</span>=<span class="string">&quot;http://www.w3.org/2001/XMLSchema-instance&quot;</span></span></span><br><span class="line"><span class="tag">         <span class="attr">xsi:schemaLocation</span>=<span class="string">&quot;http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">modelVersion</span>&gt;</span>4.0.0<span class="tag">&lt;/<span class="name">modelVersion</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">parent</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-parent<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">version</span>&gt;</span>3.0.9<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">relativePath</span>/&gt;</span> <span class="comment">&lt;!-- lookup parent from repository --&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">parent</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.atguigu<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>hello-knife4j<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>0.0.1-SNAPSHOT<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">name</span>&gt;</span>hello-knife4j<span class="tag">&lt;/<span class="name">name</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">description</span>&gt;</span>hello-knife4j<span class="tag">&lt;/<span class="name">description</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">properties</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">java.version</span>&gt;</span>17<span class="tag">&lt;/<span class="name">java.version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">properties</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependencies</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-web<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.projectlombok<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>lombok<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">optional</span>&gt;</span>true<span class="tag">&lt;/<span class="name">optional</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-test<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">scope</span>&gt;</span>test<span class="tag">&lt;/<span class="name">scope</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.github.xiaoymin<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>knife4j-openapi3-jakarta-spring-boot-starter<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">version</span>&gt;</span>4.3.0<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependencies</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">build</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">plugins</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">plugin</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-maven-plugin<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">plugin</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">plugins</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">build</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">project</span>&gt;</span></span><br></pre></td></tr></table></figure>

<ol start="3">
<li><p><strong>创建配置类</strong></p>
<p>创建 <code>com.atguigu.helloknife4j.config.Knife4jConfiguration</code> ，内容如下</p>
</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Knife4jConfiguration</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="keyword">public</span> OpenAPI <span class="title function_">openAPI</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">OpenAPI</span>()</span><br><span class="line">                .info(<span class="keyword">new</span> <span class="title class_">Info</span>()</span><br><span class="line">                        .title(<span class="string">&quot;hello-knife4j项目API&quot;</span>)</span><br><span class="line">                        .version(<span class="string">&quot;1.0&quot;</span>)</span><br><span class="line">                        .description(<span class="string">&quot;hello-knife4j项目的接口文档&quot;</span>));</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="keyword">public</span> GroupedOpenApi <span class="title function_">userAPI</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> GroupedOpenApi.builder().group(<span class="string">&quot;用户信息管理&quot;</span>).</span><br><span class="line">                pathsToMatch(<span class="string">&quot;/user/**&quot;</span>).</span><br><span class="line">                build();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="keyword">public</span> GroupedOpenApi <span class="title function_">systemAPI</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> GroupedOpenApi.builder().group(<span class="string">&quot;产品信息管理&quot;</span>).</span><br><span class="line">                pathsToMatch(<span class="string">&quot;/product/**&quot;</span>).</span><br><span class="line">                build();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ol start="4">
<li><p><strong>启动项目</strong></p>
<p>启动SpringBoot项目，访问<a href="http://localhost:8080/doc.html%EF%BC%8C%E8%A7%82%E5%AF%9F%E6%8E%A5%E5%8F%A3%E6%96%87%E6%A1%A3%E3%80%82">http://localhost:8080/doc.html，观察接口文档。</a></p>
</li>
</ol>
<h2 id="3-基本使用"><a href="#3-基本使用" class="headerlink" title="3 基本使用"></a>3 基本使用</h2><p>Knife4j的使用也十分简单，我们只需使用几个简单注解，对接口进行描述，Knife4j就能自动生成API文档了。具体操作如下</p>
<ol>
<li><p><strong>描述实体类</strong></p>
<p>创建 <code>com.atguigu.helloknife4j.entity.User</code> ，内容如下</p>
</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Data</span></span><br><span class="line"><span class="meta">@Schema(description = &quot;用户信息实体&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">User</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Schema(description = &quot;编号&quot;)</span></span><br><span class="line">    <span class="keyword">private</span> Long id;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Schema(description = &quot;用户姓名&quot;)</span></span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Schema(description = &quot;用户年龄&quot;)</span></span><br><span class="line">    <span class="keyword">private</span> Integer age;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Schema(description = &quot;用户邮箱&quot;)</span></span><br><span class="line">    <span class="keyword">private</span> String email;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>   <strong>知识点</strong>：</p>
<p><code>@Schema</code> 注解用于描述作为接口参数或者返回值的实体类的数据结构。</p>
<ol start="2">
<li><p><strong>描述Controller接口</strong></p>
<p>创建 <code>com.atguigu.helloknife4j.controller.HelloController</code> ，内容如下</p>
</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="meta">@RequestMapping(&quot;/user&quot;)</span></span><br><span class="line"><span class="meta">@Tag(name = &quot;用户信息管理&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">HelloController</span> &#123;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="meta">@Operation(summary = &quot;根据id获取用户信息&quot;)</span></span><br><span class="line">    <span class="meta">@GetMapping(&quot;getById&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> User <span class="title function_">getUserById</span><span class="params">(<span class="meta">@Parameter(description = &quot;用户id&quot;)</span> <span class="meta">@RequestParam</span> Long id)</span> &#123;</span><br><span class="line">        <span class="type">User</span> <span class="variable">user</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>();</span><br><span class="line">        user.setId(id);</span><br><span class="line">        user.setName(<span class="string">&quot;zhangsan&quot;</span>);</span><br><span class="line">        user.setAge(<span class="number">11</span>);</span><br><span class="line">        user.setEmail(<span class="string">&quot;zhangsan@email.com&quot;</span>);</span><br><span class="line">        <span class="keyword">return</span> user;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>   <strong>知识点</strong>：</p>
<p><code>@Tag</code> 注解用于对接口进行分类，相同 <code>Tag</code> 的接口会放在同一个菜单。</p>
<p><code>@Operation</code> 用于对接口进行描述。</p>
<p><code>@Parameter</code> 用于对HTTP请求参数进行描述</p>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Knife4j</tag>
        <tag>快速入门</tag>
      </tags>
  </entry>
  <entry>
    <title>Linux知识点1</title>
    <url>/2025/01/22/Linux%E7%9F%A5%E8%AF%86%E7%82%B91/</url>
    <content><![CDATA[<h1 id="1-Linux文件系统目录结构"><a href="#1-Linux文件系统目录结构" class="headerlink" title="1. Linux文件系统目录结构"></a>1. Linux文件系统目录结构</h1><p>理解和熟悉Linux系统的文件系统目录结构，对于有效的管理和定位文件十分重要。Linux文件系统的目录结构如下图所示：</p>
<p><img src="/2025/01/22/Linux%E7%9F%A5%E8%AF%86%E7%82%B91/image-20250122170306448.png" alt="image-20250122170306448"></p>
<p>其中各目录的作用如下表所示</p>
<table>
<thead>
<tr>
<th>目录</th>
<th>说明</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td>&#x2F;</td>
<td>根目录</td>
<td>根目录是整个文件系统的起点，所有其他目录和文件都位于根目录之下。</td>
</tr>
<tr>
<td>&#x2F;home</td>
<td>家目录</td>
<td>每个用户的个人目录都位于这个目录下，通常以用户名命名。例如，用户<code>john</code>的主目录路径为<code>/home/john</code>。</td>
</tr>
<tr>
<td>&#x2F;root</td>
<td>root用户的家目录</td>
<td>保存root用户的个人数据</td>
</tr>
<tr>
<td>&#x2F;boot</td>
<td>引导文件所在目录</td>
<td>保存系统启动时所需的各种文件</td>
</tr>
<tr>
<td>&#x2F;usr</td>
<td>User System Resources的简写，说法不唯一</td>
<td>存放用户可用的软件和程序文件，也包括了一些共享的数据和库文件。</td>
</tr>
<tr>
<td>&#x2F;bin</td>
<td>binary的简写</td>
<td>存放系统的基本命令（如ls、cp等）和其余可执行文件。</td>
</tr>
<tr>
<td>&#x2F;sbin</td>
<td>system binary的简写</td>
<td>存放系统管理和维护方面的二进制文件。</td>
</tr>
<tr>
<td>&#x2F;etc</td>
<td>etcetera的简写</td>
<td>存放系统的配置文件，包括各种程序的配置文件和系统的全局配置文件。</td>
</tr>
<tr>
<td>&#x2F;var</td>
<td>variable的简写</td>
<td>存放系统运行时产生的可变数据，如日志文件、数据库文件等。</td>
</tr>
<tr>
<td>&#x2F;opt</td>
<td>optional的简写</td>
<td>存放一些可选的软件包，通常由用户自行安装。</td>
</tr>
<tr>
<td>&#x2F;tmp</td>
<td>temporary的简写</td>
<td>存放临时文件，系统会定期清理该目录</td>
</tr>
</tbody></table>
<span id="more"></span>

<h1 id="2-Linux交互界面"><a href="#2-Linux交互界面" class="headerlink" title="2. Linux交互界面"></a>2. Linux交互界面</h1><p>服务器中Linux系统，通常不会安装图形用户界面（GUI，Graphical User Interface），因此我们与操作系统的交互主要靠命令行界面（CLI，Command Line Interface），如下图所示</p>
<img src="image-20250122170435993.png" style="zoom:80%;"/>

<p>在命令行界面，用户通过输入一些列命令来和操作系统进行交互，例如通过<code>mkdir /test</code>来创建<code>/test</code>目录。这些命令通常由shell解释并执行。</p>
<img src="image-20250122170712358.png" />

<h1 id="3-Linux常用命令"><a href="#3-Linux常用命令" class="headerlink" title="3. Linux常用命令"></a>3. Linux常用命令</h1><h2 id="3-1-文件和目录相关"><a href="#3-1-文件和目录相关" class="headerlink" title="3.1 文件和目录相关"></a>3.1 文件和目录相关</h2><ol>
<li><p><strong>pwd（print working directory）</strong></p>
<ul>
<li><p><strong>作用</strong></p>
<p>打印当前目录的绝对路径</p>
</li>
<li><p><strong>基本语法</strong></p>
<p><code>pwd</code></p>
</li>
<li><p><strong>案例实操</strong></p>
<p>显示当前工作目录的绝对路径</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[root@localhost ~]# pwd</span><br><span class="line">/root</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p><strong>ls（list）</strong></p>
<ul>
<li><p><strong>作用</strong></p>
<p>列出目录内容</p>
</li>
<li><p><strong>基本语法</strong></p>
<p><code>ls [选项] [目录或是文件]</code></p>
</li>
<li><p><strong>选项说明</strong></p>
<table>
<thead>
<tr>
<th>选项</th>
<th>功能</th>
</tr>
</thead>
<tbody><tr>
<td>-a</td>
<td>全部的文件，连同隐藏文件( 开头为 . 的文件) 一起列出来</td>
</tr>
<tr>
<td>-l</td>
<td>使用较长（详细）的格式列出信息，包含文件的属性与权限等等数据</td>
</tr>
</tbody></table>
</li>
<li><p><strong>实操案例</strong></p>
<p>查看当前目录的所有内容信息</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[atguigu@localhost ~]$ ls -al</span><br><span class="line">总用量 44</span><br><span class="line">drwx------. 5 atguigu atguigu 4096 5月  27 15:15 .</span><br><span class="line">drwxr-xr-x. 3 root    root    4096 5月  27 14:03 ..</span><br><span class="line">drwxrwxrwx. 2 root    root    4096 5月  27 14:14 hello</span><br><span class="line">-rwxrw-r--. 1 atguigu atguigu   34 5月  27 14:20 test.txt</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>显示说明</strong></p>
<p>每行列出的信息依次是： 文件类型与权限 链接数 文件属主 文件属组 文件大小 最近修改时间 文件名</p>
</li>
</ul>
</li>
<li><p><strong>cd（change directory）</strong></p>
<ul>
<li><p><strong>作用</strong></p>
<p>切换路径</p>
</li>
<li><p><strong>基本语法</strong></p>
<p><code>cd [参数]</code></p>
</li>
<li><p><strong>参数说明</strong></p>
<table>
<thead>
<tr>
<th>参数</th>
<th>功能</th>
</tr>
</thead>
<tbody><tr>
<td>cd 绝对路径</td>
<td><strong>切换路径</strong></td>
</tr>
<tr>
<td>cd 相对路径</td>
<td><strong>切换路径</strong></td>
</tr>
<tr>
<td>cd ~或者cd</td>
<td>回到自己的家目录</td>
</tr>
<tr>
<td>cd -</td>
<td>回到上一次所在目录</td>
</tr>
<tr>
<td>cd ..</td>
<td>回到当前目录的上一级目录</td>
</tr>
</tbody></table>
</li>
</ul>
</li>
<li><p><strong>mkdir（Make Directory）</strong></p>
<ul>
<li><p><strong>作用</strong></p>
<p>创建目录</p>
</li>
<li><p><strong>基本语法</strong></p>
<p><code>mkdir [选项] 要创建的目录</code></p>
</li>
<li><p><strong>选项说明</strong></p>
<table>
<thead>
<tr>
<th>选项</th>
<th>功能</th>
</tr>
</thead>
<tbody><tr>
<td>-p</td>
<td>创建多层目录</td>
</tr>
</tbody></table>
</li>
<li><p><strong>实操案例</strong></p>
<ul>
<li>创建一个目录</li>
</ul>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">[root@localhost ~]# mkdir hello</span><br><span class="line"></span><br><span class="line">[root@localhost ~]# mkdir hello/world</span><br></pre></td></tr></table></figure>

<ul>
<li>创建一个多级目录</li>
</ul>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">[root@localhost ~]# mkdir -p big/world</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p><strong>cp（copy）</strong></p>
<ul>
<li><p><strong>作用</strong></p>
<p>复制文件或目录</p>
</li>
<li><p><strong>基本语法</strong></p>
<p><code>cp [选项] source dest</code></p>
</li>
<li><p><strong>选项说明</strong></p>
<table>
<thead>
<tr>
<th>选项</th>
<th>功能</th>
</tr>
</thead>
<tbody><tr>
<td>-r</td>
<td>递归复制整个文件夹</td>
</tr>
</tbody></table>
</li>
<li><p><strong>参数说明</strong></p>
<table>
<thead>
<tr>
<th>参数</th>
<th>功能</th>
</tr>
</thead>
<tbody><tr>
<td>source</td>
<td>源文件</td>
</tr>
<tr>
<td>dest</td>
<td>目标文件</td>
</tr>
</tbody></table>
</li>
<li><p>实操案例</p>
<ul>
<li>复制文件</li>
</ul>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">[root@localhost ~]# cp ./initial-setup-ks.cfg ./hello</span><br></pre></td></tr></table></figure>

<ul>
<li>递归复制整个文件夹</li>
</ul>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">[root@localhost ~]# cp -r ./big ./hello</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p><strong>rm（remove）</strong></p>
<ul>
<li><p><strong>作用</strong></p>
<p>移除文件或者目录</p>
</li>
<li><p><strong>基本语法</strong></p>
<p><code>rm [选项] deleteFile</code></p>
</li>
<li><p><strong>选项说明</strong></p>
<table>
<thead>
<tr>
<th>选项</th>
<th>功能</th>
</tr>
</thead>
<tbody><tr>
<td>-r</td>
<td>递归删除目录所有内容</td>
</tr>
<tr>
<td>-f</td>
<td>强制删除,不提示用户进行确认</td>
</tr>
</tbody></table>
</li>
<li><p><strong>实操案例</strong></p>
<ul>
<li>递归删除目录中所有内容</li>
</ul>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">[root@localhost ~]# rm -rf ./big</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p><strong>mv（move）</strong></p>
<ul>
<li><p><strong>作用</strong></p>
<p>移动或重命名文件</p>
</li>
<li><p><strong>基本语法</strong></p>
<ul>
<li><p>移动文件</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">mv 源文件 目标目录</span><br></pre></td></tr></table></figure>
</li>
<li><p>重命名</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">mv 源文件 目标文件</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p><strong>案例实操</strong></p>
<ul>
<li><p>移动文件</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[root@localhost ~]# cd hello/</span><br><span class="line">[root@localhost hello]# mv initial-setup-ks.cfg world/</span><br></pre></td></tr></table></figure>
</li>
<li><p>移动文件并重命名</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[root@localhost world]# mv initial-setup-ks.cfg ../hello.cfg</span><br></pre></td></tr></table></figure>
</li>
<li><p>重命名</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[root@localhost hello]# mv hello.cfg initial-setup-ks.cfg</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
</ul>
</li>
<li><p><strong>cat</strong></p>
<ul>
<li><p><strong>作用</strong></p>
<p>查看文件内容</p>
</li>
<li><p><strong>基本语法</strong></p>
<p><code>cat  [选项] 文件</code></p>
</li>
<li><p><strong>选项说明</strong></p>
<table>
<thead>
<tr>
<th>选项</th>
<th>功能描述</th>
</tr>
</thead>
<tbody><tr>
<td>- n</td>
<td>显示所有行的行号,包括空行</td>
</tr>
</tbody></table>
</li>
<li><p><strong>实操案例</strong></p>
<p>查看文件内容并显示行号</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[atguigu@localhost ~]$ cat -n hello.cfg</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p><strong>tail</strong></p>
<ul>
<li><p><strong>作用</strong></p>
<p>输出文件尾部内容</p>
</li>
<li><p><strong>基本语法</strong></p>
<ul>
<li><code>tail 文件</code>（功能描述：查看文件后10行内容）</li>
<li><code>tail -n 5 文件</code>（功能描述：查看文件后5行内容，5可以是任意行数）</li>
<li><code>tail -f 文件</code>（功能描述：实时追踪该文档的所有更新）</li>
</ul>
</li>
<li><p><strong>选项说明</strong></p>
<table>
<thead>
<tr>
<th>选项</th>
<th>功能</th>
</tr>
</thead>
<tbody><tr>
<td>-n&lt;行数&gt;</td>
<td>输出文件尾部n行内容</td>
</tr>
<tr>
<td>-f</td>
<td>显示文件最新追加的内容，监视文件变化</td>
</tr>
</tbody></table>
</li>
<li><p><strong>实操案例</strong></p>
<ul>
<li>查看文件最后50行内容</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[root@localhost ~]# tail -n 50 helle.cfg</span><br></pre></td></tr></table></figure>

<ul>
<li>实时追踪该档的所有更新</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[root@localhost ~]# tail -f hello.cfg</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
</ol>
<h2 id="3-2-文本编辑相关"><a href="#3-2-文本编辑相关" class="headerlink" title="3.2 文本编辑相关"></a>3.2 文本编辑相关</h2><p><img src="/2025/01/22/Linux%E7%9F%A5%E8%AF%86%E7%82%B91/image-20250122170812705.png" alt="image-20250122170812705"></p>
<p>在命令行界面下，最常用的文本编辑器就是<strong>Vi&#x2F;Vim</strong>。其中<strong>Vi</strong> 是 Linux操作系统中最通用的文本编辑器。 <strong>Vim</strong>编辑器是<strong>Vi</strong>的增强版，相较于<strong>Vi</strong>，提供了代码高亮等功能。两者用法完全兼容。下面介绍<strong>Vim</strong>的用法。</p>
<ul>
<li><p><strong>基础语法</strong></p>
<p><code>vim 文件</code></p>
</li>
<li><p><strong>Vim模式介绍</strong></p>
<p>Vim文编辑器有不同的工作模式，在不同模式下，可执行不同的操作，主要模式有</p>
<ul>
<li><strong>普通模式（Normal Mode）</strong>：用于删除、复制、粘贴等基本操作。</li>
<li><strong>编辑模式（Insert Mode）</strong>：用于编辑文本。</li>
<li><strong>命令行模式（Command Mode）</strong>：用于执行命令，如保存、退出等。</li>
</ul>
<p>下图为各模式之间的切换方法</p>
<p><img src="/2025/01/22/Linux%E7%9F%A5%E8%AF%86%E7%82%B91/image-20250122170959561.png" alt="image-20250122170959561"></p>
</li>
</ul>
<p>下面分别介绍各模式下的用法</p>
<ul>
<li><p><strong>编辑模式</strong></p>
<p>编辑模式下，可进行正常的编辑操作，可在编辑模式下和其他模式下灵活切换，提高编辑效率。</p>
</li>
<li><p><strong>一般模式</strong></p>
<p>在这个模式中， 可以使用上下左右按键来移动光标，也开通过一些简单的操作来复制粘贴以及删除文本，具体操作如下</p>
<ul>
<li><p>光标操作</p>
<table>
<thead>
<tr>
<th>快捷键</th>
<th>操作</th>
</tr>
</thead>
<tbody><tr>
<td><code>shift+6(^)</code></td>
<td>移动到行头</td>
</tr>
<tr>
<td><code>shift+4($)</code></td>
<td>移动到行尾</td>
</tr>
<tr>
<td><code>shift+g</code></td>
<td>移动到页尾</td>
</tr>
<tr>
<td><code>gg</code></td>
<td>移动到页头</td>
</tr>
</tbody></table>
</li>
<li><p>复制粘贴操作</p>
<table>
<thead>
<tr>
<th>快捷键</th>
<th>操作</th>
</tr>
</thead>
<tbody><tr>
<td><code>yy</code></td>
<td>复制光标当前行</td>
</tr>
<tr>
<td><code>yw</code></td>
<td>复制光标当前词</td>
</tr>
<tr>
<td><code>p</code></td>
<td>粘贴</td>
</tr>
</tbody></table>
</li>
<li><p>删除操作</p>
<table>
<thead>
<tr>
<th>快捷键</th>
<th>操作</th>
</tr>
</thead>
<tbody><tr>
<td><code>dd</code></td>
<td>删除光标当前行</td>
</tr>
<tr>
<td><code>d n d</code></td>
<td>删除当前光标所处行及其以下共n行</td>
</tr>
<tr>
<td><code>x</code></td>
<td>删除当前光标处的一个字符</td>
</tr>
</tbody></table>
</li>
</ul>
</li>
<li><p><strong>命令模式</strong></p>
<p>命令模式下可通过一些命令实现存盘、退出、显示行号、搜索、批量替换等功能，具体命令如下</p>
<table>
<thead>
<tr>
<th>命令</th>
<th>操作</th>
</tr>
</thead>
<tbody><tr>
<td><code>:w</code></td>
<td>保存</td>
</tr>
<tr>
<td><code>:q</code></td>
<td>退出</td>
</tr>
<tr>
<td><code>:!</code></td>
<td>强制执行</td>
</tr>
<tr>
<td><code>:set nu</code></td>
<td>显示行号</td>
</tr>
<tr>
<td><code>:set nonu</code></td>
<td>取消行号</td>
</tr>
<tr>
<td><code>/文本</code></td>
<td>搜索文本，输入n：光标指向下一个，输入N：贯标指向上一个</td>
</tr>
<tr>
<td><code>:nohl</code></td>
<td>搜索文本后，匹配内容会高亮显示，该命令可取消高亮</td>
</tr>
<tr>
<td><code>:s/old/new/g</code></td>
<td>在全局范围内使用new替换old内容</td>
</tr>
</tbody></table>
</li>
</ul>
<h2 id="3-3-文件权限相关"><a href="#3-3-文件权限相关" class="headerlink" title="3.3 文件权限相关"></a>3.3 文件权限相关</h2><h3 id="3-3-1-文件权限概述"><a href="#3-3-1-文件权限概述" class="headerlink" title="3.3.1 文件权限概述"></a>3.3.1 文件权限概述</h3><p>Linux中的所有文件均有严格的访问权限控制，只有当用户拥有某个文件的相关权限时，才可对文件执行相关操作。</p>
<p><strong>权限系统相关概念</strong></p>
<ul>
<li><p><strong>用户和用户组</strong></p>
<p>在Linux系统中注册的个体称为一个<strong>用户</strong>，例如root和atguigu，除此之外还能使用<code>useradd &lt;用户名&gt;</code>命令创建新的用户。每个用户都可以属于一个或多个<strong>用户组</strong>，可以使用<code>groups &lt;用户名&gt;</code>命令查看用户所属的组，使用<code>usermod -g &lt;新组名&gt; &lt;用户名&gt;</code>修改用户所属组。</p>
<p>每个文件或者目录都有一个所属用户和一个所属组。</p>
</li>
<li><p><strong>文件权限</strong></p>
<p>在Linux中，文件和目录的权限主要分为三个部分：读（read）、写（write）和执行（execute）。</p>
<ul>
<li><strong>读权限</strong>，表示可以读取文件的内容或者查看目录的内容。</li>
<li><strong>写权限</strong>，表示可以修改文件内容或者在目录中创建、删除文件。</li>
<li><strong>执行权限</strong>，表示可以运行可执行文件或者进入目录。</li>
</ul>
</li>
<li><p><strong>权限字符串</strong>：</p>
<p>在Linux系统中，文件和目录的权限用一组字符来表示，通常以<code>rwxr-xr--</code>的形式呈现，其中<code>r</code>、<code>w</code>、<code>x</code>分别表示读、写和执行权限。以下是权限字符的含义：</p>
<ul>
<li><p>第1-3位确定属主（该文件的所有者）拥有该文件的权限。</p>
</li>
<li><p>第4-6位确定属组（所有者的同组用户）拥有该文件的权限。</p>
</li>
<li><p>第7-9位确定其他用户拥有该文件的权限。</p>
</li>
</ul>
</li>
</ul>
<p><strong>思考题</strong></p>
<p>若某文件的权限字符串为<code>rwxr-xr--</code>，请分析该文件的权限策略。</p>
<h3 id="3-3-2-常用命令"><a href="#3-3-2-常用命令" class="headerlink" title="3.3.2 常用命令"></a>3.3.2 常用命令</h3><ol>
<li><p><strong>chmod（change mode）</strong></p>
<ul>
<li><p><strong>作用</strong></p>
<p>修改文件或目录权限</p>
</li>
<li><p><strong>基本语法</strong></p>
<ul>
<li><p><code>chmod [选项] [u/g/o/a][+/-/=][r/w/x] 文件或目录</code></p>
<ul>
<li>u：user</li>
<li>g：group</li>
<li>o：other</li>
<li>a：all</li>
</ul>
</li>
<li><p><code>chmod [选项] 777 文件或目录</code></p>
<table>
<thead>
<tr>
<th>权限表示法</th>
<th>user</th>
<th>group</th>
<th>other</th>
</tr>
</thead>
<tbody><tr>
<td>字符串</td>
<td>rwx</td>
<td>r-x</td>
<td>rw-</td>
</tr>
<tr>
<td>数字（二进制）</td>
<td>111</td>
<td>101</td>
<td>110</td>
</tr>
<tr>
<td>数字（八进制）</td>
<td>7</td>
<td>5</td>
<td>6</td>
</tr>
</tbody></table>
</li>
</ul>
</li>
<li><p><strong>选项说明</strong></p>
<table>
<thead>
<tr>
<th>选项</th>
<th>功能</th>
</tr>
</thead>
<tbody><tr>
<td>-R</td>
<td>递归操作</td>
</tr>
</tbody></table>
<ul>
<li><p><strong>实操案例</strong></p>
<ul>
<li>修改文件使其所属主用户具有执行权限</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[root@localhost ~]# chmod u+x hello.cfg</span><br></pre></td></tr></table></figure>

<ul>
<li>修改文件使其所属组用户具有执行权限</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[root@localhost ~]# chmod g+x hello.cfg</span><br></pre></td></tr></table></figure>

<ul>
<li>修改文件所属主用户执行权限,并使其他用户具有执行权限</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[root@localhost ~]# chmod u-x,o+x hello.cfg</span><br></pre></td></tr></table></figure>

<ul>
<li>采用数字的方式，设置文件所有者、所属组、其他用户都具有可读可写可执行权限。</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[root@localhost ~]# chmod 777 hello.txt</span><br></pre></td></tr></table></figure>

<ul>
<li>修改整个文件夹里面的所有文件的所有者、所属组、其他用户都具有可读写执行权限。</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[root@localhost ~]# chmod -R 777 hello/</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
</ul>
</li>
<li><p><strong>chown（change owner）</strong></p>
<ul>
<li><p><strong>作用</strong></p>
<p>改变文件或目录所有者</p>
</li>
<li><p><strong>基本语法</strong></p>
<p><code>chown [选项] 用户[:用户组] 文件或目录</code></p>
<ul>
<li><p><strong>选项说明</strong></p>
<table>
<thead>
<tr>
<th>选项</th>
<th>功能</th>
</tr>
</thead>
<tbody><tr>
<td>-R</td>
<td>递归操作</td>
</tr>
</tbody></table>
</li>
<li><p><strong>实操案例</strong></p>
<ul>
<li>修改文件所有者</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[root@localhost ~]# chown atguigu helle.cfg </span><br></pre></td></tr></table></figure>

<ul>
<li>递归改变文件所有者和所有组</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[root@localhost ~]# chown -R atguigu:atguigu hello/</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
</ul>
</li>
</ol>
<h2 id="3-4-进程管理相关"><a href="#3-4-进程管理相关" class="headerlink" title="3.4 进程管理相关"></a>3.4 进程管理相关</h2><p>操作系统中正在运行的一个程序称为一个进程。</p>
<ul>
<li><p><strong>查看进程</strong></p>
<p>可以使用<code>ps</code>（process status）命令查看操作系统中当前的进程。</p>
<ul>
<li><p><strong>常见用法</strong></p>
<ul>
<li><p><strong><code>ps aux</code></strong></p>
<p>选项说明如下</p>
<table>
<thead>
<tr>
<th>选项</th>
<th>功能</th>
</tr>
</thead>
<tbody><tr>
<td>a</td>
<td>显示所有前台进程</td>
</tr>
<tr>
<td>u</td>
<td>以用户友好的方式打印进程信息</td>
</tr>
<tr>
<td>x</td>
<td>显示后台进程</td>
</tr>
</tbody></table>
<p><code>ps aux</code>显示内容如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">USER        PID %CPU %MEM    VSZ   RSS TTY      STAT START   TIME COMMAND</span><br><span class="line">root          1  0.0  0.1 193964  7048 ?        Ss   08:56   0:01 /usr/lib/systemd/systemd</span><br><span class="line">root          2  0.0  0.0      0     0 ?        S    08:56   0:00 [kthreadd]</span><br><span class="line">root          4  0.0  0.0      0     0 ?        S&lt;   08:56   0:00 [kworker/0:0H]</span><br><span class="line">root          5  0.0  0.0      0     0 ?        S    08:56   0:00 [kworker/u256:0]</span><br></pre></td></tr></table></figure>

<p>注：</p>
<ul>
<li><strong>USER</strong>：启动该进程的用户</li>
<li><strong>PID</strong>：进程ID</li>
<li><strong>%CPU</strong>：该进程占用CPU资源的百分比</li>
<li><strong>%MEM</strong>：该进程占用物理内存的百分比</li>
<li><strong>COMMAND</strong>：产生此进程的命令名</li>
</ul>
</li>
<li><p><strong><code>ps -ef</code></strong></p>
<p>选项说明如下</p>
<table>
<thead>
<tr>
<th>选项</th>
<th>功能</th>
</tr>
</thead>
<tbody><tr>
<td>-e</td>
<td>显示所有进程</td>
</tr>
<tr>
<td>-f</td>
<td>以完整的格式显示进程的详细信息</td>
</tr>
</tbody></table>
<p><code>ps -ef</code>显示内容如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">UID         PID   PPID  C STIME TTY          TIME CMD</span><br><span class="line">root          1      0  0 08:56 ?        00:00:01 /usr/lib/systemd/systemd </span><br><span class="line">root          2      0  0 08:56 ?        00:00:00 [kthreadd]</span><br><span class="line">root          4      2  0 08:56 ?        00:00:00 [kworker/0:0H]</span><br><span class="line">root          5      2  0 08:56 ?        00:00:00 [kworker/u256:0]</span><br><span class="line">root          6      2  0 08:56 ?        00:00:00 [ksoftirqd/0]</span><br><span class="line">root          7      2  0 08:56 ?        00:00:00 [migration/0]</span><br></pre></td></tr></table></figure>

<p>注：</p>
<ul>
<li><strong>UID</strong>：用户ID</li>
<li><strong>PID</strong>：进程ID</li>
<li><strong>PPID</strong>：父进程ID</li>
<li><strong>CMD</strong>：启动进程所用的命令和参数</li>
</ul>
</li>
</ul>
</li>
<li><p><strong>使用技巧</strong></p>
<p>日常查看进程时，通常会将上述命令与<code>grep</code>命令配合使用，<code>grep</code>可用于过滤文本行。</p>
<p>例如想查看所有的Java进程，便可执行以下命令</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ps -ef | grep java</span><br></pre></td></tr></table></figure>

<p><code>|</code>符号称为管道符，其作用是将前边命令的输出作为下一命令的输入。</p>
</li>
</ul>
</li>
<li><p><strong>终止进程</strong></p>
<p>若想停止进程，可使用<code>kill</code>命令，其用法如下</p>
<ul>
<li><p>基本语法</p>
<p><code>kill [选项] 进程号</code></p>
</li>
<li><p>选项说明</p>
<table>
<thead>
<tr>
<th>选项</th>
<th>功能</th>
</tr>
</thead>
<tbody><tr>
<td>-9</td>
<td>表示强迫进程立即停止</td>
</tr>
</tbody></table>
</li>
<li><p>案例实操，启动火狐浏览器，并杀死相关进程。</p>
<p>提示：火狐浏览器的英文名称为firefox。</p>
</li>
</ul>
</li>
</ul>
<h2 id="3-5-压缩归档相关"><a href="#3-5-压缩归档相关" class="headerlink" title="3.5 压缩归档相关"></a>3.5 压缩归档相关</h2><ol>
<li><p><strong>zip&#x2F;unzip</strong></p>
<p><code>zip/unzip</code>可用于压缩和解压缩zip文件，使用方式如下</p>
<ul>
<li><p><strong>基本语法</strong></p>
<p><code>zip [选项] XXX.zip 文件或路径</code><br><code>unzip [选项] XXX.zip</code></p>
</li>
<li><p><strong>选项说明</strong></p>
<table>
<thead>
<tr>
<th>zip选项</th>
<th>功能</th>
</tr>
</thead>
<tbody><tr>
<td>-r</td>
<td>压缩目录</td>
</tr>
</tbody></table>
<table>
<thead>
<tr>
<th>unzip选项</th>
<th>功能</th>
</tr>
</thead>
<tbody><tr>
<td>-d 目录</td>
<td>指定解压后文件的存放目录</td>
</tr>
</tbody></table>
</li>
<li><p><strong>案例实操</strong></p>
<ul>
<li><p>压缩文件</p>
<p>创建两个文件<code>file1.txt</code>和<code>file2.txt</code>，并将其压缩为<code>first.zip</code></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[root@localhost ~]# vim file1.txt</span><br><span class="line">[root@localhost ~]# vim file2.txt</span><br><span class="line"></span><br><span class="line">[root@localhost ~]# zip first.zip file1.txt file2.txt</span><br></pre></td></tr></table></figure>
</li>
<li><p>压缩目录</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[root@lcoalhost ~]# zip -r hello.zip hello</span><br></pre></td></tr></table></figure>
</li>
<li><p>解压</p>
<p>创建一个<code>zip</code>目录，并将<code>first.zip</code>中的内容解压到<code>zip</code>目录</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[root@lcoalhost ~]# mkdir zip</span><br><span class="line">[root@lcoalhost ~]# unzip first.zip -d zip/</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
</ul>
</li>
<li><p><strong>tar</strong></p>
<p><code>tar</code>可用于对文件进行归档（打包）和压缩，使用方式如下</p>
<ul>
<li><p><strong>基本语法</strong></p>
<p>打包：<code>tar [选项] -c -f 包名 文件1 文件2 文件3 ...</code></p>
<p>解包：<code>tar [选项] -x -f 包名 -C 指定目录</code></p>
</li>
<li><p><strong>选项说明</strong></p>
<table>
<thead>
<tr>
<th>选项</th>
<th>功能</th>
<th></th>
</tr>
</thead>
<tbody><tr>
<td>-c</td>
<td>打包</td>
<td></td>
</tr>
<tr>
<td>-x</td>
<td>解包</td>
<td></td>
</tr>
<tr>
<td>-z</td>
<td>打包的同时进行压缩（解包的时候进行解压）</td>
<td></td>
</tr>
<tr>
<td>-v</td>
<td>打包或解包时显示正在处理的文件信息</td>
<td></td>
</tr>
<tr>
<td>-f 包名</td>
<td>指定打包后的文件名后者要解包的文件名</td>
<td></td>
</tr>
<tr>
<td>-C 目录</td>
<td>解包到指定目录</td>
<td></td>
</tr>
</tbody></table>
</li>
<li><p><strong>案例实操</strong></p>
<ul>
<li><p>打包<code>file1.txt</code>和<code>file2.txt</code>到<code>first.tar</code></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[root@lcoalhost ~]# tar -cvf first.tar file1.txt file2.txt</span><br></pre></td></tr></table></figure>
</li>
<li><p>打包并压缩<code>file1.txt</code>和<code>file2.txt</code>到<code>first.tar.gz</code></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[root@lcoalhost ~]# tar -zcvf first.tar.gz file1.txt file2.txt</span><br></pre></td></tr></table></figure>
</li>
<li><p>解包并解压<code>first.tar.gz</code>到<code>tar</code>目录</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[root@lcoalhost ~]# mkdir tar</span><br><span class="line">[root@lcoalhost ~]# tar -zxvf first.tar.gz -C ./tar</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
</ul>
</li>
</ol>
<h2 id="3-6-安装应用相关"><a href="#3-6-安装应用相关" class="headerlink" title="3.6 安装应用相关"></a>3.6 安装应用相关</h2><h3 id="3-6-1-RPM离线安装"><a href="#3-6-1-RPM离线安装" class="headerlink" title="3.6.1 RPM离线安装"></a>3.6.1 <strong>RPM离线安装</strong></h3><ol>
<li><p><strong>RPM概述</strong></p>
<p>RPM（RedHat Package Manager），RedHat软件包管理工具，在基于 RedHat 的 Linux 发行版中广泛使用。常见软件包如下<code>Apache-1.3.23-11.i386.rpm</code>，类似于Windows系统中的<code>exe</code>文件。</p>
</li>
<li><p><strong>rpm命令</strong></p>
<p>可以使用<code>rpm</code>命令安装、卸载和查看系统中的应用，具体用法如下</p>
<ul>
<li><p><strong>安装</strong></p>
<ul>
<li><p>基本语法</p>
<p><code>rpm -ivh 软件包路径</code></p>
</li>
<li><p>选项说明</p>
<table>
<thead>
<tr>
<th>选项</th>
<th>功能</th>
</tr>
</thead>
<tbody><tr>
<td>-i</td>
<td>-i&#x3D;install，安装</td>
</tr>
<tr>
<td>-v</td>
<td>-v&#x3D;verbose，显示详细信息</td>
</tr>
<tr>
<td>-h</td>
<td>-h&#x3D;hash，进度条</td>
</tr>
</tbody></table>
</li>
<li><p>案例实操</p>
<p>在以下地址下载一个<em><strong>sl软件</strong></em>的rpm安装包，并安装</p>
<p>下载地址：<a href="https://dl.fedoraproject.org/pub/epel/7/x86_64/Packages/s/sl-5.02-1.el7.x86_64.rpm">https://dl.fedoraproject.org/pub/epel/7/x86_64/Packages/s/sl-5.02-1.el7.x86_64.rpm</a></p>
</li>
</ul>
</li>
<li><p><strong>查看</strong></p>
<ul>
<li><p>基本语法</p>
<p><code>rpm -qa</code>（功能描述：查询所安装的所有rpm软件包）</p>
</li>
<li><p>选项说明</p>
<table>
<thead>
<tr>
<th>选项</th>
<th>功能</th>
</tr>
</thead>
<tbody><tr>
<td>-q</td>
<td>查询模式</td>
</tr>
<tr>
<td>-a</td>
<td>表示所有</td>
</tr>
</tbody></table>
</li>
<li><p>案例实操</p>
<p>查询刚刚安装的软件</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[root@localhost ~]# rpm -qa | grep sl</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p><strong>卸载</strong></p>
<ul>
<li><p>基本语法</p>
<p><code>rpm -e 软件包名</code></p>
</li>
<li><p>案例实操</p>
<p>卸载刚刚安装的sl软件</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[root@localhost ~]# rpm -e sl-5.02-1.el7.x86_64</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
</ul>
</li>
</ol>
<h3 id="3-6-2-Yum在线安装"><a href="#3-6-2-Yum在线安装" class="headerlink" title="3.6.2 Yum在线安装"></a>3.6.2 <strong>Yum在线安装</strong></h3><ol>
<li><p><strong>Yum概述</strong></p>
<p>YUM（全称为 Yellowdog Updater Modified）是一个Linux软件包管理器。基于RPM包管理，能够从指定的服务器自动下载RPM包并且安装，并且可以<strong>自动处理依赖关系</strong>，并且一次安装所有依赖的软件包，无须繁琐地一次次下载、安装，该功能类似在JAVA中使用Maven工具从远程仓库中下载依赖一样,可以自动处理依赖关系。</p>
</li>
<li><p><strong>yum命令</strong></p>
<p>可以使用yum安装和卸载应用，具体用法如下</p>
<ul>
<li><p><strong>安装软件包</strong></p>
<ul>
<li><p>基本语法</p>
<p><code>yum [选项] install 软件包名</code></p>
</li>
<li><p>选项说明</p>
<table>
<thead>
<tr>
<th>选项</th>
<th>功能</th>
</tr>
</thead>
<tbody><tr>
<td>-y</td>
<td>对所有提问都回答“yes”</td>
</tr>
</tbody></table>
</li>
<li><p>案例实操</p>
<p>使用yum在线安装<em><strong>sl</strong></em>软件</p>
<ul>
<li><p>查询sl软件的包名</p>
<p>可在如下网站搜索Linux中的各种软件包，地址为<a href="https://pkgs.org/%EF%BC%8C%E6%90%9C%E7%B4%A2sl%EF%BC%8C%E5%B9%B6%E6%9F%A5%E7%9C%8B%E5%AF%B9%E5%BA%94%E7%9A%84Linux%E5%8F%91%E8%A1%8C%E7%89%88%EF%BC%8C%E6%90%9C%E7%B4%A2%E7%BB%93%E6%9E%9C%E4%B8%BA%60yum">https://pkgs.org/，搜索sl，并查看对应的Linux发行版，搜索结果为`yum</a> install sl&#96;。</p>
</li>
<li><p>执行yum安装命令</p>
<p>执行<code>yum install sl</code>后的结果为</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">没有可用软件包 sl。</span><br><span class="line">错误：无须任何处理</span><br></pre></td></tr></table></figure>

<p>原因分析，yum的工作原理是从我们所配置的远程仓库下载rpm包，然后进行安装，远程仓库的配置文件位于<code>/etc/yum.repo.d/</code>目录中。上述问题的原因就是sl软件的rpm包不在系统预先配置的这些远程仓库中。因此需要先配置sl所在的远程仓库，才可使用yum进行安装，具体操作如下。</p>
</li>
<li><p>配置远程仓库</p>
<p>远程仓库可手动配置，也可在线配置，下面在线配置</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[root@localhost ~]# yum -y install epel-release </span><br></pre></td></tr></table></figure>

<p>完成后观察<code>/etc/yum.repo.d/</code>目录下配置文件的变化。</p>
</li>
<li><p>重新安装sl</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[root@localhost ~]# yum -y install sl</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
</ul>
</li>
<li><p><strong>卸载软件包</strong></p>
<ul>
<li><p>基本语法</p>
<p><code>yum remove 软件包名</code></p>
</li>
</ul>
</li>
<li><p><strong>案例实操</strong></p>
<p>卸载sl软件</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[root@localhost ~]# yum remove sl</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
</ol>
<h1 id="4-Linux系统远程访问"><a href="#4-Linux系统远程访问" class="headerlink" title="4. Linux系统远程访问"></a>4. Linux系统远程访问</h1><p>多数情况下，工作时所用的服务器并不在身边，这些计算机要么位于机房，要么位于云端，因此我们不能向操作个人电脑一样操作服务器，而是需要通过远程登录的方式来操作。</p>
<h2 id="4-1-SSH概述"><a href="#4-1-SSH概述" class="headerlink" title="4.1 SSH概述"></a>4.1 SSH概述</h2><p>SSH（Secure Shell）使用一种网络协议，用于在计算机之间建立加密的连接，从而允许用户以安全的方式在远程的计算机中执行命令。</p>
<p><img src="/2025/01/22/Linux%E7%9F%A5%E8%AF%86%E7%82%B91/image-20250122171024978.png" alt="image-20250122171024978"></p>
<h2 id="4-2-SSH实操"><a href="#4-2-SSH实操" class="headerlink" title="4.2 SSH实操"></a>4.2 SSH实操</h2><p>Linux系统中都会预装SSH服务，因此我们只需在自己的个人电脑安装SSH客户端，然后配置连接即可。可以选择的SSH客户端有很多，目前，比较主流的有 Xshell, SecureCRT，FinalShell，MobaXterm 等，同学们可以根据自己的习惯自行选择。本课程使用MobaXterm，下面演示具体使用方式。</p>
<ol>
<li><p>在Windows主机安装MobaXterm软件，安装包位于项目资料中。</p>
</li>
<li><p>查询虚拟机IP地址</p>
<p>使用<code>ifconfig</code>（network interfaces configuring）命令可查看虚拟机ip地址，其输出内容如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ens33: flags=4163&lt;UP,BROADCAST,RUNNING,MULTICAST&gt;  mtu 1500</span><br><span class="line">        inet 172.17.0.104  netmask 255.255.255.0  broadcast 172.17.0.255</span><br><span class="line">        inet6 fe80::5fdd:f01e:a4fb:966  prefixlen 64  scopeid 0x20&lt;link&gt;</span><br><span class="line">        ether 00:0c:29:fc:a3:fe  txqueuelen 1000  (Ethernet)</span><br><span class="line">        RX packets 183030  bytes 70503744 (67.2 MiB)</span><br><span class="line">        RX errors 0  dropped 78  overruns 0  frame 0</span><br><span class="line">        TX packets 23385  bytes 1917128 (1.8 MiB)</span><br><span class="line">        TX errors 0  dropped 0 overruns 0  carrier 0  collisions 0</span><br><span class="line"></span><br><span class="line">lo: flags=73&lt;UP,LOOPBACK,RUNNING&gt;  mtu 65536</span><br><span class="line">        inet 127.0.0.1  netmask 255.0.0.0</span><br><span class="line">        inet6 ::1  prefixlen 128  scopeid 0x10&lt;host&gt;</span><br><span class="line">        loop  txqueuelen 1000  (Local Loopback)</span><br><span class="line">        RX packets 0  bytes 0 (0.0 B)</span><br><span class="line">        RX errors 0  dropped 0  overruns 0  frame 0</span><br><span class="line">        TX packets 0  bytes 0 (0.0 B)</span><br><span class="line">        TX errors 0  dropped 0 overruns 0  carrier 0  collisions 0</span><br><span class="line"></span><br><span class="line">virbr0: flags=4099&lt;UP,BROADCAST,MULTICAST&gt;  mtu 1500</span><br><span class="line">        inet 192.168.122.1  netmask 255.255.255.0  broadcast 192.168.122.255</span><br><span class="line">        ether 52:54:00:ea:8a:fb  txqueuelen 1000  (Ethernet)</span><br><span class="line">        RX packets 0  bytes 0 (0.0 B)</span><br><span class="line">        RX errors 0  dropped 0  overruns 0  frame 0</span><br><span class="line">        TX packets 0  bytes 0 (0.0 B)</span><br><span class="line">        TX errors 0  dropped 0 overruns 0  carrier 0  collisions 0</span><br></pre></td></tr></table></figure>

<p>其中<code>ens33</code>，为虚拟机的以太网络接口，此处获取的ip地址为<code>172.17.0.104</code>。</p>
</li>
<li><p>在MobaXterm中配置SSH（Secure Shell）连接</p>
<ul>
<li><p>点击Session选项</p>
<p><img src="/2025/01/22/Linux%E7%9F%A5%E8%AF%86%E7%82%B91/image-20250122171033492.png" alt="image-20250122171033492"></p>
</li>
<li><p>点击SSH选项，并配置连接信息</p>
<p><img src="/2025/01/22/Linux%E7%9F%A5%E8%AF%86%E7%82%B91/image-20250122171039845.png" alt="image-20250122171039845"></p>
</li>
<li><p>输入root用户密码</p>
<p><img src="/2025/01/22/Linux%E7%9F%A5%E8%AF%86%E7%82%B91/image-20250122171046864.png" alt="image-20250122171046864">远程登录成功，如下图所示</p>
<p><img src="/2025/01/22/Linux%E7%9F%A5%E8%AF%86%E7%82%B91/image-20250122171052673.png" alt="image-20250122171052673"></p>
</li>
</ul>
</li>
</ol>
]]></content>
      <categories>
        <category>OS</category>
      </categories>
      <tags>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>Mybatis-Plus快速入门</title>
    <url>/2025/01/22/Mybatis-Plus%E5%BF%AB%E9%80%9F%E5%85%A5%E9%97%A8/</url>
    <content><![CDATA[<h1 id="MyBatis-Plus快速入门"><a href="#MyBatis-Plus快速入门" class="headerlink" title="MyBatis Plus快速入门"></a>MyBatis Plus快速入门</h1><h2 id="1、概述"><a href="#1、概述" class="headerlink" title="1、概述"></a>1、概述</h2><p>MyBatis-Plus（简称 MP）是一个MyBatis 的增强工具，在 MyBatis 的基础上只做增强不做改变，为简化开发、提高效率而生。其突出的特性如下：</p>
<ul>
<li><strong>无侵入</strong>：只做增强不做改变，引入它不会对现有工程产生影响，如丝般顺滑</li>
<li><strong>强大的 CRUD 操作</strong>：内置通用 Mapper、通用 Service，提供了大量的通用的CRUD方法，因此可以省去大量手写sql的语句的工作。</li>
<li><strong>条件构造器</strong>：提供了强大的条件构造器，可以构造各种复杂的查询条件，以应对各种复杂查询。</li>
<li><strong>内置分页插件</strong>：配置好插件之后，写分页等同于普通 List 查询，无需关注分页逻辑。</li>
</ul>
<p>下面通过一个简单案例快速熟悉MyBatis Plus的基本使用</p>
<span id="more"></span>

<h2 id="2、数据库准备"><a href="#2、数据库准备" class="headerlink" title="2、数据库准备"></a>2、数据库准备</h2><p>首先在数据库中准备一张表，为后序的学习做准备。</p>
<ol>
<li><p><strong>创建数据库</strong></p>
<p>在MySQL中创建一个数据库<code>hello_mp</code></p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> DATABASE hello_mp <span class="type">CHARACTER</span> <span class="keyword">SET</span> utf8mb4 <span class="keyword">COLLATE</span> utf8mb4_general_ci;</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>创建表</strong></p>
<p>在<code>hello-mp</code>库中创建一个表<code>user</code></p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">DROP</span> <span class="keyword">TABLE</span> IF <span class="keyword">EXISTS</span> <span class="keyword">user</span>;</span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> <span class="keyword">user</span></span><br><span class="line">(</span><br><span class="line">    id <span class="type">BIGINT</span>(<span class="number">20</span>) <span class="keyword">NOT</span> <span class="keyword">NULL</span> AUTO_INCREMENT COMMENT <span class="string">&#x27;主键ID&#x27;</span>,</span><br><span class="line">    name <span class="type">VARCHAR</span>(<span class="number">30</span>) <span class="keyword">NULL</span> <span class="keyword">DEFAULT</span> <span class="keyword">NULL</span> COMMENT <span class="string">&#x27;姓名&#x27;</span>,</span><br><span class="line">    age <span class="type">INT</span>(<span class="number">11</span>) <span class="keyword">NULL</span> <span class="keyword">DEFAULT</span> <span class="keyword">NULL</span> COMMENT <span class="string">&#x27;年龄&#x27;</span>,</span><br><span class="line">    email <span class="type">VARCHAR</span>(<span class="number">50</span>) <span class="keyword">NULL</span> <span class="keyword">DEFAULT</span> <span class="keyword">NULL</span> COMMENT <span class="string">&#x27;邮箱&#x27;</span>,</span><br><span class="line">    <span class="keyword">PRIMARY</span> KEY (id)</span><br><span class="line">);</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>插入数据</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">INSERT INTO user (<span class="built_in">id</span>, name, age, email) VALUES</span><br><span class="line">(1, <span class="string">&#x27;Jone&#x27;</span>, 18, <span class="string">&#x27;test1@baomidou.com&#x27;</span>),</span><br><span class="line">(2, <span class="string">&#x27;Jack&#x27;</span>, 20, <span class="string">&#x27;test2@baomidou.com&#x27;</span>),</span><br><span class="line">(3, <span class="string">&#x27;Tom&#x27;</span>, 28, <span class="string">&#x27;test3@baomidou.com&#x27;</span>),</span><br><span class="line">(4, <span class="string">&#x27;Sandy&#x27;</span>, 21, <span class="string">&#x27;test4@baomidou.com&#x27;</span>),</span><br><span class="line">(5, <span class="string">&#x27;Billie&#x27;</span>, 24, <span class="string">&#x27;test5@baomidou.com&#x27;</span>);</span><br></pre></td></tr></table></figure></li>
</ol>
<h2 id="3-与SpringBoot集成"><a href="#3-与SpringBoot集成" class="headerlink" title="3 与SpringBoot集成"></a>3 与SpringBoot集成</h2><p>Mybatis Plus与SpringBoot的集成十分简单，具体操作如下</p>
<ol>
<li><p><strong>引入Maven 依赖</strong></p>
<p>提前创建好一个SpringBoot项目，然后在项目中引入MyBatis Plus依赖</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.baomidou<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>mybatis-plus-boot-starter<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>3.5.3.2<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>本案例完整的<code>pom.xml</code>文件如下</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version=<span class="string">&quot;1.0&quot;</span> encoding=<span class="string">&quot;UTF-8&quot;</span>?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">project</span> <span class="attr">xmlns</span>=<span class="string">&quot;http://maven.apache.org/POM/4.0.0&quot;</span> <span class="attr">xmlns:xsi</span>=<span class="string">&quot;http://www.w3.org/2001/XMLSchema-instance&quot;</span></span></span><br><span class="line"><span class="tag">         <span class="attr">xsi:schemaLocation</span>=<span class="string">&quot;http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">modelVersion</span>&gt;</span>4.0.0<span class="tag">&lt;/<span class="name">modelVersion</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">parent</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-parent<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">version</span>&gt;</span>3.0.9<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">relativePath</span>/&gt;</span> <span class="comment">&lt;!-- lookup parent from repository --&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">parent</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.atguigu<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>hello-mp<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>0.0.1-SNAPSHOT<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">name</span>&gt;</span>hello-mp<span class="tag">&lt;/<span class="name">name</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">description</span>&gt;</span>hello-mp<span class="tag">&lt;/<span class="name">description</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">properties</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">java.version</span>&gt;</span>17<span class="tag">&lt;/<span class="name">java.version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">properties</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependencies</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-web<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.mysql<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>mysql-connector-j<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">scope</span>&gt;</span>runtime<span class="tag">&lt;/<span class="name">scope</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.projectlombok<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>lombok<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">optional</span>&gt;</span>true<span class="tag">&lt;/<span class="name">optional</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-test<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">scope</span>&gt;</span>test<span class="tag">&lt;/<span class="name">scope</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.baomidou<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>mybatis-plus-boot-starter<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">version</span>&gt;</span>3.5.3.2<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependencies</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">build</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">plugins</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">plugin</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-maven-plugin<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">plugin</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">plugins</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">build</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;/<span class="name">project</span>&gt;</span></span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>配置<code>application.yml</code>文件</strong></p>
<p>配置数据库相关内容如下</p>
<figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="attr">spring:</span></span><br><span class="line">  <span class="attr">datasource:</span></span><br><span class="line">    <span class="attr">driver-class-name:</span> <span class="string">com.mysql.cj.jdbc.Driver</span></span><br><span class="line">    <span class="attr">username:</span> <span class="string">root</span></span><br><span class="line">    <span class="attr">password:</span> <span class="string">Atguigu.123</span></span><br><span class="line">    <span class="attr">url:</span> <span class="string">jdbc:mysql://192.168.10.101:3306/hello_mp?useUnicode=true&amp;characterEncoding=utf-8&amp;serverTimezone=GMT%2b8</span></span><br></pre></td></tr></table></figure></li>
</ol>
<h2 id="4-创建实体类"><a href="#4-创建实体类" class="headerlink" title="4 创建实体类"></a>4 创建实体类</h2><p>创建与<code>user</code>表相对应的实体类，如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Data</span></span><br><span class="line"><span class="meta">@TableName(&quot;user&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">User</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@TableId(value = &quot;id&quot;, type = IdType.AUTO)</span></span><br><span class="line">    <span class="keyword">private</span> Long id;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@TableField(&quot;name&quot;)</span></span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@TableField(&quot;age&quot;)</span></span><br><span class="line">    <span class="keyword">private</span> Integer age;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@TableField(&quot;email&quot;)</span></span><br><span class="line">    <span class="keyword">private</span> String email;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>知识点</strong>：</p>
<p>实体类中的三个注解的含义如下</p>
<ul>
<li><p><code>@TableName</code>：表名注解，用于标识实体类所对应的表</p>
<ul>
<li><code>value</code>：用于声明表名</li>
</ul>
</li>
<li><p><code>@TableId</code>：主键注解，用于标识主键字段</p>
<ul>
<li><code>value</code>：用于声明主键的字段名</li>
<li><code>type</code>：用于声明主键的生成策略，常用的策略有<code>AUTO</code>、<code>ASSIGN_UUID</code>、<code>INPUT</code>等等</li>
</ul>
</li>
<li><p><code>@TableField</code>：普通字段注解，用于标识属性所对应的表字段</p>
<ul>
<li><code>value</code>：用于声明普通字段的字段名</li>
</ul>
</li>
</ul>
<h2 id="5-通用Mapper"><a href="#5-通用Mapper" class="headerlink" title="5 通用Mapper"></a>5 通用Mapper</h2><p>通用Mapper提供了通用的CRUD方法，使用它可以省去大量编写简单重复的SQL语句的工作，具体用法如下</p>
<ol>
<li><p><strong>创建Mapper接口</strong></p>
<p>创建<code>UserMapper</code>接口，并继承由Mybatis Plus提供的<code>BaseMapper&lt;T&gt;</code>接口，如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Mapper</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">UserMapper</span> <span class="keyword">extends</span> <span class="title class_">BaseMapper</span>&lt;User&gt; &#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>知识点</strong>：</p>
<p>若Mapper接口过多，可不用逐一配置<code>@Mapper</code>注解，而使用<code>@MapperScan</code>注解指定包扫描路径进行统一管理，例如</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@SpringBootApplication</span></span><br><span class="line"><span class="meta">@MapperScan(&quot;com.atguigu.hellomp.mapper&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">HelloMpApplication</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        SpringApplication.run(HelloMpApplication.class, args);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>测试通用Mapper</strong></p>
<p>创建<code>userMapperTest</code>测试类型，内容如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@SpringBootTest</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">UserMapperTest</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> UserMapper userMapper;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">testSelectList</span><span class="params">()</span> &#123;</span><br><span class="line">        List&lt;User&gt; users = userMapper.selectList(<span class="literal">null</span>);</span><br><span class="line">        users.forEach(System.out::println);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">testSelectById</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="type">User</span> <span class="variable">user</span> <span class="operator">=</span> userMapper.selectById(<span class="number">1</span>);</span><br><span class="line">        System.out.println(user);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">testInsert</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="type">User</span> <span class="variable">user</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>();</span><br><span class="line">        user.setName(<span class="string">&quot;zhangsan&quot;</span>);</span><br><span class="line">        user.setAge(<span class="number">11</span>);</span><br><span class="line">        user.setEmail(<span class="string">&quot;test@test.com&quot;</span>);</span><br><span class="line">        userMapper.insert(user);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">testUpdateById</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="type">User</span> <span class="variable">user</span> <span class="operator">=</span> userMapper.selectById(<span class="number">1</span>);</span><br><span class="line">        user.setName(<span class="string">&quot;xiaoming&quot;</span>);</span><br><span class="line">        userMapper.updateById(user);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">testDeleteById</span><span class="params">()</span> &#123;</span><br><span class="line">        userMapper.deleteById(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ol>
<h2 id="6-通用Service"><a href="#6-通用Service" class="headerlink" title="6 通用Service"></a>6 通用Service</h2><p>通用Service进一步封装了通用Mapper的CRUD方法，并提供了例如<code>saveOrUpdate</code>、<code>saveBatch</code>等高级方法。</p>
<ol>
<li><p><strong>创建Service接口</strong></p>
<p>创建<code>UserService</code>，内容如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">UserService</span> <span class="keyword">extends</span> <span class="title class_">IService</span>&lt;User&gt; &#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>创建Service实现类</strong></p>
<p>创建<code>UserServiceImpl</code>，内容如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Service</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UserServiceImpl</span> <span class="keyword">extends</span> <span class="title class_">ServiceImpl</span>&lt;UserMapper, User&gt; <span class="keyword">implements</span> <span class="title class_">UserService</span> &#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>测试通用Service</strong></p>
<p>创建<code>UserServiceImplTest</code>测试类，内容如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@SpringBootTest</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">UserServiceImplTest</span> &#123;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> UserService userService;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">testSaveOrUpdate</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="type">User</span> <span class="variable">user1</span> <span class="operator">=</span> userService.getById(<span class="number">2</span>);</span><br><span class="line">        user1.setName(<span class="string">&quot;xiaohu&quot;</span>);</span><br><span class="line"></span><br><span class="line">        <span class="type">User</span> <span class="variable">user2</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>();</span><br><span class="line">        user2.setName(<span class="string">&quot;lisi&quot;</span>);</span><br><span class="line">        user2.setAge(<span class="number">27</span>);</span><br><span class="line">        user2.setEmail(<span class="string">&quot;lisi@email.com&quot;</span>);</span><br><span class="line">        userService.saveOrUpdate(user1);</span><br><span class="line">        userService.saveOrUpdate(user2);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">testSaveBatch</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="type">User</span> <span class="variable">user1</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>();</span><br><span class="line">        user1.setName(<span class="string">&quot;dongdong&quot;</span>);</span><br><span class="line">        user1.setAge(<span class="number">49</span>);</span><br><span class="line">        user1.setEmail(<span class="string">&quot;dongdong@email.com&quot;</span>);</span><br><span class="line"></span><br><span class="line">        <span class="type">User</span> <span class="variable">user2</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>();</span><br><span class="line">        user2.setName(<span class="string">&quot;nannan&quot;</span>);</span><br><span class="line">        user2.setAge(<span class="number">29</span>);</span><br><span class="line">        user2.setEmail(<span class="string">&quot;nannan@email.com&quot;</span>);</span><br><span class="line"></span><br><span class="line">        List&lt;User&gt; users = List.of(user1, user2);</span><br><span class="line">        userService.saveBatch(users);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ol>
<h2 id="7-条件构造器"><a href="#7-条件构造器" class="headerlink" title="7 条件构造器"></a>7 条件构造器</h2><p>条件构造器用于构造复杂的查询条件，例如获取<code>name=&#39;zhangsan&#39;</code>的用户。MyBatis Plus共提供了两类构造器，分别是<code>QueryWrapper</code>和<code>UpdateWrapper</code>。其中<code>QueryWrapper</code>主要用于查询、删除操作，<code>UpdateWrapper</code>主要用于更新操作，下面通过几个案例学习条件构造器的基础用法。</p>
<ol>
<li><p>创建<code>WrapperTest</code>测试类，内容如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@SpringBootTest</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">WrapperTest</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> UserService userService;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">testQueryWrapper</span><span class="params">()</span> &#123;</span><br><span class="line"></span><br><span class="line">        <span class="comment">//查询name=Tom的所有用户</span></span><br><span class="line">        QueryWrapper&lt;User&gt; queryWrapper1 = <span class="keyword">new</span> <span class="title class_">QueryWrapper</span>&lt;&gt;();</span><br><span class="line">        queryWrapper1.eq(<span class="string">&quot;name&quot;</span>, <span class="string">&quot;Tom&quot;</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">//查询邮箱域名为baomidou.com的所有用户</span></span><br><span class="line">        QueryWrapper&lt;User&gt; queryWrapper2 = <span class="keyword">new</span> <span class="title class_">QueryWrapper</span>&lt;&gt;();</span><br><span class="line">        queryWrapper2.like(<span class="string">&quot;email&quot;</span>, <span class="string">&quot;baomidou.com&quot;</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">//查询所有用户信息并按照age字段降序排序</span></span><br><span class="line">        QueryWrapper&lt;User&gt; queryWrapper3 = <span class="keyword">new</span> <span class="title class_">QueryWrapper</span>&lt;&gt;();</span><br><span class="line">        queryWrapper3.orderByDesc(<span class="string">&quot;age&quot;</span>);</span><br><span class="line">        </span><br><span class="line">        <span class="comment">//查询age介于[20,30]的所有用户</span></span><br><span class="line">        QueryWrapper&lt;User&gt; queryWrapper4 = <span class="keyword">new</span> <span class="title class_">QueryWrapper</span>&lt;&gt;();</span><br><span class="line">        queryWrapper4.between(<span class="string">&quot;age&quot;</span>, <span class="number">20</span>, <span class="number">30</span>);</span><br><span class="line">        </span><br><span class="line">        <span class="comment">//查询age小于20或大于30的用户</span></span><br><span class="line">        QueryWrapper&lt;User&gt; queryWrapper5 = <span class="keyword">new</span> <span class="title class_">QueryWrapper</span>&lt;&gt;();</span><br><span class="line">        queryWrapper5.lt(<span class="string">&quot;age&quot;</span>, <span class="number">20</span>).or().gt(<span class="string">&quot;age&quot;</span>, <span class="number">30</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">//邮箱域名为baomidou.com且年龄小于30或大于40且的用户</span></span><br><span class="line">        QueryWrapper&lt;User&gt; queryWrapper6 = <span class="keyword">new</span> <span class="title class_">QueryWrapper</span>&lt;&gt;();</span><br><span class="line">        queryWrapper6.like(<span class="string">&quot;email&quot;</span>, <span class="string">&quot;baomidou.com&quot;</span>).and(wrapper -&gt; wrapper.lt(<span class="string">&quot;age&quot;</span>, <span class="number">30</span>).or().gt(<span class="string">&quot;age&quot;</span>, <span class="number">40</span>));</span><br><span class="line">        </span><br><span class="line">        List&lt;User&gt; list = userService.list(queryWrapper6);</span><br><span class="line">        list.forEach(System.out::println);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">testUpdateWrapper</span><span class="params">()</span> &#123;</span><br><span class="line"></span><br><span class="line">        <span class="comment">//将name=Tom的用户的email改为Tom@baobidou.com</span></span><br><span class="line">        UpdateWrapper&lt;User&gt; updateWrapper = <span class="keyword">new</span> <span class="title class_">UpdateWrapper</span>&lt;&gt;();</span><br><span class="line">        updateWrapper.eq(<span class="string">&quot;name&quot;</span>, <span class="string">&quot;Tom&quot;</span>);</span><br><span class="line">        updateWrapper.set(<span class="string">&quot;email&quot;</span>, <span class="string">&quot;Tom@baobidou.com&quot;</span>);</span><br><span class="line"></span><br><span class="line">        userService.update(updateWrapper);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>创建<code>LambdaWrapperTest</code>测试类，内容如下</p>
<p>上述的<code>QueryWrapper</code>和<code>UpdateWrapper</code>均有一个<code>Lambda</code>版本，也就是<code>LambdaQueryWrapper</code>和<code>LambdaUpdateWrapper</code>，<code>Lambda</code>版本的优势在于，可以省去字段名的硬编码，具体案例如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@SpringBootTest</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">LambdaWrapperTest</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> UserService userService;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">testLambdaQueryWrapper</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="comment">//查询name=Tom的所有用户</span></span><br><span class="line">        LambdaQueryWrapper&lt;User&gt; lambdaQueryWrapper = <span class="keyword">new</span> <span class="title class_">LambdaQueryWrapper</span>&lt;&gt;();</span><br><span class="line">        lambdaQueryWrapper.eq(User::getName, <span class="string">&quot;Tom&quot;</span>);</span><br><span class="line"></span><br><span class="line">        List&lt;User&gt; list = userService.list(lambdaQueryWrapper);</span><br><span class="line">        list.forEach(System.out::println);</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">testLambdaUpdateWrapper</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="comment">//将name=Tom的用户的邮箱改为Tom@tom.com</span></span><br><span class="line">        LambdaUpdateWrapper&lt;User&gt; lambdaUpdateWrapper = <span class="keyword">new</span> <span class="title class_">LambdaUpdateWrapper</span>&lt;&gt;();</span><br><span class="line">        lambdaUpdateWrapper.eq(User::getName, <span class="string">&quot;Tom&quot;</span>);</span><br><span class="line">        lambdaUpdateWrapper.set(User::getEmail, <span class="string">&quot;Tom@Tom.com&quot;</span>);</span><br><span class="line"></span><br><span class="line">        userService.update(lambdaUpdateWrapper);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ol>
<h2 id="8-分页插件"><a href="#8-分页插件" class="headerlink" title="8 分页插件"></a>8 分页插件</h2><p>分页查询是一个很常见的需求，故Mybatis-Plus提供了一个分页插件，使用它可以十分方便的完成分页查询。下面介绍Mybatis-Plus分页插件的用法，详细信息可参考<a href="https://baomidou.com/pages/97710a/">官方文档</a>。</p>
<ul>
<li><p>配置分页插件</p>
<p>创建<code>com.atguigu.hellomp.config.MPConfiguration</code>配置类，增加如下内容</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MPConfiguration</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="keyword">public</span> MybatisPlusInterceptor <span class="title function_">mybatisPlusInterceptor</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="type">MybatisPlusInterceptor</span> <span class="variable">interceptor</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">MybatisPlusInterceptor</span>();</span><br><span class="line">        interceptor.addInnerInterceptor(<span class="keyword">new</span> <span class="title class_">PaginationInnerInterceptor</span>(DbType.MYSQL));</span><br><span class="line">        <span class="keyword">return</span> interceptor;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>分页插件使用说明</p>
<ul>
<li><p>构造分页对象</p>
<p>分页对象包含了分页的各项信息，其核心属性如下：</p>
<table>
<thead>
<tr>
<th>属性名</th>
<th>类型</th>
<th>默认值</th>
<th>描述</th>
</tr>
</thead>
<tbody><tr>
<td>records</td>
<td>List</td>
<td>emptyList</td>
<td>查询数据列表</td>
</tr>
<tr>
<td>total</td>
<td>Long</td>
<td>0</td>
<td>查询列表总记录数</td>
</tr>
<tr>
<td>size</td>
<td>Long</td>
<td>10</td>
<td>每页显示条数，默认<code>10</code></td>
</tr>
<tr>
<td>current</td>
<td>Long</td>
<td>1</td>
<td>当前页</td>
</tr>
</tbody></table>
<p>分页对象既作为分页查询的参数，也作为分页查询的返回结果，当作为查询参数时，通常只需提供<code>current</code>和<code>size</code>属性，如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">IPage&lt;T&gt; page = <span class="keyword">new</span> <span class="title class_">Page</span>&lt;&gt;(current, size);</span><br></pre></td></tr></table></figure>

<p>注：<code>IPage</code>为分页接口，<code>Page</code>为<code>IPage</code>接口的一个实现类。<code>Page</code> 类是具体的实现类，主要用于创建分页对象、传递分页参数以及接收分页结果。<code>IPage</code> 接口是抽象的接口，用于定义分页的通用行为和属性，提升代码的灵活性与可扩展性。在实际开发中，通常先用 <code>Page</code> 类创建分页对象，再用 <code>IPage</code> 接口来接收和处理分页结果。</p>
</li>
<li><p>分页查询</p>
<p>Mybatis Plus的<code>BaseMapper</code>和<code>ServiceImpl</code>均提供了常用的分页查询的方法，例如：</p>
<ul>
<li><p><code>BaseMapper</code>的分页查询：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">IPage&lt;T&gt; <span class="title function_">selectPage</span><span class="params">(IPage&lt;T&gt; page,Wrapper&lt;T&gt; queryWrapper)</span>;</span><br></pre></td></tr></table></figure>
</li>
<li><p><code>ServiceImpl</code>的分页查询：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 无条件分页查询</span></span><br><span class="line">IPage&lt;T&gt; <span class="title function_">page</span><span class="params">(IPage&lt;T&gt; page)</span>;</span><br><span class="line"><span class="comment">// 条件分页查询</span></span><br><span class="line">IPage&lt;T&gt; <span class="title function_">page</span><span class="params">(IPage&lt;T&gt; page, Wrapper&lt;T&gt; queryWrapper)</span>;</span><br></pre></td></tr></table></figure>
</li>
<li><p>自定义Mapper</p>
<p>对于自定义SQL，也可以十分方便的完成分页查询，如下</p>
<p><code>Mapper</code>接口：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">IPage&lt;UserVo&gt; <span class="title function_">selectPageVo</span><span class="params">(IPage&lt;?&gt; page, Integer state)</span>;</span><br></pre></td></tr></table></figure>

<p><code>Mapper.xml</code>：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">&lt;select id=<span class="string">&quot;selectPageVo&quot;</span> resultType=<span class="string">&quot;xxx.xxx.xxx.UserVo&quot;</span>&gt;</span><br><span class="line">    SELECT id,name FROM user WHERE state=#&#123;state&#125;</span><br><span class="line">&lt;/select&gt;</span><br></pre></td></tr></table></figure>

<p><strong>注意</strong>：<code>Mapper.xml</code>中的SQL只需实现查询<code>list</code>的逻辑即可，无需关注分页的逻辑。</p>
</li>
</ul>
</li>
</ul>
</li>
<li><p>案例实操</p>
<p>分页查询案例如下：</p>
<p>创建<code>PageTest</code>测试类，内容如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@SpringBootTest</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">PageTest</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> UserService userService;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> UserMapper userMapper;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//通用Service分页查询</span></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">testPageService</span><span class="params">()</span> &#123;</span><br><span class="line">        Page&lt;User&gt; page = <span class="keyword">new</span> <span class="title class_">Page</span>&lt;&gt;(<span class="number">2</span>, <span class="number">3</span>);</span><br><span class="line">        Page&lt;User&gt; userPage = userService.page(page);</span><br><span class="line">        userPage.getRecords().forEach(System.out::println);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//通用Mapper分页查询</span></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">testPageMapper</span><span class="params">()</span> &#123;</span><br><span class="line">        IPage&lt;User&gt; page = <span class="keyword">new</span> <span class="title class_">Page</span>&lt;&gt;(<span class="number">2</span>, <span class="number">3</span>);</span><br><span class="line">        IPage&lt;User&gt; userPage = userMapper.selectPage(page, <span class="literal">null</span>);</span><br><span class="line">        userPage.getRecords().forEach(System.out::println);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//自定义SQL分页查询</span></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">testCustomMapper</span><span class="params">()</span> &#123;</span><br><span class="line">        IPage&lt;User&gt; page = <span class="keyword">new</span> <span class="title class_">Page</span>&lt;&gt;(<span class="number">2</span>, <span class="number">3</span>);</span><br><span class="line">        IPage&lt;User&gt; userPage = userMapper.selectUserPage(page);</span><br><span class="line">        userPage.getRecords().forEach(System.out::println);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在UserMapper中声明分页查询方法如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">IPage&lt;User&gt; <span class="title function_">selectUserPage</span><span class="params">(IPage&lt;User&gt; page)</span>;</span><br></pre></td></tr></table></figure>

<p>创建<code>resources/mapper/UserMapper.xml</code>文件，内容如下</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version=<span class="string">&quot;1.0&quot;</span> encoding=<span class="string">&quot;UTF-8&quot;</span>?&gt;</span></span><br><span class="line"><span class="meta">&lt;!DOCTYPE <span class="keyword">mapper</span></span></span><br><span class="line"><span class="meta">        <span class="keyword">PUBLIC</span> <span class="string">&quot;-//mybatis.org//DTD Mapper 3.0//EN&quot;</span></span></span><br><span class="line"><span class="meta">        <span class="string">&quot;http://mybatis.org/dtd/mybatis-3-mapper.dtd&quot;</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">mapper</span> <span class="attr">namespace</span>=<span class="string">&quot;com.atguigu.hellomp.mapper.UserMapper&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">&quot;selectUserPage&quot;</span> <span class="attr">resultType</span>=<span class="string">&quot;com.atguigu.hellomp.entity.User&quot;</span>&gt;</span></span><br><span class="line">        select *</span><br><span class="line">        from user</span><br><span class="line">    <span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">mapper</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p><strong>注意</strong>：</p>
<p>Mybatis-Plus中<code>Mapper.xml</code>文件路径默认为：<code>classpath*:/mapper/**/*.xml</code>，可在<code>application.yml</code>中配置以下参数进行修改</p>
   <figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="attr">mybatis-plus:</span></span><br><span class="line">  <span class="attr">mapper-locations:</span> <span class="string">classpath*:/mapper/**/*.xml</span></span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="9-MyBatisX插件"><a href="#9-MyBatisX插件" class="headerlink" title="9 MyBatisX插件"></a>9 MyBatisX插件</h2><p>MyBatis Plus提供了一个IDEA插件——<code>MybatisX</code>,使用它可根据数据库快速生成<code>Entity</code>、<code>Mapper</code>、<code>Mapper.xml</code>、<code>Service</code>、<code>ServiceImpl</code>等代码，使用户更专注于业务。</p>
<p>下面演示具体用法</p>
<ol>
<li><p><strong>安装插件</strong></p>
<p>在IDEA插件市场搜索<code>MyBatisX</code>，进行在线安装</p>
<p><img src="/2025/01/22/Mybatis-Plus%E5%BF%AB%E9%80%9F%E5%85%A5%E9%97%A8/image-20250122173825745.png" alt="image-20250122173825745"></p>
</li>
<li><p><strong>配置数据库连接</strong></p>
<p>在IDEA中配置数据库连接</p>
<p><img src="/2025/01/22/Mybatis-Plus%E5%BF%AB%E9%80%9F%E5%85%A5%E9%97%A8/image-20250122173835578.png" alt="image-20250122173835578"></p>
</li>
<li><p><strong>生成代码</strong></p>
<p>首先将之前编写的<code>User</code>、<code>UserMapper</code>、<code>UserServcie</code>、<code>UserServiceImpl</code>全部删除，然后按照下图指示使用插件生成代码</p>
<p><img src="/2025/01/22/Mybatis-Plus%E5%BF%AB%E9%80%9F%E5%85%A5%E9%97%A8/image-20250122173844446.png" alt="image-20250122173844446"></p>
<p>配置实体类相关信息</p>
<p><img src="/2025/01/22/Mybatis-Plus%E5%BF%AB%E9%80%9F%E5%85%A5%E9%97%A8/image-20250122173850326.png" alt="image-20250122173850326"></p>
<p>配置代码模版信息</p>
<p><img src="/2025/01/22/Mybatis-Plus%E5%BF%AB%E9%80%9F%E5%85%A5%E9%97%A8/image-20250122173901559.png" alt="image-20250122173901559"></p>
<p>点击Finish然后查看生成的代码。</p>
</li>
</ol>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>快速入门</tag>
        <tag>Mybatis-Plus</tag>
        <tag>Mybatis</tag>
      </tags>
  </entry>
  <entry>
    <title>Minio快速入门</title>
    <url>/2025/01/22/Minio%E5%BF%AB%E9%80%9F%E5%85%A5%E9%97%A8/</url>
    <content><![CDATA[<h1 id="MinIO快速入门"><a href="#MinIO快速入门" class="headerlink" title="MinIO快速入门"></a>MinIO快速入门</h1><h2 id="1-MinIO核心概念"><a href="#1-MinIO核心概念" class="headerlink" title="1 MinIO核心概念"></a>1 MinIO核心概念</h2><p>下面介绍MinIO中的几个核心概念，这些概念在所有的对象存储服务中也都是通用的。</p>
<ul>
<li><p><strong>对象（Object）</strong></p>
<p>对象是实际的数据单元，例如我们上传的一个图片。</p>
</li>
<li><p><strong>存储桶（Bucket）</strong></p>
<p>存储桶是用于组织对象的命名空间，类似于文件夹。每个存储桶可以包含多个对象。</p>
</li>
<li><p><strong>端点（Endpoint）</strong></p>
<p>端点是MinIO服务器的网络地址，用于访问存储桶和对象，例如 <code>http://192.168.10.101:9000</code></p>
<p><strong>注意：</strong></p>
</li>
</ul>
<p><code>9000</code> 为MinIO的API的默认端口，前边配置的 <code>9001</code> 以为管理页面端口。</p>
<ul>
<li><p><strong>Access Key 和 Secret Key</strong></p>
<p><strong>Access Key</strong>是用于标识和验证访问者身份的唯一标识符，相当于用户名。</p>
<p><strong>Secret Key</strong>是与<strong>Access Key</strong>关联的密码，用于验证访问者的身份。</p>
</li>
</ul>
<span id="more"></span>

<h2 id="2-MinIO管理页面操作"><a href="#2-MinIO管理页面操作" class="headerlink" title="2 MinIO管理页面操作"></a>2 MinIO管理页面操作</h2><ol>
<li><p><strong>登录</strong></p>
<p>管理页面的地址为<a href="http://192.168.10.101:9001，登录的用户名和密码为部署时在">http://192.168.10.101:9001，登录的用户名和密码为部署时在</a> <code>EnvironmentFile</code> 文件中配置的如下参数</p>
</li>
</ol>
<figure class="highlight ini"><table><tr><td class="code"><pre><span class="line"><span class="attr">MINIO_ROOT_USER</span>=minioadmin</span><br><span class="line"><span class="attr">MINIO_ROOT_PASSWORD</span>=minioadmin</span><br></pre></td></tr></table></figure>

<ol start="2">
<li><p><strong>创建存储桶</strong></p>
<p><img src="/2025/01/22/Minio%E5%BF%AB%E9%80%9F%E5%85%A5%E9%97%A8/MinIO%E5%85%A5%E9%97%A8-%E5%88%9B%E5%BB%BA%E6%A1%B6.png"></p>
</li>
<li><p><strong>上传图片</strong></p>
<ul>
<li><p>找到目标桶</p>
<p><img src="/2025/01/22/Minio%E5%BF%AB%E9%80%9F%E5%85%A5%E9%97%A8/MinIO%E5%85%A5%E9%97%A8-%E9%80%89%E6%8B%A9%E6%A1%B6.png"></p>
</li>
<li><p>上传图片</p>
<p><img src="/2025/01/22/Minio%E5%BF%AB%E9%80%9F%E5%85%A5%E9%97%A8/MinIO%E5%85%A5%E9%97%A8-%E4%B8%8A%E4%BC%A0%E5%9B%BE%E7%89%87.png"></p>
</li>
</ul>
</li>
<li><p><strong>访问图片</strong></p>
<ul>
<li><p><strong>图片URL</strong></p>
<p>由于MinIO提供了HTTP访问功能，所以可以通过浏览器直接访问对象。对象URL为MinIO的 <code>Endpoint</code> + <code>对象的存储路径</code> ，例如下图中的图片对象的URL为<a href="http://192.168.10.101:9000/test/%E5%85%AC%E5%AF%93-%E5%A4%96%E8%A7%82.jpg">http:192.168.10.101:9000&#x2F;test&#x2F;公寓-外观.jpg</a>。</p>
<p><img src="/2025/01/22/Minio%E5%BF%AB%E9%80%9F%E5%85%A5%E9%97%A8/MinIO%E5%85%A5%E9%97%A8-%E5%AD%98%E5%82%A8%E8%B7%AF%E5%BE%84.png"></p>
</li>
<li><p><strong>访问权限</strong></p>
<p>不出意外的话，使用浏览器访问上述URL，会得到如下响应，很显然是没有访问权限。</p>
</li>
</ul>
</li>
</ol>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">Error</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">Code</span>&gt;</span>AccessDenied<span class="tag">&lt;/<span class="name">Code</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">Message</span>&gt;</span>Access Denied.<span class="tag">&lt;/<span class="name">Message</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">Key</span>&gt;</span>公寓-外观.jpg<span class="tag">&lt;/<span class="name">Key</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">BucketName</span>&gt;</span>test<span class="tag">&lt;/<span class="name">BucketName</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">Resource</span>&gt;</span>/test/公寓-外观.jpg<span class="tag">&lt;/<span class="name">Resource</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">RequestId</span>&gt;</span>177BC92022FC5684<span class="tag">&lt;/<span class="name">RequestId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">HostId</span>&gt;</span>dd9025bab4ad464b049177c95eb6ebf374d3b3fd1af9251148b658df7ac2e3e8<span class="tag">&lt;/<span class="name">HostId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">Error</span>&gt;</span></span><br></pre></td></tr></table></figure>

<pre><code> 若想继续访问图片，需要修改图片**所在桶**的访问权限，如下图所示

 ![](MinIO入门-访问权限.png)

 如上图所示，可选的访问权限共有三个选项，分别是 `Private` 、 `Public` 和 `Custom` ，具体说明如下

 - `Private`

   只允许桶的所有者对该桶进行读写。

 - `Public`

   允许所有人对该桶进行读写。

 - `Custom`

   自定义访问权限。

 若想将权限设置为只允许所有者写，但允许所有人读，就需要自定义访问权限。自定义访问权限，需要使用一个规定格式的JSON字符串进行描述，具体格式可参考[官方文档](https://min.io/docs/minio/linux/administration/identity-access-management/policy-based-access-control.html#policy-document-structure)。

 例如以下JSON字符串表达的含义是：允许( `Allow` )所有人( `*` )读取( `s3:GetObject` )指定桶( `test` )的所有内容。

 
</code></pre>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;Statement&quot;</span> <span class="punctuation">:</span> <span class="punctuation">[</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;Action&quot;</span> <span class="punctuation">:</span> <span class="string">&quot;s3:GetObject&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;Effect&quot;</span> <span class="punctuation">:</span> <span class="string">&quot;Allow&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;Principal&quot;</span> <span class="punctuation">:</span> <span class="string">&quot;*&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;Resource&quot;</span> <span class="punctuation">:</span> <span class="string">&quot;arn:aws:s3:::test/*&quot;</span></span><br><span class="line">  <span class="punctuation">&#125;</span> <span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;Version&quot;</span> <span class="punctuation">:</span> <span class="string">&quot;2012-10-17&quot;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<pre><code> 将 `test` 桶访问权限设置为 `Custom` ，并添加上述内容

 ![](MinIO入门-自定义权限.png)

 重新访问[http:192.168.10.101:9000/test/公寓-外观.jpg](http:192.168.10.101:9000/test/公寓-外观.jpg)，观察是否正常。
</code></pre>
<h2 id="3-MinIO-Java-SDK"><a href="#3-MinIO-Java-SDK" class="headerlink" title="3 MinIO Java SDK"></a>3 MinIO Java SDK</h2><p>MinIO提供了多种语言的SDK供开发者使用，本项目需要用到Java SDK，下面通过一个简单案例熟悉一下其基本用法，具体内容可参考<a href="https://www.minio.org.cn/docs/minio/linux/developers/java/minio-java.html#">官方文档</a>。</p>
<ol>
<li><p><strong>创建一个Maven项目</strong></p>
</li>
<li><p><strong>引入如下依赖</strong></p>
</li>
</ol>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>io.minio<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>minio<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>8.5.3<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<ol start="3">
<li><strong>编写如下内容</strong></li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">App</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> IOException, NoSuchAlgorithmException, InvalidKeyException &#123;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">//构造MinIO Client</span></span><br><span class="line">            <span class="type">MinioClient</span> <span class="variable">minioClient</span> <span class="operator">=</span> MinioClient.builder()</span><br><span class="line">                    .endpoint(<span class="string">&quot;http://192.168.10.101:9000&quot;</span>)</span><br><span class="line">                    .credentials(<span class="string">&quot;minioadmin&quot;</span>, <span class="string">&quot;minioadmin&quot;</span>)</span><br><span class="line">                    .build();</span><br><span class="line"></span><br><span class="line">            <span class="comment">//创建hello-minio桶</span></span><br><span class="line">            <span class="type">boolean</span> <span class="variable">found</span> <span class="operator">=</span> minioClient.bucketExists(BucketExistsArgs.builder().bucket(<span class="string">&quot;hello-minio&quot;</span>).build());</span><br><span class="line">            <span class="keyword">if</span> (!found) &#123;</span><br><span class="line">                <span class="comment">//创建hello-minio桶</span></span><br><span class="line">                minioClient.makeBucket(MakeBucketArgs.builder().bucket(<span class="string">&quot;hello-minio&quot;</span>).build());</span><br><span class="line">                <span class="comment">//设置hello-minio桶的访问权限</span></span><br><span class="line">                <span class="type">String</span> <span class="variable">policy</span> <span class="operator">=</span> <span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">                        &#123;</span></span><br><span class="line"><span class="string">                          &quot;Statement&quot; : [ &#123;</span></span><br><span class="line"><span class="string">                            &quot;Action&quot; : &quot;s3:GetObject&quot;,</span></span><br><span class="line"><span class="string">                            &quot;Effect&quot; : &quot;Allow&quot;,</span></span><br><span class="line"><span class="string">                            &quot;Principal&quot; : &quot;*&quot;,</span></span><br><span class="line"><span class="string">                            &quot;Resource&quot; : &quot;arn:aws:s3:::hello-minio/*&quot;</span></span><br><span class="line"><span class="string">                          &#125; ],</span></span><br><span class="line"><span class="string">                          &quot;Version&quot; : &quot;2012-10-17&quot;</span></span><br><span class="line"><span class="string">                        &#125;&quot;&quot;&quot;</span>;</span><br><span class="line">                minioClient.setBucketPolicy(SetBucketPolicyArgs.builder().bucket(<span class="string">&quot;hello-minio&quot;</span>).config(policy).build());</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                System.out.println(<span class="string">&quot;Bucket &#x27;hello-minio&#x27; already exists.&quot;</span>);</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="comment">//上传图片</span></span><br><span class="line">            minioClient.uploadObject(</span><br><span class="line">                    UploadObjectArgs.builder()</span><br><span class="line">                            .bucket(<span class="string">&quot;hello-minio&quot;</span>)</span><br><span class="line">                            .object(<span class="string">&quot;公寓-外观.jpg&quot;</span>)</span><br><span class="line">                            .filename(<span class="string">&quot;D:\\workspace\\hello-minio\\src\\main\\resources\\公寓-外观.jpg&quot;</span>)</span><br><span class="line">                            .build());</span><br><span class="line">            System.out.println(<span class="string">&quot;上传成功&quot;</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (MinioException e) &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;Error occurred: &quot;</span> + e);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ol start="4">
<li><p><strong>运行测试</strong></p>
<p>运行上述代码，然后查看MinIO管理页面，观察是否上传成功。</p>
</li>
</ol>
]]></content>
      <categories>
        <category>数据库</category>
      </categories>
      <tags>
        <tag>快速入门</tag>
        <tag>MinIO</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring Boot全局异常处理</title>
    <url>/2025/01/22/Spring-Boot%E5%85%A8%E5%B1%80%E5%BC%82%E5%B8%B8%E5%A4%84%E7%90%86/</url>
    <content><![CDATA[<h2 id="为什么使用全局异常处理"><a href="#为什么使用全局异常处理" class="headerlink" title="为什么使用全局异常处理"></a>为什么使用全局异常处理</h2><p>在开发时，需要对可能有异常的Controller层方法增加<code>try-catch</code>逻辑，而使用Spring MVC提供的<strong>全局异常处理</strong>功能，可以将所有处理异常的逻辑集中起来，进而统一处理所有异常，可以使代码更容易维护。<br>具体用法如下，详细信息可参考<a href="https://docs.spring.io/spring-framework/reference/web/webmvc/mvc-controller/ann-exceptionhandler.html">官方文档</a>：</p>
<span id="more"></span>

<h2 id="例子-尚庭公寓全局异常处理"><a href="#例子-尚庭公寓全局异常处理" class="headerlink" title="例子-尚庭公寓全局异常处理"></a>例子-尚庭公寓全局异常处理</h2><p>在<strong>common模块</strong>中创建<code>com.atguigu.lease.common.exception.GlobalExceptionHandler</code>类，内容如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@ControllerAdvice</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">GlobalExceptionHandler</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@ExceptionHandler(Exception.class)</span></span><br><span class="line">    <span class="meta">@ResponseBody</span></span><br><span class="line">    <span class="keyword">public</span> Result <span class="title function_">error</span><span class="params">(Exception e)</span>&#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">        <span class="keyword">return</span> Result.fail();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">`</span><br><span class="line">上述代码中的关键注解的作用如下</span><br><span class="line">`<span class="meta">@ControllerAdvice</span>`用于声明处理全局Controller方法异常的类</span><br><span class="line">`<span class="meta">@ExceptionHandler</span>`用于声明处理异常的方法，`value`属性用于声明该方法处理的异常类型</span><br><span class="line">`<span class="meta">@ResponseBody</span>`表示将方法的返回值作为HTTP的响应体</span><br><span class="line">**注意：**</span><br><span class="line">全局异常处理功能由SpringMVC提供，因此需要在**common模块**的`pom.xml`中引入如下依赖</span><br><span class="line">```xml</span><br><span class="line">&lt;!--spring-web--&gt;</span><br><span class="line">&lt;dependency&gt;</span><br><span class="line">    &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;</span><br><span class="line">    &lt;artifactId&gt;spring-boot-starter-web&lt;/artifactId&gt;</span><br><span class="line">&lt;/dependency&gt;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Spring</tag>
        <tag>异常处理</tag>
      </tags>
  </entry>
  <entry>
    <title>Nginx快速入门</title>
    <url>/2025/01/25/Nginx%E5%BF%AB%E9%80%9F%E5%85%A5%E9%97%A8/</url>
    <content><![CDATA[<h1 id="Nginx快速入门"><a href="#Nginx快速入门" class="headerlink" title="Nginx快速入门"></a>Nginx快速入门</h1><h2 id="Nginx安装"><a href="#Nginx安装" class="headerlink" title="Nginx安装"></a>Nginx安装</h2><p>根据前文的部署方案，需要在<code>server02</code>部署Nginx。Nginx官网有详细的安装步骤，具体内容可参考<a href="https://nginx.org/en/linux_packages.html">官方文档</a>。</p>
<ol>
<li><p><strong>配置Nginx yum存储库</strong></p>
<p>创建<code>/etc/yum.repos.d/nginx.repo</code>文件</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vim /etc/yum.repos.d/nginx.repo</span><br></pre></td></tr></table></figure>

<p>增加如下内容</p>
<figure class="highlight ini"><table><tr><td class="code"><pre><span class="line"><span class="section">[nginx-stable]</span></span><br><span class="line"><span class="attr">name</span>=nginx stable repo</span><br><span class="line"><span class="attr">baseurl</span>=http://nginx.org/packages/centos/<span class="variable">$releasever</span>/<span class="variable">$basearch</span>/</span><br><span class="line"><span class="attr">gpgcheck</span>=<span class="number">1</span></span><br><span class="line"><span class="attr">enabled</span>=<span class="number">1</span></span><br><span class="line"><span class="attr">gpgkey</span>=https://nginx.org/keys/nginx_signing.key</span><br><span class="line"><span class="attr">module_hotfixes</span>=<span class="literal">true</span></span><br><span class="line"></span><br><span class="line"><span class="section">[nginx-mainline]</span></span><br><span class="line"><span class="attr">name</span>=nginx mainline repo</span><br><span class="line"><span class="attr">baseurl</span>=http://nginx.org/packages/mainline/centos/<span class="variable">$releasever</span>/<span class="variable">$basearch</span>/</span><br><span class="line"><span class="attr">gpgcheck</span>=<span class="number">1</span></span><br><span class="line"><span class="attr">enabled</span>=<span class="number">0</span></span><br><span class="line"><span class="attr">gpgkey</span>=https://nginx.org/keys/nginx_signing.key</span><br><span class="line"><span class="attr">module_hotfixes</span>=<span class="literal">true</span></span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>在线安装Nginx</strong></p>
<p>执行以下命令，安装Nginx</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum -y install nginx</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>启动Nginx</strong></p>
<p>执行以下命令启动Nginx</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">systemctl start nginx</span><br></pre></td></tr></table></figure>

<p>执行以下命令查看Nginx运行状态</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">systemctl status nginx</span><br></pre></td></tr></table></figure>

<p>执行以下命令设置开机自启</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">systemctl <span class="built_in">enable</span> nginx</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>访问Nginx服务默认首页</strong></p>
<p>访问<code>http://192.168.10.102</code>，能访问到如下页面，则证明Nginx运行正常。</p>
<img src="images/Nginx首页.png" style="zoom:50%;" /></li>
</ol>
<h2 id="9-2-2-重要的目录、文件"><a href="#9-2-2-重要的目录、文件" class="headerlink" title="9.2.2 重要的目录、文件"></a>9.2.2 重要的目录、文件</h2><p>Nginx中有很多十分重要的目录或者文件，下面对核心内容进行介绍</p>
<ol>
<li><strong>配置文件相关</strong><ul>
<li><code>/etc/nginx/</code>：主要的Nginx配置文件目录。</li>
<li><code>/etc/nginx/nginx.conf</code>：Nginx的主配置文件，包含全局配置信息。</li>
</ul>
</li>
<li><strong>日志相关</strong><ul>
<li><code>/var/log/nginx/</code>：Nginx的日志文件目录，包括访问日志和错误日志。</li>
<li><code>/var/log/nginx/access.log</code>：访问日志，记录所有进入服务器的请求。</li>
<li><code>/var/log/nginx/error.log</code>：错误日志，记录服务器处理过程中的错误信息。</li>
</ul>
</li>
</ol>
<h2 id="9-2-3-配置文件概述"><a href="#9-2-3-配置文件概述" class="headerlink" title="9.2.3 配置文件概述"></a>9.2.3 配置文件概述</h2><ol>
<li><p><strong>配置文件结构</strong></p>
<p><code>nginx.conf</code>文件层次分明，整个文件分为多个区块（block），每个区块下可配置各种参数，也可包含其子级区块，具体结构如下图所示</p>
<img src="images/nginx配置文件结构.drawio.png" style="zoom:50%;" />

<p><code>nginx.conf</code>通过<code>include /etc/nginx/conf.d/*.conf</code>引入了<code>/etc/nginx/conf.d</code>目录下的所有<code>.conf</code>文件，该目录下的配置文件结构如下图所示</p>
<img src="images/nginx配置文件结构-conf.drawio.png" style="zoom: 50%;" />
</li>
<li><p><strong>重要配置说明</strong></p>
<p>下面分块介绍重要的配置参数</p>
<ul>
<li><p><strong>main block</strong></p>
<p><code>main block</code>位于配置文件的最外层，其包含了影响Nginx服务器整体行为的全局参数，例如</p>
<ul>
<li><code>user</code>：定义Nginx工作进程的用户和用户组。</li>
<li><code>worker_processes</code>：指定Nginx使用的工作进程数。</li>
<li><code>error_log</code>：配置全局错误日志文件路径。</li>
</ul>
</li>
<li><p><strong>events block</strong></p>
<p><code>events block</code>位于<code>main block</code>中，用于配置Nginx服务器的事件处理机制，主要配置Nginx如何处理客户端连接。</p>
</li>
<li><p><strong>http block</strong></p>
<p><code>http block</code>位于<code>main block</code>中，用于配置HTTP服务器相关功能。例如</p>
<ul>
<li><code>access_log</code>：指定访问日志的路径</li>
<li><code>log_format</code>：指定访问日志的格式</li>
</ul>
</li>
<li><p><strong>server block</strong></p>
<p><code>server block</code>位于<code>http block</code>，用于配置虚拟主机，一个Nginx服务可包含多个虚拟主机，每个虚拟主机都可以独立的提供服务，因此借助Nginx，我们可以在一台服务器部署多个独立的网站，如下图所示</p>
<img src="images/nginx-虚拟主机.drawio.png" style="zoom:50%;" />

<p>每个虚拟主机使用一个<code>server block</code>进行配置，配置的内容包括</p>
<ul>
<li><code>listen</code>：虚拟主机监听的端口号。</li>
<li><code>server_name</code>：指定虚拟主机的域名或者IP。</li>
</ul>
</li>
<li><p><strong>location block</strong></p>
<p><code>location block</code>位于<code>server block</code>，用于配置请求的处理逻辑，一个<code>server block</code>中可以包含多个<code>location block</code>，例如</p>
<figure class="highlight nginx"><table><tr><td class="code"><pre><span class="line"><span class="section">server</span> &#123;</span><br><span class="line">    <span class="attribute">listen</span> <span class="number">80</span>;</span><br><span class="line">    <span class="attribute">server_name</span> www.atguigu.com;</span><br><span class="line">    <span class="section">location</span> /index &#123;</span><br><span class="line">        <span class="attribute">root</span> /var/www/html;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="section">location</span> /api &#123;</span><br><span class="line">        <span class="attribute">proxy_pass</span> http://backend-api;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
</ol>
<h2 id="9-2-4-静态资源服务器案例"><a href="#9-2-4-静态资源服务器案例" class="headerlink" title="9.2.4 静态资源服务器案例"></a>9.2.4 静态资源服务器案例</h2><p>下面完成一个简单案例，使用Nginx作为静态资源服务器。</p>
<p>项目资料中有一个简单的前端项目<code>hello-nginx</code>，其中只包含html、css等静态资源，现将其部署在<code>server02</code>上。</p>
<ol>
<li><p><strong>上传静态资源到服务器</strong></p>
<p>将<code>hello-nginx.zip</code>上传到<code>server02</code>服务器任意路径。</p>
</li>
<li><p><strong>解压<code>hello-nginx.zip</code>到<code>/usr/share/nginx/html</code>中</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">unzip hello-nginx.zip -d /usr/share/nginx/html</span><br></pre></td></tr></table></figure>

<p>最终的路径结构如下</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line">/usr</span><br><span class="line">└── share</span><br><span class="line">    └── nginx</span><br><span class="line">        └── html</span><br><span class="line">            └── hello-nginx</span><br><span class="line">                ├── css</span><br><span class="line">                │   └── style.css</span><br><span class="line">                ├── images</span><br><span class="line">                │   └── img.png</span><br><span class="line">                └── index.html</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>配置Nginx虚拟主机</strong></p>
<p>虚拟主机的配置应位于<code>/etc/nginx/nginx.conf</code>的<strong>server block</strong>中，由于<code>/etc/nginx/nginx.conf</code>的<strong>http bolck</strong>中引入了<code>/etc/nginx/conf.d/*.conf</code>，所以虚拟主机在<code>/etc/nginx/conf.d/</code>目录下的任意<code>.conf</code>文件配置即可。</p>
<ul>
<li><p>创建<code>/etc/nginx/conf.d/hello-nginx.conf</code>文件</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vim /etc/nginx/conf.d/hello-nginx.conf</span><br></pre></td></tr></table></figure>
</li>
<li><p>添加如下内容</p>
<figure class="highlight nginx"><table><tr><td class="code"><pre><span class="line"><span class="section">server</span> &#123;</span><br><span class="line">    <span class="attribute">listen</span>       <span class="number">8080</span>;</span><br><span class="line">    <span class="attribute">server_name</span>  <span class="number">192.168.10.102</span>;</span><br><span class="line"></span><br><span class="line">    <span class="section">location</span> /hello-nginx &#123;</span><br><span class="line">        <span class="attribute">root</span>   /usr/share/nginx/html;</span><br><span class="line">        <span class="attribute">index</span>  index.html;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>重新加载Nginx的配置文件</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">systemctl reload nginx</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p><strong>访问项目</strong></p>
<p>访问路径为<a href="http://192.168.10.102:8080/hello-nginx%EF%BC%8C%E8%8B%A5%E9%83%A8%E7%BD%B2%E6%88%90%E5%8A%9F%EF%BC%8C%E5%8F%AF%E8%A7%81%E5%88%B0%E5%A6%82%E4%B8%8B%E9%A1%B5%E9%9D%A2">http://192.168.10.102:8080/hello-nginx，若部署成功，可见到如下页面</a></p>
<img src="images/Nginx案例-http服务.png" style="zoom: 67%;" />
</li>
<li><p><strong>案例剖析</strong></p>
<p>下面通过上述案例来了解Ngxin处理请求的逻辑。</p>
<ul>
<li><p><strong>匹配server</strong></p>
<p>由于Nginx中可存在多个虚拟主机的配置，故接收到一个请求后，Nginx首先要确定请求交给哪个虚拟主机进行处理。这很显然是根据<code>server_name</code>和<code>listen</code>进行判断的。例如上述的请求路径<a href="http://192.168.10.102:8080/hello-nginx%EF%BC%8C%E5%B0%B1%E4%BC%9A%E5%8C%B9%E9%85%8D%E5%88%B0%E4%BB%A5%E4%B8%8B%E7%9A%84%E8%99%9A%E6%8B%9F%E4%B8%BB%E6%9C%BA">http://192.168.10.102:8080/hello-nginx，就会匹配到以下的虚拟主机</a></p>
<figure class="highlight nginx"><table><tr><td class="code"><pre><span class="line"><span class="section">server</span> &#123;</span><br><span class="line">    <span class="attribute">listen</span>       <span class="number">8080</span>;</span><br><span class="line">    <span class="attribute">server_name</span>  <span class="number">192.168.10.102</span>;</span><br><span class="line"> ......</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>匹配location</strong></p>
<p>由于一个<strong>server block</strong>中可能包含多个<strong>location block</strong>，故Nginx在完成<strong>server</strong>匹配后，还要匹配<strong>location</strong>，<strong>location</strong>的匹配是根据请求路径进行判断的。例如以下写法<code>location</code>关键字后边的<code>/hello-nginx</code>就是匹配规则，它表达的含义是匹配以<code>/hello-nginx</code>为前缀的请求，例如上述的<a href="http://192.168.10.102:8080/hello-nginx%E8%AF%B7%E6%B1%82%E5%B0%B1%E4%BC%9A%E5%8C%B9%E9%85%8D%E5%88%B0%E8%AF%A5**location**%EF%BC%8C%E8%80%8C">http://192.168.10.102:8080/hello-nginx请求就会匹配到该**location**，而</a></p>
<p><a href="http://192.168.10.102:8080/nginx%E5%88%99%E4%B8%8D%E4%BC%9A%E3%80%82">http://192.168.10.102:8080/nginx则不会。</a></p>
<figure class="highlight nginx"><table><tr><td class="code"><pre><span class="line"><span class="section">location</span> /hello-nginx &#123;</span><br><span class="line"> ......</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>定位文件</strong></p>
<p>完成<strong>location</strong>的匹配后，Nginx会以<strong>location block</strong>中的<code>root</code>作为根目录，然后查找请求路径对应的资源，例如以下配置</p>
<figure class="highlight nginx"><table><tr><td class="code"><pre><span class="line"><span class="section">location</span> /hello-nginx &#123;</span><br><span class="line">    <span class="attribute">root</span>   /usr/share/nginx/html;</span><br><span class="line">    <span class="attribute">index</span>  index.html;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>当请求<a href="http://192.168.10.102:8080/hello-nginx">http://192.168.10.102:8080/hello-nginx</a> 时，Ngxin会在<code>/usr/share/nginx/html/hello-nginx</code>路径中查找资源，由于该路径为<strong>目录</strong>（而非文件），故Nginx会在该目录下寻找<code>index</code>，也就是上述配置的<code>index.html</code>。然后将<code>index.html</code>响应给客户端。至此，该请求的处理就结束了。</p>
<p><strong>注意</strong>：上述提到的<strong>server_name</strong>和<strong>location</strong>均有多种匹配模式，例如精确匹配、前缀匹配、正则匹配，此处不再展开。</p>
</li>
</ul>
</li>
</ol>
<h2 id="9-2-5-反向代理案例"><a href="#9-2-5-反向代理案例" class="headerlink" title="9.2.5 反向代理案例"></a>9.2.5 反向代理案例</h2><p>下面完成一个简单案例，使用Nginx作为反向代理。</p>
<p>使用Nginx反向代理其他网站，比如<code>http://www.atguigu.com</code>。</p>
<ol>
<li><p><strong>配置虚拟主机</strong></p>
<p>创建<code>/etc/nginx/conf.d/hello-proxy.conf</code>文件</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vim /etc/nginx/conf.d/hello-proxy.conf</span><br></pre></td></tr></table></figure>

<p>内容如下</p>
<figure class="highlight nginx"><table><tr><td class="code"><pre><span class="line"><span class="section">server</span> &#123;</span><br><span class="line">    <span class="attribute">listen</span>       <span class="number">9090</span>;</span><br><span class="line">    <span class="attribute">server_name</span>  <span class="number">192.168.10.102</span>;</span><br><span class="line"></span><br><span class="line">    <span class="section">location</span> / &#123;</span><br><span class="line">        <span class="attribute">proxy_pass</span> http://www.atguigu.com;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>重新加载Nginx配置文件</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">systemctl reload nginx</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>观察代理效果</strong></p>
<p>使用浏览器访问<a href="http://192.168.10.102:9090，观察响应结果。">http://192.168.10.102:9090，观察响应结果。</a></p>
</li>
</ol>
<p><strong>注意</strong>：借助反向代理功能，Nginx可以实现负载均衡等高级功能，此处不再展开。</p>
<h2 id="9-3-配置域名映射"><a href="#9-3-配置域名映射" class="headerlink" title="9.3 配置域名映射"></a>9.3 配置域名映射</h2><p>现实生活中，几乎所有的网站都是通过域名去访问。真正的域名需要付费购买，此处在宿主机本地配置一下域名映射，模拟一下域名的效果即可。</p>
<p>我们准备两个域名<code>lease.atguigu.com</code>和<code>admin.lease.atguigu.com</code>，前者用于访问移动端网站，后者用于访问后台管理系统。由于两个前端项目都部署在<code>server02</code>上，所以两个域名均指向<code>server02</code>的IP。</p>
<p>Windows的域名映射配置文件位于<code>C:\Windows\System32\drivers\etc\hosts</code>，需要使用管理员身份修改。使用管理员身份运行任意文本编辑器，然后使用其打开<code>hosts</code>文件，并增加如下内容：</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line">192.168.10.102 lease.atguigu.com admin.lease.atguigu.com</span><br></pre></td></tr></table></figure>

<p>修改完毕记得保存。</p>
]]></content>
      <categories>
        <category>代理</category>
      </categories>
      <tags>
        <tag>快速入门</tag>
        <tag>Nginx</tag>
      </tags>
  </entry>
  <entry>
    <title>Redis快速入门</title>
    <url>/2025/01/22/Redis%E5%BF%AB%E9%80%9F%E5%85%A5%E9%97%A8/</url>
    <content><![CDATA[<h1 id="Redis快速入门"><a href="#Redis快速入门" class="headerlink" title="Redis快速入门"></a>Redis快速入门</h1><h2 id="1-Redis概述"><a href="#1-Redis概述" class="headerlink" title="1 Redis概述"></a>1 Redis概述</h2><ol>
<li><p><strong>Reids定义</strong></p>
<p>Redis（Remote Dictionary Server）是一个<strong>基于内存</strong>的键值对存储系统，常用作缓存服务。由于Reids将数据都保存在内存中，因此其读写性能十分惊人，同时，为保证数据的可靠性，Redis会将数据备份到硬盘上，用于故障发生时的数据恢复。</p>
</li>
<li><p><strong>Redis特点</strong></p>
<ul>
<li><strong>高性能</strong>：Redis主要将数据存储在内存中，因此读写速度非常快，适合对速度有较高要求的场景。</li>
<li><strong>支持多种数据结构</strong>：Redis中键值对的值（Value）支持多种数据结构，如字符串、哈希表、列表、集合等，这使得它可以应用于多种不同的场景。</li>
<li><strong>持久化</strong>：Redis可以通过<strong>定期快照</strong>或者<strong>实时记录写操作日志</strong>的方式将内存中的数据持久化到硬盘，确保数据在重启后不会丢失。</li>
<li><strong>灵活的数据过期策略</strong>：可以为每个键设置过期时间，一旦过期，Redis会自动删除。</li>
</ul>
</li>
<li><p><strong>Redis应用场景</strong></p>
<p>Redis最为常见的一个应用场景就是用作<strong>缓存</strong>，缓存可以显著提升访问速度，降低数据库压力。</p>
<img src="缓存概述.drawio.svg" style="zoom:45%;" /></li>
</ol>
<span id="more"></span>

<h2 id="2-Reids客户端"><a href="#2-Reids客户端" class="headerlink" title="2 Reids客户端"></a>2 Reids客户端</h2><ol>
<li><p><strong>命令行客户端</strong></p>
<p>Reids提供了一个命令行客户端<code>redis-cli</code>，我们可以使用它连接到Redis服务器，然后通过命令与服务器进行各种交互，例如数据的增删改查。下面介绍该命令的基本用法。</p>
<ul>
<li><p><strong>启动客户端</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">redis-cli -h 127.0.0.1 -p 6379</span><br></pre></td></tr></table></figure>

<p><strong>说明</strong>：</p>
<ul>
<li><code>-h &lt;hostname&gt;</code>选项用于声明Redis服务器的主机名或IP地址，默认值为<code>127.0.0.1</code>。</li>
<li><code>-p &lt;port&gt;</code>选项用于声明Redis服务器监听的端口号，默认值为<code>6379</code>。</li>
</ul>
</li>
<li><p><strong>测试连接状态</strong></p>
<p><code>ping</code>命令可用于测试连接状态，语法如下</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ping</span><br></pre></td></tr></table></figure>

<p><strong>说明</strong>：若连接正常，则会返回pong。</p>
</li>
<li><p><strong>退出客户端</strong></p>
<p><code>quit</code>命令可用于断开客户端与服务端的连接，并退出客户端，语法如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">quit</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p><strong>图形化客户端</strong></p>
<p>Redis官方推荐的图形化客户端为<strong>RedisInsight</strong>，该客户端开源免费，且功能强大，下面演示如何使用。</p>
<ul>
<li><p>安装</p>
<p>安装包为<code>RedisInsight-v2-win-installer.exe</code>，可在项目资料中获取，安装步骤比较简单，大家自行安装即可。</p>
</li>
<li><p>选择配置数据库连接</p>
<p><img src="/ReidsInsight.png"></p>
</li>
<li><p>填写连接信息，并测试连接，通过后保存连接</p>
<p><img src="/ReidsInsight-Reids%E8%BF%9E%E6%8E%A5%E4%BF%A1%E6%81%AF.png"></p>
</li>
<li><p>点击创建好的连接</p>
<p><img src="/2025/01/22/Redis%E5%BF%AB%E9%80%9F%E5%85%A5%E9%97%A8/ReidsInsight-Redis%E8%BF%9E%E6%8E%A5%E5%88%97%E8%A1%A8.png"></p>
</li>
<li><p>工作界面如下</p>
<p><img src="/2025/01/22/Redis%E5%BF%AB%E9%80%9F%E5%85%A5%E9%97%A8/ReidsInsight-Redis%E5%B7%A5%E4%BD%9C%E7%95%8C%E9%9D%A2.png"></p>
</li>
</ul>
</li>
</ol>
<h2 id="3-Redis常用数据类型及命令"><a href="#3-Redis常用数据类型及命令" class="headerlink" title="3 Redis常用数据类型及命令"></a>3 Redis常用数据类型及命令</h2><h3 id="3-1-通用命令"><a href="#3-1-通用命令" class="headerlink" title="3.1 通用命令"></a>3.1 通用命令</h3><ul>
<li><p><strong>查看所有键</strong></p>
<p><code>keys</code>命令可用于查看所有键，语法如下</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">keys pattern</span><br></pre></td></tr></table></figure>

<p><strong>说明</strong>：pattern用于匹配key，其中<code>*</code>表示任意个任意字符，<code>?</code>表示一个任意字符。</p>
<p>示例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">127.0.0.1:6379&gt; KEYS *</span><br><span class="line">1) &quot;k3&quot;</span><br><span class="line">2) &quot;k2&quot;</span><br><span class="line">3) &quot;k1&quot;</span><br></pre></td></tr></table></figure>

<p><strong>注意</strong>：该命令会遍历Redis服务器中保存的所有键，因此当键很多时会影响整个Redis服务的性能，线上环境需要谨慎使用。</p>
</li>
<li><p><strong>键总数</strong></p>
<p><code>dbsize</code>可用于查看键的总数，语法如下</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">dbsize</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>判断键是否存在</strong></p>
<p><code>exists</code>命令可用于判断一个键是否存在，语法如下</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">exists key</span><br></pre></td></tr></table></figure>

<p><strong>说明</strong>：若键存在则返回1，不存在则返回0。</p>
</li>
<li><p><strong>删除键</strong></p>
<p><code>del</code>可用于删除指定键，语法如下</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">del key [key ...]</span><br></pre></td></tr></table></figure>

<p><strong>说明</strong>：返回值为删除键的个数，若删除一个不存在的键，则返回0。</p>
</li>
<li><p><strong>查询键的剩余过期时间</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ttl key</span><br></pre></td></tr></table></figure>

<p><strong>说明</strong>：<code>ttl</code>的含义为<strong>time to live</strong>，用于查询一个定时键的剩余存活时间，返回值以秒为单位。若查询的键的未设置过期时间，则返回<code>-1</code>，若查询的键不存在，则返回<code>-2</code>。</p>
</li>
<li><p><strong>数据库管理命令</strong></p>
<p>Redis默认有编号为0~15的16个逻辑数据库，每个数据库之间的数据是相互独立的，所有连接默认使用的都是0号数据库。</p>
<ul>
<li><p><strong>切换数据库</strong></p>
<p><code>select</code>命令可用于切换数据库，语法如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">select index</span><br></pre></td></tr></table></figure>

<p><strong>说明</strong>：若index超出范围，会报错</p>
</li>
<li><p><strong>清空数据库</strong></p>
<p><code>flushdb</code>命令会清空当前所选用的数据库，<code>flushall</code>命令会清空0~15号所有的数据库。</p>
<p><strong>注意</strong>：生产环境慎用</p>
</li>
</ul>
</li>
</ul>
<h3 id="3-2-string类型"><a href="#3-2-string类型" class="headerlink" title="3.2 string类型"></a>3.2 string类型</h3><ol>
<li><p><strong>概述</strong></p>
<p>Redis中的string类型保存的是字节序列（Sequence of bytes），因此任意类型的数据，只要经过序列化之后都可以保存到Redis的string类型中，包括文本、数字甚至是一个对象。</p>
</li>
<li><p><strong>常用命令</strong></p>
<ul>
<li><p><strong>set</strong></p>
<p><code>set</code>命令用于添加string类型的键值对，具体语法如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">SET key value [NX|XX] [EX seconds|PX milliseconds]</span><br></pre></td></tr></table></figure>

<p>各选项含义如下</p>
<ul>
<li>NX：仅在key不存在时set</li>
<li>XX：仅在key存在时set</li>
<li>EX seconds：设置过期时间，单位为秒</li>
<li>PX milliseconds：设置过期时间，单位为毫秒</li>
</ul>
</li>
<li><p><strong>get</strong></p>
<p><code>get</code>命令用于获取某个string类型的键对应的值，具体语法如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">GET key</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>incr</strong></p>
<p><code>incr</code>命令用于对数值做自增操作，具体语法如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">INCR key</span><br></pre></td></tr></table></figure>

<p>若key对应的value是整数，则返回自增后的结果，若不是整数则报错，若key不存在则创建并返回1。</p>
</li>
<li><p><strong>decr</strong></p>
<p><code>decr</code>命令用于对数值做自减操作，具体语法如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">DECR key</span><br></pre></td></tr></table></figure>

<p>若key对应的value是整数，则返回自减后的结果，若不是整数则报错，若key不存在则创建并返回-1。</p>
</li>
</ul>
</li>
<li><p><strong>应用场景</strong></p>
<p>string类型常用于缓存、计数器等场景。</p>
</li>
</ol>
<h3 id="3-3-list类型"><a href="#3-3-list类型" class="headerlink" title="3.3 list类型"></a>3.3 list类型</h3><ol>
<li><p><strong>概述</strong></p>
<p>list类型可用于存储多个string类型的元素，并且所有元素按照被添加的顺序存储。</p>
</li>
<li><p><strong>常用命令</strong></p>
<p>list类型相关的命令较多，下面分类进行进行介绍。</p>
<ul>
<li><p><strong>添加元素</strong></p>
<p>向列表中添加元素的命令有<code>lpush</code>、<code>rpush</code> 、<code>linsert</code>，各命令的功能与用法如下</p>
<img src="Redis-list-添加元素操作.drawio.png" style="zoom:50%;" />

<ul>
<li><p><code>lpush</code></p>
<p>该命令用于向list左侧添加元素，语法如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">lpush key element [element ...]</span><br></pre></td></tr></table></figure>

<p>示例</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">lpush l1 a b c</span><br></pre></td></tr></table></figure>
</li>
<li><p><code>rpush</code></p>
<p>该命令用于向list右侧添加元素，语法如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">rpush key element [element ...]</span><br></pre></td></tr></table></figure>
</li>
<li><p><code>linsert</code></p>
<p>该命令用于向list指定位置添加元素，语法如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">linsert key before|after pivot element</span><br></pre></td></tr></table></figure>

<p>示例</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">linsert l1 after b new</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p><strong>查询元素</strong></p>
<p>查询list元素的命令有<code>lindex</code>和<code>lrange</code>，各命令的功能与用法如下</p>
<ul>
<li><p><code>lindex</code></p>
<p>该命令用于获取指定索引位置的元素，语法如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">lindex key index</span><br></pre></td></tr></table></figure>

<p><strong>说明</strong>：index从左到右依次是0，1，2…，从右到左依次是-1，-2，-3…</p>
</li>
<li><p><code>lrange</code></p>
<p>该命令用于获取指定范围内的元素列表，语法如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">lrange key start stop</span><br></pre></td></tr></table></figure>

<p>示例</p>
<p>获取list全部元素，命令如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">lrange l1 0 -1</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p><strong>删除元素</strong></p>
<p>删除list元素的命令有<code>lpop</code>、<code>rpop</code>、<code>lrem</code>，各命令的功能与用法如下</p>
<img src="Redis-list-移除元素.drawio.png" style="zoom:50%;" />

<ul>
<li><p><code>lpop</code></p>
<p>该命令用于移除并返回list左侧元素，语法如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">lpop key [count]</span><br></pre></td></tr></table></figure>

<p><strong>说明</strong>：count参数表示移除元素的个数</p>
</li>
<li><p><code>rpop</code></p>
<p>该命令用于移除并返回list右侧的元素，语法如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">rpop key [count]</span><br></pre></td></tr></table></figure>
</li>
<li><p><code>lrem</code></p>
<p>该命令用于移除list中的指定元素，语法如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">lrem key count element</span><br></pre></td></tr></table></figure>

<p><strong>说明</strong>：count参数表示要移除element元素的个数（list中可以存在多个相同的元素），count的用法如下</p>
<ul>
<li>若count&gt;0，则从左到右删除最多count个element元素</li>
<li>若count&lt;0，则从右到左删除最多count（的绝对值）个element元素</li>
<li>若count&#x3D;0，则删除所有的element元素</li>
</ul>
</li>
</ul>
</li>
<li><p><strong>修改元素</strong></p>
<p><code>lset</code>命令可用于修改指定索引位置的元素，语法如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">lset key index element</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>其他</strong></p>
<p><code>llen</code>命令可用于查看list长度，语法如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">llen key</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p><strong>应用场景</strong></p>
<ul>
<li>社交应用中，可使用list缓存每个用户发布的最新的N条记录。</li>
<li>list可用作异步消息队列。</li>
</ul>
</li>
</ol>
<h3 id="3-4-set类型"><a href="#3-4-set类型" class="headerlink" title="3.4 set类型"></a>3.4 set类型</h3><ol>
<li><p><strong>概述</strong></p>
<p>和list类型相似，set类型也可用来存储多个string类型的元素，但与list类型不同，set中的元素是无序的，且set中不会包含相同元素。</p>
</li>
<li><p><strong>常用命令</strong></p>
<ul>
<li><p><strong>集合内</strong></p>
<ul>
<li><p><strong>sadd</strong></p>
<p>该命令用于向set中添加元素，语法如下</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sadd key member [member ...]</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>smembers</strong></p>
<p>该命令用于查询set中的全部元素，语法如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">smembers key</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>srem</strong></p>
<p>该命令用于移除set中的指定元素，语法如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">srem key member [member ...]</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>spop</strong></p>
<p>该命令随机移除并返回set中的n个元素，语法如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">spop key [count]</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>srandmember</strong></p>
<p>该命令随机返回set中的n个元素（不删除），语法如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">srandmember key [count]</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>scard</strong>（Cardinality，基数）</p>
<p>该命令用于查询set中的元素个数，语法如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">scard key</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>sismember</strong></p>
<p>该命令用于元素是否在set中，语法如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sismember key element</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p><strong>集合间</strong></p>
<ul>
<li><p><strong>sinter</strong></p>
<p>该命令用于计算多个集合的交集，语法如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sinter key [key ...]</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>sunion</strong></p>
<p>该命令用于计算多个集合的并集，语法如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sunion key [key ...]</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>sdiff</strong></p>
<p>该命令用于计算多个集合的差集，语法如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sdiff key [key ...]</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
</ul>
</li>
<li><p><strong>应用场景</strong></p>
<p>set可用于计算共同关注好友，随机抽奖系统等等。</p>
</li>
</ol>
<h3 id="3-5-hash类型"><a href="#3-5-hash类型" class="headerlink" title="3.5 hash类型"></a>3.5 hash类型</h3><ol>
<li><p><strong>概述</strong></p>
<p>hash类型类似于Java语言中的<code>HashMap</code>，可用于存储键值对。</p>
<img src="Redis-hash.drawio.png" style="zoom:50%;" />
</li>
<li><p><strong>常用命令</strong></p>
<ul>
<li><p><strong>hset</strong></p>
<p>该命令用于向hash中增加键值对，语法如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">hset key field value [field value ...]</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>hget</strong></p>
<p>该命令用于获取hash中某个键对应的值，语法如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">hget key field</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>hdel</strong></p>
<p>该命令用于删除hash中的指定的键值对，语法如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">hdel key field [field ...]</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>hlen</strong></p>
<p>该命令用于查询hash中的键值对个数，语法如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">hlen key</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>hexists</strong></p>
<p>该命令用于判断hash中的某个键是否存在，语法如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">hexists key field</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>hkeys</strong></p>
<p>该命令用于返回hash中所有的键，语法如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">hkeys key</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>hvals</strong></p>
<p>该命令用于返回hash中所有的值，语法如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">hvals key</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>hgetall</strong></p>
<p>该命令用于返回hash中所有的键与值，语法如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">hgetall key</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p><strong>应用场景</strong></p>
<p>hash类型可用于缓存对象等。</p>
</li>
</ol>
<h3 id="3-6-zset类型"><a href="#3-6-zset类型" class="headerlink" title="3.6 zset类型"></a>3.6 zset类型</h3><ol>
<li><p><strong>概述</strong></p>
<p>zset（sorted set）被称为有序集合，同set相似，zset中也不会包含相同元素，但不同的是，zset中的元素是有序的。并且zset中的元素并非像list一样按照元素的插入顺序排序，而是按照每个元素的分数（score）排序。</p>
<img src="Redis-zset结构.drawio.png" style="zoom:50%;" />
</li>
<li><p><strong>常用命令</strong></p>
<ul>
<li><p><strong>zadd</strong></p>
<p>该命令用于向zset中添加元素，语法如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ZADD key [NX|XX] score member</span><br></pre></td></tr></table></figure>

<p><strong>说明：</strong></p>
<ul>
<li>NX：仅当member不存在时才add</li>
<li>XX：仅当member存在时才add</li>
</ul>
</li>
<li><p><strong>zcard</strong></p>
<p>该命令用于计算zset中的元素个数，语法如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">zcard key</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>zscore</strong></p>
<p>改名用于查看某个元素的分数，语法如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">zscore key member</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>zrank&#x2F;zrevrank</strong></p>
<p>这组命令用于计算元素的排名，其中zrank按照score的升序排序，zrevrank则按照降序排序，语法如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">zrank/zrevrank key member</span><br></pre></td></tr></table></figure>

<p><strong>说明：</strong>名次从0开始。</p>
</li>
<li><p><strong>zrem</strong></p>
<p>该命令用于删除元素，语法如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">zrem key member [member ...]</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>zincrby</strong></p>
<p>该命令用于增加元素的分数，语法如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">zincrby key increment member</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>zrange</strong></p>
<p>该命令用于查询指定区间范围的元素，语法如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">zrange key start stop [byscore] [rev] [limit offset count] [withscores]</span><br></pre></td></tr></table></figure>

<p><strong>说明：</strong></p>
<ul>
<li>start&#x2F;stop：用于指定查询区间，但是在不同模式下，其代表的含义也不相同<ul>
<li>默认模式下，<code>start~stop</code>表示的是名次区间，且该区间为闭区间。名次从0开始，且可为负数，-1表示倒数第一，-2表示倒数第二，以此类推。</li>
<li>byscore模式下（声明了byscore参数），则<code>start~stop</code>表示的就是分数区间，该区间默认仍为闭区间。在该模式下，可以在<code>start</code>或<code>stop</code>前增加<code>(</code>来表示开区间，例如<code>(1 (5</code>，表示的就是<code>(1,5)</code>这个开区间。除此之外，还可以使用<code>-inf</code>和<code>+inf</code>表示负无穷和正无穷。</li>
</ul>
</li>
<li>byscore：用于切换到分数模式</li>
<li>rev：表示降序排序。在byscore模式下使用rev参数需要注意查询区间，start应大于stop。</li>
<li>limit：该选项只用于byscore模式，作用和sql语句中的limit一致</li>
<li>withscores：用于打印分数</li>
</ul>
</li>
</ul>
</li>
<li><p><strong>应用场景</strong></p>
<p>zset主要用于各种排行榜。</p>
</li>
</ol>
<h2 id="4-SpringBoot整合Redis"><a href="#4-SpringBoot整合Redis" class="headerlink" title="4 SpringBoot整合Redis"></a>4 SpringBoot整合Redis</h2><h3 id="4-1-Spring-Data-Redis概述"><a href="#4-1-Spring-Data-Redis概述" class="headerlink" title="4.1 Spring Data Redis概述"></a>4.1 Spring Data Redis概述</h3><p><strong>Spring Data Redis</strong> 是Spring大家族中的一个子项目，主要用于Spring程序和Redis的交互。它基于的Redis Java客户端（<a href="https://github.com/redis/jedis">Jedis</a>和<a href="https://lettuce.io/">Lettuce</a>）做了抽象，提供了一个统一的编程模型，使得Spring程序与Redis的交互变得十分简单。</p>
<p><strong>Spring Data Redis</strong> 中有一个十分重要的类——<code>RedisTemplate</code>，它封装了与Redis进行的交互的各种方法，我们主要用使用它与Redis进行交互。</p>
<h3 id="4-2-Spring-Data-Redis快速入门"><a href="#4-2-Spring-Data-Redis快速入门" class="headerlink" title="4.2 Spring Data Redis快速入门"></a>4.2 Spring Data Redis快速入门</h3><ol>
<li><p><strong>创建SpringBoot项目</strong></p>
</li>
<li><p><strong>引入Maven依赖</strong></p>
<p>Spring Boot提供了对Spring Data Redis的支持，在Spring Boot项目中可以直接引入<code>spring-boot-starter-data-redis</code>来完成Spring Data Redis的自动配置，具体依赖如下</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-data-redis<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>配置application.yml文件</strong></p>
<p>在<code>application.yml</code>文件中增加如下参数</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">spring:</span></span><br><span class="line">  <span class="attr">data:</span></span><br><span class="line">    <span class="attr">redis:</span></span><br><span class="line">      <span class="attr">host:</span> <span class="number">192.168</span><span class="number">.10</span><span class="number">.101</span></span><br><span class="line">      <span class="attr">port:</span> <span class="number">6379</span></span><br><span class="line">      <span class="attr">database:</span> <span class="number">0</span></span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>RedisTemplate使用</strong></p>
<p>由于<code>spring-boot-starter-data-redis</code>中提供了<code>RedisTemplate</code>的自动配置，所以我们可以将<code>RedisTemplate</code>注入自己的类中，如下边的案例所示</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@SpringBootTest</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">TestRedisTemplate</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> RedisTemplate redisTemplate;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">testRedisTemplate</span><span class="params">()</span> &#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>根据Redis的数据类型，RedisTemplate对各种交互方法做了分组，以下是常用的几个分组</p>
<table>
<thead>
<tr>
<th align="left">分组</th>
<th align="left">说明</th>
</tr>
</thead>
<tbody><tr>
<td align="left"><code>redisTemplate.opsForValue()</code></td>
<td align="left">操作string类型的方法</td>
</tr>
<tr>
<td align="left"><code>redisTemplate.opsForList()</code></td>
<td align="left">操作list类型的方法</td>
</tr>
<tr>
<td align="left"><code>redisTemplate.opsForSet()</code></td>
<td align="left">操作set类型的方法</td>
</tr>
<tr>
<td align="left"><code>redisTemplate.opsForHash()</code></td>
<td align="left">操作hash类型的方法</td>
</tr>
<tr>
<td align="left"><code>redisTemplate.opsForZSet()</code></td>
<td align="left">操作zset类型的方法</td>
</tr>
<tr>
<td align="left"><code>redisTemplate</code></td>
<td align="left">通用方法</td>
</tr>
</tbody></table>
<p>下面简单测试几个简单的方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@SpringBootTest</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">TestRedisTemplate</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> RedisTemplate redisTemplate;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">testSet</span><span class="params">()</span> &#123;</span><br><span class="line">        redisTemplate.opsForValue().set(<span class="string">&quot;key1&quot;</span>, <span class="string">&quot;value1&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">testGet</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="type">String</span> <span class="variable">result</span> <span class="operator">=</span> (String) redisTemplate.opsForValue().get(<span class="string">&quot;key1&quot;</span>);</span><br><span class="line">        System.out.println(result);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">testDel</span><span class="params">()</span> &#123;</span><br><span class="line">        redisTemplate.delete(<span class="string">&quot;key1&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>序列化问题</strong></p>
<ul>
<li><p><strong>问题演示</strong></p>
<ul>
<li><p><strong>问题一</strong></p>
<p>使用RedisTemplate向Redis中增加一个键值对</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">redisTemplate.opsForValue().set(<span class="string">&quot;key2&quot;</span>,<span class="string">&quot;value2&quot;</span>);</span><br></pre></td></tr></table></figure>

<p>使用RedisTemplate查询key2所对应的value，有结果</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">redisTemplate.opsForValue().get(<span class="string">&quot;key2&quot;</span>);</span><br></pre></td></tr></table></figure>

<p>使用命令行客户端查询key2所对应的value，无结果</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">get key2</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>问题二</strong></p>
<p>在图形化客户端或者命令行客户端观察key2，显示异常</p>
<p>RedisInsight中的key2显示如下</p>
<img src="RedisInsight-Key显示异常.png" style="zoom: 80%;" />

<p>命令行客户端中的key2显示如下</p>
<img src="CLI-Key显示异常.png" style="zoom:80%;" /></li>
</ul>
</li>
<li><p><strong>问题说明</strong></p>
<p>上述问题的根本原因是，Redis中的key和value均是以二进制的形式存储的，因此客户端输入的key和value都会经过序列化之后才发往Redis服务端。而RedisTemplate所使用序列化方式和命令行客户端采用序列化方式不相同，进而导致序列化之后的二进制数据不同，所以才会导致上述的现象。</p>
<img src="Redis序列化问题.drawio.png" style="zoom: 45%;" /></li>
</ul>
</li>
<li><p><strong>StringRedisTemplate使用</strong></p>
<p>为解决上述问题，可使用<code>StringRedisTemplate</code>代替<code>RedisTemplate</code>，因为<code>StringRedisTemplate</code>使用的序列化器和命令行所使用的序列化器是相同的。</p>
<p><code>spring-boot-starter-data-redis</code>同样提供了<code>StringRedisTemplate</code>的自动配置，因此我们也可以直接将其注入到自己的类中。实例代码如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@SpringBootTest</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">TestStringRedisTemplate</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> StringRedisTemplate redisTemplate;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">testSet</span><span class="params">()</span> &#123;</span><br><span class="line">        redisTemplate.opsForValue().set(<span class="string">&quot;key4&quot;</span>, <span class="string">&quot;value4&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">testGet</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="type">String</span> <span class="variable">result</span> <span class="operator">=</span> redisTemplate.opsForValue().get(<span class="string">&quot;key4&quot;</span>);</span><br><span class="line">        System.out.println(result);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">testDel</span><span class="params">()</span> &#123;</span><br><span class="line">        redisTemplate.delete(<span class="string">&quot;key4&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ol>
]]></content>
      <categories>
        <category>数据库</category>
      </categories>
      <tags>
        <tag>快速入门</tag>
        <tag>Redis</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring Security</title>
    <url>/2025/01/27/Spring-Security/</url>
    <content><![CDATA[<h1 id="RBAC权限控制"><a href="#RBAC权限控制" class="headerlink" title="RBAC权限控制"></a>RBAC权限控制</h1><h2 id="SpringSecurity介绍"><a href="#SpringSecurity介绍" class="headerlink" title="SpringSecurity介绍"></a>SpringSecurity介绍</h2><p>Spring Security是一个功能强大的Java安全框架，它提供了全面的安全认证和授权的支持。</p>
<p>与RBAC模型结合使用时，Spring Security能够实现灵活的权限控制。</p>
<p>我们来看下它的二大核心概念，认证和授权。让我们用一个简单的例子来领会它们的意义。</p>
<p><strong>1）认证（Authentication）</strong>想象一下，小智同学去图书馆借书。门口的图书管理员会要求小智出示借书证。这个借书证上有小智的照片和姓名，管理员通过它来确认小智的身份。这个过程，就是一个认证的实例。在Spring  Security的世界里，认证就像用户登录时提交的用户名和密码，系统通过这些信息来验证“你是谁”。</p>
<p>Spring Security不仅支持传统的用户名和密码认证，还支持OAuth2、JWT等现代认证方式。</p>
<p><strong>2）授权（Authorization）</strong>  现在，假设小智同学已经踏入了图书馆的大门，但当小智想进入只有持特定权限才能进入的参考书籍区时，管理员会再次检查小智的借书证，看是否有相应的权限标记。如果有，那么小智就能进入这个区域；如果没有，那么小智只能遗憾地止步。这就是授权的精髓。在Spring  Security中，授权是确认用户在通过认证之后，是否有权限执行某些操作或访问特定资源。</p>
<h2 id="SpringSecurity配置"><a href="#SpringSecurity配置" class="headerlink" title="SpringSecurity配置"></a>SpringSecurity配置</h2><p>Spring Security的配置类是实现安全控制的核心部分</p>
<p>开启Spring Security各种功能，以确保Web应用程序的安全性，包括认证、授权、会话管理、过滤器添加等。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.dkd.framework.config;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.springframework.beans.factory.annotation.Autowired;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.annotation.Bean;</span><br><span class="line"><span class="keyword">import</span> org.springframework.http.HttpMethod;</span><br><span class="line"><span class="keyword">import</span> org.springframework.security.authentication.AuthenticationManager;</span><br><span class="line"><span class="keyword">import</span> org.springframework.security.config.annotation.authentication.builders.AuthenticationManagerBuilder;</span><br><span class="line"><span class="keyword">import</span> org.springframework.security.config.annotation.method.configuration.EnableGlobalMethodSecurity;</span><br><span class="line"><span class="keyword">import</span> org.springframework.security.config.annotation.web.builders.HttpSecurity;</span><br><span class="line"><span class="keyword">import</span> org.springframework.security.config.annotation.web.configuration.WebSecurityConfigurerAdapter;</span><br><span class="line"><span class="keyword">import</span> org.springframework.security.config.annotation.web.configurers.ExpressionUrlAuthorizationConfigurer;</span><br><span class="line"><span class="keyword">import</span> org.springframework.security.config.http.SessionCreationPolicy;</span><br><span class="line"><span class="keyword">import</span> org.springframework.security.core.userdetails.UserDetailsService;</span><br><span class="line"><span class="keyword">import</span> org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder;</span><br><span class="line"><span class="keyword">import</span> org.springframework.security.web.authentication.UsernamePasswordAuthenticationFilter;</span><br><span class="line"><span class="keyword">import</span> org.springframework.security.web.authentication.logout.LogoutFilter;</span><br><span class="line"><span class="keyword">import</span> org.springframework.web.filter.CorsFilter;</span><br><span class="line"><span class="keyword">import</span> com.dkd.framework.config.properties.PermitAllUrlProperties;</span><br><span class="line"><span class="keyword">import</span> com.dkd.framework.security.filter.JwtAuthenticationTokenFilter;</span><br><span class="line"><span class="keyword">import</span> com.dkd.framework.security.handle.AuthenticationEntryPointImpl;</span><br><span class="line"><span class="keyword">import</span> com.dkd.framework.security.handle.LogoutSuccessHandlerImpl;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * spring security配置</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> ruoyi</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="comment">// 开启方法级别的权限控制 ==&gt; @PreAuthorize</span></span><br><span class="line"><span class="meta">@EnableGlobalMethodSecurity(prePostEnabled = true, securedEnabled = true)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">SecurityConfig</span> <span class="keyword">extends</span> <span class="title class_">WebSecurityConfigurerAdapter</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 自定义用户认证逻辑</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> UserDetailsService userDetailsService;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 认证失败处理类</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> AuthenticationEntryPointImpl unauthorizedHandler;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 退出处理类</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> LogoutSuccessHandlerImpl logoutSuccessHandler;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * token认证过滤器</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> JwtAuthenticationTokenFilter authenticationTokenFilter;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 跨域过滤器</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> CorsFilter corsFilter;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 允许匿名访问的地址</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> PermitAllUrlProperties permitAllUrl;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 解决 无法直接注入 AuthenticationManager</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span></span></span><br><span class="line"><span class="comment">     * <span class="doctag">@throws</span> Exception</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> AuthenticationManager <span class="title function_">authenticationManagerBean</span><span class="params">()</span> <span class="keyword">throws</span> Exception</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">super</span>.authenticationManagerBean();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * anyRequest          |   匹配所有请求路径</span></span><br><span class="line"><span class="comment">     * access              |   SpringEl表达式结果为true时可以访问</span></span><br><span class="line"><span class="comment">     * anonymous           |   匿名可以访问</span></span><br><span class="line"><span class="comment">     * denyAll             |   用户不能访问</span></span><br><span class="line"><span class="comment">     * fullyAuthenticated  |   用户完全认证可以访问（非remember-me下自动登录）</span></span><br><span class="line"><span class="comment">     * hasAnyAuthority     |   如果有参数，参数表示权限，则其中任何一个权限可以访问</span></span><br><span class="line"><span class="comment">     * hasAnyRole          |   如果有参数，参数表示角色，则其中任何一个角色可以访问</span></span><br><span class="line"><span class="comment">     * hasAuthority        |   如果有参数，参数表示权限，则其权限可以访问</span></span><br><span class="line"><span class="comment">     * hasIpAddress        |   如果有参数，参数表示IP地址，如果用户IP和参数匹配，则可以访问</span></span><br><span class="line"><span class="comment">     * hasRole             |   如果有参数，参数表示角色，则其角色可以访问</span></span><br><span class="line"><span class="comment">     * permitAll           |   用户可以任意访问</span></span><br><span class="line"><span class="comment">     * rememberMe          |   允许通过remember-me登录的用户访问</span></span><br><span class="line"><span class="comment">     * authenticated       |   用户登录后可访问</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">configure</span><span class="params">(HttpSecurity httpSecurity)</span> <span class="keyword">throws</span> Exception</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">// 配置URL访问授权规则</span></span><br><span class="line">        ExpressionUrlAuthorizationConfigurer&lt;HttpSecurity&gt;.<span class="type">ExpressionInterceptUrlRegistry</span> <span class="variable">registry</span> <span class="operator">=</span> httpSecurity.authorizeRequests();</span><br><span class="line">        <span class="comment">// 遍历无需认证即可访问的URL列表，设置这些URL对所有用户可访问</span></span><br><span class="line">        permitAllUrl.getUrls().forEach(url -&gt; registry.antMatchers(url).permitAll());</span><br><span class="line">        <span class="comment">// 配置Web应用程序规则</span></span><br><span class="line">        httpSecurity</span><br><span class="line">                <span class="comment">// CSRF（跨站请求伪造）禁用，因为不使用session</span></span><br><span class="line">                .csrf().disable()</span><br><span class="line">                <span class="comment">// 禁用HTTP响应标头</span></span><br><span class="line">                .headers().cacheControl().disable().and()</span><br><span class="line">                <span class="comment">// 认证失败处理类</span></span><br><span class="line">                .exceptionHandling().authenticationEntryPoint(unauthorizedHandler).and()</span><br><span class="line">                <span class="comment">// 基于token，所以不需要session</span></span><br><span class="line">                .sessionManagement().sessionCreationPolicy(SessionCreationPolicy.STATELESS).and()</span><br><span class="line">                <span class="comment">// 过滤请求</span></span><br><span class="line">                .authorizeRequests()</span><br><span class="line">                <span class="comment">// 对于登录login 注册register 验证码captchaImage 允许匿名访问</span></span><br><span class="line">                .antMatchers(<span class="string">&quot;/login&quot;</span>, <span class="string">&quot;/register&quot;</span>, <span class="string">&quot;/captchaImage&quot;</span>).permitAll()</span><br><span class="line">                <span class="comment">// 静态资源，可匿名访问</span></span><br><span class="line">                .antMatchers(HttpMethod.GET, <span class="string">&quot;/&quot;</span>, <span class="string">&quot;/*.html&quot;</span>, <span class="string">&quot;/**/*.html&quot;</span>, <span class="string">&quot;/**/*.css&quot;</span>, <span class="string">&quot;/**/*.js&quot;</span>, <span class="string">&quot;/profile/**&quot;</span>).permitAll()</span><br><span class="line">                .antMatchers(<span class="string">&quot;/swagger-ui.html&quot;</span>, <span class="string">&quot;/swagger-resources/**&quot;</span>, <span class="string">&quot;/webjars/**&quot;</span>, <span class="string">&quot;/*/api-docs&quot;</span>, <span class="string">&quot;/druid/**&quot;</span>).permitAll()</span><br><span class="line">                <span class="comment">// 除上面外的所有请求全部需要鉴权认证</span></span><br><span class="line">                .anyRequest().authenticated()</span><br><span class="line">                .and()</span><br><span class="line">                .headers().frameOptions().disable();</span><br><span class="line">        <span class="comment">// 添加Logout filter</span></span><br><span class="line">        httpSecurity.logout().logoutUrl(<span class="string">&quot;/logout&quot;</span>).logoutSuccessHandler(logoutSuccessHandler);</span><br><span class="line">        <span class="comment">// 添加JWT filter</span></span><br><span class="line">        httpSecurity.addFilterBefore(authenticationTokenFilter, UsernamePasswordAuthenticationFilter.class);</span><br><span class="line">        <span class="comment">// 添加CORS filter</span></span><br><span class="line">        httpSecurity.addFilterBefore(corsFilter, JwtAuthenticationTokenFilter.class);</span><br><span class="line">        httpSecurity.addFilterBefore(corsFilter, LogoutFilter.class);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 强散列哈希加密实现</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="keyword">public</span> BCryptPasswordEncoder <span class="title function_">bCryptPasswordEncoder</span><span class="params">()</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">BCryptPasswordEncoder</span>();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 身份认证接口</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">configure</span><span class="params">(AuthenticationManagerBuilder auth)</span> <span class="keyword">throws</span> Exception</span><br><span class="line">    &#123;</span><br><span class="line">        auth.userDetailsService(userDetailsService).passwordEncoder(bCryptPasswordEncoder());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="用户登录流程"><a href="#用户登录流程" class="headerlink" title="用户登录流程"></a>用户登录流程</h2><p>管理员在登录页面，输入用户名和密码以及验证码后，点击登录按钮，向后端发送请求，后端通过springSecurity认证管理器进行登录校验</p>
<p><img src="/2025/01/27/Spring-Security/image-20240813172027984.png" alt="image-20240813172027984"></p>
<p>此功能涉及前端相关的代码位于<code>views/login.vue</code>这个视图组件中，负责实现用户登录界面和交互逻辑。</p>
<p>后端处理逻辑则在<code>dkd-admin</code>模块的SysLoginController中，负责接收前端的请求，处理登录逻辑，并返回token令牌</p>
<h3 id="前端"><a href="#前端" class="headerlink" title="前端"></a>前端</h3><ol>
<li><p>点击<code>login.vue</code>中的登录按钮</p>
</li>
<li><p>调用<code>login.vue</code>中的<code>handleLogin</code>方法</p>
</li>
<li><p>调用<code>store/mondles/user.js</code>中的<code>login</code>方法，将返回结果存入useUserStore对象中（用于管理用户相关的状态和操作）</p>
</li>
<li><p>调用<code>api/login.js</code>中的<code>login</code>方法</p>
</li>
<li><p>调用<code>utils/request.js</code>中的<code>service</code>实例基于axios发送ajax请求（.env.development文件设置了统一请求路径前缀）</p>
</li>
</ol>
<h3 id="后端"><a href="#后端" class="headerlink" title="后端"></a>后端</h3><h4 id="SysLoginController"><a href="#SysLoginController" class="headerlink" title="SysLoginController"></a>SysLoginController</h4><p>在<code>ruoyi-admin</code>模块中<code>com.ruoyi.web.controller.system.SysLoginController</code>类的<code>login</code>方法接收前端登录请求</p>
<h4 id="SysLoginService"><a href="#SysLoginService" class="headerlink" title="SysLoginService"></a>SysLoginService</h4><p>在<code>ruoyi-framework</code>模块中<code>com.ruoyi.framework.web.service.SysLoginService</code>类的<code>login</code>方法处理登录逻辑</p>
<ol>
<li>验证码校验</li>
<li>登录前置校验</li>
<li>SS认证管理器用户校验，调用执行UserDetailsServiceImpl.loadUserByUsername<ol>
<li>认证通过后，创建登录用户对象LoginUser包括用户ID、部门ID、用户信息和用户权限信息</li>
</ol>
</li>
<li>登录成功，记录日志</li>
<li>修改用户表更新登录信息</li>
<li>生成token</li>
</ol>
<p><strong>具体的执行的流程如下图：</strong></p>
<p><img src="/2025/01/27/Spring-Security/image-20240812223100253.png" alt="image-20240812223100253">  </p>
<h2 id="获取用户角色和权限"><a href="#获取用户角色和权限" class="headerlink" title="获取用户角色和权限"></a>获取用户角色和权限</h2><p>超级管理员登录帝可得系统后，可以查看区域管理所有的权限按钮，实现增删改查操作</p>
<p><img src="/2025/01/27/Spring-Security/image-20240814114152698.png" alt="image-20240814114152698"></p>
<p>而财务人员登录帝可得系统后，仅可以查看区域列表，其他权限没有</p>
<p><img src="/2025/01/27/Spring-Security/image-20240814114217839.png" alt="image-20240814114217839"></p>
<p>1）首先创建一个财务人员的角色，权限字符串 Accountant，仅设置区域管理查询权限</p>
<p><img src="/2025/01/27/Spring-Security/image-20240814114404431.png" alt="image-20240814114404431"></p>
<p>2）再创建一个用户，昵称为黑马彭于晏，用户名hmpyy，密码admin123 角色为财务人员</p>
<p><img src="/2025/01/27/Spring-Security/image-20240814114420586.png" alt="image-20240814114420586"></p>
<h3 id="前端-1"><a href="#前端-1" class="headerlink" title="前端"></a>前端</h3><ol>
<li>在全局<code>permission.js</code>中的<code>router.beforeEach</code>方法用于在用户导航到不同路由之前进行一些预处理</li>
<li>调用<code>store/mondles/user.js</code>中的<code>getInfo</code>方法，将返回结果存入useUserStore对象中（用于管理用户相关的状态和操作）</li>
<li>调用<code>api/login.js</code>中的<code>getInfo</code>方法</li>
</ol>
<h3 id="后端-1"><a href="#后端-1" class="headerlink" title="后端"></a>后端</h3><h4 id="SysLoginController-1"><a href="#SysLoginController-1" class="headerlink" title="SysLoginController"></a>SysLoginController</h4><p>在<code>ruoyi-admin</code>模块中<code>com.ruoyi.web.controller.system.SysLoginController</code>类的<code>getInfo</code>方法接收前端获取用户信息请求</p>
<h4 id="SysPermissionService"><a href="#SysPermissionService" class="headerlink" title="SysPermissionService"></a>SysPermissionService</h4><p>在<code>ruoyi-framework</code>模块中<code>com.ruoyi.framework.web.service.SysPermissionService</code>类</p>
<ol>
<li>getRolePermission查询该用户角色集合</li>
<li>getMenuPermission查询该用户权限（菜单）集合</li>
</ol>
<p><strong>具体的执行的流程如下图：</strong></p>
<p><img src="/2025/01/27/Spring-Security/image-20240810163729060.png" alt="image-20240810163729060"></p>
<h3 id="页面权限"><a href="#页面权限" class="headerlink" title="页面权限"></a>页面权限</h3><p>前端封装了一个指令权限，能简单快速的实现按钮级别的权限判断。</p>
<p><strong>使用权限字符串 v-hasPermi</strong>：<code>@/directive/permission/hasPermi.js</code></p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line">// 单个</span><br><span class="line"><span class="tag">&lt;<span class="name">el-button</span> <span class="attr">v-hasPermi</span>=<span class="string">&quot;[&#x27;system:user:add&#x27;]&quot;</span>&gt;</span>存在权限字符串才能看到<span class="tag">&lt;/<span class="name">el-button</span>&gt;</span></span><br><span class="line">// 多个</span><br><span class="line"><span class="tag">&lt;<span class="name">el-button</span> <span class="attr">v-hasPermi</span>=<span class="string">&quot;[&#x27;system:user:add&#x27;, &#x27;system:user:edit&#x27;]&quot;</span>&gt;</span>包含权限字符串才能看到<span class="tag">&lt;/<span class="name">el-button</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p><strong>使用角色字符串 v-hasRole</strong><code>@/directive/permission/hasRole.js</code></p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line">// 单个</span><br><span class="line"><span class="tag">&lt;<span class="name">el-button</span> <span class="attr">v-hasRole</span>=<span class="string">&quot;[&#x27;admin&#x27;]&quot;</span>&gt;</span>管理员才能看到<span class="tag">&lt;/<span class="name">el-button</span>&gt;</span></span><br><span class="line">// 多个</span><br><span class="line"><span class="tag">&lt;<span class="name">el-button</span> <span class="attr">v-hasRole</span>=<span class="string">&quot;[&#x27;role1&#x27;, &#x27;role2&#x27;]&quot;</span>&gt;</span>包含角色才能看到<span class="tag">&lt;/<span class="name">el-button</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h2 id="获取动态菜单路由"><a href="#获取动态菜单路由" class="headerlink" title="获取动态菜单路由"></a>获取动态菜单路由</h2><p>在上个小节我们创建了“黑马彭于晏”这个用户，并为他分配了特定的角色权限。这意味着，当他登录帝可得系统时，看到的侧边栏菜单将根据他的角色权限而有所不同。而超级管理员是可以查看所有菜单的</p>
<p>实现此功能的前端代码位于<code>src/permission.js</code>文件。它在登录成功后，会在跳转到新路由之前，去查询当前用户有权访问的动态菜单的路由列表。</p>
<p>后端处理逻辑则在<code>dkd-admin</code>模块的SysLoginController中，它负责接收前端发来的请求，处理查询，并构建起一个完整的菜单树结构，然后返回给前端。</p>
<h3 id="前端-2"><a href="#前端-2" class="headerlink" title="前端"></a>前端</h3><ol>
<li>在全局<code>permission.js</code>中的<code>router.beforeEach</code>方法用于在用户导航到不同路由之前进行一些预处理</li>
<li>调用<code>store/mondles/permission.js</code>中的<code>generateRoutes</code>方法，将返回结果存入usePermissionStore对象中</li>
<li>调用<code>api/menu.js</code>中的<code>getRouters</code>方法</li>
</ol>
<h3 id="后端-2"><a href="#后端-2" class="headerlink" title="后端"></a>后端</h3><h4 id="SysLoginController-2"><a href="#SysLoginController-2" class="headerlink" title="SysLoginController"></a>SysLoginController</h4><p>在<code>ruoyi-admin</code>模块中<code>com.ruoyi.web.controller.system.SysLoginController</code>类的<code>getRouters</code>方法接收前端获取路由信息请求</p>
<h4 id="ISysMenuService"><a href="#ISysMenuService" class="headerlink" title="ISysMenuService"></a>ISysMenuService</h4><p>在<code>ruoyi-system</code>模块中<code>com.ruoyi.web.system.service.ISysMenuService</code>类</p>
<ol>
<li>selectMenuTreeByUserId根据用户ID查询菜单树信息（递归生成父子菜单）</li>
<li>buildMenus构建前端路由所需要的菜单路由格式RouterVo</li>
</ol>
<p><strong>具体的执行的流程如下图：</strong></p>
<p><img src="/2025/01/27/Spring-Security/image-20240810163753513.png" alt="image-20240810163753513"></p>
<h3 id="路由菜单加载"><a href="#路由菜单加载" class="headerlink" title="路由菜单加载"></a>路由菜单加载</h3><ol>
<li>用户登录成功后，通过路由<code>router/index.js</code>跳转到首页并加载layout布局组件</li>
<li>在<code>layout/index.vue</code>中加载sidbar侧边栏</li>
<li>在<code>layout/components/Sidebar/index.vue</code>中遍历动态路由菜单在页面显示</li>
<li>用户点击菜单后会根据路由的path跳转到对应的视图组件在<app-main />显示</li>
</ol>
<h2 id="权限注解"><a href="#权限注解" class="headerlink" title="权限注解"></a>权限注解</h2><h3 id="源码分析"><a href="#源码分析" class="headerlink" title="源码分析"></a>源码分析</h3><p>在若依框架中，权限的验证最核心的是使用的Spring Security的提供的权限注解<code>@PreAuthorize</code></p>
<ul>
<li><p>@PreAuthorize 是 Spring Security 框架中提供的一个安全注解，用于实现基于注解的访问控制。它允许开发者在<strong>方法级别</strong>上声明特定的安全约束，以确保只有满足指定条件的用户才能调用该方法</p>
</li>
<li><p>当 @PreAuthorize 注解被应用于某个方法时，Spring Security 在<strong>该方法执行前</strong>会先对当前认证的用户进行权限检查。如果检查通过，方法调用得以继续；否则，框架会抛出相应的权限异常（如 AccessDeniedException），阻止方法执行。</p>
</li>
</ul>
<p>若依框架中的权限控制代码，如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@PreAuthorize(&quot;@ss.hasPermi(&#x27;manage:order:list&#x27;)&quot;)</span></span><br><span class="line"><span class="keyword">public</span> TableDataInfo <span class="title function_">list</span><span class="params">(Order order)</span></span><br><span class="line">&#123;</span><br><span class="line">    startPage();</span><br><span class="line">    List&lt;Order&gt; list = orderService.selectOrderList(order);</span><br><span class="line">    <span class="keyword">return</span> getDataTable(list);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>@PreAuthorize是Spring Security框架的权限注解，在执行方法前执行</p>
</li>
<li><p>@ss.hasPermi(‘manage:order:list’)</p>
<ul>
<li>其中的ss是指的一个spring管理的bean<ul>
<li>位置：ruoyi-framework模块中的com.ruoyi.framework.web.service.PermissionService</li>
</ul>
</li>
<li>hasPermi  是PermissionService类中的一个方法，判断是否拥有该权限</li>
<li>manage:order:list为方法的参数</li>
</ul>
</li>
</ul>
<blockquote>
<p>注意：在SecurityConfig类中添加<code>@EnableGlobalMethodSecurity(prePostEnabled = true, securedEnabled = true)</code>@PreAuthorize才能生效</p>
</blockquote>
<p><strong>权限控制流程：</strong></p>
<p><img src="/2025/01/27/Spring-Security/image-20240815101708181.png" alt="image-20240815101708181"></p>
<h3 id="权限方法"><a href="#权限方法" class="headerlink" title="权限方法"></a>权限方法</h3><p><code>@PreAuthorize</code>注解用于配置接口要求用户拥有某些权限才可访问，它拥有如下方法</p>
<table>
<thead>
<tr>
<th>方法</th>
<th>参数</th>
<th>描述</th>
</tr>
</thead>
<tbody><tr>
<td>hasPermi</td>
<td>String</td>
<td>验证用户是否具备某权限</td>
</tr>
<tr>
<td>lacksPermi</td>
<td>String</td>
<td>验证用户是否不具备某权限，与 hasPermi逻辑相反</td>
</tr>
<tr>
<td>hasAnyPermi</td>
<td>String</td>
<td>验证用户是否具有以下任意一个权限</td>
</tr>
<tr>
<td>hasRole</td>
<td>String</td>
<td>判断用户是否拥有某个角色</td>
</tr>
<tr>
<td>lacksRole</td>
<td>String</td>
<td>验证用户是否不具备某角色，与 hasRole逻辑相反</td>
</tr>
<tr>
<td>hasAnyRoles</td>
<td>String</td>
<td>验证用户是否具有以下任意一个角色，多个逗号分隔</td>
</tr>
</tbody></table>
<h3 id="使用示例"><a href="#使用示例" class="headerlink" title="使用示例"></a>使用示例</h3><p>1）数据权限示例。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 符合system:user:list权限要求</span></span><br><span class="line"><span class="meta">@PreAuthorize(&quot;@ss.hasPermi(&#x27;system:user:list&#x27;)&quot;)</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 不符合system:user:list权限要求</span></span><br><span class="line"><span class="meta">@PreAuthorize(&quot;@ss.lacksPermi(&#x27;system:user:list&#x27;)&quot;)</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 符合system:user:add或system:user:edit权限要求即可</span></span><br><span class="line"><span class="meta">@PreAuthorize(&quot;@ss.hasAnyPermi(&#x27;system:user:add,system:user:edit&#x27;)&quot;)</span></span><br></pre></td></tr></table></figure>

<p><strong>编程式判断是否有资源权限</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (SecurityUtils.hasPermi(<span class="string">&quot;sys:user:edit&quot;</span>))</span><br><span class="line">&#123;</span><br><span class="line">    System.out.println(<span class="string">&quot;当前用户有编辑用户权限&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>2）角色权限示例。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 属于user角色</span></span><br><span class="line"><span class="meta">@PreAuthorize(&quot;@ss.hasRole(&#x27;user&#x27;)&quot;)</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 不属于user角色</span></span><br><span class="line"><span class="meta">@PreAuthorize(&quot;@ss.lacksRole(&#x27;user&#x27;)&quot;)</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 属于user或者admin之一</span></span><br><span class="line"><span class="meta">@PreAuthorize(&quot;@ss.hasAnyRoles(&#x27;user,admin&#x27;)&quot;)</span></span><br></pre></td></tr></table></figure>

<p><strong>编程式判断是否有角色权限</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (SecurityUtils.hasRole(<span class="string">&quot;admin&quot;</span>))</span><br><span class="line">&#123;</span><br><span class="line">    System.out.println(<span class="string">&quot;当前用户有admin角色权限&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>权限提示：超级管理员拥有所有权限，不受权限约束。</p>
</blockquote>
<h3 id="公开接口"><a href="#公开接口" class="headerlink" title="公开接口"></a>公开接口</h3><p>如果有些接口是不需要验证权限可以公开访问的，这个时候就需要我们给接口放行。</p>
<p>使用注解方式，只需要在<code>Controller</code>的类或方法上加入<code>@Anonymous</code>该注解即可</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// @PreAuthorize(&quot;@ss.xxxx(&#x27;....&#x27;)&quot;) 注释或删除掉原有的权限注解</span></span><br><span class="line"><span class="meta">@Anonymous</span></span><br><span class="line"><span class="meta">@GetMapping(&quot;/list&quot;)</span></span><br><span class="line"><span class="keyword">public</span> List&lt;SysXxxx&gt; <span class="title function_">list</span><span class="params">(SysXxxx xxxx)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">return</span> xxxxList;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Spring</tag>
      </tags>
  </entry>
  <entry>
    <title>尚硅谷尚庭公寓-01</title>
    <url>/2025/01/22/%E5%B0%9A%E7%A1%85%E8%B0%B7%E5%B0%9A%E5%BA%AD%E5%85%AC%E5%AF%93-01/</url>
    <content><![CDATA[<h1 id="枚举类型转换（WebDataBinder、TypeHandler、HTTPMessageConverter）"><a href="#枚举类型转换（WebDataBinder、TypeHandler、HTTPMessageConverter）" class="headerlink" title="枚举类型转换（WebDataBinder、TypeHandler、HTTPMessageConverter）"></a>枚举类型转换（WebDataBinder、TypeHandler、HTTPMessageConverter）</h1><h2 id="为什么要用枚举类"><a href="#为什么要用枚举类" class="headerlink" title="为什么要用枚举类"></a>为什么要用枚举类</h2><ul>
<li><p>实体类中的公共字段（例如<code>id</code>、<code>create_time</code>、<code>update_time</code>、<code>is_deleted</code>）抽取到一个基类，进行统一管理，然后让各实体类继承该基类。</p>
</li>
<li><p>实体类中的状态字段（例如<code>status</code>）或类型字段（例如<code>type</code>），全部使用枚举类型。</p>
<blockquote>
<p>状态（类型）字段，在数据库中通常用一个数字表示一个状态（类型）。例如：订单状态（1:待支付，2:待发货，3:待收货，4:已收货，5:已完结）。若实体类中对应的字段也用数字类型，例如<code>int</code>，那么程序中就会有大量的如下代码：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">order.setStatus(<span class="number">1</span>);</span><br><span class="line"><span class="keyword">if</span> (order.getStatus() == <span class="number">1</span>) &#123;</span><br><span class="line"> order.setStatus(<span class="number">2</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这些代码后期维护起来会十分麻烦，所以本项目中所有的此类字段均使用枚举类型。例如上述订单状态可定义为以下枚举：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">enum</span> <span class="title class_">Status</span> &#123;</span><br><span class="line"></span><br><span class="line"> CANCEL(<span class="number">0</span>, <span class="string">&quot;已取消&quot;</span>),</span><br><span class="line"> WAIT_PAY(<span class="number">1</span>, <span class="string">&quot;待支付&quot;</span>),</span><br><span class="line"> WAIT_TRANSFER(<span class="number">2</span>, <span class="string">&quot;待发货&quot;</span>),</span><br><span class="line"> WAIT_RECEIPT(<span class="number">3</span>, <span class="string">&quot;待收货&quot;</span>),</span><br><span class="line"> RECEIVE(<span class="number">4</span>, <span class="string">&quot;已收货&quot;</span>),</span><br><span class="line"> COMPLETE(<span class="number">5</span>, <span class="string">&quot;已完结&quot;</span>);</span><br><span class="line"> </span><br><span class="line"> <span class="keyword">private</span> <span class="keyword">final</span> Integer value;</span><br><span class="line"> <span class="keyword">private</span> <span class="keyword">final</span> String desc;</span><br><span class="line"> </span><br><span class="line"> <span class="keyword">public</span> Integer <span class="title function_">value</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> value;</span><br><span class="line"> &#125;</span><br><span class="line"> <span class="keyword">public</span> String <span class="title function_">desc</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> desc;</span><br><span class="line"> &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>订单实体类中的状态字段定义为<code>Status</code>类型：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Data</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Order</span>&#123;</span><br><span class="line">  <span class="keyword">private</span> Integer id;</span><br><span class="line">  <span class="keyword">private</span> Integer userId;</span><br><span class="line">  <span class="keyword">private</span> Status status;</span><br><span class="line"> ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这样上述代码便可调整为如下效果，后期维护起来会容易许多。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">order.setStatus(Status.WAIT_PAY);</span><br></pre></td></tr></table></figure></blockquote>
</li>
</ul>
<span id="more"></span>

<h2 id="转换场景"><a href="#转换场景" class="headerlink" title="转换场景"></a>转换场景</h2><p>在[根据类型]查询标签列表API中，前端携带的参数为枚举类的<strong>code</strong>变量值，在<strong>controller层</strong>需要将<strong>code</strong>转换为完整的枚举类，需要使用<strong>WebDataBinder</strong>进行转换，在于数据库的交互中，需要将枚举类的<strong>type</strong>值拿出来与数据库中的字段匹配，需要使用<strong>Mybatis-Plus</strong>的<strong>TypeHandler</strong>进行转换，在返回数据至前端的过程中，又需要使用<strong>HTTPMessageConverter</strong>将<strong>Controller</strong>方法的返回值（Java对象）转换为HTTP响应体中的JSON字符串。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Operation(summary = &quot;（根据类型）查询标签列表&quot;)</span></span><br><span class="line"><span class="meta">@GetMapping(&quot;list&quot;)</span></span><br><span class="line"><span class="keyword">public</span> Result&lt;List&lt;LabelInfo&gt;&gt; <span class="title function_">labelList</span><span class="params">(<span class="meta">@RequestParam(required = false)</span> ItemType type)</span> &#123;</span><br><span class="line"></span><br><span class="line">    LambdaQueryWrapper&lt;LabelInfo&gt; queryWrapper = <span class="keyword">new</span> <span class="title class_">LambdaQueryWrapper</span>&lt;&gt;();</span><br><span class="line">    queryWrapper.eq(type != <span class="literal">null</span>, LabelInfo::getType, type);</span><br><span class="line">    List&lt;LabelInfo&gt; list = service.list(queryWrapper);</span><br><span class="line">    <span class="keyword">return</span> Result.ok(list);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上述接口的功能是根据<strong>type</strong>（公寓&#x2F;房间），查询标签列表。由于这个<strong>type</strong>字段在数据库、实体类、前后端交互的过程中有多种不同的形式，因此在请求和响应的过程中，<strong>type</strong>字段会涉及到多次类型转换。</p>
<p>首先明确一下<strong>type</strong>字段的各种形式：</p>
<ul>
<li><p><strong>数据库中</strong></p>
<p>数据库中的<strong>type</strong>字段为<code>tinyint</code>类型</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">+-------------+--------------+</span><br><span class="line">| Field       | Type         |</span><br><span class="line">+-------------+--------------+</span><br><span class="line">| id          | bigint       |</span><br><span class="line">| type        | tinyint      |</span><br><span class="line">| name        | varchar(255) |</span><br><span class="line">| create_time | timestamp    |</span><br><span class="line">| update_time | timestamp    |</span><br><span class="line">| is_deleted  | tinyint      |</span><br><span class="line">+-------------+--------------+</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>实体类</strong></p>
<p>实体类中的<strong>type</strong>字段为<code>ItemType</code>枚举类型</p>
<p><code>LabelInfo</code>实体类如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Schema(description = &quot;标签信息表&quot;)</span></span><br><span class="line"><span class="meta">@TableName(value = &quot;label_info&quot;)</span></span><br><span class="line"><span class="meta">@Data</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">LabelInfo</span> <span class="keyword">extends</span> <span class="title class_">BaseEntity</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">long</span> <span class="variable">serialVersionUID</span> <span class="operator">=</span> <span class="number">1L</span>;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Schema(description = &quot;类型&quot;)</span></span><br><span class="line">    <span class="meta">@TableField(value = &quot;type&quot;)</span></span><br><span class="line">    <span class="keyword">private</span> ItemType type;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Schema(description = &quot;标签名称&quot;)</span></span><br><span class="line">    <span class="meta">@TableField(value = &quot;name&quot;)</span></span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>ItemType</code>枚举类如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">enum</span> <span class="title class_">ItemType</span> &#123;</span><br><span class="line"></span><br><span class="line">    APARTMENT(<span class="number">1</span>, <span class="string">&quot;公寓&quot;</span>),</span><br><span class="line">    ROOM(<span class="number">2</span>, <span class="string">&quot;房间&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> Integer code;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line"></span><br><span class="line">    ItemType(Integer code, String name) &#123;</span><br><span class="line">        <span class="built_in">this</span>.code = code;</span><br><span class="line">        <span class="built_in">this</span>.name = name;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>前后端交互中</strong></p>
<p>前后端交互所传递的数据中<strong>type</strong>字段为<strong>数字</strong>（1&#x2F;2)。</p>
<img src="image-20250122160519880.png" style="zoom: 70%; border: 1px solid rgb(0, 0, 0);" /></li>
</ul>
<p>具体转换过程如下图所示：</p>
<ul>
<li><p><strong>请求流程</strong></p>
<img src="枚举类型转换过程-请求参数.drawio.png" style="zoom: 200%;" />

<p><strong>说明</strong></p>
<ul>
<li>SpringMVC中的<code>WebDataBinder</code>组件负责将HTTP的请求参数绑定到Controller方法的参数，并实现参数类型的转换。</li>
<li>Mybatis中的<code>TypeHandler</code>用于处理Java中的实体对象与数据库之间的数据类型转换。</li>
</ul>
</li>
<li><p><strong>响应流程</strong></p>
<img src="枚举类型转换过程-响应体.drawio.png" style="zoom:200%;" />

<p><strong>说明</strong></p>
<ul>
<li>SpringMVC中的<code>HTTPMessageConverter</code>组件负责将Controller方法的返回值（Java对象）转换为HTTP响应体中的JSON字符串，或者将请求体中的JSON字符串转换为Controller方法中的参数（Java对象），例如下一个接口<strong>保存或更新标签信息</strong></li>
</ul>
<img src="枚举类型转换过程-请求体.drawio.png" style="zoom:200%;" /></li>
</ul>
<h2 id="WebDataBinder枚举类型和属性的转换"><a href="#WebDataBinder枚举类型和属性的转换" class="headerlink" title="WebDataBinder枚举类型和属性的转换"></a>WebDataBinder枚举类型和属性的转换</h2><p><code>WebDataBinder</code>依赖于<a href="https://docs.spring.io/spring-framework/reference/core/validation/convert.html"><code>Converter</code></a>实现类型转换，若Controller方法声明的<code>@RequestParam</code>参数的类型不是<code>String</code>，<code>WebDataBinder</code>就会自动进行数据类型转换。SpringMVC提供了常用类型的转换器，例如<code>String</code>到<code>Integer</code>、<code>String</code>到<code>Date</code>，<code>String</code>到<code>Boolean</code>等等，其中也包括<code>String</code>到枚举类型，但是<code>String</code>到枚举类型的默认转换规则是根据实例名称（”APARTMENT”）转换为枚举对象实例（ItemType.APARTMENT）。若想实现<code>code</code>属性到枚举对象实例的转换，需要自定义<code>Converter</code>，代码如下，具体内容可参考<a href="https://docs.spring.io/spring-framework/reference/core/validation/convert.html#core-convert-Converter-API">官方文档</a>。</p>
<ul>
<li><p>在<strong>web-admin模块</strong>自定义<code>com.atguigu.lease.web.admin.custom.converter.StringToItemTypeConverter</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">StringToItemTypeConverter</span> <span class="keyword">implements</span> <span class="title class_">Converter</span>&lt;String, ItemType&gt; &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> ItemType <span class="title function_">convert</span><span class="params">(String code)</span> &#123;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (ItemType value : ItemType.values()) &#123;</span><br><span class="line">            <span class="keyword">if</span> (value.getCode().equals(Integer.valueOf(code))) &#123;</span><br><span class="line">                <span class="keyword">return</span> value;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalArgumentException</span>(<span class="string">&quot;code非法&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>注册上述的<code>StringToItemTypeConverter</code>，在<strong>web-admin模块</strong>创建<code>com.atguigu.lease.web.admin.custom.config.WebMvcConfiguration</code>，内容如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">WebMvcConfiguration</span> <span class="keyword">implements</span> <span class="title class_">WebMvcConfigurer</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> StringToItemTypeConverter stringToItemTypeConverter;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">addFormatters</span><span class="params">(FormatterRegistry registry)</span> &#123;</span><br><span class="line">        registry.addConverter(<span class="built_in">this</span>.stringToItemTypeConverter);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<p>但是我们有很多的枚举类型都需要考虑类型转换这个问题，按照上述思路，我们需要为每个枚举类型都定义一个Converter，并且每个Converter的转换逻辑都完全相同，针对这种情况，我们使用<a href="https://docs.spring.io/spring-framework/reference/core/validation/convert.html#core-convert-ConverterFactory-SPI"><code>ConverterFactory</code></a>接口更为合适，这个接口可以将同一个转换逻辑应用到一个接口的所有实现类，因此我们可以定义一个<code>BaseEnum</code>接口，然后另所有的枚举类都实现该接口，然后就可以自定义<code>ConverterFactory</code>，集中编写各枚举类的转换逻辑了。具体实现如下：</p>
<ul>
<li><p>在<strong>model模块</strong>定义<code>com.atguigu.lease.model.enums.BaseEnum</code>接口</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">BaseEnum</span> &#123;</span><br><span class="line">    Integer <span class="title function_">getCode</span><span class="params">()</span>;</span><br><span class="line">    String <span class="title function_">getName</span><span class="params">()</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>令所有<code>com.atguigu.lease.model.enums</code>包下的枚举类都实现<code>BaseEnun</code>接口</p>
</li>
<li><p>在<strong>web-admin模块</strong>自定义<code>com.atguigu.lease.web.admin.custom.converter.StringToBaseEnumConverterFactory</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">StringToBaseEnumConverterFactory</span> <span class="keyword">implements</span> <span class="title class_">ConverterFactory</span>&lt;String, BaseEnum&gt; &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> &lt;T <span class="keyword">extends</span> <span class="title class_">BaseEnum</span>&gt; Converter&lt;String, T&gt; <span class="title function_">getConverter</span><span class="params">(Class&lt;T&gt; targetType)</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Converter</span>&lt;String, T&gt;() &#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="keyword">public</span> T <span class="title function_">convert</span><span class="params">(String source)</span> &#123;</span><br><span class="line"></span><br><span class="line">                <span class="keyword">for</span> (T enumConstant : targetType.getEnumConstants()) &#123;</span><br><span class="line">                    <span class="keyword">if</span> (enumConstant.getCode().equals(Integer.valueOf(source))) &#123;</span><br><span class="line">                        <span class="keyword">return</span> enumConstant;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalArgumentException</span>(<span class="string">&quot;非法的枚举值:&quot;</span> + source);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>注册上述的<code>ConverterFactory</code>，在<strong>web-admin模块</strong>创建<code>com.atguigu.lease.web.admin.custom.config.WebMvcConfiguration</code>，内容如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">WebMvcConfiguration</span> <span class="keyword">implements</span> <span class="title class_">WebMvcConfigurer</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> StringToBaseEnumConverterFactory stringToBaseEnumConverterFactory;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">addFormatters</span><span class="params">(FormatterRegistry registry)</span> &#123;</span><br><span class="line">        registry.addConverterFactory(<span class="built_in">this</span>.stringToBaseEnumConverterFactory);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>注意：</strong></p>
<p>最终采用的是<code>ConverterFactory</code>方案，因此<code>StringToItemTypeConverter</code>相关代码可以直接删除。</p>
</li>
</ul>
<h2 id="TypeHandler枚举类型和属性的转换"><a href="#TypeHandler枚举类型和属性的转换" class="headerlink" title="TypeHandler枚举类型和属性的转换"></a>TypeHandler枚举类型和属性的转换</h2><p>Mybatis预置的<code>TypeHandler</code>可以处理常用的数据类型转换，例如<code>String</code>、<code>Integer</code>、<code>Date</code>等等，其中也包含枚举类型，但是枚举类型的默认转换规则是枚举对象实例（ItemType.APARTMENT）和实例名称（”APARTMENT”）相互映射。若想实现<code>code</code>属性到枚举对象实例的相互映射，需要自定义<code>TypeHandler</code>。</p>
<p>不过MybatisPlus提供了一个<a href="https://baomidou.com/pages/8390a4/">通用的处理枚举类型的TypeHandler</a>。其使用十分简单，只需在<code>ItemType</code>枚举类的<code>code</code>属性上增加一个注解<code>@EnumValue</code>，Mybatis-Plus便可完成从<code>ItemType</code>对象到<code>code</code>属性之间的相互映射，具体配置如下。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">enum</span> <span class="title class_">ItemType</span> &#123;</span><br><span class="line"></span><br><span class="line">    APARTMENT(<span class="number">1</span>, <span class="string">&quot;公寓&quot;</span>),</span><br><span class="line">    ROOM(<span class="number">2</span>, <span class="string">&quot;房间&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="meta">@EnumValue</span></span><br><span class="line">    <span class="keyword">private</span> Integer code;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line"></span><br><span class="line">    ItemType(Integer code, String name) &#123;</span><br><span class="line">        <span class="built_in">this</span>.code = code;</span><br><span class="line">        <span class="built_in">this</span>.name = name;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="HTTPMessageConverter实现枚举类型和属性的转换"><a href="#HTTPMessageConverter实现枚举类型和属性的转换" class="headerlink" title="HTTPMessageConverter实现枚举类型和属性的转换"></a>HTTPMessageConverter实现枚举类型和属性的转换</h2><p><code>HttpMessageConverter</code>依赖于Json序列化框架（默认使用Jackson）。其对枚举类型的默认处理规则也是枚举对象实例（ItemType.APARTMENT）和实例名称（”APARTMENT”）相互映射。不过其提供了一个注解<code>@JsonValue</code>，同样只需在<code>ItemType</code>枚举类的<code>code</code>属性上增加一个注解<code>@JsonValue</code>，Jackson便可完成从<code>ItemType</code>对象到<code>code</code>属性之间的互相映射。具体配置如下，详细信息可参考Jackson<a href="https://fasterxml.github.io/jackson-annotations/javadoc/2.8/com/fasterxml/jackson/annotation/JsonValue.html">官方文档</a>。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Getter</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">enum</span> <span class="title class_">ItemType</span> &#123;</span><br><span class="line"></span><br><span class="line">    APARTMENT(<span class="number">1</span>, <span class="string">&quot;公寓&quot;</span>),</span><br><span class="line">    ROOM(<span class="number">2</span>, <span class="string">&quot;房间&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="meta">@EnumValue</span></span><br><span class="line">   <span class="meta">@JsonValue</span></span><br><span class="line">    <span class="keyword">private</span> Integer code;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line"></span><br><span class="line">    ItemType(Integer code, String name) &#123;</span><br><span class="line">        <span class="built_in">this</span>.code = code;</span><br><span class="line">        <span class="built_in">this</span>.name = name;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>尚庭公寓</tag>
        <tag>项目实战</tag>
      </tags>
  </entry>
  <entry>
    <title>尚硅谷尚庭公寓-02</title>
    <url>/2025/01/23/%E5%B0%9A%E7%A1%85%E8%B0%B7%E5%B0%9A%E5%BA%AD%E5%85%AC%E5%AF%93-02/</url>
    <content><![CDATA[<h1 id="尚硅谷尚庭公寓-02"><a href="#尚硅谷尚庭公寓-02" class="headerlink" title="尚硅谷尚庭公寓-02"></a>尚硅谷尚庭公寓-02</h1><h2 id="MinIo实现图片上传功能"><a href="#MinIo实现图片上传功能" class="headerlink" title="MinIo实现图片上传功能"></a>MinIo实现图片上传功能</h2><ul>
<li><p><strong>配置Minio Client</strong></p>
<ul>
<li><p>引入Minio Maven依赖</p>
<p>在<strong>common模块</strong>的<code>pom.xml</code>文件增加如下内容：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>io.minio<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>minio<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>配置Minio相关参数</p>
<p>在<code>application.yml</code>中配置Minio的<code>endpoint</code>、<code>accessKey</code>、<code>secretKey</code>、<code>bucketName</code>等参数</p>
<figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="attr">minio:</span></span><br><span class="line">  <span class="attr">endpoint:</span> <span class="string">http://&lt;hostname&gt;:&lt;port&gt;</span></span><br><span class="line">  <span class="attr">access-key:</span> <span class="string">&lt;access-key&gt;</span></span><br><span class="line">  <span class="attr">secret-key:</span> <span class="string">&lt;secret-key&gt;</span></span><br><span class="line">  <span class="attr">bucket-name:</span> <span class="string">&lt;bucket-name&gt;</span></span><br></pre></td></tr></table></figure>

<p><strong>注意</strong>：上述<code>&lt;hostname&gt;</code>、<code>&lt;port&gt;</code>等信息需根据实际情况进行修改。</p>
</li>
<li><p>在<strong>common模块</strong>中创建<code>com.atguigu.lease.common.minio.MinioProperties</code>，内容如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@ConfigurationProperties(prefix = &quot;minio&quot;)</span></span><br><span class="line"><span class="meta">@Data</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MinioProperties</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> String endpoint;</span><br><span class="line">    <span class="keyword">private</span> String accessKey;</span><br><span class="line">    <span class="keyword">private</span> String secretKey;</span><br><span class="line">    <span class="keyword">private</span> String bucketName;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>在<strong>common模块</strong>中创建<code>com.atguigu.lease.common.minio.MinioConfiguration</code>，内容如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="meta">@EnableConfigurationProperties(MinioProperties.class)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MinioConfiguration</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> MinioProperties properties;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="keyword">public</span> MinioClient <span class="title function_">minioClient</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> MinioClient.builder().endpoint(properties.getEndpoint()).credentials(properties.getAccessKey(), properties.getSecretKey()).build();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
</ul>
<span id="more"></span>
<ul>
<li><p><strong>开发图片上传接口</strong></p>
<ul>
<li><p>编写Controller层逻辑</p>
<p>在<code>FileUploadController</code>中增加如下内容</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Tag(name = &quot;文件管理&quot;)</span></span><br><span class="line"><span class="meta">@RequestMapping(&quot;/admin/file&quot;)</span></span><br><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">FileUploadController</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> FileService service;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Operation(summary = &quot;上传文件&quot;)</span></span><br><span class="line">    <span class="meta">@PostMapping(&quot;upload&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> Result&lt;String&gt; <span class="title function_">upload</span><span class="params">(<span class="meta">@RequestParam</span> MultipartFile file)</span> &#123;</span><br><span class="line"></span><br><span class="line">        <span class="type">String</span> <span class="variable">url</span> <span class="operator">=</span> service.upload(file);</span><br><span class="line">        <span class="keyword">return</span> Result.ok(url);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>说明：</strong><code>MultipartFile</code>是Spring框架中用于处理文件上传的类，它包含了上传文件的信息（如文件名、文件内容等）。</p>
</li>
<li><p>编写Service层逻辑</p>
<ul>
<li><p>在<code>FileService</code>中增加如下内容</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">String <span class="title function_">upload</span><span class="params">(MultipartFile file)</span>;</span><br></pre></td></tr></table></figure>
</li>
<li><p>在<code>FileServiceImpl</code>中增加如下内容</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Autowired</span></span><br><span class="line"><span class="keyword">private</span> MinioProperties properties;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Autowired</span></span><br><span class="line"><span class="keyword">private</span> MinioClient client;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> String <span class="title function_">upload</span><span class="params">(MultipartFile file)</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="type">boolean</span> <span class="variable">bucketExists</span> <span class="operator">=</span> client.bucketExists(BucketExistsArgs.builder().bucket(properties.getBucketName()).build());</span><br><span class="line">        <span class="keyword">if</span> (!bucketExists) &#123;</span><br><span class="line">            client.makeBucket(MakeBucketArgs.builder().bucket(properties.getBucketName()).build());</span><br><span class="line">            client.setBucketPolicy(SetBucketPolicyArgs.builder().bucket(properties.getBucketName()).config(createBucketPolicyConfig(properties.getBucketName())).build());</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="type">String</span> <span class="variable">filename</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">SimpleDateFormat</span>(<span class="string">&quot;yyyyMMdd&quot;</span>).format(<span class="keyword">new</span> <span class="title class_">Date</span>()) + <span class="string">&quot;/&quot;</span> + UUID.randomUUID() + <span class="string">&quot;-&quot;</span> + file.getOriginalFilename();</span><br><span class="line">        client.putObject(PutObjectArgs.builder().</span><br><span class="line">                bucket(properties.getBucketName()).</span><br><span class="line">                object(filename).</span><br><span class="line">                stream(file.getInputStream(), file.getSize(), -<span class="number">1</span>).</span><br><span class="line">                contentType(file.getContentType()).build());</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> String.join(<span class="string">&quot;/&quot;</span>, properties.getEndpoint(), properties.getBucketName(), filename);</span><br><span class="line"></span><br><span class="line">    &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> String <span class="title function_">createBucketPolicyConfig</span><span class="params">(String bucketName)</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">            &#123;</span></span><br><span class="line"><span class="string">              &quot;Statement&quot; : [ &#123;</span></span><br><span class="line"><span class="string">                &quot;Action&quot; : &quot;s3:GetObject&quot;,</span></span><br><span class="line"><span class="string">                &quot;Effect&quot; : &quot;Allow&quot;,</span></span><br><span class="line"><span class="string">                &quot;Principal&quot; : &quot;*&quot;,</span></span><br><span class="line"><span class="string">                &quot;Resource&quot; : &quot;arn:aws:s3:::%s/*&quot;</span></span><br><span class="line"><span class="string">              &#125; ],</span></span><br><span class="line"><span class="string">              &quot;Version&quot; : &quot;2012-10-17&quot;</span></span><br><span class="line"><span class="string">            &#125;</span></span><br><span class="line"><span class="string">            &quot;&quot;&quot;</span>.formatted(bucketName);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>注意</strong>：</p>
<p>上述<code>createBucketPolicyConfig</code>方法的作用是生成用于描述指定bucket访问权限的JSON字符串。最终生成的字符串格式如下，其表示，允许(<code>Allow</code>)所有人(<code>*</code>)获取(<code>s3:GetObject</code>)指定桶(<code>&lt;bucket-name&gt;</code>)的内容。</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;Statement&quot;</span> <span class="punctuation">:</span> <span class="punctuation">[</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;Action&quot;</span> <span class="punctuation">:</span> <span class="string">&quot;s3:GetObject&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;Effect&quot;</span> <span class="punctuation">:</span> <span class="string">&quot;Allow&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;Principal&quot;</span> <span class="punctuation">:</span> <span class="string">&quot;*&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;Resource&quot;</span> <span class="punctuation">:</span> <span class="string">&quot;arn:aws:s3:::&lt;bucket-name&gt;/*&quot;</span></span><br><span class="line">  <span class="punctuation">&#125;</span> <span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;Version&quot;</span> <span class="punctuation">:</span> <span class="string">&quot;2012-10-17&quot;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>由于公寓、房间的图片为公开信息，所以将其设置为所有人可访问。</p>
</li>
<li><p><strong>异常处理</strong></p>
<ul>
<li><p><strong>问题说明</strong></p>
<p>上述代码只是对<code>MinioClient</code>方法抛出的各种异常进行了捕获，然后打印了异常信息，目前这种处理逻辑，无论Minio是否发生异常，前端在上传文件时，总是会受到成功的响应信息。可按照以下步骤进行操作，查看具体现象</p>
<p>关闭虚拟机中的Minio服务</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">systemctl stop minio</span><br></pre></td></tr></table></figure>

<p>启动项目，并上传文件，观察接收的响应信息</p>
</li>
<li><p><strong>问题解决思路</strong></p>
<p>为保证前端能够接收到正常的错误提示信息，应该将Service方法的异常抛出到Controller方法中，然后在Controller方法中对异常进行捕获并处理。具体操作如下</p>
<p><strong>Service层代码</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> String <span class="title function_">upload</span><span class="params">(MultipartFile file)</span> <span class="keyword">throws</span> ServerException, InsufficientDataException, ErrorResponseException, IOException, NoSuchAlgorithmException, InvalidKeyException, InvalidResponseException, XmlParserException, InternalException&#123;</span><br><span class="line"></span><br><span class="line">    <span class="type">boolean</span> <span class="variable">bucketExists</span> <span class="operator">=</span> minioClient.bucketExists(</span><br><span class="line">            BucketExistsArgs.builder()</span><br><span class="line">                    .bucket(properties.getBucketName())</span><br><span class="line">                    .build());</span><br><span class="line">    <span class="keyword">if</span> (!bucketExists) &#123;</span><br><span class="line">        minioClient.makeBucket(</span><br><span class="line">                MakeBucketArgs.builder()</span><br><span class="line">                        .bucket(properties.getBucketName())</span><br><span class="line">                        .build());</span><br><span class="line">        minioClient.setBucketPolicy(</span><br><span class="line">                SetBucketPolicyArgs.builder()</span><br><span class="line">                        .bucket(properties.getBucketName())</span><br><span class="line">                        .config(createBucketPolicyConfig(properties.getBucketName()))</span><br><span class="line">                        .build());</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="type">String</span> <span class="variable">filename</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">SimpleDateFormat</span>(<span class="string">&quot;yyyyMMdd&quot;</span>).format(<span class="keyword">new</span> <span class="title class_">Date</span>()) +</span><br><span class="line">            <span class="string">&quot;/&quot;</span> + UUID.randomUUID() + <span class="string">&quot;-&quot;</span> + file.getOriginalFilename();</span><br><span class="line">    minioClient.putObject(</span><br><span class="line">            PutObjectArgs.builder()</span><br><span class="line">                    .bucket(properties.getBucketName())</span><br><span class="line">                    .stream(file.getInputStream(), file.getSize(), -<span class="number">1</span>)</span><br><span class="line">                    .object(filename)</span><br><span class="line">                    .contentType(file.getContentType())</span><br><span class="line">                    .build());</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> String.join(<span class="string">&quot;/&quot;</span>,properties.getEndpoint(),properties.getBucketName(),filename);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>Controller层代码</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> Result&lt;String&gt; <span class="title function_">upload</span><span class="params">(<span class="meta">@RequestParam</span> MultipartFile file)</span> &#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="type">String</span> <span class="variable">url</span> <span class="operator">=</span> service.upload(file);</span><br><span class="line">        <span class="keyword">return</span> Result.ok(url);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">        <span class="keyword">return</span> Result.fail();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>全局异常处理</strong></p>
<p>按照上述写法，所有的Controller层方法均需要增加<code>try-catch</code>逻辑，使用Spring MVC提供的<strong>全局异常处理</strong>功能，可以将所有处理异常的逻辑集中起来，进而统一处理所有异常，使代码更容易维护。</p>
<p>具体用法如下，详细信息可参考<a href="https://docs.spring.io/spring-framework/reference/web/webmvc/mvc-controller/ann-exceptionhandler.html">官方文档</a>：</p>
<p>在<strong>common模块</strong>中创建<code>com.atguigu.lease.common.exception.GlobalExceptionHandler</code>类，内容如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@ControllerAdvice</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">GlobalExceptionHandler</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@ExceptionHandler(Exception.class)</span></span><br><span class="line">    <span class="meta">@ResponseBody</span></span><br><span class="line">    <span class="keyword">public</span> Result <span class="title function_">error</span><span class="params">(Exception e)</span>&#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">        <span class="keyword">return</span> Result.fail();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上述代码中的关键注解的作用如下</p>
<p><code>@ControllerAdvice</code>用于声明处理全局Controller方法异常的类</p>
<p><code>@ExceptionHandler</code>用于声明处理异常的方法，<code>value</code>属性用于声明该方法处理的异常类型</p>
<p><code>@ResponseBody</code>表示将方法的返回值作为HTTP的响应体</p>
<p><strong>注意：</strong></p>
<p>全局异常处理功能由SpringMVC提供，因此需要在<strong>common模块</strong>的<code>pom.xml</code>中引入如下依赖</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!--spring-web--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-web<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>修改Controller层代码</strong></p>
<p>由于前文的<code>GlobalExceptionHandler</code>会处理所有Controller方法抛出的异常，因此Controller层就无序关注异常的处理逻辑了，因此Controller层代码可做出如下调整。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> Result&lt;String&gt; <span class="title function_">upload</span><span class="params">(<span class="meta">@RequestParam</span> MultipartFile file)</span> <span class="keyword">throws</span> ServerException, InsufficientDataException, ErrorResponseException, IOException, NoSuchAlgorithmException, InvalidKeyException, InvalidResponseException, XmlParserException, InternalException &#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">url</span> <span class="operator">=</span> service.upload(file);</span><br><span class="line">  <span class="keyword">return</span> Result.ok(url);</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure></li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
</ul>
<h2 id="Spring-Boot定时任务"><a href="#Spring-Boot定时任务" class="headerlink" title="Spring Boot定时任务"></a>Spring Boot定时任务</h2><ul>
<li><p>启用Spring Boot定时任务</p>
<p>在SpringBoot启动类上增加<code>@EnableScheduling</code>注解，如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@SpringBootApplication</span></span><br><span class="line"><span class="meta">@EnableScheduling</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">AdminWebApplication</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        SpringApplication.run(AdminWebApplication.class, args);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>编写定时逻辑</p>
<p>在<strong>web-admin模块</strong>下创建<code>com.atguigu.lease.web.admin.schedule.ScheduledTasks</code>类，内容如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ScheduledTasks</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> LeaseAgreementService leaseAgreementService;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Scheduled(cron = &quot;0 0 0 * * *&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">checkLeaseStatus</span><span class="params">()</span> &#123;</span><br><span class="line">        LambdaUpdateWrapper&lt;LeaseAgreement&gt; updateWrapper = <span class="keyword">new</span> <span class="title class_">LambdaUpdateWrapper</span>&lt;&gt;();</span><br><span class="line">        <span class="type">Date</span> <span class="variable">now</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Date</span>();</span><br><span class="line">        updateWrapper.le(LeaseAgreement::getLeaseEndDate, now);</span><br><span class="line">        updateWrapper.eq(LeaseAgreement::getStatus, LeaseStatus.SIGNED);</span><br><span class="line">        updateWrapper.in(LeaseAgreement::getStatus, LeaseStatus.SIGNED, LeaseStatus.WITHDRAWING);</span><br><span class="line">        leaseAgreementService.update(updateWrapper);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>知识点</strong>:</p>
<p>SpringBoot中的cron表达式语法如下</p>
<figure class="highlight txt"><table><tr><td class="code"><pre><span class="line">┌───────────── second (0-59)</span><br><span class="line">│ ┌───────────── minute (0 - 59)</span><br><span class="line">│ │ ┌───────────── hour (0 - 23)</span><br><span class="line">│ │ │ ┌───────────── day of the month (1 - 31)</span><br><span class="line">│ │ │ │ ┌───────────── month (1 - 12) (or JAN-DEC)</span><br><span class="line">│ │ │ │ │ ┌───────────── day of the week (0 - 7)</span><br><span class="line">│ │ │ │ │ │          (0 or 7 is Sunday, or MON-SUN)</span><br><span class="line">│ │ │ │ │ │</span><br><span class="line">* * * * * *</span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="密码加密处理"><a href="#密码加密处理" class="headerlink" title="密码加密处理"></a>密码加密处理</h2><p>用户的密码通常不会直接以明文的形式保存到数据库中，而是会先经过处理，然后将处理之后得到的”密文”保存到数据库，这样能够降低数据库泄漏导致的用户账号安全问题。<br>密码通常会使用一些单向函数进行处理，如下图所示<br><img src="image.png" style="zoom: 50%;" /><br>常用于处理密码的单向函数（算法）有MD5、SHA-256等，<strong>Apache Commons</strong>提供了一个工具类<code>DigestUtils</code>，其中就包含上述算法的实现。</p>
<blockquote>
<p><strong>Apache Commons</strong>是Apache软件基金会下的一个项目，其致力于提供可重用的开源软件，其中包含了很多易于使用的现成工具。<br>使用该工具类需引入<code>commons-codec</code>依赖，在<strong>common模块</strong>的pom.xml中增加如下内容</p>
</blockquote>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>commons-codec<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>commons-codec<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h2 id="Mybatis-Plus-update-strategy"><a href="#Mybatis-Plus-update-strategy" class="headerlink" title="Mybatis-Plus update strategy"></a>Mybatis-Plus update strategy</h2><p>使用Mybatis-Plus提供的更新方法时，若实体中的字段为<code>null</code>，默认情况下，最终生成的update语句中，不会包含该字段。若想改变默认行为，可做以下配置。</p>
<ul>
<li><p>全局配置<br>  在<code>application.yml</code>中配置如下参数</p>
  <figure class="highlight xml"><table><tr><td class="code"><pre><span class="line">mybatis-plus:</span><br><span class="line">  global-config:</span><br><span class="line">    db-config:</span><br><span class="line">      update-strategy: <span class="tag">&lt;<span class="name">strategy</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>  <strong>注</strong>：上述<code>&lt;strategy&gt;</code>可选值有：<code>ignore</code>、<code>not_null</code>、<code>not_empty</code>、<code>never</code>，默认值为<code>not_null</code></p>
<ul>
<li><p><code>ignore</code>：忽略空值判断，不管字段是否为空，都会进行更新</p>
</li>
<li><p><code>not_null</code>：进行非空判断，字段非空才会进行判断</p>
</li>
<li><p><code>not_empty</code>：进行非空判断，并进行非空串（””）判断，主要针对字符串类型</p>
</li>
<li><p><code>never</code>：从不进行更新，不管该字段为何值，都不更新</p>
</li>
</ul>
</li>
<li><p>局部配置<br>  在实体类中的具体字段通过<code>@TableField</code>注解进行配置，如下：</p>
  <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Schema(description = &quot;密码&quot;)</span></span><br><span class="line"><span class="meta">@TableField(value = &quot;password&quot;, updateStrategy = FieldStrategy.NOT_EMPTY)</span></span><br><span class="line"><span class="keyword">private</span> String password;</span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="登录功能的实现"><a href="#登录功能的实现" class="headerlink" title="登录功能的实现"></a>登录功能的实现</h2><h3 id="背景知识"><a href="#背景知识" class="headerlink" title="背景知识"></a>背景知识</h3><p><strong>1. 认证方案概述</strong></p>
<p>有两种常见的认证方案，分别是基于<strong>Session</strong>的认证和基于<strong>Token</strong>的认证，下面逐一进行介绍</p>
<ul>
<li><p><strong>基于Session</strong></p>
<p>基于Session的认证流程如下图所示</p>
<img src="登录流程-基于Session.drawio.png" style="zoom:50%;" />

<p>该方案的特点</p>
<ul>
<li>登录用户信息保存在服务端内存中，若访问量增加，单台节点压力会较大</li>
<li>随用户规模增大，若后台升级为集群，则需要解决集群中各服务器登录状态共享的问题。</li>
</ul>
</li>
<li><p><strong>基于Token</strong></p>
<p>基于Token的认证流程如下图所示</p>
<img src="登录流程-基于Token.drawio.png" style="zoom:50%;" />
该方案的特点
- 登录状态保存在客户端，服务器没有存储开销
- 客户端发起的每个请求自身均携带登录状态，所以即使后台为集群，也不会面临登录状态共享的问题。</li>
</ul>
<p><strong>2. Token详解</strong></p>
<p>本项目采用基于Token的登录方案，下面详细介绍Token这一概念。<br>我们所说的Token，通常指<strong>JWT</strong>（JSON Web TOKEN）。JWT是一种轻量级的安全传输方式，用于在两个实体之间传递信息，通常用于身份验证和信息传递。<br>JWT是一个字符串，如下图所示，该字符串由三部分组成，三部分由<code>.</code>分隔。三个部分分别被称为</p>
<ul>
<li><code>header</code>（头部）</li>
<li><code>payload</code>（负载）</li>
<li><code>signature</code>（签名）<img src="JWT.png" style="zoom: 80%;" /></li>
</ul>
<p>各部分的作用如下</p>
<ul>
<li><p><strong>Header（头部）</strong></p>
<p>Header部分是由一个JSON对象经过<code>base64url</code>编码得到的，这个JSON对象用于保存JWT 的类型（<code>typ</code>）、签名算法（<code>alg</code>）等元信息，例如</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;alg&quot;</span><span class="punctuation">:</span> <span class="string">&quot;HS256&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;typ&quot;</span><span class="punctuation">:</span> <span class="string">&quot;JWT&quot;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>Payload（负载）</strong></p>
<p>也称为 Claims（声明），也是由一个JSON对象经过<code>base64url</code>编码得到的，用于保存要传递的具体信息。JWT规范定义了7个官方字段，如下：</p>
<ul>
<li>iss (issuer)：签发人</li>
<li>exp (expiration time)：过期时间</li>
<li>sub (subject)：主题</li>
<li>aud (audience)：受众</li>
<li>nbf (Not Before)：生效时间</li>
<li>iat (Issued At)：签发时间</li>
<li>jti (JWT ID)：编号</li>
</ul>
<p>除此之外，我们还可以自定义任何字段，例如</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;sub&quot;</span><span class="punctuation">:</span> <span class="string">&quot;1234567890&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;John Doe&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;iat&quot;</span><span class="punctuation">:</span> <span class="number">1516239022</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>Signature（签名）</strong></p>
<p>由头部、负载和秘钥一起经过（header中指定的签名算法）计算得到的一个字符串，用于防止消息被篡改。</p>
</li>
</ul>
<h3 id="登录流程"><a href="#登录流程" class="headerlink" title="登录流程"></a>登录流程</h3><p>后台管理系统的登录流程如下图所示</p>
<img src="后台管理系统-登录流程.drawio.svg" style="zoom:50%;" />

<p>根据上述登录流程，可分析出，登录管理共需三个接口，分别是<strong>获取图形验证码</strong>、<strong>登录</strong>、<strong>获取登录用户个人信息</strong>，除此之外，我们还需为所有受保护的接口增加验证JWT合法性的逻辑，这一功能可通过<code>HandlerInterceptor</code>来实现。</p>
<h3 id="接口开发"><a href="#接口开发" class="headerlink" title="接口开发"></a>接口开发</h3><p>首先在<code>LoginController</code>中注入<code>LoginService</code>，如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Tag(name = &quot;后台管理系统登录管理&quot;)</span></span><br><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="meta">@RequestMapping(&quot;/admin&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">LoginController</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> LoginService service;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="1-获取图形验证码"><a href="#1-获取图形验证码" class="headerlink" title="1. 获取图形验证码"></a>1. 获取图形验证码</h4><ul>
<li><p><strong>查看响应的数据结构</strong></p>
<p>查看<strong>web-admin模块</strong>下的<code>com.atguigu.lease.web.admin.vo.login.CaptchaVo</code>，内容如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Data</span></span><br><span class="line"><span class="meta">@Schema(description = &quot;图像验证码&quot;)</span></span><br><span class="line"><span class="meta">@AllArgsConstructor</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">CaptchaVo</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Schema(description=&quot;验证码图片信息&quot;)</span></span><br><span class="line">    <span class="keyword">private</span> String image;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Schema(description=&quot;验证码key&quot;)</span></span><br><span class="line">    <span class="keyword">private</span> String key;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>配置所需依赖</strong></p>
<ul>
<li><p><strong>验证码生成工具</strong></p>
<p>本项目使用开源的验证码生成工具<strong>EasyCaptcha</strong>，其支持多种类型的验证码，例如gif、中文、算术等，并且简单易用，具体内容可参考其<a href="https://gitee.com/ele-admin/EasyCaptcha">官方文档</a>。</p>
<p>在<strong>common模块</strong>的pom.xml文件中增加如下内容</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.github.whvcse<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>easy-captcha<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>Redis</strong></p>
<p>在<strong>common模块</strong>的pom.xml中增加如下内容</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-data-redis<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>在<code>application.yml</code>中增加如下配置</p>
<figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="attr">spring:</span></span><br><span class="line">  <span class="attr">data:</span></span><br><span class="line">    <span class="attr">redis:</span></span><br><span class="line">      <span class="attr">host:</span> <span class="string">&lt;hostname&gt;</span></span><br><span class="line">      <span class="attr">port:</span> <span class="string">&lt;port&gt;</span></span><br><span class="line">      <span class="attr">database:</span> <span class="number">0</span></span><br></pre></td></tr></table></figure>

<p><strong>注意</strong>：上述<code>hostname</code>和<code>port</code>需根据实际情况进行修改</p>
</li>
</ul>
</li>
<li><p><strong>编写Controller层逻辑</strong></p>
<p>在<code>LoginController</code>中增加如下内容</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Operation(summary = &quot;获取图形验证码&quot;)</span></span><br><span class="line"><span class="meta">@GetMapping(&quot;login/captcha&quot;)</span></span><br><span class="line"><span class="keyword">public</span> Result&lt;CaptchaVo&gt; <span class="title function_">getCaptcha</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">CaptchaVo</span> <span class="variable">captcha</span> <span class="operator">=</span> service.getCaptcha();</span><br><span class="line">    <span class="keyword">return</span> Result.ok(captcha);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>编写Service层逻辑</strong></p>
<ul>
<li><p>在<code>LoginService</code>中增加如下内容</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">CaptchaVo <span class="title function_">getCaptcha</span><span class="params">()</span>;</span><br></pre></td></tr></table></figure>
</li>
<li><p>在<code>LoginServiceImpl</code>中增加如下内容</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Autowired</span></span><br><span class="line"><span class="keyword">private</span> StringRedisTemplate redisTemplate;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> CaptchaVo <span class="title function_">getCaptcha</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">SpecCaptcha</span> <span class="variable">specCaptcha</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">SpecCaptcha</span>(<span class="number">130</span>, <span class="number">48</span>, <span class="number">4</span>);</span><br><span class="line">    specCaptcha.setCharType(Captcha.TYPE_DEFAULT);</span><br><span class="line"></span><br><span class="line">    <span class="type">String</span> <span class="variable">code</span> <span class="operator">=</span> specCaptcha.text().toLowerCase();</span><br><span class="line">    <span class="type">String</span> <span class="variable">key</span> <span class="operator">=</span> RedisConstant.ADMIN_LOGIN_PREFIX + UUID.randomUUID();</span><br><span class="line">    <span class="type">String</span> <span class="variable">image</span> <span class="operator">=</span> specCaptcha.toBase64();</span><br><span class="line">    redisTemplate.opsForValue().set(key, code, RedisConstant.ADMIN_LOGIN_CAPTCHA_TTL_SEC, TimeUnit.SECONDS);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">CaptchaVo</span>(image, key);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> <strong>知识点</strong>：</p>
<ul>
<li><p>本项目Reids中的key需遵循以下命名规范：<strong>项目名:功能模块名:其他</strong>，例如<code>admin:login:123456</code></p>
</li>
<li><p><code>spring-boot-starter-data-redis</code>已经完成了<code>StringRedisTemplate</code>的自动配置，我们直接注入即可。</p>
</li>
<li><p>为方便管理，可以将Reids相关的一些值定义为常量，例如key的前缀、TTL时长，内容如下。大家可将这些常量统一定义在<strong>common模块</strong>下的<code>com.atguigu.lease.common.constant.RedisConstant</code>类中</p>
  <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">RedisConstant</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">ADMIN_LOGIN_PREFIX</span> <span class="operator">=</span> <span class="string">&quot;admin:login:&quot;</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">Integer</span> <span class="variable">ADMIN_LOGIN_CAPTCHA_TTL_SEC</span> <span class="operator">=</span> <span class="number">60</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">APP_LOGIN_PREFIX</span> <span class="operator">=</span> <span class="string">&quot;app:login:&quot;</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">Integer</span> <span class="variable">APP_LOGIN_CODE_RESEND_TIME_SEC</span> <span class="operator">=</span> <span class="number">60</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">Integer</span> <span class="variable">APP_LOGIN_CODE_TTL_SEC</span> <span class="operator">=</span> <span class="number">60</span> * <span class="number">10</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">APP_ROOM_PREFIX</span> <span class="operator">=</span> <span class="string">&quot;app:room:&quot;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
</ul>
</li>
</ul>
<h4 id="2-登录接口"><a href="#2-登录接口" class="headerlink" title="2. 登录接口"></a>2. 登录接口</h4><ul>
<li><p><strong>登录校验逻辑</strong></p>
<p>用户登录的校验逻辑分为三个主要步骤，分别是<strong>校验验证码</strong>，<strong>校验用户状态</strong>和<strong>校验密码</strong>，具体逻辑如下</p>
<ul>
<li>前端发送<code>username</code>、<code>password</code>、<code>captchaKey</code>、<code>captchaCode</code>请求登录。</li>
<li>判断<code>captchaCode</code>是否为空，若为空，则直接响应<code>验证码为空</code>；若不为空进行下一步判断。</li>
<li>根据<code>captchaKey</code>从Redis中查询之前保存的<code>code</code>，若查询出来的<code>code</code>为空，则直接响应<code>验证码已过期</code>；若不为空进行下一步判断。</li>
<li>比较<code>captchaCode</code>和<code>code</code>，若不相同，则直接响应<code>验证码不正确</code>；若相同则进行下一步判断。</li>
<li>根据<code>username</code>查询数据库，若查询结果为空，则直接响应<code>账号不存在</code>；若不为空则进行下一步判断。</li>
<li>查看用户状态，判断是否被禁用，若禁用，则直接响应<code>账号被禁</code>；若未被禁用，则进行下一步判断。</li>
<li>比对<code>password</code>和数据库中查询的密码，若不一致，则直接响应<code>账号或密码错误</code>，若一致则进行入最后一步。</li>
<li>创建JWT，并响应给浏览器。</li>
</ul>
</li>
<li><p><strong>接口逻辑实现</strong></p>
<ul>
<li><p><strong>查看请求数据结构</strong></p>
<p>查看<strong>web-admin</strong>模块下的<code>com.atguigu.lease.web.admin.vo.login.LoginVo</code>，具体内容如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Data</span></span><br><span class="line"><span class="meta">@Schema(description = &quot;后台管理系统登录信息&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">LoginVo</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Schema(description=&quot;用户名&quot;)</span></span><br><span class="line">    <span class="keyword">private</span> String username;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Schema(description=&quot;密码&quot;)</span></span><br><span class="line">    <span class="keyword">private</span> String password;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Schema(description=&quot;验证码key&quot;)</span></span><br><span class="line">    <span class="keyword">private</span> String captchaKey;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Schema(description=&quot;验证码code&quot;)</span></span><br><span class="line">    <span class="keyword">private</span> String captchaCode;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>配置所需依赖</strong></p>
<p>登录接口需要为登录成功的用户创建并返回JWT，本项目使用开源的JWT工具<strong>Java-JWT</strong>，配置如下，具体内容可参考<a href="https://github.com/jwtk/jjwt/tree/0.11.2">官方文档</a>。</p>
<ul>
<li><p><strong>引入Maven依赖</strong></p>
<p>在<strong>common模块</strong>的pom.xml文件中增加如下内容</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>io.jsonwebtoken<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>jjwt-api<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>io.jsonwebtoken<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>jjwt-impl<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">scope</span>&gt;</span>runtime<span class="tag">&lt;/<span class="name">scope</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>io.jsonwebtoken<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>jjwt-jackson<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">scope</span>&gt;</span>runtime<span class="tag">&lt;/<span class="name">scope</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>创建JWT工具类</strong></p>
<p>在<strong>common模块</strong>下创建<code>com.atguigu.lease.common.utils.JwtUtil</code>工具类，内容如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">JwtUtil</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="type">long</span> <span class="variable">tokenExpiration</span> <span class="operator">=</span> <span class="number">60</span> * <span class="number">60</span> * <span class="number">1000L</span>;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="type">SecretKey</span> <span class="variable">tokenSignKey</span> <span class="operator">=</span> Keys.hmacShaKeyFor(<span class="string">&quot;M0PKKI6pYGVWWfDZw90a0lTpGYX1d4AQ&quot;</span>.getBytes());</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> String <span class="title function_">createToken</span><span class="params">(Long userId, String username)</span> &#123;</span><br><span class="line">        <span class="type">String</span> <span class="variable">token</span> <span class="operator">=</span> Jwts.builder().</span><br><span class="line">                setSubject(<span class="string">&quot;USER_INFO&quot;</span>).</span><br><span class="line">                setExpiration(<span class="keyword">new</span> <span class="title class_">Date</span>(System.currentTimeMillis() + tokenExpiration)).</span><br><span class="line">                claim(<span class="string">&quot;userId&quot;</span>, userId).</span><br><span class="line">                claim(<span class="string">&quot;username&quot;</span>, username).</span><br><span class="line">                signWith(tokenSignKey).</span><br><span class="line">                compact();</span><br><span class="line">        <span class="keyword">return</span> token;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p><strong>编写Controller层逻辑</strong></p>
<p>在<code>LoginController</code>中增加如下内容</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Operation(summary = &quot;登录&quot;)</span></span><br><span class="line"><span class="meta">@PostMapping(&quot;login&quot;)</span></span><br><span class="line"><span class="keyword">public</span> Result&lt;String&gt; <span class="title function_">login</span><span class="params">(<span class="meta">@RequestBody</span> LoginVo loginVo)</span> &#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">token</span> <span class="operator">=</span> service.login(loginVo);</span><br><span class="line">    <span class="keyword">return</span> Result.ok(token);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>编写Service层逻辑</strong></p>
<ul>
<li><p>在<code>LoginService</code>中增加如下内容</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">String <span class="title function_">login</span><span class="params">(LoginVo loginVo)</span>;</span><br></pre></td></tr></table></figure>
</li>
<li><p>在<code>LoginServiceImpl</code>中增加如下内容</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> String <span class="title function_">login</span><span class="params">(LoginVo loginVo)</span> &#123;</span><br><span class="line">    <span class="comment">//1.判断是否输入了验证码</span></span><br><span class="line">    <span class="keyword">if</span> (!StringUtils.hasText(loginVo.getCaptchaCode())) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">LeaseException</span>(ResultCodeEnum.ADMIN_CAPTCHA_CODE_NOT_FOUND);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//2.校验验证码</span></span><br><span class="line">    <span class="type">String</span> <span class="variable">code</span> <span class="operator">=</span> redisTemplate.opsForValue().get(loginVo.getCaptchaKey());</span><br><span class="line">    <span class="keyword">if</span> (code == <span class="literal">null</span>) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">LeaseException</span>(ResultCodeEnum.ADMIN_CAPTCHA_CODE_EXPIRED);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (!code.equals(loginVo.getCaptchaCode().toLowerCase())) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">LeaseException</span>(ResultCodeEnum.ADMIN_CAPTCHA_CODE_ERROR);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//3.校验用户是否存在</span></span><br><span class="line">    <span class="type">SystemUser</span> <span class="variable">systemUser</span> <span class="operator">=</span> systemUserMapper.selectOneByUsername(loginVo.getUsername());</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (systemUser == <span class="literal">null</span>) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">LeaseException</span>(ResultCodeEnum.ADMIN_ACCOUNT_NOT_EXIST_ERROR);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//4.校验用户是否被禁</span></span><br><span class="line">    <span class="keyword">if</span> (systemUser.getStatus() == BaseStatus.DISABLE) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">LeaseException</span>(ResultCodeEnum.ADMIN_ACCOUNT_DISABLED_ERROR);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//5.校验用户密码</span></span><br><span class="line">    <span class="keyword">if</span> (!systemUser.getPassword().equals(DigestUtils.md5Hex(loginVo.getPassword()))) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">LeaseException</span>(ResultCodeEnum.ADMIN_ACCOUNT_ERROR);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//6.创建并返回TOKEN</span></span><br><span class="line">    <span class="keyword">return</span> JwtUtil.createToken(systemUser.getId(), systemUser.getUsername());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p><strong>编写Mapper层逻辑</strong></p>
<ul>
<li><p>在<code>LoginMapper</code>中增加如下内容</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">SystemUser <span class="title function_">selectOneByUsername</span><span class="params">(String username)</span>;</span><br></pre></td></tr></table></figure>
</li>
<li><p>在<code>LoginMapper.xml</code>中增加如下内容</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="operator">&lt;</span><span class="keyword">select</span> id<span class="operator">=</span>&quot;selectOneByUsername&quot; resultType<span class="operator">=</span>&quot;com.atguigu.lease.model.entity.SystemUser&quot;<span class="operator">&gt;</span></span><br><span class="line">    <span class="keyword">select</span> id,</span><br><span class="line">           username,</span><br><span class="line">           password,</span><br><span class="line">           name,</span><br><span class="line">           type,</span><br><span class="line">           phone,</span><br><span class="line">           avatar_url,</span><br><span class="line">           additional_info,</span><br><span class="line">           post_id,</span><br><span class="line">           status</span><br><span class="line">    <span class="keyword">from</span> <span class="built_in">system_user</span></span><br><span class="line">    <span class="keyword">where</span> is_deleted <span class="operator">=</span> <span class="number">0</span></span><br><span class="line">      <span class="keyword">and</span> username <span class="operator">=</span> #&#123;username&#125;</span><br><span class="line"><span class="operator">&lt;</span><span class="operator">/</span><span class="keyword">select</span><span class="operator">&gt;</span></span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p><strong>编写HandlerInterceptor</strong></p>
<p>我们需要为所有受保护的接口增加校验JWT合法性的逻辑。具体实现如下</p>
<ul>
<li><p>在<code>JwtUtil</code>中增加<code>parseToken</code>方法，内容如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> Claims <span class="title function_">parseToken</span><span class="params">(String token)</span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (token==<span class="literal">null</span>)&#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">LeaseException</span>(ResultCodeEnum.ADMIN_LOGIN_AUTH);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span>&#123;</span><br><span class="line">        <span class="type">JwtParser</span> <span class="variable">jwtParser</span> <span class="operator">=</span> Jwts.parserBuilder().setSigningKey(secretKey).build();</span><br><span class="line">        <span class="keyword">return</span> jwtParser.parseClaimsJws(token).getBody();</span><br><span class="line">    &#125;<span class="keyword">catch</span> (ExpiredJwtException e)&#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">LeaseException</span>(ResultCodeEnum.TOKEN_EXPIRED);</span><br><span class="line">    &#125;<span class="keyword">catch</span> (JwtException e)&#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">LeaseException</span>(ResultCodeEnum.TOKEN_INVALID);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>编写HandlerInterceptor</strong></p>
<p>在<strong>web-admin模块</strong>中创建<code>com.atguigu.lease.web.admin.custom.interceptor.AuthenticationInterceptor</code>类，内容如下，有关<code>HanderInterceptor</code>的相关内容，可参考<a href="https://docs.spring.io/spring-framework/reference/web/webmvc/mvc-servlet/handlermapping-interceptor.html">官方文档</a>。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">AuthenticationInterceptor</span> <span class="keyword">implements</span> <span class="title class_">HandlerInterceptor</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">preHandle</span><span class="params">(HttpServletRequest request, HttpServletResponse response, Object handler)</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">        <span class="type">String</span> <span class="variable">token</span> <span class="operator">=</span> request.getHeader(<span class="string">&quot;access-token&quot;</span>);</span><br><span class="line">        JwtUtil.parseToken(token);</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>注意</strong>：</p>
<p>我们约定，前端登录后，后续请求都将JWT，放置于HTTP请求的Header中，其Header的key为<code>access-token</code>。</p>
</li>
<li><p><strong>注册HandlerInterceptor</strong></p>
<p>在<strong>web-admin模块</strong>的<code>com.atguigu.lease.web.admin.custom.config.WebMvcConfiguration</code>中增加如下内容</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Autowired</span></span><br><span class="line"><span class="keyword">private</span> AuthenticationInterceptor authenticationInterceptor;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">addInterceptors</span><span class="params">(InterceptorRegistry registry)</span> &#123;</span><br><span class="line">    registry.addInterceptor(<span class="built_in">this</span>.authenticationInterceptor).addPathPatterns(<span class="string">&quot;/admin/**&quot;</span>).excludePathPatterns(<span class="string">&quot;/admin/login/**&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p><strong>Knife4j配置</strong></p>
<p>在增加上述拦截器后，为方便继续调试其他接口，可以获取一个长期有效的Token，将其配置到Knife4j的全局参数中，如下图所示。</p>
<p><img src="/2025/01/23/%E5%B0%9A%E7%A1%85%E8%B0%B7%E5%B0%9A%E5%BA%AD%E5%85%AC%E5%AF%93-02/%E5%85%A8%E5%B1%80%E5%8F%82%E6%95%B0.png"></p>
<p><strong>注意：</strong>每个接口分组需要单独配置</p>
<p>刷新页面，任选一个接口进行调试，会发现发送请求时会自动携带该header，如下图所示</p>
<p><img src="/2025/01/23/%E5%B0%9A%E7%A1%85%E8%B0%B7%E5%B0%9A%E5%BA%AD%E5%85%AC%E5%AF%93-02/knife4j%E6%8E%A5%E5%8F%A3%E8%AE%A4%E8%AF%81.png"></p>
</li>
</ul>
</li>
</ul>
<h4 id="3-获取登录用户个人信息"><a href="#3-获取登录用户个人信息" class="headerlink" title="3.获取登录用户个人信息"></a>3.获取登录用户个人信息</h4><ul>
<li><p><strong>查看请求和响应的数据结构</strong></p>
<ul>
<li><p><strong>响应的数据结构</strong></p>
<p>查看<strong>web-admin模块</strong>下的<code>com.atguigu.lease.web.admin.vo.system.user.SystemUserInfoVo</code>，内容如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Schema(description = &quot;员工基本信息&quot;)</span></span><br><span class="line"><span class="meta">@Data</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">SystemUserInfoVo</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Schema(description = &quot;用户姓名&quot;)</span></span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Schema(description = &quot;用户头像&quot;)</span></span><br><span class="line">    <span class="keyword">private</span> String avatarUrl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>请求的数据结构</strong></p>
<p>按理说，前端若想获取当前登录用户的个人信息，需要传递当前用户的<code>id</code>到后端进行查询。但是由于请求中携带的JWT中就包含了当前登录用户的<code>id</code>，故请求个人信息时，就无需再传递<code>id</code>。</p>
</li>
</ul>
</li>
<li><p><strong>修改<code>JwtUtil</code>中的<code>parseToken</code>方法</strong></p>
<p>由于需要从Jwt中获取用户<code>id</code>，因此需要为<code>parseToken</code> 方法增加返回值，如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> Claims <span class="title function_">parseToken</span><span class="params">(String token)</span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (token==<span class="literal">null</span>)&#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">LeaseException</span>(ResultCodeEnum.ADMIN_LOGIN_AUTH);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span>&#123;</span><br><span class="line">        <span class="type">JwtParser</span> <span class="variable">jwtParser</span> <span class="operator">=</span> Jwts.parserBuilder().setSigningKey(secretKey).build();</span><br><span class="line">        <span class="keyword">return</span> jwtParser.parseClaimsJws(token).getBody();</span><br><span class="line">    &#125;<span class="keyword">catch</span> (ExpiredJwtException e)&#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">LeaseException</span>(ResultCodeEnum.TOKEN_EXPIRED);</span><br><span class="line">    &#125;<span class="keyword">catch</span> (JwtException e)&#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">LeaseException</span>(ResultCodeEnum.TOKEN_INVALID);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>编写ThreadLocal工具类</strong></p>
<p>理论上我们可以在Controller方法中，使用<code>@RequestHeader</code>获取JWT，然后在进行解析，如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Operation(summary = &quot;获取登陆用户个人信息&quot;)</span></span><br><span class="line"><span class="meta">@GetMapping(&quot;info&quot;)</span></span><br><span class="line"><span class="keyword">public</span> Result&lt;SystemUserInfoVo&gt; <span class="title function_">info</span><span class="params">(<span class="meta">@RequestHeader(&quot;access-token&quot;)</span> String token)</span> &#123;</span><br><span class="line">    <span class="type">Claims</span> <span class="variable">claims</span> <span class="operator">=</span> JwtUtil.parseToken(token);</span><br><span class="line">    <span class="type">Long</span> <span class="variable">userId</span> <span class="operator">=</span> claims.get(<span class="string">&quot;userId&quot;</span>, Long.class);</span><br><span class="line">    <span class="type">SystemUserInfoVo</span> <span class="variable">userInfo</span> <span class="operator">=</span> service.getLoginUserInfo(userId);</span><br><span class="line">    <span class="keyword">return</span> Result.ok(userInfo);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上述代码的逻辑没有任何问题，但是这样做，JWT会被重复解析两次（一次在拦截器中，一次在该方法中）。为避免重复解析，通常会在拦截器将Token解析完毕后，将结果保存至<strong>ThreadLocal</strong>中，这样一来，我们便可以在整个请求的处理流程中进行访问了。</p>
<blockquote>
<p><strong>ThreadLocal概述</strong></p>
<p>ThreadLocal的主要作用是为每个使用它的线程提供一个独立的变量副本，使每个线程都可以操作自己的变量，而不会互相干扰，其用法如下图所示。</p>
<img src="ThreadLocal.drawio.png" style="zoom: 33%;" />
</blockquote>
<p>在<strong>common模块</strong>中创建<code>com.atguigu.lease.common.login.LoginUserHolder</code>工具类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">LoginUserHolder</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> ThreadLocal&lt;LoginUser&gt; threadLocal = <span class="keyword">new</span> <span class="title class_">ThreadLocal</span>&lt;&gt;();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">setLoginUser</span><span class="params">(LoginUser loginUser)</span> &#123;</span><br><span class="line">        threadLocal.set(loginUser);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> LoginUser <span class="title function_">getLoginUser</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> threadLocal.get();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">clear</span><span class="params">()</span> &#123;</span><br><span class="line">        threadLocal.remove();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>同时在<strong>common模块</strong>中创建<code>com.atguigu.lease.common.login.LoginUser</code>类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Data</span></span><br><span class="line"><span class="meta">@AllArgsConstructor</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">LoginUser</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> Long userId;</span><br><span class="line">    <span class="keyword">private</span> String username;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>修改<code>AuthenticationInterceptor</code>拦截器</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">AuthenticationInterceptor</span> <span class="keyword">implements</span> <span class="title class_">HandlerInterceptor</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">preHandle</span><span class="params">(HttpServletRequest request, HttpServletResponse response, Object handler)</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line"></span><br><span class="line">        <span class="type">String</span> <span class="variable">token</span> <span class="operator">=</span> request.getHeader(<span class="string">&quot;access-token&quot;</span>);</span><br><span class="line"></span><br><span class="line">        <span class="type">Claims</span> <span class="variable">claims</span> <span class="operator">=</span> JwtUtil.parseToken(token);</span><br><span class="line">        <span class="type">Long</span> <span class="variable">userId</span> <span class="operator">=</span> claims.get(<span class="string">&quot;userId&quot;</span>, Long.class);</span><br><span class="line">        <span class="type">String</span> <span class="variable">username</span> <span class="operator">=</span> claims.get(<span class="string">&quot;username&quot;</span>, String.class);</span><br><span class="line">        LoginUserHolder.setLoginUser(<span class="keyword">new</span> <span class="title class_">LoginUser</span>(userId, username));</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">afterCompletion</span><span class="params">(HttpServletRequest request, HttpServletResponse response, Object handler, Exception ex)</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">        LoginUserHolder.clear();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>编写Controller层逻辑</strong></p>
<p>在<code>LoginController</code>中增加如下内容</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Operation(summary = &quot;获取登陆用户个人信息&quot;)</span></span><br><span class="line"><span class="meta">@GetMapping(&quot;info&quot;)</span></span><br><span class="line"><span class="keyword">public</span> Result&lt;SystemUserInfoVo&gt; <span class="title function_">info</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">SystemUserInfoVo</span> <span class="variable">userInfo</span> <span class="operator">=</span> service.getLoginUserInfo(LoginUserHolder.getLoginUser().getUserId());</span><br><span class="line">    <span class="keyword">return</span> Result.ok(userInfo);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>编写Service层逻辑</strong></p>
<p>  在<code>LoginService</code>中增加如下内容</p>
<pre><code class="java">@Override
public SystemUserInfoVo getLoginUserInfo(Long userId) &#123;
    SystemUser systemUser = systemUserMapper.selectById(userId);
    SystemUserInfoVo systemUserInfoVo = new SystemUserInfoVo();
    systemUserInfoVo.setName(systemUser.getName());
    systemUserInfoVo.setAvatarUrl(systemUser.getAvatarUrl());
    return systemUserInfoVo;
&#125;
</code></pre>
</li>
</ul>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>尚庭公寓</tag>
        <tag>项目实战</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring web 注解</title>
    <url>/2025/01/20/Spring-web-%E6%B3%A8%E8%A7%A3/</url>
    <content><![CDATA[<h3 id="一、Spring-Web-MVC-与-Spring-Bean-注解"><a href="#一、Spring-Web-MVC-与-Spring-Bean-注解" class="headerlink" title="一、Spring Web MVC 与 Spring Bean 注解"></a><strong><font style="color:rgb(0, 0, 0);">一、Spring Web MVC 与 Spring Bean 注解</font></strong></h3><h4 id="Spring-Web-MVC-注解"><a href="#Spring-Web-MVC-注解" class="headerlink" title="Spring Web MVC 注解"></a><strong><font style="color:rgb(0, 0, 0);">Spring Web MVC 注解</font></strong></h4><p><strong><font style="color:rgb(51, 51, 51);">@RequestMapping</font></strong></p>
<p><font style="color:rgb(51, 51, 51);">@RequestMapping注解的主要用途是将Web请求与请求处理类中的方法进行映射。Spring MVC和Spring WebFlux都通过</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">RquestMappingHandlerMapping</font><font style="color:rgb(51, 51, 51);">和</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">RequestMappingHndlerAdapter</font><font style="color:rgb(51, 51, 51);">两个类来提供对@RequestMapping注解的支持。</font></p>
<p><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@RequestMapping</font><font style="color:rgb(51, 51, 51);">注解对请求处理类中的请求处理方法进行标注；</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@RequestMapping</font><font style="color:rgb(51, 51, 51);">注解拥有以下的六个配置属性：</font></p>
<ul>
<li><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">value</font><font style="color:rgb(51, 51, 51);">:映射的请求URL或者其别名</font></li>
<li><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">method</font><font style="color:rgb(51, 51, 51);">:兼容HTTP的方法名</font></li>
<li><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">params</font><font style="color:rgb(51, 51, 51);">:根据HTTP参数的存在、缺省或值对请求进行过滤</font></li>
<li><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">header</font><font style="color:rgb(51, 51, 51);">:根据HTTP Header的存在、缺省或值对请求进行过滤</font></li>
<li><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">consume</font><font style="color:rgb(51, 51, 51);">:设定在HTTP请求正文中允许使用的媒体类型</font></li>
<li><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">product</font><font style="color:rgb(51, 51, 51);">:在HTTP响应体中允许使用的媒体类型</font></li>
</ul>
<p><font style="color:rgb(51, 51, 51);">提示：在使用@RequestMapping之前，请求处理类还需要使用@Controller或@RestController进行标记</font></p>
<p><font style="color:rgb(51, 51, 51);">下面是使用@RequestMapping的两个示例：</font></p>
<p><img src="/2025/01/20/Spring-web-%E6%B3%A8%E8%A7%A3/1709106096731-61aa57df-c36c-451a-8c6a-7ea937c4c60d.png"></p>
<p><font style="color:rgb(51, 51, 51);">@RequestMapping还可以对类进行标记，这样类中的处理方法在映射请求路径时，会自动将类上@RequestMapping设置的value拼接到方法中映射路径之前，如下：</font></p>
<p><img src="/2025/01/20/Spring-web-%E6%B3%A8%E8%A7%A3/1709106096629-d7f931ea-fbd5-4c07-8a66-4ba16a806456.png"></p>
<p><strong><font style="color:rgb(51, 51, 51);">@RequestBody</font></strong></p>
<p><font style="color:rgb(51, 51, 51);">@RequestBody在处理请求方法的参数列表中使用，它可以将请求主体中的参数绑定到一个对象中，请求主体参数是通过</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">HttpMessageConverter</font><font style="color:rgb(51, 51, 51);">传递的，根据请求主体中的参数名与对象的属性名进行匹配并绑定值。此外，还可以通过@Valid注解对请求主体中的参数进行校验。</font></p>
<p><font style="color:rgb(51, 51, 51);">下面是一个使用</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@RequestBody</font><font style="color:rgb(51, 51, 51);">的示例：</font></p>
<p><img src="/2025/01/20/Spring-web-%E6%B3%A8%E8%A7%A3/1709106096701-9d9d0d6e-2fa5-43f2-94d6-811d78ffa04d.jpeg"></p>
<p><strong><font style="color:rgb(51, 51, 51);">@GetMapping</font></strong></p>
<p><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@GetMapping</font><font style="color:rgb(51, 51, 51);">注解用于处理HTTP GET请求，并将请求映射到具体的处理方法中。具体来说，@GetMapping是一个组合注解，它相当于是</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@RequestMapping(method&#x3D;RequestMethod.GET)</font><font style="color:rgb(51, 51, 51);">的快捷方式。</font></p>
<p><font style="color:rgb(51, 51, 51);">下面是</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@GetMapping</font><font style="color:rgb(51, 51, 51);">的一个使用示例：</font></p>
<p><img src="/2025/01/20/Spring-web-%E6%B3%A8%E8%A7%A3/1709106096688-c0398558-79fb-4f6f-9ec4-7100c77dbd3b.jpeg"></p>
<span id="more"></span>

<p><strong><font style="color:rgb(51, 51, 51);">@PostMapping</font></strong></p>
<p><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@PostMapping</font><font style="color:rgb(51, 51, 51);">注解用于处理HTTP POST请求，并将请求映射到具体的处理方法中。@PostMapping与@GetMapping一样，也是一个组合注解，它相当于是</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@RequestMapping(method&#x3D;HttpMethod.POST)</font><font style="color:rgb(51, 51, 51);">的快捷方式。</font></p>
<p><font style="color:rgb(51, 51, 51);">下面是使用</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@PostMapping</font><font style="color:rgb(51, 51, 51);">的一个示例：</font></p>
<p><img src="/2025/01/20/Spring-web-%E6%B3%A8%E8%A7%A3/1709106096618-3550ddca-8fa3-4a10-8faf-698b32c6ea68.jpeg"></p>
<p>&lt;</p>
<p><strong><font style="color:rgb(51, 51, 51);">@PutMapping</font></strong></p>
<p><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@PutMapping</font><font style="color:rgb(51, 51, 51);">注解用于处理HTTP PUT请求，并将请求映射到具体的处理方法中，@PutMapping是一个组合注解，相当于是</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@RequestMapping(method&#x3D;HttpMethod.PUT)</font><font style="color:rgb(51, 51, 51);">的快捷方式。</font></p>
<p><font style="color:rgb(51, 51, 51);">下面是使用</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@PutMapping</font><font style="color:rgb(51, 51, 51);">的一个示例：</font></p>
<p><img src="/2025/01/20/Spring-web-%E6%B3%A8%E8%A7%A3/1709106097163-681e9f87-039d-44b6-b272-9026ace93a00.jpeg"></p>
<p>&lt;</p>
<p><strong><font style="color:rgb(51, 51, 51);">@DeleteMapping</font></strong></p>
<p><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@DeleteMapping</font><font style="color:rgb(51, 51, 51);">注解用于处理HTTP DELETE请求，并将请求映射到删除方法中。@DeleteMapping是一个组合注解，它相当于是</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@RequestMapping(method&#x3D;HttpMethod.DELETE)</font><font style="color:rgb(51, 51, 51);">的快捷方式。</font></p>
<p><font style="color:rgb(51, 51, 51);">下面是使用</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@DeleteMapping</font><font style="color:rgb(51, 51, 51);">的一个示例：</font></p>
<p><img src="/2025/01/20/Spring-web-%E6%B3%A8%E8%A7%A3/1709106097302-924c0ec0-a8ea-41d6-8458-7ee7deaf5254.jpeg"></p>
<p>&lt;</p>
<p><strong><font style="color:rgb(51, 51, 51);">@PatchMapping</font></strong></p>
<p><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@PatchMapping</font><font style="color:rgb(51, 51, 51);">注解用于处理HTTP PATCH请求，并将请求映射到对应的处理方法中。@PatchMapping相当于是</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@RequestMapping(method&#x3D;HttpMethod.PATCH)</font><font style="color:rgb(51, 51, 51);">的快捷方式。</font></p>
<p><font style="color:rgb(51, 51, 51);">下面是一个简单的示例：</font></p>
<p><img src="/2025/01/20/Spring-web-%E6%B3%A8%E8%A7%A3/1709106097261-f0eb3826-f449-46b0-9a2f-9bb4aef3888a.jpeg"></p>
<p><strong><font style="color:rgb(51, 51, 51);">@ControllerAdvice</font></strong></p>
<p><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@ControllerAdvice</font><font style="color:rgb(51, 51, 51);">是@Component注解的一个延伸注解，Spring会自动扫描并检测被@ControllerAdvice所标注的类。</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@ControllerAdvice</font><font style="color:rgb(51, 51, 51);">需要和</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@ExceptionHandler</font><font style="color:rgb(51, 51, 51);">、</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@InitBinder</font><font style="color:rgb(51, 51, 51);">以及</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@ModelAttribute</font><font style="color:rgb(51, 51, 51);">注解搭配使用，主要是用来处理控制器所抛出的异常信息。</font></p>
<p><font style="color:rgb(51, 51, 51);">首先，我们需要定义一个被</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@ControllerAdvice</font><font style="color:rgb(51, 51, 51);">所标注的类，在该类中，定义一个用于处理具体异常的方法，并使用@ExceptionHandler注解进行标记。</font></p>
<p><font style="color:rgb(51, 51, 51);">此外，在有必要的时候，可以使用</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@InitBinder</font><font style="color:rgb(51, 51, 51);">在类中进行全局的配置，还可以使用@ModelAttribute配置与视图相关的参数。使用</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@ControllerAdvice</font><font style="color:rgb(51, 51, 51);">注解，就可以快速的创建统一的，自定义的异常处理类。</font></p>
<p><font style="color:rgb(51, 51, 51);">下面是一个使用</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@ControllerAdvice</font><font style="color:rgb(51, 51, 51);">的示例代码：</font></p>
<p><img src="/2025/01/20/Spring-web-%E6%B3%A8%E8%A7%A3/1709106097273-f1dc8409-761a-4579-8d4d-4bddafc6047f.jpeg"></p>
<p><strong><font style="color:rgb(51, 51, 51);">@ResponseBody</font></strong></p>
<p><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@ResponseBody</font><font style="color:rgb(51, 51, 51);">会自动将控制器中方法的返回值写入到HTTP响应中。特别的，</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@ResponseBody</font><font style="color:rgb(51, 51, 51);">注解只能用在被</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@Controller</font><font style="color:rgb(51, 51, 51);">注解标记的类中。如果在被</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@RestController</font><font style="color:rgb(51, 51, 51);">标记的类中，则方法不需要使用</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@ResponseBody</font><font style="color:rgb(51, 51, 51);">注解进行标注。</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@RestController</font><font style="color:rgb(51, 51, 51);">相当于是</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@Controller</font><font style="color:rgb(51, 51, 51);">和</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@ResponseBody</font><font style="color:rgb(51, 51, 51);">的组合注解。</font></p>
<p><font style="color:rgb(51, 51, 51);">下面是使用该注解的一个示例</font></p>
<p><img src="/2025/01/20/Spring-web-%E6%B3%A8%E8%A7%A3/1709106097309-d7108a06-e6bc-4b04-b442-41cc24e8ddf8.png"></p>
<p><strong><font style="color:rgb(51, 51, 51);">@ExceptionHandler</font></strong></p>
<p><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@ExceptionHander</font><font style="color:rgb(51, 51, 51);">注解用于标注处理特定类型异常类所抛出异常的方法。当控制器中的方法抛出异常时，Spring会自动捕获异常，并将捕获的异常信息传递给被</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@ExceptionHandler</font><font style="color:rgb(51, 51, 51);">标注的方法。</font></p>
<p><font style="color:rgb(51, 51, 51);">下面是使用该注解的一个示例：</font></p>
<p><img src="/2025/01/20/Spring-web-%E6%B3%A8%E8%A7%A3/1709106097716-f9cc78a3-b126-457a-a6f3-b1336fe0924e.jpeg"></p>
<p><strong><font style="color:rgb(51, 51, 51);">@ResponseStatus</font></strong></p>
<p><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@ResponseStatus</font><font style="color:rgb(51, 51, 51);">注解可以标注请求处理方法。使用此注解，可以指定响应所需要的HTTP STATUS。特别地，我们可以使用HttpStauts类对该注解的value属性进行赋值。</font></p>
<p><font style="color:rgb(51, 51, 51);">下面是使用</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@ResponseStatus</font><font style="color:rgb(51, 51, 51);">注解的一个示例：</font></p>
<p><img src="/2025/01/20/Spring-web-%E6%B3%A8%E8%A7%A3/1709106097942-28ba45a1-6ac0-4fe7-8447-19efe8bdd12c.jpeg"></p>
<p><strong><font style="color:rgb(51, 51, 51);">@PathVariable</font></strong></p>
<p><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@PathVariable</font><font style="color:rgb(51, 51, 51);">注解是将方法中的参数绑定到请求URI中的模板变量上。可以通过</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@RequestMapping</font><font style="color:rgb(51, 51, 51);">注解来指定URI的模板变量，然后使用</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@PathVariable</font><font style="color:rgb(51, 51, 51);">注解将方法中的参数绑定到模板变量上。</font></p>
<p><font style="color:rgb(51, 51, 51);">特别地，</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@PathVariable</font><font style="color:rgb(51, 51, 51);">注解允许我们使用value或name属性来给参数取一个别名。下面是使用此注解的一个示例：</font></p>
<p><img src="/2025/01/20/Spring-web-%E6%B3%A8%E8%A7%A3/1709106097865-e8ed68eb-0fea-4e35-bb37-2d52bea49b21.jpeg"></p>
<p><font style="color:rgb(51, 51, 51);">模板变量名需要使用</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">{ }</font><font style="color:rgb(51, 51, 51);">进行包裹，如果方法的参数名与URI模板变量名一致，则在</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@PathVariable</font><font style="color:rgb(51, 51, 51);">中就可以省略别名的定义。</font></p>
<p><font style="color:rgb(51, 51, 51);">下面是一个简写的示例：</font></p>
<p><img src="/2025/01/20/Spring-web-%E6%B3%A8%E8%A7%A3/1709106097968-dcbf7dc8-e1d2-412c-9639-fdfba9e545ae.jpeg"></p>
<p><font style="color:rgb(51, 51, 51);">提示：如果参数是一个非必须的，可选的项，则可以在</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@PathVariable</font><font style="color:rgb(51, 51, 51);">中设置</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">require &#x3D; false</font></p>
<p><strong><font style="color:rgb(51, 51, 51);">@RequestParam</font></strong></p>
<p><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@RequestParam</font><font style="color:rgb(51, 51, 51);">注解用于将方法的参数与Web请求的传递的参数进行绑定。使用</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@RequestParam</font><font style="color:rgb(51, 51, 51);">可以轻松的访问HTTP请求参数的值。</font></p>
<p><font style="color:rgb(51, 51, 51);">下面是使用该注解的代码示例：</font></p>
<p><img src="/2025/01/20/Spring-web-%E6%B3%A8%E8%A7%A3/1709106097953-35ae1da2-6935-4b59-9d9e-f2486c6a1410.png"></p>
<p><font style="color:rgb(51, 51, 51);">该注解的其他属性配置与</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@PathVariable</font><font style="color:rgb(51, 51, 51);">的配置相同，特别的，如果传递的参数为空，还可以通过defaultValue设置一个默认值。示例代码如下：</font></p>
<p><img src="/2025/01/20/Spring-web-%E6%B3%A8%E8%A7%A3/1709106098321-68f40df0-3918-4a04-b5d2-e6c490901228.jpeg"></p>
<p><strong><font style="color:rgb(51, 51, 51);">@Controller</font></strong></p>
<p><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@Controller</font><font style="color:rgb(51, 51, 51);">是</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@Component</font><font style="color:rgb(51, 51, 51);">注解的一个延伸，</font></p>
<p><a href="https://cloud.tencent.com/developer/tools/blog-entry?target=http://mp.weixin.qq.com/s?__biz=Mzg2MjEwMjI1Mg==&mid=2247488534&idx=1&sn=109a9ecbc8784fb71b5b92809d04449a&chksm=ce0da395f97a2a83513f039a1b9f47ccd12701cdffac8bb0f3b24bbab13033338b6f9898a518&scene=21%23wechat_redirect&source=article&objectId=1885299">Spring</a></p>
<p><font style="color:rgb(51, 51, 51);">会自动扫描并配置被该注解标注的类。此注解用于标注Spring MVC的控制器。下面是使用此注解的示例代码：</font></p>
<p><img src="/2025/01/20/Spring-web-%E6%B3%A8%E8%A7%A3/1709106098485-28e2a725-d479-46de-8ee5-8d0d6a948c72-173752061337520.jpeg"></p>
<p><strong><font style="color:rgb(51, 51, 51);">@RestController</font></strong></p>
<p><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@RestController</font><font style="color:rgb(51, 51, 51);">是在Spring 4.0开始引入的，这是一个特定的控制器注解。此注解相当于</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@Controller</font><font style="color:rgb(51, 51, 51);">和</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@ResponseBody</font><font style="color:rgb(51, 51, 51);">的快捷方式。当使用此注解时，不需要再在方法上使用</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@ResponseBody</font><font style="color:rgb(51, 51, 51);">注解。</font></p>
<p><font style="color:rgb(51, 51, 51);">下面是使用此注解的示例代码：</font></p>
<p><img src="/2025/01/20/Spring-web-%E6%B3%A8%E8%A7%A3/1709106098549-b33d3134-a3c4-47b8-a619-05bb6d59c989.jpeg"></p>
<p><strong><font style="color:rgb(51, 51, 51);">@ModelAttribute</font></strong></p>
<p><font style="color:rgb(51, 51, 51);">通过此注解，可以通过模型索引名称来访问已经存在于控制器中的model。下面是使用此注解的一个简单示例：</font></p>
<p><img src="/2025/01/20/Spring-web-%E6%B3%A8%E8%A7%A3/1709106098636-fa91e056-bf59-441a-92fa-bd64be4cdf4e.png"></p>
<p><font style="color:rgb(51, 51, 51);">与</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@PathVariable</font><font style="color:rgb(51, 51, 51);">和</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@RequestParam</font><font style="color:rgb(51, 51, 51);">注解一样，如果参数名与模型具有相同的名字，则不必指定索引名称，简写示例如下：</font></p>
<p><img src="/2025/01/20/Spring-web-%E6%B3%A8%E8%A7%A3/1709106098498-e16ba5df-5360-4215-b3e5-6136ce06c65d.png"></p>
<p><font style="color:rgb(51, 51, 51);">特别地，如果使用</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@ModelAttribute</font><font style="color:rgb(51, 51, 51);">对方法进行标注，Spring会将方法的返回值绑定到具体的Model上。示例如下：</font></p>
<p><img src="/2025/01/20/Spring-web-%E6%B3%A8%E8%A7%A3/1709106099007-048d51af-6468-4eb1-be6e-b89897316399.png"></p>
<p><font style="color:rgb(51, 51, 51);">在Spring调用具体的处理方法之前，被</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@ModelAttribute</font><font style="color:rgb(51, 51, 51);">注解标注的所有方法都将被执行。</font></p>
<p><strong><font style="color:rgb(51, 51, 51);">@CrossOrigin</font></strong></p>
<p><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@CrossOrigin</font><font style="color:rgb(51, 51, 51);">注解将为请求处理类或请求处理方法提供跨域调用支持。如果我们将此注解标注类，那么类中的所有方法都将获得支持跨域的能力。使用此注解的好处是可以微调跨域行为。使用此注解的示例如下：</font></p>
<p><img src="/2025/01/20/Spring-web-%E6%B3%A8%E8%A7%A3/1709106099056-67c9795b-60ca-4d33-9d90-79a306495b19.jpeg"></p>
<p><strong><font style="color:rgb(51, 51, 51);">@InitBinder</font></strong></p>
<p><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@InitBinder</font><font style="color:rgb(51, 51, 51);">注解用于标注初始化</font><strong><font style="color:rgb(51, 51, 51);">WebDataBinider</font></strong><font style="color:rgb(51, 51, 51);">的方法，该方法用于对Http请求传递的表单数据进行处理，如时间格式化、字符串处理等。下面是使用此注解的示例：</font></p>
<p><img src="/2025/01/20/Spring-web-%E6%B3%A8%E8%A7%A3/1709106099162-0494d48f-cdf6-450d-b1bf-3a2c258d425f.png"></p>
<h3 id="二、Spring-Bean-注解"><a href="#二、Spring-Bean-注解" class="headerlink" title="二、Spring Bean 注解"></a><strong><font style="color:rgb(0, 0, 0);">二、Spring Bean 注解</font></strong></h3><p><font style="color:rgb(51, 51, 51);">在本小节中，主要列举与Spring Bean相关的4个注解以及它们的使用方式。</font></p>
<p><strong><font style="color:rgb(51, 51, 51);">@ComponentScan</font></strong></p>
<p><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@ComponentScan</font><font style="color:rgb(51, 51, 51);">注解用于配置Spring需要扫描的被组件注解注释的类所在的包。可以通过配置其basePackages属性或者value属性来配置需要扫描的包路径。value属性是basePackages的别名。此注解的用法如下：</font></p>
<p><strong><font style="color:rgb(51, 51, 51);">@Component</font></strong></p>
<p><font style="color:rgb(51, 51, 51);">@Component注解用于标注一个普通的组件类，它没有明确的业务范围，只是通知Spring被此注解的类需要被纳入到Spring Bean</font><a href="https://cloud.tencent.com/product/tke?from_column=20065&from=20065">容器</a><font style="color:rgb(51, 51, 51);">中并进行管理。此注解的使用示例如下：</font></p>
<p><img src="/2025/01/20/Spring-web-%E6%B3%A8%E8%A7%A3/1709106099168-8db7425c-413b-4d4c-9700-09a9a481651a.png"></p>
<p><strong><font style="color:rgb(51, 51, 51);">@Service</font></strong></p>
<p><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@Service</font><font style="color:rgb(51, 51, 51);">注解是</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@Component</font><font style="color:rgb(51, 51, 51);">的一个延伸（特例），它用于标注业务逻辑类。与</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@Component</font><font style="color:rgb(51, 51, 51);">注解一样，被此注解标注的类，会自动被Spring所管理。下面是使用</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@Service</font><font style="color:rgb(51, 51, 51);">注解的示例：</font></p>
<p><img src="/2025/01/20/Spring-web-%E6%B3%A8%E8%A7%A3/1709106099385-1480782a-ea36-4b65-9937-2f39030e0aaa.jpeg"></p>
<p><strong><font style="color:rgb(51, 51, 51);">@Repository</font></strong></p>
<p><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@Repository</font><font style="color:rgb(51, 51, 51);">注解也是</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@Component</font><font style="color:rgb(51, 51, 51);">注解的延伸，与</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@Component</font><font style="color:rgb(51, 51, 51);">注解一样，被此注解标注的类会被Spring自动管理起来，</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@Repository</font><font style="color:rgb(51, 51, 51);">注解用于标注DAO层的数据持久化类。此注解的用法如下：</font></p>
<p><img src="/2025/01/20/Spring-web-%E6%B3%A8%E8%A7%A3/1709106099679-4f026999-4d2d-44b4-b837-df76e8fcd3cc.jpeg"></p>
<h3 id="三、Spring-Dependency-Inject-与-Bean-Scops注解"><a href="#三、Spring-Dependency-Inject-与-Bean-Scops注解" class="headerlink" title="三、Spring Dependency Inject 与 Bean Scops注解"></a><strong><font style="color:rgb(0, 0, 0);">三、Spring Dependency Inject 与 Bean Scops注解</font></strong></h3><h4 id="Spring-DI注解"><a href="#Spring-DI注解" class="headerlink" title="Spring DI注解"></a><strong><font style="color:rgb(0, 0, 0);">Spring DI注解</font></strong></h4><p><strong><font style="color:rgb(51, 51, 51);">@DependsOn</font></strong></p>
<p><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@DependsOn</font><font style="color:rgb(51, 51, 51);">注解可以配置Spring IoC容器在初始化一个Bean之前，先初始化其他的Bean对象。下面是此注解使用示例代码：</font></p>
<p><img src="/2025/01/20/Spring-web-%E6%B3%A8%E8%A7%A3/1709106099765-fd8e6bbc-fd54-4788-9913-3844e0e4a45c.jpeg"></p>
<p><strong><font style="color:rgb(51, 51, 51);">@Bean</font></strong></p>
<p><font style="color:rgb(51, 51, 51);">@Bean注解主要的作用是告知Spring，被此注解所标注的类将需要纳入到Bean管理工厂中。@Bean注解的用法很简单，在这里，着重介绍@Bean注解中</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">initMethod</font><font style="color:rgb(51, 51, 51);">和</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">destroyMethod</font><font style="color:rgb(51, 51, 51);">的用法。示例如下：</font></p>
<p><img src="/2025/01/20/Spring-web-%E6%B3%A8%E8%A7%A3/1709106099732-f5c0ee38-7ef5-44f3-9f0f-cf19fcbe287f.jpeg"></p>
<h4 id="Scops注解"><a href="#Scops注解" class="headerlink" title="Scops注解"></a><strong><font style="color:rgb(0, 0, 0);">Scops注解</font></strong></h4><p><strong><font style="color:rgb(51, 51, 51);">@Scope</font></strong></p>
<p><font style="color:rgb(51, 51, 51);">@Scope注解可以用来定义@Component标注的类的作用范围以及@Bean所标记的类的作用范围。@Scope所限定的作用范围有：</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">singleton</font><font style="color:rgb(51, 51, 51);">、</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">prototype</font><font style="color:rgb(51, 51, 51);">、</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">request</font><font style="color:rgb(51, 51, 51);">、</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">session</font><font style="color:rgb(51, 51, 51);">、</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">globalSession</font><font style="color:rgb(51, 51, 51);">或者其他的自定义范围。这里以prototype为例子进行讲解。</font></p>
<p><font style="color:rgb(51, 51, 51);">当一个Spring Bean被声明为prototype（原型模式）时，在每次需要使用到该类的时候，Spring IoC容器都会初始化一个新的改类的实例。在定义一个Bean时，可以设置Bean的scope属性为</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">prototype：scope&#x3D;“prototype”</font><font style="color:rgb(51, 51, 51);">,也可以使用@Scope注解设置，如下：</font></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">@<span class="title class_">Scope</span>(value=<span class="title class_">ConfigurableBeanFactory</span>.<span class="property">SCOPE_PROPTOTYPE</span>)</span><br></pre></td></tr></table></figure>

<p><font style="color:rgb(51, 51, 51);">下面将给出两种不同的方式来使用@Scope注解，示例代码如下：</font></p>
<p><img src="/2025/01/20/Spring-web-%E6%B3%A8%E8%A7%A3/1709106099941-81a1117f-f25b-451f-8b48-d5b6a748b83e.jpeg"></p>
<p><strong><font style="color:rgb(51, 51, 51);">@Scope 单例模式</font></strong></p>
<p><font style="color:rgb(51, 51, 51);">当@Scope的作用范围</font><a href="https://cloud.tencent.com/developer/tools/blog-entry?target=http://mp.weixin.qq.com/s?__biz=MzI4Njc5NjM1NQ==&mid=2247493542&idx=2&sn=43a97be735b81408b35a00d0adaf75bf&chksm=ebd5d88adca2519cd66108625b1561983a5a40b476d377b24b6c7f8c5af4331d5d790a76be07&scene=21%23wechat_redirect&source=article&objectId=1885299">设置成Singleton时</a><font style="color:rgb(51, 51, 51);">，被此注解所标注的类只会被Spring IoC容器初始化一次。在默认情况下，Spring IoC容器所初始化的类实例都为singleton。同样的原理，此情形也有两种配置方式，示例代码如下：</font></p>
<p><img src="/2025/01/20/Spring-web-%E6%B3%A8%E8%A7%A3/1709106099945-cbf8cf90-1120-4b8d-b52e-98e023a9e162.png"></p>
<h3 id="四、容器配置注解"><a href="#四、容器配置注解" class="headerlink" title="四、容器配置注解"></a><strong><font style="color:rgb(0, 0, 0);">四、容器配置注解</font></strong></h3><h4 id="Autowired"><a href="#Autowired" class="headerlink" title="@Autowired"></a><strong><font style="color:rgb(0, 0, 0);">@Autowired</font></strong></h4><p><font style="color:rgb(51, 51, 51);">@Autowired注解用于标记Spring将要解析和注入的依赖项。此注解可以作用在构造函数、字段和setter方法上。</font></p>
<p><strong><font style="color:rgb(51, 51, 51);">作用于构造函数</font></strong></p>
<p><font style="color:rgb(51, 51, 51);">下面是@Autowired注解标注构造函数的使用示例：</font></p>
<p><img src="/2025/01/20/Spring-web-%E6%B3%A8%E8%A7%A3/1709106100372-d6898280-8259-431e-a6d7-668708a02ef7.png"></p>
<p><strong><font style="color:rgb(51, 51, 51);">作用于setter方法</font></strong></p>
<p><font style="color:rgb(51, 51, 51);">下面是@Autowired注解标注setter方法的示例代码：</font></p>
<p><img src="/2025/01/20/Spring-web-%E6%B3%A8%E8%A7%A3/1709106100395-d14606e1-09a8-48e5-a7c8-c6bcb4d9364e.png"></p>
<p><strong><font style="color:rgb(51, 51, 51);">作用于字段</font></strong></p>
<p><font style="color:rgb(51, 51, 51);">@Autowired注解标注字段是最简单的，只需要在对应的字段上加入此注解即可，示例代码如下：</font></p>
<p><img src="/2025/01/20/Spring-web-%E6%B3%A8%E8%A7%A3/1709106100526-8d32a439-9a2f-4313-8c08-d905e1f673b8.png"></p>
<h4 id="Primary"><a href="#Primary" class="headerlink" title="@Primary"></a><strong><font style="color:rgb(0, 0, 0);">@Primary</font></strong></h4><p><font style="color:rgb(51, 51, 51);">当系统中需要配置多个具有相同类型的bean时，@Primary可以定义这些Bean的优先级。下面将给出一个实例代码来说明这一特性：</font></p>
<p><img src="/2025/01/20/Spring-web-%E6%B3%A8%E8%A7%A3/1709106100512-76f86389-942f-484b-8ab4-4ee1d3e03fb5.png"></p>
<p><font style="color:rgb(51, 51, 51);">输出结果：</font></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="variable language_">this</span> is send <span class="title class_">DingDing</span> method message.</span><br></pre></td></tr></table></figure>

<h4 id="PostConstruct与-PreDestroy"><a href="#PostConstruct与-PreDestroy" class="headerlink" title="@PostConstruct与@PreDestroy"></a><strong><font style="color:rgb(0, 0, 0);">@PostConstruct与@PreDestroy</font></strong></h4><p><font style="color:rgb(51, 51, 51);">值得注意的是，这两个注解不属于Spring,它们是源于JSR-250中的两个注解，位于</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">common-annotations.jar</font><font style="color:rgb(51, 51, 51);">中。@PostConstruct注解用于标注在Bean被Spring初始化之前需要执行的方法。@PreDestroy注解用于标注Bean被销毁前需要执行的方法。下面是具体的示例代码：</font></p>
<p><img src="/2025/01/20/Spring-web-%E6%B3%A8%E8%A7%A3/1709106100654-4f6b2adb-b033-42c6-8af9-ae4724aff9f5.png"></p>
<h4 id="Qualifier"><a href="#Qualifier" class="headerlink" title="@Qualifier"></a><strong><font style="color:rgb(0, 0, 0);">@Qualifier</font></strong></h4><p><font style="color:rgb(51, 51, 51);">当系统中存在同一类型的多个Bean时，@Autowired在进行依赖注入的时候就不知道该选择哪一个实现类进行注入。此时，我们可以使用@Qualifier注解来微调，帮助</font><a href="https://cloud.tencent.com/developer/tools/blog-entry?target=http://mp.weixin.qq.com/s?__biz=MzI4Njc5NjM1NQ==&mid=2247508834&idx=1&sn=ccf444814b629a320c3b979c71df2bc0&chksm=ebd59c4edca21558b045a819b4d03500164ae7bea099af4725ea1f0d6bd795ef2b03fd509fcd&scene=21%23wechat_redirect&source=article&objectId=1885299">@Autowired选择正确的依赖项</a><font style="color:rgb(51, 51, 51);">。下面是一个关于此注解的代码示例：</font></p>
<p><img src="/2025/01/20/Spring-web-%E6%B3%A8%E8%A7%A3/1709106101078-632fad2d-dbeb-4aa4-a3dd-2d80766d7adb.png"></p>
<h3 id="五、Spring-Boot注解"><a href="#五、Spring-Boot注解" class="headerlink" title="五、Spring Boot注解"></a><strong><font style="color:rgb(0, 0, 0);">五、Spring Boot注解</font></strong></h3><p><strong><font style="color:rgb(51, 51, 51);">@SpringBootApplication</font></strong></p>
<p><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@SpringBootApplication</font><font style="color:rgb(51, 51, 51);">注解是一个快捷的配置注解，在被它标注的类中，可以</font><a href="https://cloud.tencent.com/developer/tools/blog-entry?target=http://mp.weixin.qq.com/s?__biz=MzI4Njc5NjM1NQ==&mid=2247499001&idx=2&sn=a4e94719a95f22171efd8ff2907fa95a&chksm=ebd5c3d5dca24ac3f620be051883c784f062eeee41ac89b53fbd09a8c145c0c0e0d08cdcfc4b&scene=21%23wechat_redirect&source=article&objectId=1885299">定义一个或多个Bean</a><font style="color:rgb(51, 51, 51);">，并自动触发自动配置Bean和自动扫描组件。此注解相当于</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@Configuration</font><font style="color:rgb(51, 51, 51);">、</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@EnableAutoConfiguration</font><font style="color:rgb(51, 51, 51);">和</font><font style="color:rgb(10, 191, 91);background-color:rgb(243, 245, 249);">@ComponentScan</font><font style="color:rgb(51, 51, 51);">的组合。</font></p>
<p><font style="color:rgb(51, 51, 51);">在</font><font style="color:rgb(0, 82, 217);">Spring Boot</font><font style="color:rgb(51, 51, 51);">应用程序的主类中，就使用了此注解。示例代码如下：</font></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">@<span class="title class_">SpringBootApplication</span></span><br><span class="line">public <span class="keyword">class</span> <span class="title class_">Application</span>&#123;</span><br><span class="line"> public <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span>(<span class="params"><span class="built_in">String</span> [] args</span>)&#123;</span><br><span class="line">   <span class="title class_">SpringApplication</span>.<span class="title function_">run</span>(<span class="title class_">Application</span>.<span class="property">class</span>,args);</span><br><span class="line"> &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong><font style="color:rgb(51, 51, 51);">@EnableAutoConfiguration</font></strong></p>
<p><font style="color:rgb(51, 51, 51);">@EnableAutoConfiguration注解用于通知Spring，根据当前类路径下引入的依赖包，自动配置与这些依赖包相关的配置项。</font></p>
<p><strong><font style="color:rgb(51, 51, 51);">@ConditionalOnClass与@ConditionalOnMissingClass</font></strong></p>
<p><font style="color:rgb(51, 51, 51);">这两个注解属于类条件注解，它们根据是否存在某个类作为判断依据来决定是否要执行某些配置。下面是一个简单的示例代码：</font></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">@<span class="title class_">Configuration</span></span><br><span class="line">@<span class="title class_">ConditionalOnClass</span>(<span class="title class_">DataSource</span>.<span class="property">class</span>)</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MySQLAutoConfiguration</span> &#123;</span><br><span class="line"> <span class="comment">//...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong><font style="color:rgb(51, 51, 51);">@ConditionalOnBean与@ConditionalOnMissingBean</font></strong></p>
<p><font style="color:rgb(51, 51, 51);">这两个注解属于对象条件注解，根据是否存在某个对象作为依据来决定是否要执行某些配置方法。示例代码如下：</font></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">@<span class="title class_">Bean</span></span><br><span class="line">@<span class="title class_">ConditionalOnBean</span>(name=<span class="string">&quot;dataSource&quot;</span>)</span><br><span class="line"><span class="title class_">LocalContainerEntityManagerFactoryBean</span> <span class="title function_">entityManagerFactory</span>(<span class="params"></span>)&#123;</span><br><span class="line"> <span class="comment">//...</span></span><br><span class="line">&#125;</span><br><span class="line">@<span class="title class_">Bean</span></span><br><span class="line">@<span class="title class_">ConditionalOnMissingBean</span></span><br><span class="line">public <span class="title class_">MyBean</span> <span class="title function_">myBean</span>(<span class="params"></span>)&#123;</span><br><span class="line"> <span class="comment">//...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong><font style="color:rgb(51, 51, 51);">@ConditionalOnProperty</font></strong></p>
<p><font style="color:rgb(51, 51, 51);">@ConditionalOnProperty注解会根据Spring配置文件中的配置项是否满足配置要求，从而决定是否要执行被其标注的方法。示例代码如下：</font></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">@<span class="title class_">Bean</span></span><br><span class="line">@<span class="title class_">ConditionalOnProperty</span>(name=<span class="string">&quot;alipay&quot;</span>,havingValue=<span class="string">&quot;on&quot;</span>)</span><br><span class="line"><span class="title class_">Alipay</span> <span class="title function_">alipay</span>(<span class="params"></span>)&#123;</span><br><span class="line"> <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Alipay</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong><font style="color:rgb(51, 51, 51);">@ConditionalOnResource</font></strong></p>
<p><font style="color:rgb(51, 51, 51);">此注解用于检测当某个配置文件存在使，则触发被其标注的方法，下面是使用此注解的代码示例：</font></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">@<span class="title class_">ConditionalOnResource</span>(resources = <span class="string">&quot;classpath:website.properties&quot;</span>)</span><br><span class="line"><span class="title class_">Properties</span> <span class="title function_">addWebsiteProperties</span>(<span class="params"></span>)&#123;</span><br><span class="line"> <span class="comment">//...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong><font style="color:rgb(51, 51, 51);">@ConditionalOnWebApplication与@ConditionalOnNotWebApplication</font></strong></p>
<p><font style="color:rgb(51, 51, 51);">这两个注解用于判断当前的应用程序是否是Web应用程序。如果当前应用是Web应用程序，则使用Spring WebApplicationContext,并定义其会话的生命周期。下面是一个简单的示例：</font></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">@<span class="title class_">ConditionalOnWebApplication</span></span><br><span class="line"><span class="title class_">HealthCheckController</span> <span class="title function_">healthCheckController</span>(<span class="params"></span>)&#123;</span><br><span class="line"> <span class="comment">//...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong><font style="color:rgb(51, 51, 51);">@ConditionalExpression</font></strong></p>
<p><font style="color:rgb(51, 51, 51);">此注解可以让我们控制更细粒度的基于表达式的配置条件限制。当表达式满足某个条件或者表达式为真的时候，将会执行被此注解标注的方法。</font></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">@<span class="title class_">Bean</span></span><br><span class="line">@<span class="title class_">ConditionalException</span>(<span class="string">&quot;$&#123;localstore&#125; &amp;&amp; $&#123;local == &#x27;true&#x27;&#125;&quot;</span>)</span><br><span class="line"><span class="title class_">LocalFileStore</span> <span class="title function_">store</span>(<span class="params"></span>)&#123;</span><br><span class="line"> <span class="comment">//...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong><font style="color:rgb(51, 51, 51);">@Conditional</font></strong></p>
<p><font style="color:rgb(51, 51, 51);">@Conditional注解可以控制更为复杂的配置条件。在Spring内置的条件控制注解不满足应用需求的时候，可以使用此注解定义自定义的控制条件，以达到自定义的要求。下面是使用该注解的简单示例：</font></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">@<span class="title class_">Conditioanl</span>(<span class="title class_">CustomConditioanl</span>.<span class="property">class</span>)</span><br><span class="line"><span class="title class_">CustomProperties</span> <span class="title function_">addCustomProperties</span>(<span class="params"></span>)&#123;</span><br><span class="line"> <span class="comment">//...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a><strong><font style="color:rgb(0, 0, 0);">总结</font></strong></h3><p><font style="color:rgb(51, 51, 51);">本次课程总结了Spring Boot中常见的各类型注解的使用方式，让大家能够统一的对Spring Boot常用注解有一个全面的了解。另外，关注</font><font style="color:rgb(0, 82, 217);">Java</font><font style="color:rgb(51, 51, 51);">知音公众号，回复“后端面试”，送你一份面试题宝典！</font></p>
<p><font style="color:rgb(51, 51, 51);">由于篇幅的原因，关于Spring Boot不常用的一些注解，将在下一次分享中进行补充和说明。</font></p>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Spring</tag>
        <tag>注解</tag>
      </tags>
  </entry>
  <entry>
    <title>尚硅谷尚庭公寓-03</title>
    <url>/2025/01/24/%E5%B0%9A%E7%A1%85%E8%B0%B7%E5%B0%9A%E5%BA%AD%E5%85%AC%E5%AF%93-03/</url>
    <content><![CDATA[<h1 id="尚硅谷尚庭公寓-03"><a href="#尚硅谷尚庭公寓-03" class="headerlink" title="尚硅谷尚庭公寓-03"></a>尚硅谷尚庭公寓-03</h1><h2 id="ThreadLocal"><a href="#ThreadLocal" class="headerlink" title="ThreadLocal"></a>ThreadLocal</h2><h3 id="介绍"><a href="#介绍" class="headerlink" title="介绍"></a>介绍</h3><p>ThreadLocal是JDK提供的一个线程内部的数据存储工具类，它提供了一些方法，用于在当前线程中存储数据，并且每个线程都可以独立地访问自己的数据。<br><img src="thread.png" style="zoom: 33%;" /></p>
<span id="more"></span>

<h3 id="实践"><a href="#实践" class="headerlink" title="实践"></a>实践</h3><ul>
<li><p>理论上我们可以在Controller方法中，使用<code>@RequestHeader</code>获取JWT，然后在进行解析，如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Operation(summary = &quot;获取登陆用户个人信息&quot;)</span></span><br><span class="line"><span class="meta">@GetMapping(&quot;info&quot;)</span></span><br><span class="line"><span class="keyword">public</span> Result&lt;SystemUserInfoVo&gt; <span class="title function_">info</span><span class="params">(<span class="meta">@RequestHeader(&quot;access-token&quot;)</span> String token)</span> &#123;</span><br><span class="line">    <span class="type">Claims</span> <span class="variable">claims</span> <span class="operator">=</span> JwtUtil.parseToken(token);</span><br><span class="line">    <span class="type">Long</span> <span class="variable">userId</span> <span class="operator">=</span> claims.get(<span class="string">&quot;userId&quot;</span>, Long.class);</span><br><span class="line">    <span class="type">SystemUserInfoVo</span> <span class="variable">userInfo</span> <span class="operator">=</span> service.getLoginUserInfo(userId);</span><br><span class="line">    <span class="keyword">return</span> Result.ok(userInfo);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上述代码的逻辑没有任何问题，但是这样做，JWT会被重复解析两次（一次在拦截器中，一次在该方法中）。为避免重复解析，通常会在拦截器将Token解析完毕后，将结果保存至<strong>ThreadLocal</strong>中，这样一来，我们便可以在整个请求的处理流程中进行访问了。</p>
<p>在<strong>common模块</strong>中创建<code>com.atguigu.lease.common.login.LoginUserHolder</code>工具类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">LoginUserHolder</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> ThreadLocal&lt;LoginUser&gt; threadLocal = <span class="keyword">new</span> <span class="title class_">ThreadLocal</span>&lt;&gt;();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">setLoginUser</span><span class="params">(LoginUser loginUser)</span> &#123;</span><br><span class="line">        threadLocal.set(loginUser);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> LoginUser <span class="title function_">getLoginUser</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> threadLocal.get();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">clear</span><span class="params">()</span> &#123;</span><br><span class="line">        threadLocal.remove();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>同时在<strong>common模块</strong>中创建<code>com.atguigu.lease.common.login.LoginUser</code>类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Data</span></span><br><span class="line"><span class="meta">@AllArgsConstructor</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">LoginUser</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> Long userId;</span><br><span class="line">    <span class="keyword">private</span> String username;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>修改<code>AuthenticationInterceptor</code>拦截器</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">AuthenticationInterceptor</span> <span class="keyword">implements</span> <span class="title class_">HandlerInterceptor</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">preHandle</span><span class="params">(HttpServletRequest request, HttpServletResponse response, Object handler)</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line"></span><br><span class="line">        <span class="type">String</span> <span class="variable">token</span> <span class="operator">=</span> request.getHeader(<span class="string">&quot;access-token&quot;</span>);</span><br><span class="line"></span><br><span class="line">        <span class="type">Claims</span> <span class="variable">claims</span> <span class="operator">=</span> JwtUtil.parseToken(token);</span><br><span class="line">        <span class="type">Long</span> <span class="variable">userId</span> <span class="operator">=</span> claims.get(<span class="string">&quot;userId&quot;</span>, Long.class);</span><br><span class="line">        <span class="type">String</span> <span class="variable">username</span> <span class="operator">=</span> claims.get(<span class="string">&quot;username&quot;</span>, String.class);</span><br><span class="line">        LoginUserHolder.setLoginUser(<span class="keyword">new</span> <span class="title class_">LoginUser</span>(userId, username));</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">afterCompletion</span><span class="params">(HttpServletRequest request, HttpServletResponse response, Object handler, Exception ex)</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">        LoginUserHolder.clear();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>编写Controller层逻辑</strong></p>
<p>在<code>LoginController</code>中增加如下内容</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Operation(summary = &quot;获取登陆用户个人信息&quot;)</span></span><br><span class="line"><span class="meta">@GetMapping(&quot;info&quot;)</span></span><br><span class="line"><span class="keyword">public</span> Result&lt;SystemUserInfoVo&gt; <span class="title function_">info</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">SystemUserInfoVo</span> <span class="variable">userInfo</span> <span class="operator">=</span> service.getLoginUserInfo(LoginUserHolder.getLoginUser().getUserId());</span><br><span class="line">    <span class="keyword">return</span> Result.ok(userInfo);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>编写Service层逻辑</strong></p>
<p>在<code>LoginService</code>中增加如下内容</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> SystemUserInfoVo <span class="title function_">getLoginUserInfo</span><span class="params">(Long userId)</span> &#123;</span><br><span class="line">    <span class="type">SystemUser</span> <span class="variable">systemUser</span> <span class="operator">=</span> systemUserMapper.selectById(userId);</span><br><span class="line">    <span class="type">SystemUserInfoVo</span> <span class="variable">systemUserInfoVo</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">SystemUserInfoVo</span>();</span><br><span class="line">    systemUserInfoVo.setName(systemUser.getName());</span><br><span class="line">    systemUserInfoVo.setAvatarUrl(systemUser.getAvatarUrl());</span><br><span class="line">    <span class="keyword">return</span> systemUserInfoVo;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="xml文件的转义"><a href="#xml文件的转义" class="headerlink" title="xml文件&lt;和&gt;的转义"></a><strong>xml文件<code>&lt;</code>和<code>&gt;</code>的转义</strong></h2><p>由于xml文件中的<code>&lt;</code>和<code>&gt;</code>是特殊符号，需要转义处理。</p>
<table>
<thead>
<tr>
<th>原符号</th>
<th>转义符号</th>
</tr>
</thead>
<tbody><tr>
<td><code>&lt;</code></td>
<td><code>&amp;lt;</code></td>
</tr>
<tr>
<td><code>&gt;</code></td>
<td><code>&amp;gt;</code></td>
</tr>
</tbody></table>
<h2 id="Mybatis-Plus分页插件注意事项"><a href="#Mybatis-Plus分页插件注意事项" class="headerlink" title="Mybatis-Plus分页插件注意事项"></a><strong>Mybatis-Plus分页插件注意事项</strong></h2><p>使用Mybatis-Plus的分页插件进行分页查询时，如果结果需要使用<code>&lt;collection&gt;</code>进行映射，只能使用**<a href="https://mybatis.org/mybatis-3sqlmap-xml.html#nested-select-for-collection">嵌套查询（Nested Select for Collection）</a><strong>，而不能使用</strong><a href="https://mybatis.org/mybatis-3/sqlmap-xmlhtml#nested-results-for-collection">嵌套结果映射（Nested Results for Collection）</a><strong>。<br><strong>嵌套查询</strong>和</strong>嵌套结果映射**是Collection映射的两种方式，下面通过一个案例进行介绍<br>例如有<code>room_info</code>和<code>graph_info</code>两张表，其关系为一对多，如下<br><img src="mybatis-一对多.drawio.svg" style="zoom:50%;" /><br>现需要查询房间列表及其图片信息，期望返回的结果如下</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">[</span></span><br><span class="line">    <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="number">1</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;number&quot;</span><span class="punctuation">:</span> <span class="number">201</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;rent&quot;</span><span class="punctuation">:</span> <span class="number">2000</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;graphList&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">            <span class="punctuation">&#123;</span></span><br><span class="line">                <span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="number">1</span><span class="punctuation">,</span></span><br><span class="line">                <span class="attr">&quot;url&quot;</span><span class="punctuation">:</span> <span class="string">&quot;http://&quot;</span><span class="punctuation">,</span></span><br><span class="line">                <span class="attr">&quot;roomId&quot;</span><span class="punctuation">:</span> <span class="number">1</span></span><br><span class="line">            <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="punctuation">&#123;</span></span><br><span class="line">                <span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="number">2</span><span class="punctuation">,</span></span><br><span class="line">                <span class="attr">&quot;url&quot;</span><span class="punctuation">:</span> <span class="string">&quot;http://&quot;</span><span class="punctuation">,</span></span><br><span class="line">                <span class="attr">&quot;roomId&quot;</span><span class="punctuation">:</span> <span class="number">1</span></span><br><span class="line">            <span class="punctuation">&#125;</span></span><br><span class="line">        <span class="punctuation">]</span></span><br><span class="line">    <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="number">2</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;number&quot;</span><span class="punctuation">:</span> <span class="number">202</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;rent&quot;</span><span class="punctuation">:</span> <span class="number">3000</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;graphList&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">            <span class="punctuation">&#123;</span></span><br><span class="line">                <span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="number">3</span><span class="punctuation">,</span></span><br><span class="line">                <span class="attr">&quot;url&quot;</span><span class="punctuation">:</span> <span class="string">&quot;http://&quot;</span><span class="punctuation">,</span></span><br><span class="line">                <span class="attr">&quot;roomId&quot;</span><span class="punctuation">:</span> <span class="number">2</span></span><br><span class="line">            <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">            <span class="punctuation">&#123;</span></span><br><span class="line">                <span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="number">4</span><span class="punctuation">,</span></span><br><span class="line">                <span class="attr">&quot;url&quot;</span><span class="punctuation">:</span> <span class="string">&quot;http://&quot;</span><span class="punctuation">,</span></span><br><span class="line">                <span class="attr">&quot;roomId&quot;</span><span class="punctuation">:</span> <span class="number">2</span></span><br><span class="line">            <span class="punctuation">&#125;</span></span><br><span class="line">        <span class="punctuation">]</span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">]</span></span><br></pre></td></tr></table></figure>

<p>  为得到上述结果，可使用以下两种方式</p>
<ul>
<li><p><strong>嵌套结果映射</strong></p>
  <figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">&quot;selectRoomPage&quot;</span> <span class="attr">resultMap</span>=<span class="string">&quot;RoomPageMap&quot;</span>&gt;</span></span><br><span class="line">    select ri.id room_id,</span><br><span class="line">           ri.number,</span><br><span class="line">           ri.rent,</span><br><span class="line">        gi.id graph_id,</span><br><span class="line">           gi.url,</span><br><span class="line">           gi.room_id</span><br><span class="line">    from room_info ri</span><br><span class="line">    left join graph_info gi on ri.id=gi.room_id</span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">resultMap</span> <span class="attr">id</span>=<span class="string">&quot;RoomPageMap&quot;</span> <span class="attr">type</span>=<span class="string">&quot;RoomInfoVo&quot;</span> <span class="attr">autoMapping</span>=<span class="string">&quot;true&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">id</span> <span class="attr">column</span>=<span class="string">&quot;room_id&quot;</span> <span class="attr">property</span>=<span class="string">&quot;id&quot;</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">collection</span> <span class="attr">property</span>=<span class="string">&quot;graphInfoList&quot;</span> <span class="attr">ofType</span>=<span class="string">&quot;GraphInfo&quot;</span> <span class="attr">autoMapping</span>=<span class="string">&quot;true&quot;</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">id</span> <span class="attr">column</span>=<span class="string">&quot;graph_id&quot;</span> <span class="attr">property</span>=<span class="string">&quot;id&quot;</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">collection</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">resultMap</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>  这种方式的执行原理如下图所示</p>
  <img src="mybatis-嵌套结果映射.drawio.svg" style="zoom:50%;" />
</li>
<li><p><strong>嵌套查询</strong></p>
  <figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">&quot;selectRoomPage&quot;</span> <span class="attr">resultMap</span>=<span class="string">&quot;RoomPageMap&quot;</span>&gt;</span></span><br><span class="line">    select id,</span><br><span class="line">           number,</span><br><span class="line">           rent</span><br><span class="line">    from room_info</span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">resultMap</span> <span class="attr">id</span>=<span class="string">&quot;RoomPageMap&quot;</span> <span class="attr">type</span>=<span class="string">&quot;RoomInfoVo&quot;</span> <span class="attr">autoMapping</span>=<span class="string">&quot;true&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">id</span> <span class="attr">column</span>=<span class="string">&quot;id&quot;</span> <span class="attr">property</span>=<span class="string">&quot;id&quot;</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">collection</span> <span class="attr">property</span>=<span class="string">&quot;graphInfoList&quot;</span> <span class="attr">ofType</span>=<span class="string">&quot;GraphInfo&quot;</span> <span class="attr">select</span>=<span class="string">&quot;selectGraphByRoomId&quot;</span>       <span class="attr">column</span>=<span class="string">&quot;id&quot;</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">resultMap</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">&quot;selectGraphByRoomId&quot;</span> <span class="attr">resultType</span>=<span class="string">&quot;GraphInfo&quot;</span>&gt;</span></span><br><span class="line">    select id,</span><br><span class="line">           url,</span><br><span class="line">        room_id</span><br><span class="line">    from graph_info</span><br><span class="line">    where room_id = #&#123;id&#125;</span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>  这种方法使用两个独立的查询语句来获取一对多关系的数据。首先，Mybatis会执行主查询来获取<code>room_info</code>列表，然后对于每个<code>room_info</code>，Mybatis都会执行一次子查询来获取其对应的<code>graph_info</code>。</p>
  <img src="mybatis-嵌套查询.drawio.svg" style="zoom:50%;" />

<p>若现在使用MybatisPlus的分页插件进行分页查询，假如查询的内容是第<strong>1</strong>页，每页<strong>2</strong>条记录，则上述两种方式的查询结果分别是</p>
</li>
<li><p><strong>嵌套结果映射</strong></p>
  <img src="mybatis-分页查询-嵌套结果映射.drawio.svg" style="zoom:50%;" />
</li>
<li><p><strong>嵌套查询</strong></p>
  <img src="mybatis-分页查询-嵌套查询.drawio.svg" style="zoom:50%;" />

<p>显然<strong>嵌套结果映射</strong>的分页逻辑是存在问题的。</p>
</li>
</ul>
<h2 id="异步操作"><a href="#异步操作" class="headerlink" title="异步操作"></a>异步操作</h2><ul>
<li>保存浏览历史的动作不应影响前端获取房间详情信息，故此处采取异步操作。Spring Boot提供了<code>@Async</code>注解来完成异步操作，具体使用方式为：<ul>
<li><p>启用Spring Boot异步操作支持<br>  在 Spring Boot 主应用程序类上添加 <code>@EnableAsync</code> 注解，如下</p>
  <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@SpringBootApplication</span></span><br><span class="line"><span class="meta">@EnableAsync</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">AppWebApplication</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        SpringApplication.run(AppWebApplication.class);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>在要进行异步处理的方法上添加 <code>@Async</code> 注解，如下</p>
  <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="meta">@Async</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">saveHistory</span><span class="params">(Long userId, Long roomId)</span> &#123;</span><br><span class="line"></span><br><span class="line">    LambdaQueryWrapper&lt;BrowsingHistory&gt; queryWrapper = <span class="keyword">new</span> <span class="title class_">LambdaQueryWrapper</span>&lt;&gt;();</span><br><span class="line">    queryWrapper.eq(BrowsingHistory::getUserId, userId);</span><br><span class="line">    queryWrapper.eq(BrowsingHistory::getRoomId, roomId);</span><br><span class="line">    <span class="type">BrowsingHistory</span> <span class="variable">browsingHistory</span> <span class="operator">=</span> browsingHistoryMapper.selectOne(queryWrapper);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (browsingHistory != <span class="literal">null</span>) &#123;</span><br><span class="line">        browsingHistory.setBrowseTime(<span class="keyword">new</span> <span class="title class_">Date</span>());</span><br><span class="line">        browsingHistoryMapper.updateById(browsingHistory);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="type">BrowsingHistory</span> <span class="variable">newBrowsingHistory</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">BrowsingHistory</span>();</span><br><span class="line">        newBrowsingHistory.setUserId(userId);</span><br><span class="line">        newBrowsingHistory.setRoomId(roomId);</span><br><span class="line">        newBrowsingHistory.setBrowseTime(<span class="keyword">new</span> <span class="title class_">Date</span>());</span><br><span class="line">        browsingHistoryMapper.insert(newBrowsingHistory);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
</ul>
<h2 id="缓存优化"><a href="#缓存优化" class="headerlink" title="缓存优化"></a>缓存优化</h2><h3 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h3><p>缓存优化是一个性价比很高的优化手段，多数情况下，缓存优化可以通过一些简单的操作，换来性能的大幅提升。缓存优化的核心思想就是将一些原本保存在磁盘（例如MySQL）中的、经常访问并且查询开销比较大的数据，临时保存到内存（例如Redis）中。后序再访问相同数据时，就可直接从内存中获取结果，而无需再访问磁盘，由于内存的读写速度远高于磁盘，因此就能极大的提高程序的性能。</p>
<img src="缓存概述.drawio.svg" style="zoom:50%;" />

<p>在使用缓存优化时，有一个问题不得不提，那就是<strong>数据库和缓存数据的一致性</strong>，当数据库中的数据发生变化时，缓存中的数据也要同步更新，否则就会出现数据不一致的问题，解决该问题的方案有如下几个</p>
<ul>
<li>数据发生变化时，更新数据库的同时也更新缓存</li>
<li>数据发生变化时，更新数据库的同时删除缓存</li>
</ul>
<p>在了解了缓存优化的核心思想后，我们以移动端中的<code>根据ID获取房间详情</code>接口为例，进行缓存优化。该接口涉及多表查询，查询时会多次访问数据库，查询代价较高，故可采取缓存优化，加快查询速度。</p>
<h3 id="编写缓存逻辑"><a href="#编写缓存逻辑" class="headerlink" title="编写缓存逻辑"></a>编写缓存逻辑</h3><p><strong>1.自定义RedisTemplate</strong></p>
<p>本项目使用Reids保存缓存数据，因此我们需要使用RedisTemplate进行读写操作。前文提到过，<code>Spring-data-redis</code>提供了<code>StringRedisTemplate</code>和<code>RedisTemplate&lt;Object,Object&gt;</code>两个实例，但是两个实例均不满足我们当前的需求，所以我们需要自定义RedisTemplate。</p>
<p>在<strong>common模块</strong>中创建<code>com.atguigu.lease.common.redis.RedisConfiguration</code>类，内容如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">RedisConfiguration</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="keyword">public</span> RedisTemplate&lt;String, Object&gt; <span class="title function_">stringObjectRedisTemplate</span><span class="params">(RedisConnectionFactory redisConnectionFactory)</span> &#123;</span><br><span class="line">        RedisTemplate&lt;String, Object&gt; template = <span class="keyword">new</span> <span class="title class_">RedisTemplate</span>&lt;&gt;();</span><br><span class="line">        template.setConnectionFactory(redisConnectionFactory);</span><br><span class="line">        template.setKeySerializer(RedisSerializer.string());</span><br><span class="line">        template.setValueSerializer(RedisSerializer.java());</span><br><span class="line">        <span class="keyword">return</span> template;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>2.编写缓存逻辑</strong></p>
<p>修改<strong>web-app模块</strong>中的<code>com.atguigu.lease.web.app.service.impl.RoomInfoServiceImpl</code>中的<code>getDetailById</code>方法，如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> RoomDetailVo <span class="title function_">getDetailById</span><span class="params">(Long id)</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="type">String</span> <span class="variable">key</span> <span class="operator">=</span> RedisConstant.APP_ROOM_PREFIX + id;</span><br><span class="line">    <span class="type">RoomDetailVo</span> <span class="variable">roomDetailVo</span> <span class="operator">=</span> (RoomDetailVo) redisTemplate.opsForValue().get(key);</span><br><span class="line">    <span class="keyword">if</span> (roomDetailVo == <span class="literal">null</span>) &#123;</span><br><span class="line">        <span class="comment">//1.查询房间信息</span></span><br><span class="line">        ......</span><br><span class="line"></span><br><span class="line">        <span class="comment">//2.查询图片</span></span><br><span class="line">        ......</span><br><span class="line"></span><br><span class="line">        <span class="comment">//3.查询租期</span></span><br><span class="line">        ......</span><br><span class="line"></span><br><span class="line">        <span class="comment">//4.查询配套</span></span><br><span class="line">        ......</span><br><span class="line"></span><br><span class="line">        <span class="comment">//5.查询标签</span></span><br><span class="line">        ......</span><br><span class="line"></span><br><span class="line">        <span class="comment">//6.查询支付方式</span></span><br><span class="line">        ......</span><br><span class="line"></span><br><span class="line">        <span class="comment">//7.查询基本属性</span></span><br><span class="line">        ......</span><br><span class="line"></span><br><span class="line">        <span class="comment">//8.查询杂费信息</span></span><br><span class="line">        ......</span><br><span class="line"></span><br><span class="line">        <span class="comment">//9.查询公寓信息</span></span><br><span class="line">        ......</span><br><span class="line"></span><br><span class="line">        roomDetailVo = <span class="keyword">new</span> <span class="title class_">RoomDetailVo</span>();</span><br><span class="line">        ......</span><br><span class="line"></span><br><span class="line">        redisTemplate.opsForValue().set(key, roomDetailVo);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//10.保存浏览历史</span></span><br><span class="line">    browsingHistoryService.saveHistory(LoginUserHolder.getLoginUser().getUserId(), id);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> roomDetailVo;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>3.编写删除缓存逻辑</strong></p>
<p>为保证缓存数据的一致性，在房间信息发生变化时，需要删除相关缓存。<br>修改<strong>web-admin模块</strong>中的<code>com.atguigu.lease.web.admin.service.impl.RoomInfoServiceImpl</code>中的<code>saveOrUpdateRoom</code>方法，如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">saveOrUpdateRoom</span><span class="params">(RoomSubmitVo roomSubmitVo)</span> &#123;</span><br><span class="line">    <span class="type">boolean</span> <span class="variable">isUpdate</span> <span class="operator">=</span> roomSubmitVo.getId() != <span class="literal">null</span>;</span><br><span class="line">    <span class="built_in">super</span>.saveOrUpdate(roomSubmitVo);</span><br><span class="line"></span><br><span class="line">    <span class="comment">//若为更新操作，则先删除与Room相关的各项信息列表</span></span><br><span class="line">    <span class="keyword">if</span> (isUpdate) &#123;</span><br><span class="line">        <span class="comment">//1.删除原有graphInfoList</span></span><br><span class="line">        ......</span><br><span class="line"></span><br><span class="line">        <span class="comment">//2.删除原有roomAttrValueList</span></span><br><span class="line">        ......</span><br><span class="line"></span><br><span class="line">        <span class="comment">//3.删除原有roomFacilityList</span></span><br><span class="line">        ......</span><br><span class="line"></span><br><span class="line">        <span class="comment">//4.删除原有roomLabelList</span></span><br><span class="line">        ......</span><br><span class="line"></span><br><span class="line">        <span class="comment">//5.删除原有paymentTypeList</span></span><br><span class="line">        ......</span><br><span class="line"></span><br><span class="line">        <span class="comment">//6.删除原有leaseTermList</span></span><br><span class="line">        ......</span><br><span class="line"></span><br><span class="line">        <span class="comment">//7.删除缓存</span></span><br><span class="line">        redisTemplate.delete(RedisConstant.APP_LOGIN_PREFIX + roomSubmitVo.getId());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//1.保存新的graphInfoList</span></span><br><span class="line">    ......</span><br><span class="line"></span><br><span class="line">    <span class="comment">//2.保存新的roomAttrValueList</span></span><br><span class="line">    ......</span><br><span class="line"></span><br><span class="line">    <span class="comment">//3.保存新的facilityInfoList</span></span><br><span class="line">    ......</span><br><span class="line"></span><br><span class="line">    <span class="comment">//4.保存新的labelInfoList</span></span><br><span class="line">    ......</span><br><span class="line"></span><br><span class="line">    <span class="comment">//5.保存新的paymentTypeList</span></span><br><span class="line">    ......</span><br><span class="line"></span><br><span class="line">    <span class="comment">//6.保存新的leaseTermList</span></span><br><span class="line">    ......</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>修改<strong>web-admin模块</strong>中的<code>com.atguigu.lease.web.admin.service.impl.RoomInfoServiceImpl</code>中的<code>removeRoomById</code>方法，如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">removeRoomById</span><span class="params">(Long id)</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//1.删除RoomInfo</span></span><br><span class="line">    ......</span><br><span class="line"></span><br><span class="line">    <span class="comment">//2.删除graphInfoList</span></span><br><span class="line">    ......</span><br><span class="line"></span><br><span class="line">    <span class="comment">//3.删除attrValueList</span></span><br><span class="line">    ......</span><br><span class="line"></span><br><span class="line">    <span class="comment">//4.删除facilityInfoList</span></span><br><span class="line">    ......</span><br><span class="line"></span><br><span class="line">    <span class="comment">//5.删除labelInfoList</span></span><br><span class="line">    ......</span><br><span class="line"></span><br><span class="line">    <span class="comment">//6.删除paymentTypeList</span></span><br><span class="line">    ......</span><br><span class="line"></span><br><span class="line">    <span class="comment">//7.删除leaseTermList</span></span><br><span class="line">    ......</span><br><span class="line"></span><br><span class="line">    <span class="comment">//8.删除缓存</span></span><br><span class="line">    redisTemplate.delete(RedisConstant.APP_ROOM_PREFIX + id);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="压力测试"><a href="#压力测试" class="headerlink" title="压力测试"></a>压力测试</h3><p>可使用Postman或者Apifox等工具对<code>根据ID获取房间详情</code>这个接口进行压力测试，下图是增加缓存前后的测试报告</p>
<img src="缓存压力测试报告.png" style="border:solid 1px" />

<h2 id="阿里云短信"><a href="#阿里云短信" class="headerlink" title="阿里云短信"></a>阿里云短信</h2><h3 id="获取短信验证码"><a href="#获取短信验证码" class="headerlink" title="获取短信验证码"></a>获取短信验证码</h3><p>该接口需向登录手机号码发送短信验证码，各大云服务厂商都提供短信服务，本项目使用阿里云完成短信验证码功能，下面介绍具体配置。</p>
<ul>
<li><p><strong>配置短信服务</strong></p>
<ul>
<li><p><strong>开通短信服务</strong></p>
<ul>
<li><p>在<a href="https://www.aliyun.com/">阿里云官网</a>，注册阿里云账号，并按照指引，完成实名认证（不认证，无法购买服务）</p>
</li>
<li><p>找到<a href="https://www.aliyun.com/product/sms">短信服务</a>，选择<strong>免费开通</strong></p>
</li>
<li><p>进入<a href="https://dysms.console.aliyun.com/overview">短信服务控制台</a>，选择<strong>快速学习和测试</strong></p>
</li>
<li><p>找到<strong>发送测试</strong>下的<strong>API发送测试</strong>，绑定测试用的手机号（只有绑定的手机号码才能收到测试短信），然后配置短信签名和短信模版，这里选择**[专用]测试签名&#x2F;模版**。</p>
<p><img src="/2025/01/24/%E5%B0%9A%E7%A1%85%E8%B0%B7%E5%B0%9A%E5%BA%AD%E5%85%AC%E5%AF%93-03/%E7%9F%AD%E4%BF%A1%E6%9C%8D%E5%8A%A1%E6%B5%8B%E8%AF%95.png"></p>
</li>
</ul>
</li>
<li><p><strong>创建AccessKey</strong></p>
<p>云账号 AccessKey 是访问阿里云 API 的密钥，没有AccessKey无法调用短信服务。点击页面右上角的头像，选择<strong>AccessKey管理</strong>，然后<strong>创建AccessKey</strong>。</p>
<img src="AccessKey管理.png" alt="image-20230808104345383" style="zoom: 45%;" /></li>
</ul>
</li>
<li><p><strong>配置所需依赖</strong></p>
<p>如需调用阿里云的短信服务，需使用其提供的SDK，具体可参考<a href="https://next.api.aliyun.com/api-tools/sdk/Dysmsapi?spm=a2c4g.215759.0.0.43e6807dDRAZVz&version=2017-05-25&language=java-tea&tab=primer-doc#doc-summary">官方文档</a>。</p>
<p>在<strong>common模块</strong>的pom.xml文件中增加如下内容</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.aliyun<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>dysmsapi20170525<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>配置发送短信客户端</strong></p>
<ul>
<li><p>在<code>application.yml</code>中增加如下内容</p>
<figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="attr">aliyun:</span></span><br><span class="line">  <span class="attr">sms:</span></span><br><span class="line">    <span class="attr">access-key-id:</span> <span class="string">&lt;access-key-id&gt;</span></span><br><span class="line">    <span class="attr">access-key-secret:</span> <span class="string">&lt;access-key-secret&gt;</span></span><br><span class="line">    <span class="attr">endpoint:</span> <span class="string">dysmsapi.aliyuncs.com</span></span><br></pre></td></tr></table></figure>

<p><strong>注意</strong>：</p>
<p>上述<code>access-key-id</code>、<code>access-key-secret</code>需根据实际情况进行修改。</p>
</li>
<li><p>在<strong>common模块</strong>中创建<code>com.atguigu.lease.common.sms.AliyunSMSProperties</code>类，内容如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Data</span></span><br><span class="line"><span class="meta">@ConfigurationProperties(prefix = &quot;aliyun.sms&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">AliyunSMSProperties</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> String accessKeyId;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> String accessKeySecret;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> String endpoint;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>在<strong>common模块</strong>中创建<code>com.atguigu.lease.common.sms.AliyunSmsConfiguration</code>类，内容如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="meta">@EnableConfigurationProperties(AliyunSMSProperties.class)</span></span><br><span class="line"><span class="meta">@ConditionalOnProperty(name = &quot;aliyun.sms.endpoint&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">AliyunSMSConfiguration</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> AliyunSMSProperties properties;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="keyword">public</span> Client <span class="title function_">smsClient</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="type">Config</span> <span class="variable">config</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Config</span>();</span><br><span class="line">        config.setAccessKeyId(properties.getAccessKeyId());</span><br><span class="line">        config.setAccessKeySecret(properties.getAccessKeySecret());</span><br><span class="line">        config.setEndpoint(properties.getEndpoint());</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Client</span>(config);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">RuntimeException</span>(e);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p><strong>配置Redis连接参数</strong></p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">spring:</span> </span><br><span class="line">  <span class="attr">data:</span></span><br><span class="line">    <span class="attr">redis:</span></span><br><span class="line">      <span class="attr">host:</span> <span class="number">192.168</span><span class="number">.10</span><span class="number">.101</span></span><br><span class="line">      <span class="attr">port:</span> <span class="number">6379</span></span><br><span class="line">      <span class="attr">database:</span> <span class="number">0</span></span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>编写Controller层逻辑</strong></p>
<p>在<code>LoginController</code>中增加如下内容</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@GetMapping(&quot;login/getCode&quot;)</span></span><br><span class="line"><span class="meta">@Operation(summary = &quot;获取短信验证码&quot;)</span></span><br><span class="line"><span class="keyword">public</span> Result <span class="title function_">getCode</span><span class="params">(<span class="meta">@RequestParam</span> String phone)</span> &#123;</span><br><span class="line">    service.getSMSCode(phone);</span><br><span class="line">    <span class="keyword">return</span> Result.ok();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>编写Service层逻辑</strong></p>
<ul>
<li><p>编写发送短信逻辑</p>
<ul>
<li><p>在<code>SmsService</code>中增加如下内容</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> <span class="title function_">sendCode</span><span class="params">(String phone, String verifyCode)</span>;</span><br></pre></td></tr></table></figure>
</li>
<li><p>在<code>SmsServiceImpl</code>中增加如下内容</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">sendCode</span><span class="params">(String phone, String code)</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="type">SendSmsRequest</span> <span class="variable">smsRequest</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">SendSmsRequest</span>();</span><br><span class="line">    smsRequest.setPhoneNumbers(phone);</span><br><span class="line">    smsRequest.setSignName(<span class="string">&quot;阿里云短信测试&quot;</span>);</span><br><span class="line">    smsRequest.setTemplateCode(<span class="string">&quot;SMS_154950909&quot;</span>);</span><br><span class="line">    smsRequest.setTemplateParam(<span class="string">&quot;&#123;\&quot;code\&quot;:\&quot;&quot;</span> + code + <span class="string">&quot;\&quot;&#125;&quot;</span>);</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        client.sendSms(smsRequest);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">RuntimeException</span>(e);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p>编写生成随机验证码逻辑</p>
<p>在<strong>common模块</strong>中创建<code>com.atguigu.lease.common.utils.VerifyCodeUtil</code>类，内容如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">VerifyCodeUtil</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> String <span class="title function_">getVerifyCode</span><span class="params">(<span class="type">int</span> length)</span> &#123;</span><br><span class="line">        <span class="type">StringBuilder</span> <span class="variable">builder</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">StringBuilder</span>();</span><br><span class="line">        <span class="type">Random</span> <span class="variable">random</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Random</span>();</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; length; i++) &#123;</span><br><span class="line">            builder.append(random.nextInt(<span class="number">10</span>));</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> builder.toString();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>编写获取短信验证码逻辑</p>
<ul>
<li><p>在<code>LoginServcie</code>中增加如下内容</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> <span class="title function_">getSMSCode</span><span class="params">(String phone)</span>;</span><br></pre></td></tr></table></figure>
</li>
<li><p>在<code>LoginServiceImpl</code>中增加如下内容</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">getSMSCode</span><span class="params">(String phone)</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//1. 检查手机号码是否为空</span></span><br><span class="line">    <span class="keyword">if</span> (!StringUtils.hasText(phone)) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">LeaseException</span>(ResultCodeEnum.APP_LOGIN_PHONE_EMPTY);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//2. 检查Redis中是否已经存在该手机号码的key</span></span><br><span class="line">    <span class="type">String</span> <span class="variable">key</span> <span class="operator">=</span> RedisConstant.APP_LOGIN_PREFIX + phone;</span><br><span class="line">    <span class="type">boolean</span> <span class="variable">hasKey</span> <span class="operator">=</span> redisTemplate.hasKey(key);</span><br><span class="line">    <span class="keyword">if</span> (hasKey) &#123;</span><br><span class="line">        <span class="comment">//若存在，则检查其存在的时间</span></span><br><span class="line">        <span class="type">Long</span> <span class="variable">expire</span> <span class="operator">=</span> redisTemplate.getExpire(key, TimeUnit.SECONDS);</span><br><span class="line">        <span class="keyword">if</span> (RedisConstant.APP_LOGIN_CODE_TTL_SEC - expire &lt; RedisConstant.APP_LOGIN_CODE_RESEND_TIME_SEC) &#123;</span><br><span class="line">            <span class="comment">//若存在时间不足一分钟，响应发送过于频繁</span></span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">LeaseException</span>(ResultCodeEnum.APP_SEND_SMS_TOO_OFTEN);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//3.发送短信，并将验证码存入Redis</span></span><br><span class="line">    <span class="type">String</span> <span class="variable">verifyCode</span> <span class="operator">=</span> VerifyCodeUtil.getVerifyCode(<span class="number">6</span>);</span><br><span class="line">    smsService.sendCode(phone, verifyCode);</span><br><span class="line">    redisTemplate.opsForValue().set(key, verifyCode, RedisConstant.APP_LOGIN_CODE_TTL_SEC, TimeUnit.SECONDS);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>注意</strong>：</p>
<p>需要注意防止频繁发送短信。</p>
</li>
</ul>
</li>
</ul>
</li>
</ul>
<h2 id="部署"><a href="#部署" class="headerlink" title="部署"></a>部署</h2><h3 id="部署后端项目"><a href="#部署后端项目" class="headerlink" title="部署后端项目"></a>部署后端项目</h3><h4 id="打包"><a href="#打包" class="headerlink" title="打包"></a>打包</h4><p>使用IDEA的maven插件对项目进行打包，完成后，在<strong>web-admin</strong>和<strong>web-app</strong>模块的<code>target</code>目录下找到<code>web-admin-1.0-SNAPSHOT.jar</code>和<code>web-app-1.0-SNAPSHOT.jar</code>。</p>
<h4 id="安装JDK"><a href="#安装JDK" class="headerlink" title="安装JDK"></a>安装JDK</h4><p>根据前文的部署方案，需要在<code>server01</code>部署后端服务，因此需要在<code>server01</code>中安装JDK，本项目采用JDK17。</p>
<ol>
<li><strong>获取JDK安装包</strong></li>
</ol>
<p>将资料中提前下载好的JDK上传到<code>server01</code>，也在服务器执行以下命令可直接下载。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">wget https://download.oracle.com/java/17/archive/jdk-17.0.8_linux-x64_bin.tar.gz</span><br></pre></td></tr></table></figure>

<ol start="2">
<li><strong>解压JDK安装包</strong></li>
</ol>
<p>执行以下命令将jdk解压到<code>/opt</code>目录</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">tar -zxvf jdk-17.0.8_linux-x64_bin.tar.gz -C /opt</span><br></pre></td></tr></table></figure>

<ol start="3">
<li><strong>测试JDK安装效果</strong></li>
</ol>
<p>执行以下命令，观察输出是否正常</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">/opt/jdk-17.0.8/bin/java -version</span><br></pre></td></tr></table></figure>

<h4 id="部署-1"><a href="#部署-1" class="headerlink" title="部署"></a>部署</h4><ol>
<li><p><strong>上传jar包</strong></p>
<p>将后端项目的两个jar包上传到<code>server01</code>服务器的<code>/opt/lease</code>目录下，若目录不存在，自行创建即可。</p>
</li>
<li><p><strong>集成Systemd</strong></p>
<p>为方便项目的启动、停止或者重启，我们同样使用Systemd来管理后端服务的进程。</p>
<ul>
<li><p><strong>移动端集成Systemd</strong></p>
<p>创建<code>lease-app.service</code>文件</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vim /etc/systemd/system/lease-app.service</span><br></pre></td></tr></table></figure>

<p>内容如下</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">[Unit]</span><br><span class="line">Description=lease-app</span><br><span class="line">After=syslog.target</span><br><span class="line"></span><br><span class="line">[Service]</span><br><span class="line">User=root</span><br><span class="line">ExecStart=/opt/jdk-17.0.8/bin/java -jar /opt/lease/web-app-1.0-SNAPSHOT.jar 1&gt;/opt/lease/app.log 2&gt;&amp;1</span><br><span class="line">SuccessExitStatus=143</span><br><span class="line"></span><br><span class="line">[Install]</span><br><span class="line">WantedBy=multi-user.target</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>后台管理系统集成Systemd</strong></p>
<p>创建<code>lease-admin.service</code>文件</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vim /etc/systemd/system/lease-admin.service</span><br></pre></td></tr></table></figure>

<p>内容如下</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">[Unit]</span><br><span class="line">Description=lease-admin</span><br><span class="line">After=syslog.target</span><br><span class="line"></span><br><span class="line">[Service]</span><br><span class="line">User=root</span><br><span class="line">ExecStart=/opt/jdk-17.0.8/bin/java -jar /opt/lease/web-admin-1.0-SNAPSHOT.jar 1&gt;/opt/lease/admin.log 2&gt;&amp;1</span><br><span class="line">SuccessExitStatus=143</span><br><span class="line"></span><br><span class="line">[Install]</span><br><span class="line">WantedBy=multi-user.target</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li><p><strong>启动项目</strong></p>
<p>执行以下命令启动两个后端项目。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">systemctl start lease-app</span><br><span class="line">systemctl start lease-admin</span><br></pre></td></tr></table></figure></li>
</ol>
<h3 id="部署前端项目"><a href="#部署前端项目" class="headerlink" title="部署前端项目"></a>部署前端项目</h3><h4 id="Nginx配置概述"><a href="#Nginx配置概述" class="headerlink" title="Nginx配置概述"></a>Nginx配置概述</h4><p>移动端和后台管理系统的前端项目均部署在<code>server02</code>的Nginx中，Nginx的配置思路如下图所示</p>
<img src="images/部署架构图-详细.drawio.png" style="zoom:50%;" />

<h4 id="移动端"><a href="#移动端" class="headerlink" title="移动端"></a>移动端</h4><h5 id="打包-1"><a href="#打包-1" class="headerlink" title="打包"></a>打包</h5><ol>
<li><p><strong>明确前端请求的后端接口地址</strong></p>
<p>打包之前需要明确前端请求的后台接口地址，根据前文的部署规划，前端请求后台接口时走的是Ngxin反向代理，也就是请求的地址为<code>http://81.68.xxx.xxx:xxx</code>。</p>
<p>所以我们需要修改<code>.env.production</code>文件中<code>VITE_APP_BASE_URL</code>环境变量的值，修改结果如下</p>
<figure class="highlight ini"><table><tr><td class="code"><pre><span class="line"><span class="attr">VITE_APP_BASE_URL</span>=<span class="string">&#x27;http://81.68.xxx.xxx:xxx&#x27;</span></span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>构建项目</strong></p>
<p>在项目的根目录执行以下命令</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm run build</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>查看打包结果</strong></p>
<p>观察项目的根目录是否出现<code>dist</code>目录</p>
</li>
</ol>
<h5 id="部署-2"><a href="#部署-2" class="headerlink" title="部署"></a>部署</h5><ol>
<li><p><strong>上传dist文件</strong></p>
<p>将<code>rentHouseH5</code>项目编译得到<code>dist</code>文件上传至<code>server02</code>服务器的<code>/usr/share/nginx/html/app</code>目录下。</p>
<p>最终的目录结构为</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line">/usr</span><br><span class="line">└── share</span><br><span class="line">    └── nginx</span><br><span class="line">        └── html</span><br><span class="line">            └── app</span><br><span class="line">                ├── static</span><br><span class="line">                └── index.html</span><br><span class="line">                └── ...              </span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>编辑Nginx配置文件</strong></p>
<p>创建<code>/etc/nginx/conf.d/app.conf</code>文件</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vim /etc/nginx/conf.d/app.conf</span><br></pre></td></tr></table></figure>

<p>内容如下</p>
<figure class="highlight nginx"><table><tr><td class="code"><pre><span class="line"><span class="section">server</span> &#123;</span><br><span class="line">    <span class="attribute">listen</span>       xxxx;</span><br><span class="line">    <span class="attribute">server_name</span>  <span class="number">81</span>.<span class="number">68</span>.xxx.xxx;</span><br><span class="line">    </span><br><span class="line">    <span class="section">location</span> / &#123;</span><br><span class="line">        <span class="attribute">root</span>   /usr/share/nginx/html/app;</span><br><span class="line">        <span class="attribute">index</span>  index.html;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="section">location</span> /app &#123;</span><br><span class="line">        <span class="attribute">proxy_pass</span> http://81.68.xxx.xxx:xxxx;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>重新加载Nginx配置文件</strong></p>
<p>执行以下命令重新加载配置文件</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">systemctl reload nginx</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>访问项目</strong></p>
<p>访问<a href="http://xxxxxx/">http://xxxxxx</a></p>
</li>
</ol>
<h4 id="后台管理系统"><a href="#后台管理系统" class="headerlink" title="后台管理系统"></a>后台管理系统</h4><h5 id="打包-2"><a href="#打包-2" class="headerlink" title="打包"></a>打包</h5><ol>
<li><p><strong>明确前端请求的后端接口地址</strong></p>
<p>后台管理系统的前端请求后端接口时，同样会走Nginx反向代理，故其请求的接口地址为<code>http://81.68.xxx.xxx:xxxx</code>。</p>
<p>确保<strong>rentHouseAdmin</strong>项目中的<code>.env.production</code>文件中的<code>VITE_APP_BASE_URL</code>环境变量配置为如下内容</p>
<figure class="highlight ini"><table><tr><td class="code"><pre><span class="line"><span class="attr">VITE_APP_BASE_URL</span>=<span class="string">&#x27;http://81.68.xxx.xxx:xxxx&#x27;</span></span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>打包</strong></p>
<p>在项目根目录执行以下命令</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm run build</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>查看打包结果</strong></p>
<p>观察项目的根目录是否出现<code>dist</code>目录</p>
</li>
</ol>
<h5 id="部署-3"><a href="#部署-3" class="headerlink" title="部署"></a>部署</h5><ol>
<li><p><strong>上传dist文件</strong></p>
<p>将<code>rentHouseAdmin</code>项目编译得到<code>dist</code>文件上传至<code>server02</code>服务器的<code>/usr/share/nginx/html/admin</code>目录下。</p>
<p>最终的目录结构为</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line">/usr</span><br><span class="line">└── share</span><br><span class="line">    └── nginx</span><br><span class="line">        └── html</span><br><span class="line">            └── admin</span><br><span class="line">                ├── assets</span><br><span class="line">                └── index.html</span><br><span class="line">                └── ...</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>编辑Nginx配置文件</strong></p>
<p>创建<code>/etc/nginx/conf.d/admin.conf</code>文件</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vim /etc/nginx/conf.d/admin.conf</span><br></pre></td></tr></table></figure>

<p>内容如下</p>
<figure class="highlight nginx"><table><tr><td class="code"><pre><span class="line"><span class="section">server</span> &#123;</span><br><span class="line">    <span class="attribute">listen</span>       xxxx;</span><br><span class="line">    <span class="attribute">server_name</span>  <span class="number">81</span>.<span class="number">68</span>.xxx.xxx;</span><br><span class="line">    </span><br><span class="line">    <span class="section">location</span> / &#123;</span><br><span class="line">        <span class="attribute">root</span>   /usr/share/nginx/html/admin;</span><br><span class="line">        <span class="attribute">index</span>  index.html;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="section">location</span> /admin &#123;</span><br><span class="line">        <span class="attribute">proxy_pass</span> http://81.68.xx.xxxx:xxxx;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>重新加载Nginx配置文件</strong></p>
<p>执行以下命令重新加载配置文件</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">systemctl reload nginx</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>访问项目</strong></p>
<p>访问<a href="http://xxxxxx/">http://xxxxxx</a></p>
</li>
</ol>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>尚庭公寓</tag>
        <tag>项目实战</tag>
      </tags>
  </entry>
  <entry>
    <title></title>
    <url>/2025/01/27/%E6%8B%94%E9%AB%98%E5%8E%9F%E7%90%86%E7%AF%87/</url>
    <content><![CDATA[<h1 id="代码生成器"><a href="#代码生成器" class="headerlink" title="代码生成器"></a>代码生成器</h1><h2 id="源码分析"><a href="#源码分析" class="headerlink" title="源码分析"></a>源码分析</h2><p>代码生成器是提高开发效率的重要工具，它主要分为两个部分：</p>
<p>第一部分涉及将业务表结构导入到系统中，在这里，开发者可以预览、编辑、删除和同步业务表结构，实现对业务表的全面管理。</p>
<p>第二部分是在选择了特定的表之后，点击生成按钮，系统将根据表结构生成相应的前后端代码，并提供下载。</p>
<p><img src="/assets/image-20240805164855352.png" alt="image-20240805164855352"> </p>
<h3 id="表结构说明"><a href="#表结构说明" class="headerlink" title="表结构说明"></a>表结构说明</h3><p>若依提供了两张核心表来存储导入的业务表信息：</p>
<p><code>gen_table</code>：存储业务表的基本信息    ，它对应于配置代码基本信息和生成信息的页面</p>
<p><code>gen_table_column</code>：存储业务表的字段信息     它对应于配置代码字段信息的页面。</p>
<p>这两张表是一对多的关系，一张业务表可以有多个字段的信息，所以在字段信息表中有个外键table_id指向</p>
<p><img src="/assets/image-20240806221622312.png" alt="image-20240806221622312"> </p>
<h3 id="目录结构"><a href="#目录结构" class="headerlink" title="目录结构"></a>目录结构</h3><p>1）后端代码</p>
<p><img src="/assets/image-20240805164958347.png" alt="image-20240805164958347">  </p>
<p>2）前端代码</p>
<p><img src="/assets/image-20240805165012003.png" alt="image-20240805165012003">  </p>
<h3 id="查询数据库列表"><a href="#查询数据库列表" class="headerlink" title="查询数据库列表"></a>查询数据库列表</h3><p>当管理员在界面上点击导入按钮时，会弹出一个对话框，此时，前端需要向后端发送请求，查询数据库并返回到前端，展示当前项目库中所有待导入的业务表。</p>
<p><img src="/assets/image-20240806221505289.png" alt="image-20240806221505289"> </p>
<p>此功能涉及前端相关的代码位于<code>views/tool/index.vue</code>这个视图组件中，负责实现导入业务表的用户界面和交互逻辑。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">/** 打开导入表弹窗 */</span></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">openImportTable</span>(<span class="params"></span>) &#123;</span><br><span class="line">  proxy.<span class="property">$refs</span>[<span class="string">&quot;importRef&quot;</span>].<span class="title function_">show</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>后端处理逻辑则在代码生成模块的<code>GenController</code>中，负责接收前端的请求，处理业务逻辑，并返回查询结果。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 查询数据库列表</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@PreAuthorize(&quot;@ss.hasPermi(&#x27;tool:gen:list&#x27;)&quot;)</span></span><br><span class="line"><span class="meta">@GetMapping(&quot;/db/list&quot;)</span></span><br><span class="line"><span class="keyword">public</span> TableDataInfo <span class="title function_">dataList</span><span class="params">(GenTable genTable)</span></span><br><span class="line">&#123;</span><br><span class="line">    startPage();</span><br><span class="line">    List&lt;GenTable&gt; list = genTableService.selectDbTableList(genTable);</span><br><span class="line">    <span class="keyword">return</span> getDataTable(list);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>具体的执行的流程如下图：</p>
<p><img src="/assets/image-20240806221757978.png" alt="image-20240806221757978"> </p>
<h3 id="导入表结构"><a href="#导入表结构" class="headerlink" title="导入表结构"></a>导入表结构</h3><p>当管理员对话框中选中需要导入的业务表，点击确定按钮，此时，前端需要向后端发送请求，保存业务表的基本信息和字段信息</p>
<p><img src="/assets/image-20240807215834490.png" alt="image-20240807215834490"> </p>
<p>此功能涉及前端相关的代码位于<code>views/tool/importTable.vue</code>这个视图组件中，负责实现导入业务表的用户界面和交互逻辑。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">/** 导入按钮操作 */</span></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">handleImportTable</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> tableNames = tables.<span class="property">value</span>.<span class="title function_">join</span>(<span class="string">&quot;,&quot;</span>);</span><br><span class="line">  <span class="keyword">if</span> (tableNames == <span class="string">&quot;&quot;</span>) &#123;</span><br><span class="line">    proxy.<span class="property">$modal</span>.<span class="title function_">msgError</span>(<span class="string">&quot;请选择要导入的表&quot;</span>);</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  importTable(&#123; <span class="attr">tables</span>: tableNames &#125;).<span class="title function_">then</span>(<span class="function"><span class="params">res</span> =&gt;</span> &#123;</span><br><span class="line">    proxy.<span class="property">$modal</span>.<span class="title function_">msgSuccess</span>(res.<span class="property">msg</span>);</span><br><span class="line">    <span class="keyword">if</span> (res.<span class="property">code</span> === <span class="number">200</span>) &#123;</span><br><span class="line">      visible.<span class="property">value</span> = <span class="literal">false</span>;</span><br><span class="line">      <span class="title function_">emit</span>(<span class="string">&quot;ok&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>后端处理逻辑则在代码生成模块的<code>GenController</code>中，负责接收前端的请求，处理业务逻辑，保存业务表的基本信息和字段信息</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 导入表结构（保存）</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@PreAuthorize(&quot;@ss.hasPermi(&#x27;tool:gen:import&#x27;)&quot;)</span></span><br><span class="line"><span class="meta">@Log(title = &quot;代码生成&quot;, businessType = BusinessType.IMPORT)</span></span><br><span class="line"><span class="meta">@PostMapping(&quot;/importTable&quot;)</span></span><br><span class="line"><span class="keyword">public</span> AjaxResult <span class="title function_">importTableSave</span><span class="params">(String tables)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 将表名字符串转换为数组 tb_task_collect,tb_vendout_running</span></span><br><span class="line">    String[] tableNames = Convert.toStrArray(tables);</span><br><span class="line">    <span class="comment">// 查询表信息</span></span><br><span class="line">    List&lt;GenTable&gt; tableList = genTableService.selectDbTableListByNames(tableNames);</span><br><span class="line">    <span class="comment">// 导入表结构（保存）</span></span><br><span class="line">    genTableService.importGenTable(tableList, SecurityUtils.getUsername());</span><br><span class="line">    <span class="keyword">return</span> success();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>具体的执行的流程如下图：</p>
<p><img src="/assets/image-20240807220423331.png" alt="image-20240807220423331"> </p>
<h3 id="生成代码"><a href="#生成代码" class="headerlink" title="生成代码"></a>生成代码</h3><p>首先管理员，选中需要下载的业务表，并点击生成按钮来触发代码生成并下载的过程。</p>
<p>前端随后向后端发送请求，这个请求会告知服务器需要生成代码的业务表。</p>
<p><img src="assets/image-20240807220454389.png" alt="image-20240807220454389" style="zoom: 67%;" /> <img src="assets/image-20240807220506939.png" alt="image-20240807220506939" style="zoom:80%;" /> </p>
<p>负责实现这一功能的前端代码位于<code>views/tool/index.vue</code>这个视图组件中，负责实现生成业务表的用户界面和交互逻辑。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">/** 生成代码操作 */</span></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">handleGenTable</span>(<span class="params">row</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> tbNames = row.<span class="property">tableName</span> || tableNames.<span class="property">value</span>;</span><br><span class="line">  <span class="keyword">if</span> (tbNames == <span class="string">&quot;&quot;</span>) &#123;</span><br><span class="line">    proxy.<span class="property">$modal</span>.<span class="title function_">msgError</span>(<span class="string">&quot;请选择要生成的数据&quot;</span>);</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">if</span> (row.<span class="property">genType</span> === <span class="string">&quot;1&quot;</span>) &#123;</span><br><span class="line">    <span class="title function_">genCode</span>(row.<span class="property">tableName</span>).<span class="title function_">then</span>(<span class="function"><span class="params">response</span> =&gt;</span> &#123;</span><br><span class="line">      proxy.<span class="property">$modal</span>.<span class="title function_">msgSuccess</span>(<span class="string">&quot;成功生成到自定义路径：&quot;</span> + row.<span class="property">genPath</span>);</span><br><span class="line">    &#125;);</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    proxy.<span class="property">$download</span>.<span class="title function_">zip</span>(<span class="string">&quot;/tool/gen/batchGenCode?tables=&quot;</span> + tbNames, <span class="string">&quot;ruoyi.zip&quot;</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>后端的逻辑处理则在代码生成模块的<code>GenController</code>中，这里是处理前端请求、执行代码生成逻辑，将生成的代码字节流通过HTTP响应返回给客户端。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 批量生成代码</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@PreAuthorize(&quot;@ss.hasPermi(&#x27;tool:gen:code&#x27;)&quot;)</span></span><br><span class="line"><span class="meta">@Log(title = &quot;代码生成&quot;, businessType = BusinessType.GENCODE)</span></span><br><span class="line"><span class="meta">@GetMapping(&quot;/batchGenCode&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">batchGenCode</span><span class="params">(HttpServletResponse response, String tables)</span> <span class="keyword">throws</span> IOException</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 将表名字符串转换为数组 tb_task_collect,tb_vendout_running</span></span><br><span class="line">    String[] tableNames = Convert.toStrArray(tables);</span><br><span class="line">    <span class="comment">// 根据表名下载生成的代码字节数组</span></span><br><span class="line">    <span class="type">byte</span>[] data = genTableService.downloadCode(tableNames);</span><br><span class="line">    <span class="comment">// 将生成的代码字节流通过HTTP响应返回给客户端</span></span><br><span class="line">    genCode(response, data);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>具体的执行的流程如下图：</p>
<p><img src="/assets/image-20240807220744325.png" alt="image-20240807220744325"> </p>
<h3 id="问题分析"><a href="#问题分析" class="headerlink" title="问题分析"></a>问题分析</h3><p>我们已经对代码生成器的工作原理有了一定的了解，接下来我们解决一些项目中使用的问题，比如：</p>
<p>每次生成代码都需要修改作者，去除实体类前缀过于繁琐，现在我们可以修改<code>generator.yml</code>配置文件来调整为自己项目的</p>
<p><img src="/assets/image-20240802142443371.png" alt="image-20240802142443371"> </p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 代码生成</span></span><br><span class="line"><span class="attr">gen:</span></span><br><span class="line">  <span class="comment"># 作者</span></span><br><span class="line">  <span class="attr">author:</span> <span class="string">itheima</span></span><br><span class="line">  <span class="comment"># 默认生成包路径 system 需改成自己的模块名称 如 system monitor tool</span></span><br><span class="line">  <span class="attr">packageName:</span> <span class="string">com.dkd.manage</span></span><br><span class="line">  <span class="comment"># 自动去除表前缀，默认是false</span></span><br><span class="line">  <span class="attr">autoRemovePre:</span> <span class="literal">true</span></span><br><span class="line">  <span class="comment"># 表前缀（生成类名不会包含表前缀，多个用逗号分隔）</span></span><br><span class="line">  <span class="attr">tablePrefix:</span> <span class="string">sys_,tb_</span></span><br></pre></td></tr></table></figure>



<p>我们还想在若依代码生成的基础上继续进行增强</p>
<ul>
<li><p>实体类支持Lombok</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Data</span></span><br><span class="line"><span class="meta">@NoArgsConstructor</span></span><br><span class="line"><span class="meta">@AllArgsConstructor</span></span><br><span class="line"><span class="meta">@Builder</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Order</span> <span class="keyword">extends</span> <span class="title class_">BaseEntity</span> &#123;</span><br><span class="line">	</span><br><span class="line">    <span class="keyword">private</span> Long id;</span><br><span class="line">    <span class="keyword">private</span> String orderNo;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 没有get、set、toString方法了</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>


</li>
<li><p>Controller类支持Swagger</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Api(tags = &quot;订单管理Controller&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">OrderController</span> <span class="keyword">extends</span> <span class="title class_">BaseController</span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@ApiOperation(&quot;查询订单管理列表&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> TableDataInfo <span class="title function_">list</span><span class="params">(...)</span>&#123;</span><br><span class="line">    	<span class="keyword">return</span> success(...);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="meta">@ApiOperation(&quot;获取订单管理详细信息&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> AjaxResult <span class="title function_">getInfo</span><span class="params">(...)</span> &#123;</span><br><span class="line">       <span class="keyword">return</span> success(...);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<p>要实现这些增强功能，我们需要掌握Velocity模板引擎的使用。Velocity允许我们定制和优化代码生成模板。</p>
<p>在下一个小节中，我们将开始学习Velocity模板引擎，这将帮助我们更好地理解和改造代码生成器的模板。</p>
<h2 id="Velocity模版引擎"><a href="#Velocity模版引擎" class="headerlink" title="Velocity模版引擎"></a>Velocity模版引擎</h2><h3 id="介绍"><a href="#介绍" class="headerlink" title="介绍"></a>介绍</h3><p>Velocity是一个基于Java的模板引擎，可以通过特定的语法获取在java对象的数据 , 填充到模板中，从而实现界面和java代码的分离 !</p>
<p><img src="/assets/image-20240802123001578.png" alt="image-20240802123001578"> </p>
<p>常见的应用场景：</p>
<ul>
<li>Web内容生成 : 生成动态Web页面。</li>
<li>代码生成  : 生成Java源代码、SQL脚本、XML配置文件等。</li>
<li>网页静态化  : 生成静态网页。</li>
</ul>
<h3 id="入门"><a href="#入门" class="headerlink" title="入门"></a>入门</h3><p>需求：根据下面html模板，完成对数据的填充</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE <span class="keyword">html</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span> <span class="attr">lang</span>=<span class="string">&quot;en&quot;</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">meta</span> <span class="attr">charset</span>=<span class="string">&quot;UTF-8&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">&quot;viewport&quot;</span> <span class="attr">content</span>=<span class="string">&quot;width=device-width, initial-scale=1.0&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">title</span>&gt;</span>velocity快速入门<span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">h3</span>&gt;</span>心怀梦想，坚持不懈，成功即在前方。加油少年！！<span class="tag">&lt;/<span class="name">h3</span>&gt;</span></span><br><span class="line">    </span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>要求：<strong>加油少年，</strong>这几个字，需要动态填充进来</p>
<blockquote>
<p>加油同学！！</p>
<p>加油女孩！！</p>
<p>加油朋友！！</p>
</blockquote>
<h4 id="准备模板"><a href="#准备模板" class="headerlink" title="准备模板"></a>准备模板</h4><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE <span class="keyword">html</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span> <span class="attr">lang</span>=<span class="string">&quot;en&quot;</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">meta</span> <span class="attr">charset</span>=<span class="string">&quot;UTF-8&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">&quot;viewport&quot;</span> <span class="attr">content</span>=<span class="string">&quot;width=device-width, initial-scale=1.0&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">title</span>&gt;</span>velocity快速入门<span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">h3</span>&gt;</span>心怀梦想，坚持不懈，成功即在前方。$&#123;message&#125;<span class="tag">&lt;/<span class="name">h3</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>

<blockquote>
<p>上述代码中的 加油少年  修改为了  ${message}  这是一个动态变量（占位符），方便<strong>动态</strong>填充数据</p>
</blockquote>
<h4 id="数据填充"><a href="#数据填充" class="headerlink" title="数据填充"></a>数据填充</h4><p>编写java代码实现数据填充，并生成文件</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.dkd.test;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> com.dkd.generator.util.VelocityInitializer;</span><br><span class="line"><span class="keyword">import</span> org.apache.velocity.Template;</span><br><span class="line"><span class="keyword">import</span> org.apache.velocity.VelocityContext;</span><br><span class="line"><span class="keyword">import</span> org.apache.velocity.app.Velocity;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.io.FileWriter;</span><br><span class="line"><span class="keyword">import</span> java.util.List;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">VelocityDemoTest</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">        <span class="comment">//1. 初始化模板引擎</span></span><br><span class="line">        VelocityInitializer.initVelocity();</span><br><span class="line">        <span class="comment">//2. 准备数据模型</span></span><br><span class="line">        <span class="type">VelocityContext</span> <span class="variable">velocityContext</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">VelocityContext</span>();</span><br><span class="line">        velocityContext.put(<span class="string">&quot;message&quot;</span>, <span class="string">&quot;加油朋友！！&quot;</span>);</span><br><span class="line">        <span class="comment">//3. 读取模板</span></span><br><span class="line">        <span class="type">Template</span> <span class="variable">template</span> <span class="operator">=</span> Velocity.getTemplate(<span class="string">&quot;vm/index.html.vm&quot;</span>, <span class="string">&quot;UTF-8&quot;</span>);</span><br><span class="line">        <span class="comment">//4. 渲染模板</span></span><br><span class="line">        <span class="type">FileWriter</span> <span class="variable">fileWriter</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">FileWriter</span>(<span class="string">&quot;D:\\workspace\\index.html&quot;</span>);</span><br><span class="line">        template.merge(velocityContext, fileWriter);</span><br><span class="line">        fileWriter.close();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h4 id="效果测试"><a href="#效果测试" class="headerlink" title="效果测试"></a>效果测试</h4><p>在指定的目录中生成index.html文件</p>
<img src="assets/image-20240802124658129.png" alt="image-20240802124658129" style="zoom: 50%;" /> 

<p>打开之后的效果：</p>
<p><img src="/assets/image-20240802124607834.png" alt="image-20240802124607834"> </p>
<h3 id="基础语法"><a href="#基础语法" class="headerlink" title="基础语法"></a>基础语法</h3><h4 id="变量"><a href="#变量" class="headerlink" title="变量"></a>变量</h4><p>Velocity中的变量有两类</p>
<ul>
<li>在模板中定义变量： <code>#set</code>开头，比如 <code>#set($name = &quot;velocity&quot;)</code></li>
<li>获取变量的的值：  <code>$name</code>  或者  <code>$&#123;name&#125;</code></li>
</ul>
<p>下面是案例，基于刚才的入门案例模板改进</p>
<p><code>##</code>双#号  是vm的注释</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;!DOCTYPE html&gt;</span><br><span class="line">&lt;html lang=&quot;en&quot;&gt;</span><br><span class="line">&lt;head&gt;</span><br><span class="line">    &lt;meta charset=&quot;UTF-8&quot;&gt;</span><br><span class="line">    &lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1.0&quot;&gt;</span><br><span class="line">    &lt;title&gt;velocity快速入门&lt;/title&gt;</span><br><span class="line">&lt;/head&gt;</span><br><span class="line">&lt;body&gt;</span><br><span class="line"></span><br><span class="line">&lt;h3&gt;心怀梦想，坚持不懈，成功即在前方。$&#123;message&#125;&lt;/h3&gt;</span><br><span class="line"></span><br><span class="line">## 定义变量</span><br><span class="line">#set($name = &quot;velocity&quot;)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">## 输出变量</span><br><span class="line">第一种情况：$&#123;name&#125; &lt;br&gt;</span><br><span class="line">第二种情况：$name &lt;br&gt;</span><br><span class="line"></span><br><span class="line">## 第三种情况：orderService</span><br><span class="line">#set($column = &quot;order&quot;)</span><br><span class="line">字符串拼接：$&#123;column&#125;Service &lt;br&gt;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">&lt;/body&gt;</span><br><span class="line">&lt;/html&gt;</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>对象的定义获取</p>
<p>在ruoyi-generator模块下新增一个区域的实体类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.dkd.test;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> com.dkd.common.annotation.Excel;</span><br><span class="line"><span class="keyword">import</span> com.dkd.common.core.domain.BaseEntity;</span><br><span class="line"><span class="keyword">import</span> lombok.AllArgsConstructor;</span><br><span class="line"><span class="keyword">import</span> lombok.Data;</span><br><span class="line"><span class="keyword">import</span> lombok.NoArgsConstructor;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 区域管理对象 tb_region</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> itheima</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@date</span> 2024-06-05</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@Data</span></span><br><span class="line"><span class="meta">@NoArgsConstructor</span></span><br><span class="line"><span class="meta">@AllArgsConstructor</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Region</span> <span class="keyword">extends</span> <span class="title class_">BaseEntity</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">long</span> <span class="variable">serialVersionUID</span> <span class="operator">=</span> <span class="number">1L</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 主键ID</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> Long id;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 区域名称</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Excel(name = &quot;区域名称&quot;)</span></span><br><span class="line">    <span class="keyword">private</span> String regionName;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>准备模型数据</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.dkd.test;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> com.dkd.generator.util.VelocityInitializer;</span><br><span class="line"><span class="keyword">import</span> org.apache.velocity.Template;</span><br><span class="line"><span class="keyword">import</span> org.apache.velocity.VelocityContext;</span><br><span class="line"><span class="keyword">import</span> org.apache.velocity.app.Velocity;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.io.FileWriter;</span><br><span class="line"><span class="keyword">import</span> java.util.List;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">VelocityDemoTest</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">        <span class="comment">//1. 初始化模板引擎</span></span><br><span class="line">        VelocityInitializer.initVelocity();</span><br><span class="line">        <span class="comment">//2. 准备数据模型</span></span><br><span class="line">        <span class="type">VelocityContext</span> <span class="variable">velocityContext</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">VelocityContext</span>();</span><br><span class="line">        velocityContext.put(<span class="string">&quot;message&quot;</span>, <span class="string">&quot;加油朋友！！&quot;</span>);</span><br><span class="line">        <span class="comment">// 创建区域对象</span></span><br><span class="line">        <span class="type">Region</span> <span class="variable">region</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Region</span>(<span class="number">1L</span>, <span class="string">&quot;北京北五环&quot;</span>);</span><br><span class="line">        velocityContext.put(<span class="string">&quot;region&quot;</span>, region);</span><br><span class="line"></span><br><span class="line">        <span class="comment">//3. 读取模板</span></span><br><span class="line">        <span class="type">Template</span> <span class="variable">template</span> <span class="operator">=</span> Velocity.getTemplate(<span class="string">&quot;vm/index.html.vm&quot;</span>, <span class="string">&quot;UTF-8&quot;</span>);</span><br><span class="line">        <span class="comment">//4. 渲染模板</span></span><br><span class="line">        <span class="type">FileWriter</span> <span class="variable">fileWriter</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">FileWriter</span>(<span class="string">&quot;D:\\workspace\\index.html&quot;</span>);</span><br><span class="line">        template.merge(velocityContext, fileWriter);</span><br><span class="line">        fileWriter.close();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>动态模板展示数据</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;!DOCTYPE html&gt;</span><br><span class="line">&lt;html lang=&quot;en&quot;&gt;</span><br><span class="line">&lt;head&gt;</span><br><span class="line">    &lt;meta charset=&quot;UTF-8&quot;&gt;</span><br><span class="line">    &lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1.0&quot;&gt;</span><br><span class="line">    &lt;title&gt;velocity快速入门&lt;/title&gt;</span><br><span class="line">&lt;/head&gt;</span><br><span class="line">&lt;body&gt;</span><br><span class="line"></span><br><span class="line">&lt;h3&gt;心怀梦想，坚持不懈，成功即在前方。$&#123;message&#125;&lt;/h3&gt;</span><br><span class="line"></span><br><span class="line">## 定义变量</span><br><span class="line">#set($name = &quot;velocity&quot;)</span><br><span class="line"></span><br><span class="line">## 输出变量</span><br><span class="line">第一种情况：$&#123;name&#125; &lt;br&gt;</span><br><span class="line">第二种情况：$name &lt;br&gt;</span><br><span class="line"></span><br><span class="line">## 第三种情况：orderService</span><br><span class="line">#set($column = &quot;order&quot;)</span><br><span class="line">字符串拼接：$&#123;column&#125;Service &lt;br&gt;</span><br><span class="line">&lt;hr&gt;</span><br><span class="line"></span><br><span class="line">## 获取区域对象中的数据</span><br><span class="line">区域ID:$region.id &lt;br&gt;</span><br><span class="line">区域名称:$&#123;region.regionName&#125; &lt;br&gt;</span><br><span class="line">&lt;hr&gt;</span><br><span class="line">&lt;/body&gt;</span><br><span class="line">&lt;/html&gt;</span><br></pre></td></tr></table></figure>

<h4 id="循环"><a href="#循环" class="headerlink" title="循环"></a>循环</h4><p>循环的语法：<code>#foreach(...) ... #end</code></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">##定义一个集合</span><br><span class="line">#set($list = [&quot;春&quot;, &quot;夏&quot;, &quot;秋&quot;, &quot;冬&quot;])    </span><br><span class="line"></span><br><span class="line">## 遍历循环</span><br><span class="line">#foreach($item in $list)</span><br><span class="line">   序号[$foreach.count] $item &lt;br&gt; ## count从1开始 index从0开始</span><br><span class="line">#end</span><br></pre></td></tr></table></figure>

<p>准备模型数据</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 创建区域对象</span></span><br><span class="line"><span class="type">Region</span> <span class="variable">region1</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Region</span>(<span class="number">1L</span>, <span class="string">&quot;北京北五环&quot;</span>);</span><br><span class="line"><span class="type">Region</span> <span class="variable">region2</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Region</span>(<span class="number">2L</span>, <span class="string">&quot;北京北四环&quot;</span>);</span><br><span class="line">velocityContext.put(<span class="string">&quot;region&quot;</span>, region1);</span><br><span class="line">List&lt;Region&gt; regionList = List.of(region1, region2);</span><br><span class="line">velocityContext.put(<span class="string">&quot;regionList&quot;</span>, regionList);</span><br></pre></td></tr></table></figure>

<p>修改父工程的pom.xml文件，把jdk版本升级为11</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">java.version</span>&gt;</span>11<span class="tag">&lt;/<span class="name">java.version</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>动态模板展示数据</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">## 遍历区域</span><br><span class="line">#foreach($item in $regionList)</span><br><span class="line">   序号[$foreach.count],区域ID:$item.id,区域名称:$item.regionName &lt;br&gt;</span><br><span class="line">#end</span><br><span class="line">&lt;/br&gt;</span><br></pre></td></tr></table></figure>



<h4 id="if判断"><a href="#if判断" class="headerlink" title="if判断"></a>if判断</h4><p>判断的语法：<code>#if(condition) ... #elseif(condition) ... #else ... #end</code></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">##定义变量</span><br><span class="line">#set($score=80)</span><br><span class="line"></span><br><span class="line">## if判断</span><br><span class="line">#if($score&gt;=80)</span><br><span class="line">    优秀</span><br><span class="line">#elseif($score&gt;=60)</span><br><span class="line">    及格</span><br><span class="line">#else</span><br><span class="line">    不及格</span><br><span class="line">#end</span><br></pre></td></tr></table></figure>

<p>其他的判断条件：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">## 对象obj不为空才会执行里面的逻辑</span><br><span class="line">#if($obj) ..... #end</span><br><span class="line"></span><br><span class="line">## 对象obj为空才会执行里面的逻辑</span><br><span class="line">#if(!$obj) ..... #end</span><br></pre></td></tr></table></figure>

<p>在条件判断中，velocity支持常见的关系操作符，比如：<code>&amp;&amp;(与), ||(或), !(非)</code></p>
<h3 id="模板阅读"><a href="#模板阅读" class="headerlink" title="模板阅读"></a>模板阅读</h3><p>我们不需要使用velocity去开发新的模板，若依已经提供好了，在它基础上进行调整即可</p>
<p>下面这个是关于实体类的模板</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">package $&#123;packageName&#125;.domain;</span><br><span class="line"></span><br><span class="line">## 根据列类型获取导入包</span><br><span class="line">#foreach ($import in $importList)</span><br><span class="line">import $&#123;import&#125;;</span><br><span class="line">#end</span><br><span class="line">## 导入Apache Commons Lang库，用于对象的toString方法</span><br><span class="line">import org.apache.commons.lang3.builder.ToStringBuilder;</span><br><span class="line">import org.apache.commons.lang3.builder.ToStringStyle;</span><br><span class="line">## 导入项目自定义的Excel注解，用于生成Excel文档</span><br><span class="line">import com.dkd.common.annotation.Excel;</span><br><span class="line">#if($table.crud || $table.sub)</span><br><span class="line">## 如果表具有CRUD操作或子表，继承BaseEntity</span><br><span class="line">import com.dkd.common.core.domain.BaseEntity;</span><br><span class="line">#elseif($table.tree)</span><br><span class="line">## 如果表是树形结构，继承TreeEntity</span><br><span class="line">import com.dkd.common.core.domain.TreeEntity;</span><br><span class="line">#end</span><br><span class="line"></span><br><span class="line">/**</span><br><span class="line"> * $&#123;functionName&#125;对象 $&#123;tableName&#125;</span><br><span class="line"> *</span><br><span class="line"> * @author $&#123;author&#125;</span><br><span class="line"> * @date $&#123;datetime&#125;</span><br><span class="line"> */</span><br><span class="line">#if($table.crud || $table.sub)</span><br><span class="line">    #set($Entity=&quot;BaseEntity&quot;)</span><br><span class="line">#elseif($table.tree)</span><br><span class="line">    #set($Entity=&quot;TreeEntity&quot;)</span><br><span class="line">#end</span><br><span class="line">public class $&#123;ClassName&#125; extends $&#123;Entity&#125;</span><br><span class="line">        &#123;   ## 定义类的序列化版本ID</span><br><span class="line">private static final long serialVersionUID = 1L;</span><br><span class="line">## 根据表的列定义实体类的属性</span><br><span class="line">#foreach ($column in $columns)</span><br><span class="line">    ## 如果不是父类的属性，则生成属性</span><br><span class="line">    #if(!$table.isSuperColumn($column.javaField))</span><br><span class="line">    /** $column.columnComment */</span><br><span class="line">        ## 如果字段需要在列表中展示，使用Excel注解标记</span><br><span class="line">        #if($column.list)</span><br><span class="line">            #set($parentheseIndex=$column.columnComment.indexOf(&quot;（&quot;))</span><br><span class="line">            #if($parentheseIndex != -1)</span><br><span class="line">                #set($comment=$column.columnComment.substring(0, $parentheseIndex))</span><br><span class="line">            #else</span><br><span class="line">                #set($comment=$column.columnComment)</span><br><span class="line">            #end</span><br><span class="line">            #if($parentheseIndex != -1)</span><br><span class="line">            @Excel(name = &quot;$&#123;comment&#125;&quot;, readConverterExp = &quot;$column.readConverterExp()&quot;)</span><br><span class="line">            #elseif($column.javaType == &#x27;Date&#x27;)</span><br><span class="line">            @JsonFormat(pattern = &quot;yyyy-MM-dd&quot;)</span><br><span class="line">            @Excel(name = &quot;$&#123;comment&#125;&quot;, width = 30, dateFormat = &quot;yyyy-MM-dd&quot;)</span><br><span class="line">            #else</span><br><span class="line">            @Excel(name = &quot;$&#123;comment&#125;&quot;)</span><br><span class="line">            #end</span><br><span class="line">        #end</span><br><span class="line">    private $column.javaType $column.javaField;</span><br><span class="line"></span><br><span class="line">    #end</span><br><span class="line">#end</span><br><span class="line">## 如果表有子表，定义子表信息的集合</span><br><span class="line">#if($table.sub)</span><br><span class="line">/** $table.subTable.functionName信息 */</span><br><span class="line">private List&lt;$&#123;subClassName&#125;&gt; $&#123;subclassName&#125;List;</span><br><span class="line"></span><br><span class="line">#end</span><br><span class="line">## 为每个属性字段生成getter和setter方法</span><br><span class="line">#foreach ($column in $columns)</span><br><span class="line">    #if(!$table.isSuperColumn($column.javaField))</span><br><span class="line">        #if($column.javaField.length() &gt; 2 &amp;&amp; $column.javaField.substring(1,2).matches(&quot;[A-Z]&quot;))</span><br><span class="line">            #set($AttrName=$column.javaField)</span><br><span class="line">        #else</span><br><span class="line">            #set($AttrName=$column.javaField.substring(0,1).toUpperCase() + $&#123;column.javaField.substring(1)&#125;)</span><br><span class="line">        #end</span><br><span class="line">    public void set$&#123;AttrName&#125;($column.javaType $column.javaField)</span><br><span class="line">            &#123;</span><br><span class="line">            this.$column.javaField = $column.javaField;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">    public $column.javaType get$&#123;AttrName&#125;()</span><br><span class="line">            &#123;</span><br><span class="line">            return $column.javaField;</span><br><span class="line">            &#125;</span><br><span class="line">    #end</span><br><span class="line">#end</span><br><span class="line">## 如果表有子表，生成子表信息的getter和setter方法</span><br><span class="line">#if($table.sub)</span><br><span class="line">public List&lt;$&#123;subClassName&#125;&gt; get$&#123;subClassName&#125;List()</span><br><span class="line">        &#123;</span><br><span class="line">        return $&#123;subclassName&#125;List;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">public void set$&#123;subClassName&#125;List(List&lt;$&#123;subClassName&#125;&gt; $&#123;subclassName&#125;List)</span><br><span class="line">        &#123;</span><br><span class="line">        this.$&#123;subclassName&#125;List = $&#123;subclassName&#125;List;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">#end</span><br><span class="line">## 重写toString方法，使用Apache Commons Lang的ToStringBuilder</span><br><span class="line">@Override</span><br><span class="line">public String toString() &#123;</span><br><span class="line">        return new ToStringBuilder(this,ToStringStyle.MULTI_LINE_STYLE)</span><br><span class="line">    #foreach ($column in $columns)</span><br><span class="line">        #if($column.javaField.length() &gt; 2 &amp;&amp; $column.javaField.substring(1,2).matches(&quot;[A-Z]&quot;))</span><br><span class="line">            #set($AttrName=$column.javaField)</span><br><span class="line">        #else</span><br><span class="line">            #set($AttrName=$column.javaField.substring(0,1).toUpperCase() + $&#123;column.javaField.substring(1)&#125;)</span><br><span class="line">        #end</span><br><span class="line">            .append(&quot;$&#123;column.javaField&#125;&quot;, get$&#123;AttrName&#125;())</span><br><span class="line">    #end</span><br><span class="line">    #if($table.sub)</span><br><span class="line">            .append(&quot;$&#123;subclassName&#125;List&quot;, get$&#123;subClassName&#125;List())</span><br><span class="line">    #end</span><br><span class="line">        .toString();</span><br><span class="line">        &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>



<h2 id="Lombok集成"><a href="#Lombok集成" class="headerlink" title="Lombok集成"></a>Lombok集成</h2><p>目前，我们已经基本熟悉了velocity的作用和一些语法，那接下来，我们就通过这些知识来去改造若依框架的代码生成部分</p>
<h3 id="导入坐标（已完成）"><a href="#导入坐标（已完成）" class="headerlink" title="导入坐标（已完成）"></a>导入坐标（已完成）</h3><p>在<code>dkd-common</code>模块的<code>pom.xml</code>中添加lombok坐标</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!--  lombok工具--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.projectlombok<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>lombok<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="修改模板"><a href="#修改模板" class="headerlink" title="修改模板"></a>修改模板</h3><p>在<code>dkd-generator</code>模块的<code>domain.java.vm</code>模板中添加lombok注解</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">package $&#123;packageName&#125;.domain;</span><br><span class="line"></span><br><span class="line">## 根据列类型获取导入包</span><br><span class="line">#foreach ($import in $importList)</span><br><span class="line">import $&#123;import&#125;;</span><br><span class="line">#end</span><br><span class="line">## 导入项目自定义的Excel注解，用于生成Excel文档</span><br><span class="line">import com.dkd.common.annotation.Excel;</span><br><span class="line">#if($table.crud || $table.sub)</span><br><span class="line">## 如果表具有CRUD操作或子表，继承BaseEntity</span><br><span class="line">import com.dkd.common.core.domain.BaseEntity;</span><br><span class="line">#elseif($table.tree)</span><br><span class="line">## 如果表是树形结构，继承TreeEntity</span><br><span class="line">import com.dkd.common.core.domain.TreeEntity;</span><br><span class="line">#end</span><br><span class="line">## 注意lombok导包</span><br><span class="line">import lombok.AllArgsConstructor;</span><br><span class="line">import lombok.Builder;</span><br><span class="line">import lombok.Data;</span><br><span class="line">import lombok.NoArgsConstructor;</span><br><span class="line"></span><br><span class="line">/**</span><br><span class="line"> * $&#123;functionName&#125;对象 $&#123;tableName&#125;</span><br><span class="line"> *</span><br><span class="line"> * @author $&#123;author&#125;</span><br><span class="line"> * @date $&#123;datetime&#125;</span><br><span class="line"> */</span><br><span class="line">#if($table.crud || $table.sub)</span><br><span class="line">#set($Entity=&quot;BaseEntity&quot;)</span><br><span class="line">#elseif($table.tree)</span><br><span class="line">#set($Entity=&quot;TreeEntity&quot;)</span><br><span class="line">#end</span><br><span class="line">@Data</span><br><span class="line">@NoArgsConstructor</span><br><span class="line">@AllArgsConstructor</span><br><span class="line">@Builder</span><br><span class="line">public class $&#123;ClassName&#125; extends $&#123;Entity&#125;</span><br><span class="line">        &#123;   ## 定义类的序列化版本ID</span><br><span class="line">private static final long serialVersionUID=1L;</span><br><span class="line">## 根据表的列定义实体类的属性</span><br><span class="line">#foreach ($column in $columns)</span><br><span class="line">    ## 如果不是父类的属性，则生成属性</span><br><span class="line">    #if(!$table.isSuperColumn($column.javaField))</span><br><span class="line">    /** $column.columnComment */</span><br><span class="line">        ## 如果字段需要在列表中展示，使用Excel注解标记</span><br><span class="line">        #if($column.list)</span><br><span class="line">            #set($parentheseIndex=$column.columnComment.indexOf(&quot;（&quot;))</span><br><span class="line">            #if($parentheseIndex != -1)</span><br><span class="line">                #set($comment=$column.columnComment.substring(0, $parentheseIndex))</span><br><span class="line">            #else</span><br><span class="line">                #set($comment=$column.columnComment)</span><br><span class="line">            #end</span><br><span class="line">            #if($parentheseIndex != -1)</span><br><span class="line">            @Excel(name = &quot;$&#123;comment&#125;&quot;, readConverterExp = &quot;$column.readConverterExp()&quot;)</span><br><span class="line">            #elseif($column.javaType == &#x27;Date&#x27;)</span><br><span class="line">            @JsonFormat(pattern = &quot;yyyy-MM-dd&quot;)</span><br><span class="line">            @Excel(name = &quot;$&#123;comment&#125;&quot;, width = 30, dateFormat = &quot;yyyy-MM-dd&quot;)</span><br><span class="line">            #else</span><br><span class="line">            @Excel(name = &quot;$&#123;comment&#125;&quot;)</span><br><span class="line">            #end</span><br><span class="line">        #end</span><br><span class="line">    private $column.javaType $column.javaField;</span><br><span class="line"></span><br><span class="line">    #end</span><br><span class="line">#end</span><br><span class="line">## 如果表有子表，定义子表信息的集合</span><br><span class="line">#if($table.sub)</span><br><span class="line">/** $table.subTable.functionName信息 */</span><br><span class="line">private List&lt;$&#123;subClassName&#125;&gt; $&#123;subclassName&#125;List;</span><br><span class="line"></span><br><span class="line">#end</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="生成后的效果"><a href="#生成后的效果" class="headerlink" title="生成后的效果"></a>生成后的效果</h3><p>修改完成之后，重启项目，找到代码生成的功能，通过<strong>代码预览</strong>可以查看实体类的代码：</p>
<p><img src="/assets/image-20240802133527311.png" alt="image-20240802133527311"> </p>
<blockquote>
<ul>
<li>正常添加了关于lombok的注解</li>
<li>删除了set 、 get  、toString 等方法</li>
</ul>
</blockquote>
<p>可以把生成后的代码，拷贝到项目中，如果订单管理能够正常访问和操作，就算修改成功了，后期再次生成的代码，全部都支持lombok</p>
<h2 id="Swagger集成"><a href="#Swagger集成" class="headerlink" title="Swagger集成"></a>Swagger集成</h2><h3 id="修改模板-1"><a href="#修改模板-1" class="headerlink" title="修改模板"></a>修改模板</h3><p>在<code>dkd-generator</code>模块的 <code>controller.java.vm</code>模板中添加Swagger注解</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">package $&#123;packageName&#125;.controller;</span><br><span class="line"></span><br><span class="line">import java.util.List;</span><br><span class="line">import javax.servlet.http.HttpServletResponse;</span><br><span class="line">import org.springframework.security.access.prepost.PreAuthorize;</span><br><span class="line">import org.springframework.beans.factory.annotation.Autowired;</span><br><span class="line">import org.springframework.web.bind.annotation.GetMapping;</span><br><span class="line">import org.springframework.web.bind.annotation.PostMapping;</span><br><span class="line">import org.springframework.web.bind.annotation.PutMapping;</span><br><span class="line">import org.springframework.web.bind.annotation.DeleteMapping;</span><br><span class="line">import org.springframework.web.bind.annotation.PathVariable;</span><br><span class="line">import org.springframework.web.bind.annotation.RequestBody;</span><br><span class="line">import org.springframework.web.bind.annotation.RequestMapping;</span><br><span class="line">import org.springframework.web.bind.annotation.RestController;</span><br><span class="line">import com.dkd.common.annotation.Log;</span><br><span class="line">import com.dkd.common.core.controller.BaseController;</span><br><span class="line">import com.dkd.common.core.domain.AjaxResult;</span><br><span class="line">import com.dkd.common.enums.BusinessType;</span><br><span class="line">import $&#123;packageName&#125;.domain.$&#123;ClassName&#125;;</span><br><span class="line">import $&#123;packageName&#125;.service.I$&#123;ClassName&#125;Service;</span><br><span class="line">import com.dkd.common.utils.poi.ExcelUtil;</span><br><span class="line">#if($table.crud || $table.sub)</span><br><span class="line">import com.dkd.common.core.page.TableDataInfo;</span><br><span class="line">#elseif($table.tree)</span><br><span class="line">#end</span><br><span class="line">import io.swagger.annotations.Api;</span><br><span class="line">import io.swagger.annotations.ApiOperation;</span><br><span class="line"></span><br><span class="line">/**</span><br><span class="line"> * $&#123;functionName&#125;Controller</span><br><span class="line"> *</span><br><span class="line"> * @author $&#123;author&#125;</span><br><span class="line"> * @date $&#123;datetime&#125;</span><br><span class="line"> */</span><br><span class="line">@Api(tags = &quot;$&#123;functionName&#125;Controller&quot;)</span><br><span class="line">@RestController</span><br><span class="line">@RequestMapping(&quot;/$&#123;moduleName&#125;/$&#123;businessName&#125;&quot;)</span><br><span class="line">public class $&#123;ClassName&#125;Controller extends BaseController</span><br><span class="line">&#123;</span><br><span class="line">    @Autowired</span><br><span class="line">    private I$&#123;ClassName&#125;Service $&#123;className&#125;Service;</span><br><span class="line"></span><br><span class="line">    /**</span><br><span class="line">     * 查询$&#123;functionName&#125;列表</span><br><span class="line">     */</span><br><span class="line">    @ApiOperation(&quot;查询$&#123;functionName&#125;列表&quot;)</span><br><span class="line">    @PreAuthorize(&quot;@ss.hasPermi(&#x27;$&#123;permissionPrefix&#125;:list&#x27;)&quot;)</span><br><span class="line">    @GetMapping(&quot;/list&quot;)</span><br><span class="line">#if($table.crud || $table.sub)</span><br><span class="line">    public TableDataInfo list($&#123;ClassName&#125; $&#123;className&#125;)</span><br><span class="line">    &#123;</span><br><span class="line">        startPage();</span><br><span class="line">        List&lt;$&#123;ClassName&#125;&gt; list = $&#123;className&#125;Service.select$&#123;ClassName&#125;List($&#123;className&#125;);</span><br><span class="line">        return getDataTable(list);</span><br><span class="line">    &#125;</span><br><span class="line">#elseif($table.tree)</span><br><span class="line">    public AjaxResult list($&#123;ClassName&#125; $&#123;className&#125;)</span><br><span class="line">    &#123;</span><br><span class="line">        List&lt;$&#123;ClassName&#125;&gt; list = $&#123;className&#125;Service.select$&#123;ClassName&#125;List($&#123;className&#125;);</span><br><span class="line">        return success(list);</span><br><span class="line">    &#125;</span><br><span class="line">#end</span><br><span class="line"></span><br><span class="line">    /**</span><br><span class="line">     * 导出$&#123;functionName&#125;列表</span><br><span class="line">     */</span><br><span class="line">    @ApiOperation(&quot;导出$&#123;functionName&#125;列表&quot;)</span><br><span class="line">    @PreAuthorize(&quot;@ss.hasPermi(&#x27;$&#123;permissionPrefix&#125;:export&#x27;)&quot;)</span><br><span class="line">    @Log(title = &quot;$&#123;functionName&#125;&quot;, businessType = BusinessType.EXPORT)</span><br><span class="line">    @PostMapping(&quot;/export&quot;)</span><br><span class="line">    public void export(HttpServletResponse response, $&#123;ClassName&#125; $&#123;className&#125;)</span><br><span class="line">    &#123;</span><br><span class="line">        List&lt;$&#123;ClassName&#125;&gt; list = $&#123;className&#125;Service.select$&#123;ClassName&#125;List($&#123;className&#125;);</span><br><span class="line">        ExcelUtil&lt;$&#123;ClassName&#125;&gt; util = new ExcelUtil&lt;$&#123;ClassName&#125;&gt;($&#123;ClassName&#125;.class);</span><br><span class="line">        util.exportExcel(response, list, &quot;$&#123;functionName&#125;数据&quot;);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    /**</span><br><span class="line">     * 获取$&#123;functionName&#125;详细信息</span><br><span class="line">     */</span><br><span class="line">    @ApiOperation(&quot;获取$&#123;functionName&#125;详细信息&quot;)</span><br><span class="line">    @PreAuthorize(&quot;@ss.hasPermi(&#x27;$&#123;permissionPrefix&#125;:query&#x27;)&quot;)</span><br><span class="line">    @GetMapping(value = &quot;/&#123;$&#123;pkColumn.javaField&#125;&#125;&quot;)</span><br><span class="line">    public AjaxResult getInfo(@PathVariable(&quot;$&#123;pkColumn.javaField&#125;&quot;) $&#123;pkColumn.javaType&#125; $&#123;pkColumn.javaField&#125;)</span><br><span class="line">    &#123;</span><br><span class="line">        return success($&#123;className&#125;Service.select$&#123;ClassName&#125;By$&#123;pkColumn.capJavaField&#125;($&#123;pkColumn.javaField&#125;));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    /**</span><br><span class="line">     * 新增$&#123;functionName&#125;</span><br><span class="line">     */</span><br><span class="line">    @ApiOperation(&quot;新增$&#123;functionName&#125;&quot;)</span><br><span class="line">    @PreAuthorize(&quot;@ss.hasPermi(&#x27;$&#123;permissionPrefix&#125;:add&#x27;)&quot;)</span><br><span class="line">    @Log(title = &quot;$&#123;functionName&#125;&quot;, businessType = BusinessType.INSERT)</span><br><span class="line">    @PostMapping</span><br><span class="line">    public AjaxResult add(@RequestBody $&#123;ClassName&#125; $&#123;className&#125;)</span><br><span class="line">    &#123;</span><br><span class="line">        return toAjax($&#123;className&#125;Service.insert$&#123;ClassName&#125;($&#123;className&#125;));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    /**</span><br><span class="line">     * 修改$&#123;functionName&#125;</span><br><span class="line">     */</span><br><span class="line">    @ApiOperation(&quot;修改$&#123;functionName&#125;&quot;)</span><br><span class="line">    @PreAuthorize(&quot;@ss.hasPermi(&#x27;$&#123;permissionPrefix&#125;:edit&#x27;)&quot;)</span><br><span class="line">    @Log(title = &quot;$&#123;functionName&#125;&quot;, businessType = BusinessType.UPDATE)</span><br><span class="line">    @PutMapping</span><br><span class="line">    public AjaxResult edit(@RequestBody $&#123;ClassName&#125; $&#123;className&#125;)</span><br><span class="line">    &#123;</span><br><span class="line">        return toAjax($&#123;className&#125;Service.update$&#123;ClassName&#125;($&#123;className&#125;));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    /**</span><br><span class="line">     * 删除$&#123;functionName&#125;</span><br><span class="line">     */</span><br><span class="line">    @ApiOperation(&quot;删除$&#123;functionName&#125;&quot;)</span><br><span class="line">    @PreAuthorize(&quot;@ss.hasPermi(&#x27;$&#123;permissionPrefix&#125;:remove&#x27;)&quot;)</span><br><span class="line">    @Log(title = &quot;$&#123;functionName&#125;&quot;, businessType = BusinessType.DELETE)</span><br><span class="line">	@DeleteMapping(&quot;/&#123;$&#123;pkColumn.javaField&#125;s&#125;&quot;)</span><br><span class="line">    public AjaxResult remove(@PathVariable $&#123;pkColumn.javaType&#125;[] $&#123;pkColumn.javaField&#125;s)</span><br><span class="line">    &#123;</span><br><span class="line">        return toAjax($&#123;className&#125;Service.delete$&#123;ClassName&#125;By$&#123;pkColumn.capJavaField&#125;s($&#123;pkColumn.javaField&#125;s));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="生成后的效果-1"><a href="#生成后的效果-1" class="headerlink" title="生成后的效果"></a>生成后的效果</h3><p>修改完成之后，重启项目，找到代码生成的功能，通过<strong>代码预览</strong>可以查看Controller类的代码：</p>
<p><img src="/assets/image-20240802135228448.png" alt="image-20240802135228448"> </p>
<p>可以把生成后的代码，拷贝到项目中，如果订单管理能够正常访问和操作，且系统接口显示工单管理就算修改成功了，后期再次生成的代码，全部都支持Swagger</p>
<p><img src="/assets/image-20240802141918105.png" alt="image-20240802141918105"> </p>
<h1 id="RBAC权限控制"><a href="#RBAC权限控制" class="headerlink" title="RBAC权限控制"></a>RBAC权限控制</h1><h2 id="SpringSecurity介绍"><a href="#SpringSecurity介绍" class="headerlink" title="SpringSecurity介绍"></a>SpringSecurity介绍</h2><p>Spring Security是一个功能强大的Java安全框架，它提供了全面的安全认证和授权的支持。</p>
<p>与RBAC模型结合使用时，Spring Security能够实现灵活的权限控制。</p>
<p>我们来看下它的二大核心概念，认证和授权。让我们用一个简单的例子来领会它们的意义。</p>
<p><strong>1）认证（Authentication）</strong>想象一下，小智同学去图书馆借书。门口的图书管理员会要求小智出示借书证。这个借书证上有小智的照片和姓名，管理员通过它来确认小智的身份。这个过程，就是一个认证的实例。在Spring  Security的世界里，认证就像用户登录时提交的用户名和密码，系统通过这些信息来验证“你是谁”。</p>
<p>Spring Security不仅支持传统的用户名和密码认证，还支持OAuth2、JWT等现代认证方式。</p>
<p><strong>2）授权（Authorization）</strong>  现在，假设小智同学已经踏入了图书馆的大门，但当小智想进入只有持特定权限才能进入的参考书籍区时，管理员会再次检查小智的借书证，看是否有相应的权限标记。如果有，那么小智就能进入这个区域；如果没有，那么小智只能遗憾地止步。这就是授权的精髓。在Spring  Security中，授权是确认用户在通过认证之后，是否有权限执行某些操作或访问特定资源。</p>
<h2 id="SpringSecurity配置"><a href="#SpringSecurity配置" class="headerlink" title="SpringSecurity配置"></a>SpringSecurity配置</h2><p>Spring Security的配置类是实现安全控制的核心部分</p>
<p>开启Spring Security各种功能，以确保Web应用程序的安全性，包括认证、授权、会话管理、过滤器添加等。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.dkd.framework.config;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.springframework.beans.factory.annotation.Autowired;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.annotation.Bean;</span><br><span class="line"><span class="keyword">import</span> org.springframework.http.HttpMethod;</span><br><span class="line"><span class="keyword">import</span> org.springframework.security.authentication.AuthenticationManager;</span><br><span class="line"><span class="keyword">import</span> org.springframework.security.config.annotation.authentication.builders.AuthenticationManagerBuilder;</span><br><span class="line"><span class="keyword">import</span> org.springframework.security.config.annotation.method.configuration.EnableGlobalMethodSecurity;</span><br><span class="line"><span class="keyword">import</span> org.springframework.security.config.annotation.web.builders.HttpSecurity;</span><br><span class="line"><span class="keyword">import</span> org.springframework.security.config.annotation.web.configuration.WebSecurityConfigurerAdapter;</span><br><span class="line"><span class="keyword">import</span> org.springframework.security.config.annotation.web.configurers.ExpressionUrlAuthorizationConfigurer;</span><br><span class="line"><span class="keyword">import</span> org.springframework.security.config.http.SessionCreationPolicy;</span><br><span class="line"><span class="keyword">import</span> org.springframework.security.core.userdetails.UserDetailsService;</span><br><span class="line"><span class="keyword">import</span> org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder;</span><br><span class="line"><span class="keyword">import</span> org.springframework.security.web.authentication.UsernamePasswordAuthenticationFilter;</span><br><span class="line"><span class="keyword">import</span> org.springframework.security.web.authentication.logout.LogoutFilter;</span><br><span class="line"><span class="keyword">import</span> org.springframework.web.filter.CorsFilter;</span><br><span class="line"><span class="keyword">import</span> com.dkd.framework.config.properties.PermitAllUrlProperties;</span><br><span class="line"><span class="keyword">import</span> com.dkd.framework.security.filter.JwtAuthenticationTokenFilter;</span><br><span class="line"><span class="keyword">import</span> com.dkd.framework.security.handle.AuthenticationEntryPointImpl;</span><br><span class="line"><span class="keyword">import</span> com.dkd.framework.security.handle.LogoutSuccessHandlerImpl;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * spring security配置</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> ruoyi</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="comment">// 开启方法级别的权限控制 ==&gt; @PreAuthorize</span></span><br><span class="line"><span class="meta">@EnableGlobalMethodSecurity(prePostEnabled = true, securedEnabled = true)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">SecurityConfig</span> <span class="keyword">extends</span> <span class="title class_">WebSecurityConfigurerAdapter</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 自定义用户认证逻辑</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> UserDetailsService userDetailsService;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 认证失败处理类</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> AuthenticationEntryPointImpl unauthorizedHandler;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 退出处理类</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> LogoutSuccessHandlerImpl logoutSuccessHandler;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * token认证过滤器</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> JwtAuthenticationTokenFilter authenticationTokenFilter;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 跨域过滤器</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> CorsFilter corsFilter;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 允许匿名访问的地址</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> PermitAllUrlProperties permitAllUrl;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 解决 无法直接注入 AuthenticationManager</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span></span></span><br><span class="line"><span class="comment">     * <span class="doctag">@throws</span> Exception</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> AuthenticationManager <span class="title function_">authenticationManagerBean</span><span class="params">()</span> <span class="keyword">throws</span> Exception</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">super</span>.authenticationManagerBean();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * anyRequest          |   匹配所有请求路径</span></span><br><span class="line"><span class="comment">     * access              |   SpringEl表达式结果为true时可以访问</span></span><br><span class="line"><span class="comment">     * anonymous           |   匿名可以访问</span></span><br><span class="line"><span class="comment">     * denyAll             |   用户不能访问</span></span><br><span class="line"><span class="comment">     * fullyAuthenticated  |   用户完全认证可以访问（非remember-me下自动登录）</span></span><br><span class="line"><span class="comment">     * hasAnyAuthority     |   如果有参数，参数表示权限，则其中任何一个权限可以访问</span></span><br><span class="line"><span class="comment">     * hasAnyRole          |   如果有参数，参数表示角色，则其中任何一个角色可以访问</span></span><br><span class="line"><span class="comment">     * hasAuthority        |   如果有参数，参数表示权限，则其权限可以访问</span></span><br><span class="line"><span class="comment">     * hasIpAddress        |   如果有参数，参数表示IP地址，如果用户IP和参数匹配，则可以访问</span></span><br><span class="line"><span class="comment">     * hasRole             |   如果有参数，参数表示角色，则其角色可以访问</span></span><br><span class="line"><span class="comment">     * permitAll           |   用户可以任意访问</span></span><br><span class="line"><span class="comment">     * rememberMe          |   允许通过remember-me登录的用户访问</span></span><br><span class="line"><span class="comment">     * authenticated       |   用户登录后可访问</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">configure</span><span class="params">(HttpSecurity httpSecurity)</span> <span class="keyword">throws</span> Exception</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">// 配置URL访问授权规则</span></span><br><span class="line">        ExpressionUrlAuthorizationConfigurer&lt;HttpSecurity&gt;.<span class="type">ExpressionInterceptUrlRegistry</span> <span class="variable">registry</span> <span class="operator">=</span> httpSecurity.authorizeRequests();</span><br><span class="line">        <span class="comment">// 遍历无需认证即可访问的URL列表，设置这些URL对所有用户可访问</span></span><br><span class="line">        permitAllUrl.getUrls().forEach(url -&gt; registry.antMatchers(url).permitAll());</span><br><span class="line">        <span class="comment">// 配置Web应用程序规则</span></span><br><span class="line">        httpSecurity</span><br><span class="line">                <span class="comment">// CSRF（跨站请求伪造）禁用，因为不使用session</span></span><br><span class="line">                .csrf().disable()</span><br><span class="line">                <span class="comment">// 禁用HTTP响应标头</span></span><br><span class="line">                .headers().cacheControl().disable().and()</span><br><span class="line">                <span class="comment">// 认证失败处理类</span></span><br><span class="line">                .exceptionHandling().authenticationEntryPoint(unauthorizedHandler).and()</span><br><span class="line">                <span class="comment">// 基于token，所以不需要session</span></span><br><span class="line">                .sessionManagement().sessionCreationPolicy(SessionCreationPolicy.STATELESS).and()</span><br><span class="line">                <span class="comment">// 过滤请求</span></span><br><span class="line">                .authorizeRequests()</span><br><span class="line">                <span class="comment">// 对于登录login 注册register 验证码captchaImage 允许匿名访问</span></span><br><span class="line">                .antMatchers(<span class="string">&quot;/login&quot;</span>, <span class="string">&quot;/register&quot;</span>, <span class="string">&quot;/captchaImage&quot;</span>).permitAll()</span><br><span class="line">                <span class="comment">// 静态资源，可匿名访问</span></span><br><span class="line">                .antMatchers(HttpMethod.GET, <span class="string">&quot;/&quot;</span>, <span class="string">&quot;/*.html&quot;</span>, <span class="string">&quot;/**/*.html&quot;</span>, <span class="string">&quot;/**/*.css&quot;</span>, <span class="string">&quot;/**/*.js&quot;</span>, <span class="string">&quot;/profile/**&quot;</span>).permitAll()</span><br><span class="line">                .antMatchers(<span class="string">&quot;/swagger-ui.html&quot;</span>, <span class="string">&quot;/swagger-resources/**&quot;</span>, <span class="string">&quot;/webjars/**&quot;</span>, <span class="string">&quot;/*/api-docs&quot;</span>, <span class="string">&quot;/druid/**&quot;</span>).permitAll()</span><br><span class="line">                <span class="comment">// 除上面外的所有请求全部需要鉴权认证</span></span><br><span class="line">                .anyRequest().authenticated()</span><br><span class="line">                .and()</span><br><span class="line">                .headers().frameOptions().disable();</span><br><span class="line">        <span class="comment">// 添加Logout filter</span></span><br><span class="line">        httpSecurity.logout().logoutUrl(<span class="string">&quot;/logout&quot;</span>).logoutSuccessHandler(logoutSuccessHandler);</span><br><span class="line">        <span class="comment">// 添加JWT filter</span></span><br><span class="line">        httpSecurity.addFilterBefore(authenticationTokenFilter, UsernamePasswordAuthenticationFilter.class);</span><br><span class="line">        <span class="comment">// 添加CORS filter</span></span><br><span class="line">        httpSecurity.addFilterBefore(corsFilter, JwtAuthenticationTokenFilter.class);</span><br><span class="line">        httpSecurity.addFilterBefore(corsFilter, LogoutFilter.class);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 强散列哈希加密实现</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="keyword">public</span> BCryptPasswordEncoder <span class="title function_">bCryptPasswordEncoder</span><span class="params">()</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">BCryptPasswordEncoder</span>();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 身份认证接口</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">configure</span><span class="params">(AuthenticationManagerBuilder auth)</span> <span class="keyword">throws</span> Exception</span><br><span class="line">    &#123;</span><br><span class="line">        auth.userDetailsService(userDetailsService).passwordEncoder(bCryptPasswordEncoder());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="用户登录流程"><a href="#用户登录流程" class="headerlink" title="用户登录流程"></a>用户登录流程</h2><p>管理员在登录页面，输入用户名和密码以及验证码后，点击登录按钮，向后端发送请求，后端通过springSecurity认证管理器进行登录校验</p>
<p><img src="/assets/image-20240813172027984.png" alt="image-20240813172027984"> </p>
<p>此功能涉及前端相关的代码位于<code>views/login.vue</code>这个视图组件中，负责实现用户登录界面和交互逻辑。</p>
<p>后端处理逻辑则在<code>dkd-admin</code>模块的SysLoginController中，负责接收前端的请求，处理登录逻辑，并返回token令牌</p>
<h3 id="前端"><a href="#前端" class="headerlink" title="前端"></a>前端</h3><ol>
<li><p>点击<code>login.vue</code>中的登录按钮</p>
</li>
<li><p>调用<code>login.vue</code>中的<code>handleLogin</code>方法</p>
</li>
<li><p>调用<code>store/mondles/user.js</code>中的<code>login</code>方法，将返回结果存入useUserStore对象中（用于管理用户相关的状态和操作）</p>
</li>
<li><p>调用<code>api/login.js</code>中的<code>login</code>方法</p>
</li>
<li><p>调用<code>utils/request.js</code>中的<code>service</code>实例基于axios发送ajax请求（.env.development文件设置了统一请求路径前缀）</p>
</li>
</ol>
<h3 id="后端"><a href="#后端" class="headerlink" title="后端"></a>后端</h3><h4 id="SysLoginController"><a href="#SysLoginController" class="headerlink" title="SysLoginController"></a>SysLoginController</h4><p>在<code>ruoyi-admin</code>模块中<code>com.ruoyi.web.controller.system.SysLoginController</code>类的<code>login</code>方法接收前端登录请求</p>
<h4 id="SysLoginService"><a href="#SysLoginService" class="headerlink" title="SysLoginService"></a>SysLoginService</h4><p>在<code>ruoyi-framework</code>模块中<code>com.ruoyi.framework.web.service.SysLoginService</code>类的<code>login</code>方法处理登录逻辑</p>
<ol>
<li>验证码校验</li>
<li>登录前置校验</li>
<li>SS认证管理器用户校验，调用执行UserDetailsServiceImpl.loadUserByUsername<ol>
<li>认证通过后，创建登录用户对象LoginUser包括用户ID、部门ID、用户信息和用户权限信息</li>
</ol>
</li>
<li>登录成功，记录日志</li>
<li>修改用户表更新登录信息</li>
<li>生成token</li>
</ol>
<p><strong>具体的执行的流程如下图：</strong></p>
<p><img src="/assets/image-20240812223100253.png" alt="image-20240812223100253">  </p>
<h2 id="获取用户角色和权限"><a href="#获取用户角色和权限" class="headerlink" title="获取用户角色和权限"></a>获取用户角色和权限</h2><p>超级管理员登录帝可得系统后，可以查看区域管理所有的权限按钮，实现增删改查操作</p>
<p><img src="/assets/image-20240814114152698.png" alt="image-20240814114152698"> </p>
<p>而财务人员登录帝可得系统后，仅可以查看区域列表，其他权限没有</p>
<p><img src="/assets/image-20240814114217839.png" alt="image-20240814114217839"> </p>
<p>1）首先创建一个财务人员的角色，权限字符串 Accountant，仅设置区域管理查询权限</p>
<p><img src="/assets/image-20240814114404431.png" alt="image-20240814114404431"> </p>
<p>2）再创建一个用户，昵称为黑马彭于晏，用户名hmpyy，密码admin123 角色为财务人员</p>
<p><img src="/assets/image-20240814114420586.png" alt="image-20240814114420586"> </p>
<h3 id="前端-1"><a href="#前端-1" class="headerlink" title="前端"></a>前端</h3><ol>
<li>在全局<code>permission.js</code>中的<code>router.beforeEach</code>方法用于在用户导航到不同路由之前进行一些预处理</li>
<li>调用<code>store/mondles/user.js</code>中的<code>getInfo</code>方法，将返回结果存入useUserStore对象中（用于管理用户相关的状态和操作）</li>
<li>调用<code>api/login.js</code>中的<code>getInfo</code>方法</li>
</ol>
<h3 id="后端-1"><a href="#后端-1" class="headerlink" title="后端"></a>后端</h3><h4 id="SysLoginController-1"><a href="#SysLoginController-1" class="headerlink" title="SysLoginController"></a>SysLoginController</h4><p>在<code>ruoyi-admin</code>模块中<code>com.ruoyi.web.controller.system.SysLoginController</code>类的<code>getInfo</code>方法接收前端获取用户信息请求</p>
<h4 id="SysPermissionService"><a href="#SysPermissionService" class="headerlink" title="SysPermissionService"></a>SysPermissionService</h4><p>在<code>ruoyi-framework</code>模块中<code>com.ruoyi.framework.web.service.SysPermissionService</code>类</p>
<ol>
<li>getRolePermission查询该用户角色集合</li>
<li>getMenuPermission查询该用户权限（菜单）集合</li>
</ol>
<p><strong>具体的执行的流程如下图：</strong></p>
<p><img src="/assets/image-20240810163729060.png" alt="image-20240810163729060"> </p>
<h3 id="页面权限"><a href="#页面权限" class="headerlink" title="页面权限"></a>页面权限</h3><p>前端封装了一个指令权限，能简单快速的实现按钮级别的权限判断。</p>
<p><strong>使用权限字符串 v-hasPermi</strong>：<code>@/directive/permission/hasPermi.js</code></p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line">// 单个</span><br><span class="line"><span class="tag">&lt;<span class="name">el-button</span> <span class="attr">v-hasPermi</span>=<span class="string">&quot;[&#x27;system:user:add&#x27;]&quot;</span>&gt;</span>存在权限字符串才能看到<span class="tag">&lt;/<span class="name">el-button</span>&gt;</span></span><br><span class="line">// 多个</span><br><span class="line"><span class="tag">&lt;<span class="name">el-button</span> <span class="attr">v-hasPermi</span>=<span class="string">&quot;[&#x27;system:user:add&#x27;, &#x27;system:user:edit&#x27;]&quot;</span>&gt;</span>包含权限字符串才能看到<span class="tag">&lt;/<span class="name">el-button</span>&gt;</span></span><br></pre></td></tr></table></figure>



<p><strong>使用角色字符串 v-hasRole</strong><code>@/directive/permission/hasRole.js</code></p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line">// 单个</span><br><span class="line"><span class="tag">&lt;<span class="name">el-button</span> <span class="attr">v-hasRole</span>=<span class="string">&quot;[&#x27;admin&#x27;]&quot;</span>&gt;</span>管理员才能看到<span class="tag">&lt;/<span class="name">el-button</span>&gt;</span></span><br><span class="line">// 多个</span><br><span class="line"><span class="tag">&lt;<span class="name">el-button</span> <span class="attr">v-hasRole</span>=<span class="string">&quot;[&#x27;role1&#x27;, &#x27;role2&#x27;]&quot;</span>&gt;</span>包含角色才能看到<span class="tag">&lt;/<span class="name">el-button</span>&gt;</span></span><br></pre></td></tr></table></figure>



<h2 id="获取动态菜单路由"><a href="#获取动态菜单路由" class="headerlink" title="获取动态菜单路由"></a>获取动态菜单路由</h2><p>在上个小节我们创建了“黑马彭于晏”这个用户，并为他分配了特定的角色权限。这意味着，当他登录帝可得系统时，看到的侧边栏菜单将根据他的角色权限而有所不同。而超级管理员是可以查看所有菜单的</p>
<p>实现此功能的前端代码位于<code>src/permission.js</code>文件。它在登录成功后，会在跳转到新路由之前，去查询当前用户有权访问的动态菜单的路由列表。</p>
<p>后端处理逻辑则在<code>dkd-admin</code>模块的SysLoginController中，它负责接收前端发来的请求，处理查询，并构建起一个完整的菜单树结构，然后返回给前端。</p>
<h3 id="前端-2"><a href="#前端-2" class="headerlink" title="前端"></a>前端</h3><ol>
<li>在全局<code>permission.js</code>中的<code>router.beforeEach</code>方法用于在用户导航到不同路由之前进行一些预处理</li>
<li>调用<code>store/mondles/permission.js</code>中的<code>generateRoutes</code>方法，将返回结果存入usePermissionStore对象中</li>
<li>调用<code>api/menu.js</code>中的<code>getRouters</code>方法</li>
</ol>
<h3 id="后端-2"><a href="#后端-2" class="headerlink" title="后端"></a>后端</h3><h4 id="SysLoginController-2"><a href="#SysLoginController-2" class="headerlink" title="SysLoginController"></a>SysLoginController</h4><p>在<code>ruoyi-admin</code>模块中<code>com.ruoyi.web.controller.system.SysLoginController</code>类的<code>getRouters</code>方法接收前端获取路由信息请求</p>
<h4 id="ISysMenuService"><a href="#ISysMenuService" class="headerlink" title="ISysMenuService"></a>ISysMenuService</h4><p>在<code>ruoyi-system</code>模块中<code>com.ruoyi.web.system.service.ISysMenuService</code>类</p>
<ol>
<li>selectMenuTreeByUserId根据用户ID查询菜单树信息（递归生成父子菜单）</li>
<li>buildMenus构建前端路由所需要的菜单路由格式RouterVo</li>
</ol>
<p><strong>具体的执行的流程如下图：</strong></p>
<p><img src="/assets/image-20240810163753513.png" alt="image-20240810163753513"> </p>
<h3 id="路由菜单加载"><a href="#路由菜单加载" class="headerlink" title="路由菜单加载"></a>路由菜单加载</h3><ol>
<li>用户登录成功后，通过路由<code>router/index.js</code>跳转到首页并加载layout布局组件</li>
<li>在<code>layout/index.vue</code>中加载sidbar侧边栏</li>
<li>在<code>layout/components/Sidebar/index.vue</code>中遍历动态路由菜单在页面显示</li>
<li>用户点击菜单后会根据路由的path跳转到对应的视图组件在<app-main />显示</li>
</ol>
<h2 id="权限注解"><a href="#权限注解" class="headerlink" title="权限注解"></a>权限注解</h2><h3 id="源码分析-1"><a href="#源码分析-1" class="headerlink" title="源码分析"></a>源码分析</h3><p>在若依框架中，权限的验证最核心的是使用的Spring Security的提供的权限注解<code>@PreAuthorize </code></p>
<ul>
<li><p>@PreAuthorize 是 Spring Security 框架中提供的一个安全注解，用于实现基于注解的访问控制。它允许开发者在<strong>方法级别</strong>上声明特定的安全约束，以确保只有满足指定条件的用户才能调用该方法</p>
</li>
<li><p>当 @PreAuthorize 注解被应用于某个方法时，Spring Security 在<strong>该方法执行前</strong>会先对当前认证的用户进行权限检查。如果检查通过，方法调用得以继续；否则，框架会抛出相应的权限异常（如 AccessDeniedException），阻止方法执行。</p>
</li>
</ul>
<p>若依框架中的权限控制代码，如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@PreAuthorize(&quot;@ss.hasPermi(&#x27;manage:order:list&#x27;)&quot;)</span></span><br><span class="line"><span class="keyword">public</span> TableDataInfo <span class="title function_">list</span><span class="params">(Order order)</span></span><br><span class="line">&#123;</span><br><span class="line">    startPage();</span><br><span class="line">    List&lt;Order&gt; list = orderService.selectOrderList(order);</span><br><span class="line">    <span class="keyword">return</span> getDataTable(list);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>@PreAuthorize是Spring Security框架的权限注解，在执行方法前执行</p>
</li>
<li><p>@ss.hasPermi(‘manage:order:list’)</p>
<ul>
<li>其中的ss是指的一个spring管理的bean<ul>
<li>位置：ruoyi-framework模块中的com.ruoyi.framework.web.service.PermissionService</li>
</ul>
</li>
<li>hasPermi  是PermissionService类中的一个方法，判断是否拥有该权限</li>
<li>manage:order:list为方法的参数</li>
</ul>
</li>
</ul>
<blockquote>
<p>注意：在SecurityConfig类中添加<code>@EnableGlobalMethodSecurity(prePostEnabled = true, securedEnabled = true)</code>@PreAuthorize才能生效</p>
</blockquote>
<p><strong>权限控制流程：</strong></p>
<p><img src="/assets/image-20240815101708181.png" alt="image-20240815101708181">   </p>
<h3 id="权限方法"><a href="#权限方法" class="headerlink" title="权限方法"></a>权限方法</h3><p><code>@PreAuthorize</code>注解用于配置接口要求用户拥有某些权限才可访问，它拥有如下方法</p>
<table>
<thead>
<tr>
<th>方法</th>
<th>参数</th>
<th>描述</th>
</tr>
</thead>
<tbody><tr>
<td>hasPermi</td>
<td>String</td>
<td>验证用户是否具备某权限</td>
</tr>
<tr>
<td>lacksPermi</td>
<td>String</td>
<td>验证用户是否不具备某权限，与 hasPermi逻辑相反</td>
</tr>
<tr>
<td>hasAnyPermi</td>
<td>String</td>
<td>验证用户是否具有以下任意一个权限</td>
</tr>
<tr>
<td>hasRole</td>
<td>String</td>
<td>判断用户是否拥有某个角色</td>
</tr>
<tr>
<td>lacksRole</td>
<td>String</td>
<td>验证用户是否不具备某角色，与 hasRole逻辑相反</td>
</tr>
<tr>
<td>hasAnyRoles</td>
<td>String</td>
<td>验证用户是否具有以下任意一个角色，多个逗号分隔</td>
</tr>
</tbody></table>
<h3 id="使用示例"><a href="#使用示例" class="headerlink" title="使用示例"></a>使用示例</h3><p>1）数据权限示例。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 符合system:user:list权限要求</span></span><br><span class="line"><span class="meta">@PreAuthorize(&quot;@ss.hasPermi(&#x27;system:user:list&#x27;)&quot;)</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 不符合system:user:list权限要求</span></span><br><span class="line"><span class="meta">@PreAuthorize(&quot;@ss.lacksPermi(&#x27;system:user:list&#x27;)&quot;)</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 符合system:user:add或system:user:edit权限要求即可</span></span><br><span class="line"><span class="meta">@PreAuthorize(&quot;@ss.hasAnyPermi(&#x27;system:user:add,system:user:edit&#x27;)&quot;)</span></span><br></pre></td></tr></table></figure>

<p><strong>编程式判断是否有资源权限</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (SecurityUtils.hasPermi(<span class="string">&quot;sys:user:edit&quot;</span>))</span><br><span class="line">&#123;</span><br><span class="line">    System.out.println(<span class="string">&quot;当前用户有编辑用户权限&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>2）角色权限示例。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 属于user角色</span></span><br><span class="line"><span class="meta">@PreAuthorize(&quot;@ss.hasRole(&#x27;user&#x27;)&quot;)</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 不属于user角色</span></span><br><span class="line"><span class="meta">@PreAuthorize(&quot;@ss.lacksRole(&#x27;user&#x27;)&quot;)</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 属于user或者admin之一</span></span><br><span class="line"><span class="meta">@PreAuthorize(&quot;@ss.hasAnyRoles(&#x27;user,admin&#x27;)&quot;)</span></span><br></pre></td></tr></table></figure>

<p><strong>编程式判断是否有角色权限</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (SecurityUtils.hasRole(<span class="string">&quot;admin&quot;</span>))</span><br><span class="line">&#123;</span><br><span class="line">    System.out.println(<span class="string">&quot;当前用户有admin角色权限&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>权限提示：超级管理员拥有所有权限，不受权限约束。</p>
</blockquote>
<h3 id="公开接口"><a href="#公开接口" class="headerlink" title="公开接口"></a>公开接口</h3><p>如果有些接口是不需要验证权限可以公开访问的，这个时候就需要我们给接口放行。</p>
<p>使用注解方式，只需要在<code>Controller</code>的类或方法上加入<code>@Anonymous</code>该注解即可</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// @PreAuthorize(&quot;@ss.xxxx(&#x27;....&#x27;)&quot;) 注释或删除掉原有的权限注解</span></span><br><span class="line"><span class="meta">@Anonymous</span></span><br><span class="line"><span class="meta">@GetMapping(&quot;/list&quot;)</span></span><br><span class="line"><span class="keyword">public</span> List&lt;SysXxxx&gt; <span class="title function_">list</span><span class="params">(SysXxxx xxxx)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">return</span> xxxxList;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h1 id="异步任务管理器"><a href="#异步任务管理器" class="headerlink" title="异步任务管理器"></a>异步任务管理器</h1><h2 id="介绍-1"><a href="#介绍-1" class="headerlink" title="介绍"></a>介绍</h2><p>若依框架的前后端分离版本中，异步任务管理器扮演着重要角色，主要用于处理一些不需要即时返回结果的后台任务，从而提高应用程序的整体性能</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 多线程执行任务AsyncManager.me().execute(AsyncFactory.createTimerTask());</span></span><br><span class="line">AsyncManager.me().execute(AsyncFactory.createTimerTask());</span><br></pre></td></tr></table></figure>

<p>场景：记录日志<br><img src="/assets/image-20240816144639462.png" alt="image-20240816144639462"> </p>
<p>那么，为什么使用异步任务管理器能够提高性能呢？让我们通过一个实际的例子来说明。</p>
<p>用户在浏览器中输入用户名、密码和验证码，然后发送登录请求。</p>
<p>在sysLoginService业务层代码中实现登录逻辑，核心业务是登录认证，包括验证码校验、用户名查询和密码校验。</p>
<p>如果校验失败，我们会记录日志并返回提示信息；如果校验成功，我们同样记录日志并返回token。</p>
<p>假设登录认证核心业务代码执行耗时50ms，记录日志也需要耗时50ms，那么整个登录请求耗时多久呢？没错就是100ms。</p>
<p>现在，如果产品经理根据客户的要求，增加了一个功能：在后台系统登录成功后给用户发送一条提示短信。这意味着我们需要在业务层增加发送短信的代码，而这同样需要耗时50ms。这样一来，整个登录请求的耗时就变成了150ms，用户的等待时间增加了，系统性能也随之下降。</p>
<p>此外，我们的登录业务还涉及到事务控制，以保证业务的一致性。</p>
<p>如果登录认证业务成功，但记录日志时出现异常，导致事务回滚，这样的设计显然是不合理的。这就是级联失败的现象。</p>
<p>其实对于用户只关心登录是否成功，而对于记录日志和发送短信这些非核心业务，他们并不关心是否立即完成。</p>
<p><img src="/assets/image-20240816144753018.png" alt="image-20240816144753018"> </p>
<p>因此，我们可以通过异步任务管理器的多线程来处理这些任务。我们来看下</p>
<p>在用户发送登录请求时，<code>sysLoginService</code>业务层只需关注登录认证的核心业务代码。</p>
<p>记录日志和发送短信的任务可以交给异步管理器来处理。</p>
<p>这样，用户登录的耗时只需要50ms，无需等待异步任务的完成，从而显著提高了性能。</p>
<p>更重要的是，登录认证作为一个独立的事务运行在主线程中，而记录日志则在另一个线程中进行。这样，即使记录日志失败，也不会影响登录认证的事务，实现了故障隔离。</p>
<p><img src="/assets/image-20240816144922035.png" alt="image-20240816144922035"> </p>
<p>通过使用异步任务管理器，我们可以将那些不需要立即返回结果的非核心业务代码交给它来执行，从而优化系统性能，提高用户体验。</p>
<h2 id="源码"><a href="#源码" class="headerlink" title="源码"></a>源码</h2><p>若依异步任务管理器是一个单例对象使用了线程池+异步工厂（产生任务用）</p>
<ul>
<li>com.dkd.framework.manager.AsyncManager 异步任务管理器</li>
<li>com.dkd.framework.manager.factory.AsyncFactory 异步线程工厂</li>
</ul>
<p>1、 AsyncManager.me()获取AsyncManager对象</p>
<p>2、调用execute方法，执行TimerTask任务（记录登录日志），它实现了runnable接口，由线程Thread去执行</p>
<p>3、execute方法内部调用ScheduledExecutorService异步操作任务调度线程池的schedule方法用于延迟10毫秒执行一个任务</p>
<p><strong>具体的流程图如下：</strong></p>
<p><img src="/assets/image-20240816145052754.png" alt="image-20240816145052754"> </p>
<h1 id="操作日志"><a href="#操作日志" class="headerlink" title="操作日志"></a>操作日志</h1><h2 id="介绍-2"><a href="#介绍-2" class="headerlink" title="介绍"></a>介绍</h2><p>在日常编程中，记录日志是我们的得力助手，尤其在处理关键业务时，它能帮助我们追踪和审查操作过程。</p>
<p>然而，手动记录日志常常是繁琐且容易出错的。我们需要收集操作内容、参数等信息，这不仅增加了工作量，还可能导致代码重复，影响业务逻辑的清晰度。</p>
<p>那么，有没有什么办法可以让我们更优雅地处理这个问题呢？答案是肯定的，那就是使用注解</p>
<p>在需要被记录日志的<code>controller</code>方法上添加<code>@Log</code>注解，使用方法如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Log(title = &quot;订单管理&quot;, businessType = BusinessType.INSERT)</span></span><br><span class="line"><span class="keyword">public</span> AjaxResult <span class="title function_">add</span><span class="params">(...)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">return</span> toAjax(...);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p><strong>注解参数说明：</strong></p>
<table>
<thead>
<tr>
<th>参数</th>
<th>类型</th>
<th>默认值</th>
<th>描述</th>
</tr>
</thead>
<tbody><tr>
<td>title</td>
<td>String</td>
<td>空</td>
<td>操作模块</td>
</tr>
<tr>
<td>businessType</td>
<td>BusinessType</td>
<td>OTHER</td>
<td>操作功能（<code>OTHER</code>其他、<code>INSERT</code>新增、<code>UPDATE</code>修改、<code>DELETE</code>删除、<code>GRANT</code>授权、<code>EXPORT</code>导出、<code>IMPORT</code>导入、<code>FORCE</code>强退、<code>GENCODE</code>生成代码、<code>CLEAN</code>清空数据）</td>
</tr>
<tr>
<td>operatorType</td>
<td>OperatorType</td>
<td>MANAGE</td>
<td>操作人类别（<code>OTHER</code>其他、<code>MANAGE</code>后台用户、<code>MOBILE</code>手机端用户）</td>
</tr>
<tr>
<td>isSaveRequestData</td>
<td>boolean</td>
<td>true</td>
<td>是否保存请求的参数</td>
</tr>
<tr>
<td>isSaveResponseData</td>
<td>boolean</td>
<td>true</td>
<td>是否保存响应的参数</td>
</tr>
<tr>
<td>excludeParamNames</td>
<td>String[]</td>
<td>{}</td>
<td>排除指定的请求参数</td>
</tr>
</tbody></table>
<h2 id="源码-1"><a href="#源码-1" class="headerlink" title="源码"></a>源码</h2><p>若依操作日志使用了自定义注解+AOP切面+异步任务管理器</p>
<ul>
<li><p>com.ruoyi.common.annotation.Log  自定义注解</p>
</li>
<li><p>com.ruoyi.framework.aspectj.LogAspect  在一个aop切面类</p>
<ul>
<li>通过实现AOP切面编程，对目标方法进行拦截(标注Log注解的方法)，实现了操作日志的自动记录</li>
<li>异步任务管理器来将任务（记录操作日志到数据库）交给线程池来完成</li>
</ul>
</li>
</ul>
<p><img src="/assets/image-20240816160259072.png" alt="image-20240816160259072">  </p>
<h1 id="定时任务"><a href="#定时任务" class="headerlink" title="定时任务"></a>定时任务</h1><h2 id="源码分析-2"><a href="#源码分析-2" class="headerlink" title="源码分析"></a>源码分析</h2><p>在实际项目开发中Web应用有一类不可缺少的，那就是定时任务。 定时任务的场景可以说非常广泛，比如某些视频网站，购买会员后，每天会给会员送成长值，每月会给会员送一些电影券； 比如在保证最终一致性的场景中，往往利用定时任务调度进行一些比对工作；比如一些定时需要生成的报表、邮件；比如一些需要定时清理数据的任务等。 所以我们提供方便友好的web界面，实现动态管理任务，可以达到动态控制定时任务启动、暂停、重启、删除、添加、修改等操作，极大地方便了开发过程。</p>
<p><img src="/assets/image-20240817143607371.png" alt="image-20240817143607371"> </p>
<h3 id="表结构说明-1"><a href="#表结构说明-1" class="headerlink" title="表结构说明"></a>表结构说明</h3><p><strong><code>sys_job</code> 表</strong>：这是核心的定时任务表，用于存储定时任务的配置信息，如任务名称、任务组、执行的类全名、执行的参数、cron表达式等 </p>
<p><strong><code>sys_job_log</code> 表</strong>：用于记录定时任务的执行日志，包括任务的开始执行时间、结束执行时间、执行结果等 </p>
<p><img src="/assets/image-20240817141604879.png" alt="image-20240817141604879"> </p>
<h3 id="目录结构-1"><a href="#目录结构-1" class="headerlink" title="目录结构"></a>目录结构</h3><p><strong>1）后端代码</strong></p>
<p><img src="/assets/image-20240817143806315.png" alt="image-20240817143806315"> </p>
<ul>
<li>ScheduleUtils 定时任务工具类</li>
<li>抽象quartz调用类AbstractQuartzJob（实现Job接口）</li>
<li>定时任务处理类（禁止并发执行） 继承抽象类</li>
<li>定时任务处理类（允许并发执行） 继承抽象类</li>
<li>任务执行工具类 JobInvokeUtil</li>
<li>cron表达式工具类</li>
</ul>
<p><strong>2）前端代码</strong></p>
<p><img src="/assets/image-20240817143817826.png" alt="image-20240817143817826"> </p>
<h3 id="Quartz体系结构"><a href="#Quartz体系结构" class="headerlink" title="Quartz体系结构"></a>Quartz体系结构</h3><p><img src="/assets/image-20240817144336018.png" alt="image-20240817144336018"> </p>
<h3 id="Quartz核心API"><a href="#Quartz核心API" class="headerlink" title="Quartz核心API"></a>Quartz核心API</h3><table>
<thead>
<tr>
<th>API</th>
<th>描述</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Job</strong></td>
<td>- 实际要执行的任务类<br>- 必须实现Quartz的<code>Job</code>接口。</td>
</tr>
<tr>
<td><strong>JobDetail</strong></td>
<td>- 代表一个<code>Job</code>实例<br>- 通过<code>JobBuilder</code>类创建。</td>
</tr>
<tr>
<td><strong>JobBuilder</strong></td>
<td>- 用于声明一个任务实例<br>- 可以定义关于该任务的详情，如任务名、组名等。</td>
</tr>
<tr>
<td><strong>Trigger</strong></td>
<td>- 触发器，用来触发并执行<code>Job</code>实例的机制。</td>
</tr>
<tr>
<td><strong>SimpleTrigger</strong></td>
<td>- 用于简单重复执行作业的触发器<br>- 例如：每隔一定时间执行一次。</td>
</tr>
<tr>
<td><strong>CronTrigger</strong></td>
<td>- 使用Cron表达式定义执行计划的触发器<br>- 适用于定义复杂的执行时间。</td>
</tr>
<tr>
<td><strong>TriggerBuilder</strong></td>
<td>- 用于创建触发器<code>Trigger</code>实例的构建器。</td>
</tr>
<tr>
<td><strong>Scheduler</strong></td>
<td>- Quartz中的核心组件<br>- 负责启动、停止、暂停和恢复任务。</td>
</tr>
</tbody></table>
<h3 id="定时任务执行"><a href="#定时任务执行" class="headerlink" title="定时任务执行"></a>定时任务执行</h3><p>项目在启动时，初始化定时任务</p>
<p><img src="/assets/image-20240817165327170.png" alt="image-20240817165327170"> </p>
<p>流程图：</p>
<p><img src="/assets/image-20240819204756016.png" alt="image-20240819204756016">      </p>
<h3 id="添加定时任务"><a href="#添加定时任务" class="headerlink" title="添加定时任务"></a>添加定时任务</h3><p>刚才我们分析了在项目启动时，从数据库中查询任务配置列表，然后创建定时任务，并根据状态判断是否执行任务调度，那如果是新添加的定时任务该如何处理呢？为了解答这个问题，我们来对这部分的源码进行分析</p>
<p>我们已经对若依前端的代码交互模式比较熟悉了，所以本次我们关注后端部分，入口在定时任务模块的sysJobController中</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 修改定时任务</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@PreAuthorize(&quot;@ss.hasPermi(&#x27;monitor:job:edit&#x27;)&quot;)</span></span><br><span class="line"><span class="meta">@Log(title = &quot;定时任务&quot;, businessType = BusinessType.UPDATE)</span></span><br><span class="line"><span class="meta">@PutMapping</span></span><br><span class="line"><span class="keyword">public</span> AjaxResult <span class="title function_">edit</span><span class="params">(<span class="meta">@RequestBody</span> SysJob job)</span> <span class="keyword">throws</span> SchedulerException, TaskException</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span> (!CronUtils.isValid(job.getCronExpression()))</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span> error(<span class="string">&quot;修改任务&#x27;&quot;</span> + job.getJobName() + <span class="string">&quot;&#x27;失败，Cron表达式不正确&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (StringUtils.containsIgnoreCase(job.getInvokeTarget(), Constants.LOOKUP_RMI))</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span> error(<span class="string">&quot;修改任务&#x27;&quot;</span> + job.getJobName() + <span class="string">&quot;&#x27;失败，目标字符串不允许&#x27;rmi&#x27;调用&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (StringUtils.containsAnyIgnoreCase(job.getInvokeTarget(), <span class="keyword">new</span> <span class="title class_">String</span>[] &#123; Constants.LOOKUP_LDAP, Constants.LOOKUP_LDAPS &#125;))</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span> error(<span class="string">&quot;修改任务&#x27;&quot;</span> + job.getJobName() + <span class="string">&quot;&#x27;失败，目标字符串不允许&#x27;ldap(s)&#x27;调用&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (StringUtils.containsAnyIgnoreCase(job.getInvokeTarget(), <span class="keyword">new</span> <span class="title class_">String</span>[] &#123; Constants.HTTP, Constants.HTTPS &#125;))</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span> error(<span class="string">&quot;修改任务&#x27;&quot;</span> + job.getJobName() + <span class="string">&quot;&#x27;失败，目标字符串不允许&#x27;http(s)&#x27;调用&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (StringUtils.containsAnyIgnoreCase(job.getInvokeTarget(), Constants.JOB_ERROR_STR))</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span> error(<span class="string">&quot;修改任务&#x27;&quot;</span> + job.getJobName() + <span class="string">&quot;&#x27;失败，目标字符串存在违规&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (!ScheduleUtils.whiteList(job.getInvokeTarget()))</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span> error(<span class="string">&quot;修改任务&#x27;&quot;</span> + job.getJobName() + <span class="string">&quot;&#x27;失败，目标字符串不在白名单内&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    job.setUpdateBy(getUsername());</span><br><span class="line">    <span class="keyword">return</span> toAjax(jobService.updateJob(job));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>代码流程图：</strong></p>
<p><img src="/assets/image-20240820161737496.png" alt="image-20240820161737496"> </p>
<h3 id="定时任务状态修改"><a href="#定时任务状态修改" class="headerlink" title="定时任务状态修改"></a>定时任务状态修改</h3><p>刚才我们分析新增定时任务的源码时，发现了任务在初始化时是处于暂停状态的。</p>
<p>如果要启动任务，可以在页面进行任务状态的开关控制，所以接下来我们对此功能的源码进行分析</p>
<p>入口在定时任务模块的sysJobController中</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 定时任务状态修改</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@PreAuthorize(&quot;@ss.hasPermi(&#x27;monitor:job:changeStatus&#x27;)&quot;)</span></span><br><span class="line"><span class="meta">@Log(title = &quot;定时任务&quot;, businessType = BusinessType.UPDATE)</span></span><br><span class="line"><span class="meta">@PutMapping(&quot;/changeStatus&quot;)</span></span><br><span class="line"><span class="keyword">public</span> AjaxResult <span class="title function_">changeStatus</span><span class="params">(<span class="meta">@RequestBody</span> SysJob job)</span> <span class="keyword">throws</span> SchedulerException</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 根据任务ID查询数据库，获取当前任务的配置信息</span></span><br><span class="line">    <span class="type">SysJob</span> <span class="variable">newJob</span> <span class="operator">=</span> jobService.selectJobById(job.getJobId());</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 将前端的状态设置到任务配置对象中</span></span><br><span class="line">    newJob.setStatus(job.getStatus());</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 调用服务层的方法根据状态启动/暂停任务，并返回操作结果</span></span><br><span class="line">    <span class="keyword">return</span> toAjax(jobService.changeStatus(newJob));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>代码流程图：</strong></p>
<p><img src="/assets/image-20240820194620546.png" alt="image-20240820194620546"> </p>
<h2 id="集群模式"><a href="#集群模式" class="headerlink" title="集群模式"></a>集群模式</h2><h3 id="介绍-3"><a href="#介绍-3" class="headerlink" title="介绍"></a>介绍</h3><p>首先我们来聊下为什么需要quartz集群</p>
<p>在单机模式下，默认所有的<code>jobDetail</code>和<code>trigger</code>都存储在内存中。这样做的好处是读取速度快，但缺点也很明显：一旦服务器故障，所有的任务数据就会丢失，这就是所谓的单点故障问题。</p>
<p>还有如果在一个高峰时段，比如上午9点，需要触发500个任务，这将给服务器带来巨大的负载压力。这不仅影响性能，还可能引发服务中断。</p>
<p>缺点：单点故障、负载压力大</p>
<p><img src="/assets/image-20240818154838493.png" alt="image-20240818154838493"> </p>
<p>为了解决这些问题，我们可以部署多个服务器节点，将任务信息存储到数据库中。这样，多个节点就可以通过共享数据库来协调任务的执行，形成Quartz集群模式。</p>
<p>这种方式不仅解决了单点故障问题，还能通过负载均衡提升效率。</p>
<p>集群模式的优势</p>
<ul>
<li><strong>高可用性</strong>：即使某个节点出现问题，其他节点仍然可以正常运行。</li>
<li><strong>负载均衡</strong>：将任务分散到不同的节点执行，避免单个节点过载。</li>
</ul>
<p>通常在生产环境中，我们会部署多台服务器，所以采用集群模式不会产生额外的成本。</p>
<p><img src="/assets/image-20240818154822651.png" alt="image-20240818154822651"> </p>
<p><strong>quartz集群所需数据库表</strong></p>
<table>
<thead>
<tr>
<th>表名</th>
<th>用途</th>
</tr>
</thead>
<tbody><tr>
<td><code>qrtz_triggers</code></td>
<td>存储触发器的基本信息，如触发器名称、组、类型等。</td>
</tr>
<tr>
<td><code>qrtz_cron_triggers</code></td>
<td>存储Cron触发器的额外信息，如Cron表达式。</td>
</tr>
<tr>
<td><code>qrtz_simple_triggers</code></td>
<td>存储简单触发器的额外信息，如重复次数和间隔。</td>
</tr>
<tr>
<td><code>qrtz_blob_triggers</code></td>
<td>存储BLOB类型触发器的额外信息，如持久化的数据。</td>
</tr>
<tr>
<td><code>qrtz_simprop_triggers</code></td>
<td>存储具有单一触发器属性的触发器的详细信息。</td>
</tr>
<tr>
<td><code>qrtz_job_details</code></td>
<td>存储作业详细信息，如作业名称、组、描述、作业类名等。</td>
</tr>
<tr>
<td><code>qrtz_scheduler_state</code></td>
<td>存储调度器的状态信息，如当前主节点信息等。</td>
</tr>
<tr>
<td><code>qrtz_locks</code></td>
<td>存储锁信息，用于控制并发和防止资源冲突。</td>
</tr>
<tr>
<td><code>qrtz_paused_trigger_grps</code></td>
<td>存储被暂停的触发器组的信息。</td>
</tr>
<tr>
<td><code>qrtz_fired_triggers</code></td>
<td>存储已触发的触发器的详细信息，包括执行历史。</td>
</tr>
<tr>
<td><code>qrtz_calendars</code></td>
<td>存储日历信息，定义工作日和非工作日，用于调度时间约束。</td>
</tr>
</tbody></table>
<h3 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h3><h4 id="导入sql"><a href="#导入sql" class="headerlink" title="导入sql"></a>导入sql</h4><p>将若依提供的<code>quartz.sql</code>导入到数据库中</p>
<p><img src="/assets/image-20240818162629489.png" alt="image-20240818162629489"> </p>
<h4 id="开启配置"><a href="#开启配置" class="headerlink" title="开启配置"></a>开启配置</h4><p>打开<code>dkd-quartz</code>模块中<code>ScheduleConfig</code>配置类注释</p>
<p><img src="/assets/image-20240818162644643.png" alt="image-20240818162644643"> </p>
<h4 id="节点复制"><a href="#节点复制" class="headerlink" title="节点复制"></a>节点复制</h4><p>首先修改当前SpringBoot的启动类的名称</p>
<p><img src="/assets/image-20240820223648071.png" alt="image-20240820223648071"> </p>
<p><img src="/assets/image-20240820223219142.png" alt="image-20240820223219142"> </p>
<p> 我们再添加（复制）一个SpringBoot的启动配置</p>
<p><code>-Dserver.port=8081</code></p>
<p><img src="/assets/image-20240820223401056.png" alt="image-20240820223401056"> </p>
<h4 id="观察数据库"><a href="#观察数据库" class="headerlink" title="观察数据库"></a>观察数据库</h4><p>重启项目即可，观察数据库，已存入jobDetail和trigger，多个服务器节点可以实现共享</p>
<img src="assets/image-20240818162857837.png" alt="image-20240818162857837" style="zoom:79%;" /> 

<p><img src="/assets/image-20240818162841736.png" alt="image-20240818162841736"> </p>
<h3 id="源码-2"><a href="#源码-2" class="headerlink" title="源码"></a>源码</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.dkd.quartz.config;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.springframework.context.annotation.Bean;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.annotation.Configuration;</span><br><span class="line"><span class="keyword">import</span> org.springframework.scheduling.quartz.SchedulerFactoryBean;</span><br><span class="line"><span class="keyword">import</span> javax.sql.DataSource;</span><br><span class="line"><span class="keyword">import</span> java.util.Properties;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 定时任务配置（单机部署建议删除此类和qrtz数据库表，默认走内存会最高效）</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> ruoyi</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ScheduleConfig</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="keyword">public</span> SchedulerFactoryBean <span class="title function_">schedulerFactoryBean</span><span class="params">(DataSource dataSource)</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="type">SchedulerFactoryBean</span> <span class="variable">factory</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">SchedulerFactoryBean</span>();</span><br><span class="line">        <span class="comment">// 设置数据源</span></span><br><span class="line">        factory.setDataSource(dataSource);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// quartz参数</span></span><br><span class="line">        <span class="type">Properties</span> <span class="variable">prop</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Properties</span>();</span><br><span class="line">        <span class="comment">// 集群实例名称，在同一个集群内多个节点上该实例名称必须一致</span></span><br><span class="line">        prop.put(<span class="string">&quot;org.quartz.scheduler.instanceName&quot;</span>, <span class="string">&quot;RuoyiScheduler&quot;</span>);</span><br><span class="line">        <span class="comment">// 集群实例Id，在同一个集群内多个节点上该实例Id必须唯一</span></span><br><span class="line">        <span class="comment">// AUTO表示由schedulerFactoryBean自动生成（计算机名+时间戳）</span></span><br><span class="line">        prop.put(<span class="string">&quot;org.quartz.scheduler.instanceId&quot;</span>, <span class="string">&quot;AUTO&quot;</span>);</span><br><span class="line">        <span class="comment">// 线程池配置</span></span><br><span class="line">        prop.put(<span class="string">&quot;org.quartz.threadPool.class&quot;</span>, <span class="string">&quot;org.quartz.simpl.SimpleThreadPool&quot;</span>);</span><br><span class="line">        prop.put(<span class="string">&quot;org.quartz.threadPool.threadCount&quot;</span>, <span class="string">&quot;20&quot;</span>); <span class="comment">// 线程数</span></span><br><span class="line">        prop.put(<span class="string">&quot;org.quartz.threadPool.threadPriority&quot;</span>, <span class="string">&quot;5&quot;</span>);<span class="comment">// 线程优先级（1~10）</span></span><br><span class="line">        <span class="comment">// JobStore配置：使用数据库来存储Job和Trigger信息</span></span><br><span class="line">        prop.put(<span class="string">&quot;org.quartz.jobStore.class&quot;</span>, <span class="string">&quot;org.springframework.scheduling.quartz.LocalDataSourceJobStore&quot;</span>);</span><br><span class="line">        <span class="comment">// 集群配置</span></span><br><span class="line">        prop.put(<span class="string">&quot;org.quartz.jobStore.isClustered&quot;</span>, <span class="string">&quot;true&quot;</span>); <span class="comment">// 开启集群</span></span><br><span class="line">        prop.put(<span class="string">&quot;org.quartz.jobStore.clusterCheckinInterval&quot;</span>, <span class="string">&quot;15000&quot;</span>);<span class="comment">// 心跳间隔</span></span><br><span class="line">        prop.put(<span class="string">&quot;org.quartz.jobStore.maxMisfiresToHandleAtATime&quot;</span>, <span class="string">&quot;10&quot;</span>);<span class="comment">// 最大允许处理的misfire数量</span></span><br><span class="line">        prop.put(<span class="string">&quot;org.quartz.jobStore.txIsolationLevelSerializable&quot;</span>, <span class="string">&quot;true&quot;</span>);<span class="comment">// 隔离级别</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// sqlserver 启用</span></span><br><span class="line">        <span class="comment">// prop.put(&quot;org.quartz.jobStore.selectWithLockSQL&quot;, &quot;SELECT * FROM &#123;0&#125;LOCKS UPDLOCK WHERE LOCK_NAME = ?&quot;);</span></span><br><span class="line">        prop.put(<span class="string">&quot;org.quartz.jobStore.misfireThreshold&quot;</span>, <span class="string">&quot;12000&quot;</span>);<span class="comment">// 允许的misfire时间间隔</span></span><br><span class="line">        prop.put(<span class="string">&quot;org.quartz.jobStore.tablePrefix&quot;</span>, <span class="string">&quot;QRTZ_&quot;</span>);<span class="comment">// 表前缀</span></span><br><span class="line">        factory.setQuartzProperties(prop);</span><br><span class="line"></span><br><span class="line">        factory.setSchedulerName(<span class="string">&quot;RuoyiScheduler&quot;</span>);<span class="comment">// 集群调度器名称</span></span><br><span class="line">        <span class="comment">// 延时启动</span></span><br><span class="line">        factory.setStartupDelay(<span class="number">1</span>);</span><br><span class="line">        factory.setApplicationContextSchedulerContextKey(<span class="string">&quot;applicationContextKey&quot;</span>);</span><br><span class="line">        <span class="comment">// 可选，QuartzScheduler</span></span><br><span class="line">        <span class="comment">// 启动时更新己存在的Job，这样就不用每次修改targetObject后删除qrtz_job_details表对应记录了</span></span><br><span class="line">        factory.setOverwriteExistingJobs(<span class="literal">true</span>);</span><br><span class="line">        <span class="comment">// 设置自动启动，默认为true</span></span><br><span class="line">        factory.setAutoStartup(<span class="literal">true</span>);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> factory;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h1 id="数据权限"><a href="#数据权限" class="headerlink" title="数据权限"></a>数据权限</h1><h2 id="介绍-4"><a href="#介绍-4" class="headerlink" title="介绍"></a>介绍</h2><p>在我们开始深入学习数据权限控制之前，让我们先来思考一个实际开发中经常遇到的关键问题：<strong>如何确保用户只能访问他们被授权查看的数据？</strong></p>
<p>这个问题的答案就是<strong>数据权限控制</strong>。</p>
<p>想象一下，我们有一个大型集团公司，旗下有多个子公司和部门。每个部门，比如市场部或财务部，都有自己的敏感数据。我们肯定不希望一个部门的员工能够访问另一个部门的敏感信息，对吧？</p>
<p><img src="/assets/image-20240823211352507.png" alt="image-20240823211352507"> </p>
<p>这时，可能会有同学问，为什么我们如此重视数据权限控制呢？原因很简单：<strong>保护敏感信息</strong>：它像一把锁，防止敏感数据泄露给未授权的人员，确保信息安全。</p>
<p><strong>数据权限的场景：</strong></p>
<ol>
<li><strong>部门级权限</strong>：比如，市场部的员工应该只能访问到销售部的数据，确保他们只能触及自己部门的信息。</li>
<li><strong>公司级权限</strong>：子公司的经理可能需要有更广阔的视野，他们需要查看整个子公司的数据，以做出战略决策。</li>
<li><strong>跨部门权限</strong>：而对于公司的高级领导或特定角色，他们可能需要有更全面的数据访问权限，以便跨部门或跨公司地进行管理和决策。</li>
</ol>
<p>通过数据权限控制，我们不仅保护了公司的数据安全，还确保了数据的合理利用和流程的顺畅。</p>
<h2 id="演示"><a href="#演示" class="headerlink" title="演示"></a>演示</h2><p>在系统中，权限的分配和控制主要依赖于角色。每个角色可以被赋予不同的菜单权限和数据权限，用户则通过他们的角色来继承这些权限，进而决定他们能访问哪些系统资源。</p>
<p>目前，系统支持以下五种数据权限类型：</p>
<ul>
<li><strong>全部数据权限</strong>：无限制访问所有数据，相当于拥有最高权限的通行证。</li>
<li><strong>自定数据权限</strong>：用户可以根据自己的需求设定访问特定数据的规则。</li>
<li><strong>部门数据权限</strong>：只能访问自己所在部门的数据，限制在本部门范围内。</li>
<li><strong>部门及以下数据权限</strong>：可以访问自己部门及下属部门的数据，适用于管理层级。</li>
<li><strong>仅本人数据权限</strong>：只能访问和操作自己的数据，保障个人隐私和数据隔离。</li>
</ul>
<p><img src="/assets/image-20240823100449151.png" alt="image-20240823100449151">  </p>
<blockquote>
<p>提示：默认系统管理员<code>admin</code>拥有所有数据权限<code>（userId=1）</code>，默认角色拥有所有数据权限（如不需要数据权限不用设置数据权限操作）</p>
</blockquote>
<h2 id="源码-3"><a href="#源码-3" class="headerlink" title="源码"></a>源码</h2><p>1、若依系统的数据权限设计主要通过用户、角色、部门表建立关系，实现对数据的访问控制：</p>
<p><img src="/assets/image-20240823141942730.png" alt="image-20240823141942730">  </p>
<p>2、在需要数据权限控制方法上添加<code>@DataScope</code>注解，其中<code>d</code>和<code>u</code>用来表示表的别名</p>
<p><strong>部门数据权限注解</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@DataScope(deptAlias = &quot;d&quot;)</span></span><br><span class="line"><span class="keyword">public</span> List&lt;...&gt; select(...)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">return</span> mapper.select(...);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>部门及用户权限注解</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@DataScope(deptAlias = &quot;d&quot;, userAlias = &quot;u&quot;)</span></span><br><span class="line"><span class="keyword">public</span> List&lt;...&gt; select(...)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">return</span> mapper.select(...);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<table>
<thead>
<tr>
<th>参数</th>
<th>类型</th>
<th>默认值</th>
<th>描述</th>
</tr>
</thead>
<tbody><tr>
<td>deptAlias</td>
<td>String</td>
<td>空</td>
<td>部门表的别名</td>
</tr>
<tr>
<td>userAlias</td>
<td>String</td>
<td>空</td>
<td>用户表的别名</td>
</tr>
</tbody></table>
<p>3、在<code>mybatis</code>查询底部标签添加数据范围过滤</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">&quot;select&quot;</span> <span class="attr">parameterType</span>=<span class="string">&quot;...&quot;</span> <span class="attr">resultMap</span>=<span class="string">&quot;...Result&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">include</span> <span class="attr">refid</span>=<span class="string">&quot;select...Vo&quot;</span>/&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- 数据范围过滤 --&gt;</span></span><br><span class="line">    $&#123;params.dataScope&#125;</span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>其作用就是相当于在一个 select 语句后面拼接一个 and 条件语句，来实现查询限制，例如下面</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 用户管理（未过滤数据权限的情况）：</span></span><br><span class="line"><span class="keyword">select</span> u.user_id, u.dept_id,  u.user_name, u.email</span><br><span class="line">	, u.phonenumber, u.password, u.sex, u.avatar</span><br><span class="line">	, u.status, u.del_flag, u.login_ip, u.login_date, u.create_by</span><br><span class="line">	, u.create_time, u.remark, d.dept_name</span><br><span class="line"><span class="keyword">from</span> sys_user u</span><br><span class="line">	<span class="keyword">left</span> <span class="keyword">join</span> sys_dept d <span class="keyword">on</span> u.dept_id <span class="operator">=</span> d.dept_id</span><br><span class="line"><span class="keyword">where</span> u.del_flag <span class="operator">=</span> <span class="string">&#x27;0&#x27;</span>;</span><br></pre></td></tr></table></figure>



<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 用户管理（已过滤数据权限的情况）：</span></span><br><span class="line"><span class="keyword">select</span> u.user_id, u.dept_id,  u.user_name, u.email</span><br><span class="line">	, u.phonenumber, u.password, u.sex, u.avatar</span><br><span class="line">	, u.status, u.del_flag, u.login_ip, u.login_date, u.create_by</span><br><span class="line">	, u.create_time, u.remark, d.dept_name</span><br><span class="line"><span class="keyword">from</span> sys_user u</span><br><span class="line">	<span class="keyword">left</span> <span class="keyword">join</span> sys_dept d <span class="keyword">on</span> u.dept_id <span class="operator">=</span> d.dept_id</span><br><span class="line"><span class="keyword">where</span> u.del_flag <span class="operator">=</span> <span class="string">&#x27;0&#x27;</span></span><br><span class="line">	<span class="keyword">and</span> u.dept_id <span class="keyword">in</span> (</span><br><span class="line">        <span class="keyword">select</span> dept_id</span><br><span class="line">        <span class="keyword">from</span> sys_role_dept</span><br><span class="line">        <span class="keyword">where</span> role_id <span class="operator">=</span> <span class="number">2</span></span><br><span class="line">    );</span><br></pre></td></tr></table></figure>

<p>结果很明显，我们多了如下语句。通过角色部门表<code>（sys_role_dept）</code>完成了自定义类型的数据权限过滤</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">and</span> u.dept_id <span class="keyword">in</span> (</span><br><span class="line">	<span class="keyword">select</span> dept_id</span><br><span class="line">	<span class="keyword">from</span> sys_role_dept</span><br><span class="line">	<span class="keyword">where</span> role_id <span class="operator">=</span> <span class="number">2</span></span><br><span class="line">)</span><br></pre></td></tr></table></figure>



<p>若依数据权限底层使用了自定义注解+AOP切面+SQL拼接</p>
<ul>
<li><p>com.dkd.common.annotation.DataScope 自定义注解</p>
</li>
<li><p>com.ruoyi.framework.aspectj.DataScopeAspect：切面类</p>
<ul>
<li>通过实现AOP编程，对目标方法进行拦截（标注DataScope 注解的方法），实现了构建数据范围SQL过滤条件</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.dkd.framework.aspectj;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> com.dkd.common.annotation.DataScope;</span><br><span class="line"><span class="keyword">import</span> com.dkd.common.core.domain.BaseEntity;</span><br><span class="line"><span class="keyword">import</span> com.dkd.common.core.domain.entity.SysRole;</span><br><span class="line"><span class="keyword">import</span> com.dkd.common.core.domain.entity.SysUser;</span><br><span class="line"><span class="keyword">import</span> com.dkd.common.core.domain.model.LoginUser;</span><br><span class="line"><span class="keyword">import</span> com.dkd.common.core.text.Convert;</span><br><span class="line"><span class="keyword">import</span> com.dkd.common.utils.SecurityUtils;</span><br><span class="line"><span class="keyword">import</span> com.dkd.common.utils.StringUtils;</span><br><span class="line"><span class="keyword">import</span> com.dkd.framework.security.context.PermissionContextHolder;</span><br><span class="line"><span class="keyword">import</span> org.aspectj.lang.JoinPoint;</span><br><span class="line"><span class="keyword">import</span> org.aspectj.lang.annotation.Aspect;</span><br><span class="line"><span class="keyword">import</span> org.aspectj.lang.annotation.Before;</span><br><span class="line"><span class="keyword">import</span> org.springframework.stereotype.Component;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.ArrayList;</span><br><span class="line"><span class="keyword">import</span> java.util.List;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 数据过滤处理</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> ruoyi</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@Aspect</span></span><br><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">DataScopeAspect</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 全部数据权限</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">DATA_SCOPE_ALL</span> <span class="operator">=</span> <span class="string">&quot;1&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 自定数据权限</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">DATA_SCOPE_CUSTOM</span> <span class="operator">=</span> <span class="string">&quot;2&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 部门数据权限</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">DATA_SCOPE_DEPT</span> <span class="operator">=</span> <span class="string">&quot;3&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 部门及以下数据权限</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">DATA_SCOPE_DEPT_AND_CHILD</span> <span class="operator">=</span> <span class="string">&quot;4&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 仅本人数据权限</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">DATA_SCOPE_SELF</span> <span class="operator">=</span> <span class="string">&quot;5&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 数据权限过滤关键字</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">DATA_SCOPE</span> <span class="operator">=</span> <span class="string">&quot;dataScope&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Before(&quot;@annotation(controllerDataScope)&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">doBefore</span><span class="params">(JoinPoint point, DataScope controllerDataScope)</span> <span class="keyword">throws</span> Throwable</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">// 清理数据范围（权限）过滤条件（params.dataScope）防止sql注入</span></span><br><span class="line">        clearDataScope(point);</span><br><span class="line">        <span class="comment">// 设置数据范围（权限）过滤条件</span></span><br><span class="line">        handleDataScope(point, controllerDataScope);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">handleDataScope</span><span class="params">(<span class="keyword">final</span> JoinPoint joinPoint, DataScope controllerDataScope)</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">// 获取当前的登录用户</span></span><br><span class="line">        <span class="type">LoginUser</span> <span class="variable">loginUser</span> <span class="operator">=</span> SecurityUtils.getLoginUser();</span><br><span class="line">        <span class="keyword">if</span> (StringUtils.isNotNull(loginUser))</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="comment">// 获取用户基本信息</span></span><br><span class="line">            <span class="type">SysUser</span> <span class="variable">currentUser</span> <span class="operator">=</span> loginUser.getUser();</span><br><span class="line">            <span class="comment">// 如果是超级管理员，则不过滤数据</span></span><br><span class="line">            <span class="keyword">if</span> (StringUtils.isNotNull(currentUser) &amp;&amp; !currentUser.isAdmin())</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="comment">// 获取目标方法的权限字符串  例如：用户列表（system:user:list）</span></span><br><span class="line">                <span class="type">String</span> <span class="variable">permission</span> <span class="operator">=</span> StringUtils.defaultIfEmpty(controllerDataScope.permission(), PermissionContextHolder.getContext());</span><br><span class="line">                <span class="comment">// 设置数据范围（权限）过滤条件，根据当前用户、部门别名、用户别名和权限标识对切点对象进行过滤处理</span></span><br><span class="line">                dataScopeFilter(joinPoint, currentUser, controllerDataScope.deptAlias(),</span><br><span class="line">                        controllerDataScope.userAlias(), permission);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 数据范围过滤</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> joinPoint 切点</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> user 用户</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> deptAlias 部门别名</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> userAlias 用户别名</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> permission 权限字符</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">dataScopeFilter</span><span class="params">(JoinPoint joinPoint, SysUser user, String deptAlias, String userAlias, String permission)</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">// 构建SQL字符串，用于拼接数据范围条件</span></span><br><span class="line">        <span class="type">StringBuilder</span> <span class="variable">sqlString</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">StringBuilder</span>();</span><br><span class="line">        <span class="comment">// 用于存储已经添加的数据范围类型，避免重复添加</span></span><br><span class="line">        List&lt;String&gt; conditions = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;String&gt;();</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 遍历用户的所有角色</span></span><br><span class="line">        <span class="keyword">for</span> (SysRole role : user.getRoles())</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="comment">// 获取当前角色的数据范围条件类型 1~5</span></span><br><span class="line">            <span class="type">String</span> <span class="variable">dataScope</span> <span class="operator">=</span> role.getDataScope();</span><br><span class="line">            <span class="comment">// 如果数据范围类型是自定义类型且已添加，则跳过本次循环</span></span><br><span class="line">            <span class="keyword">if</span> (!DATA_SCOPE_CUSTOM.equals(dataScope) &amp;&amp; conditions.contains(dataScope))</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 如果当前角色权限列表不包含目标方法的权限字符串（system:user:list），则跳过本次循环</span></span><br><span class="line">            <span class="keyword">if</span> (StringUtils.isNotEmpty(permission) &amp;&amp; StringUtils.isNotEmpty(role.getPermissions())</span><br><span class="line">                    &amp;&amp; !StringUtils.containsAny(role.getPermissions(), Convert.toStrArray(permission)))</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 如果角色的数据范围类型是全部数据，则清空SQL字符串并添加数据范围类型，结束循环</span></span><br><span class="line">            <span class="keyword">if</span> (DATA_SCOPE_ALL.equals(dataScope))</span><br><span class="line">            &#123;</span><br><span class="line">                sqlString = <span class="keyword">new</span> <span class="title class_">StringBuilder</span>();</span><br><span class="line">                conditions.add(dataScope);</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 如果角色的数据范围类型是自定义数据</span></span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (DATA_SCOPE_CUSTOM.equals(dataScope))</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="comment">// 拼接SQL条件，限制部门ID在角色所关联的部门范围内</span></span><br><span class="line">                sqlString.append(StringUtils.format(</span><br><span class="line">                        <span class="string">&quot; OR &#123;&#125;.dept_id IN ( SELECT dept_id FROM sys_role_dept WHERE role_id = &#123;&#125; ) &quot;</span>, deptAlias,</span><br><span class="line">                        role.getRoleId()));</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 如果角色的数据范围类型是本部门数据</span></span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (DATA_SCOPE_DEPT.equals(dataScope))</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="comment">// 拼接SQL条件，限制部门ID等于用户所在部门ID</span></span><br><span class="line">                sqlString.append(StringUtils.format(<span class="string">&quot; OR &#123;&#125;.dept_id = &#123;&#125; &quot;</span>, deptAlias, user.getDeptId()));</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 如果角色的数据范围类型是本部门及子部门数据</span></span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (DATA_SCOPE_DEPT_AND_CHILD.equals(dataScope))</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="comment">// 拼接SQL条件，限制部门ID等于用户所在部门ID或在用户所在部门的子孙部门中</span></span><br><span class="line">                sqlString.append(StringUtils.format(</span><br><span class="line">                        <span class="string">&quot; OR &#123;&#125;.dept_id IN ( SELECT dept_id FROM sys_dept WHERE dept_id = &#123;&#125; or find_in_set( &#123;&#125; , ancestors ) )&quot;</span>,</span><br><span class="line">                        deptAlias, user.getDeptId(), user.getDeptId()));</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 如果角色的数据范围类型是仅本人数据</span></span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (DATA_SCOPE_SELF.equals(dataScope))</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="comment">// 如果用户表别名不为空，拼接SQL条件限制用户ID等于当前用户ID</span></span><br><span class="line">                <span class="keyword">if</span> (StringUtils.isNotBlank(userAlias))</span><br><span class="line">                &#123;</span><br><span class="line">                    sqlString.append(StringUtils.format(<span class="string">&quot; OR &#123;&#125;.user_id = &#123;&#125; &quot;</span>, userAlias, user.getUserId()));</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">else</span></span><br><span class="line">                &#123; <span class="comment">// 否则，拼接SQL条件限制部门ID为0，即不查询任何数据</span></span><br><span class="line">                    sqlString.append(StringUtils.format(<span class="string">&quot; OR &#123;&#125;.dept_id = 0 &quot;</span>, deptAlias));</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 添加当前角色的数据范围类型条件</span></span><br><span class="line">            conditions.add(dataScope);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 如果数据范围类型集合（即多角色情况下，所有角色都不包含传递过来目标方法的权限字符），则添加一个条件使SQL查询不返回任何数据</span></span><br><span class="line">        <span class="keyword">if</span> (StringUtils.isEmpty(conditions))</span><br><span class="line">        &#123;</span><br><span class="line">            sqlString.append(StringUtils.format(<span class="string">&quot; OR &#123;&#125;.dept_id = 0 &quot;</span>, deptAlias));</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 如果SQL字符串不为空，则将构造好的数据范围条件添加到方法参数对象中，用于后续的SQL查询</span></span><br><span class="line">        <span class="keyword">if</span> (StringUtils.isNotBlank(sqlString.toString()))</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="comment">// 获取切点方法的第一个参数</span></span><br><span class="line">            <span class="type">Object</span> <span class="variable">params</span> <span class="operator">=</span> joinPoint.getArgs()[<span class="number">0</span>];</span><br><span class="line">            <span class="comment">// 检查参数是否非空且为BaseEntity类型</span></span><br><span class="line">            <span class="keyword">if</span> (StringUtils.isNotNull(params) &amp;&amp; params <span class="keyword">instanceof</span> BaseEntity)</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="comment">// 将参数转换为BaseEntity类型</span></span><br><span class="line">                <span class="type">BaseEntity</span> <span class="variable">baseEntity</span> <span class="operator">=</span> (BaseEntity) params;</span><br><span class="line">                <span class="comment">// 向BaseEntity的params属性中添加数据范围条件</span></span><br><span class="line">                baseEntity.getParams().put(DATA_SCOPE, <span class="string">&quot; AND (&quot;</span> + sqlString.substring(<span class="number">4</span>) + <span class="string">&quot;)&quot;</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 拼接权限sql前先清空params.dataScope参数防止注入</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">clearDataScope</span><span class="params">(<span class="keyword">final</span> JoinPoint joinPoint)</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">// 获取切点方法的第一个参数</span></span><br><span class="line">        <span class="type">Object</span> <span class="variable">params</span> <span class="operator">=</span> joinPoint.getArgs()[<span class="number">0</span>];</span><br><span class="line">        <span class="comment">// 检查参数不为null且是否为BaseEntity类型</span></span><br><span class="line">        <span class="keyword">if</span> (StringUtils.isNotNull(params) &amp;&amp; params <span class="keyword">instanceof</span> BaseEntity)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="comment">// 将参数转为BaseEntity类型</span></span><br><span class="line">            <span class="type">BaseEntity</span> <span class="variable">baseEntity</span> <span class="operator">=</span> (BaseEntity) params;</span><br><span class="line">            <span class="comment">// 将数据权限过滤条件设置为空</span></span><br><span class="line">            baseEntity.getParams().put(DATA_SCOPE, <span class="string">&quot;&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<p><img src="/assets/image-20240822194124221.png" alt="image-20240822194124221"> </p>
<blockquote>
<p>提示：仅实体继承<code>BaseEntity</code>才会进行处理，<code>SQL</code>语句会存放到<code>BaseEntity</code>对象中的<code>params</code>属性中，然后在<code>xml</code>中通过<code>$&#123;params.dataScope&#125;</code>获取拼接后的语句。</p>
</blockquote>
<h2 id="改造"><a href="#改造" class="headerlink" title="改造"></a>改造</h2><h3 id="需求"><a href="#需求" class="headerlink" title="需求"></a>需求</h3><p>我们有一个系统登录日志，里面记录了所有用户的登录信息。</p>
<p>但是，并不是所有人都应该看到所有的日志数据。所以，我们需要根据用户的角色来控制他们能查看的数据范围。</p>
<p><img src="/assets/image-20240822205934262.png" alt="image-20240822205934262"> </p>
<h3 id="添加权限注解"><a href="#添加权限注解" class="headerlink" title="添加权限注解"></a>添加权限注解</h3><p>在<code>dkd-system</code>模块的<code>com.dkd.system.service.impl.SysLogininforServiceImpl</code>在服务层的方法上使用 <code>@DataScope</code> 注解</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 系统访问日志情况信息 服务层处理</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> ruoyi</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@Service</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">SysLogininforServiceImpl</span> <span class="keyword">implements</span> <span class="title class_">ISysLogininforService</span></span><br><span class="line">&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> SysLogininforMapper logininforMapper;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 查询系统登录日志集合</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> logininfor 访问日志对象</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> 登录记录集合</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@DataScope(deptAlias = &quot;d&quot;, userAlias = &quot;u&quot;)</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> List&lt;SysLogininfor&gt; <span class="title function_">selectLogininforList</span><span class="params">(SysLogininfor logininfor)</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span> logininforMapper.selectLogininforList(logininfor);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h3 id="添加表字段"><a href="#添加表字段" class="headerlink" title="添加表字段"></a>添加表字段</h3><p>如果<code>sys_logininfo</code>业务表需要实现数据权限，需要有<code>dept_id</code>和<code>user_id</code>这两个字段。</p>
<p><img src="/assets/image-20240822201954247.png" alt="image-20240822201954247"> </p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- prompt：你是一名软件工程师，请为sys_logininfor表添加dept_id和user_id二个字段和注释，类型为bigint，生成sql语句</span></span><br><span class="line"></span><br><span class="line"><span class="comment">-- 添加 dept_id 和 user_id 字段</span></span><br><span class="line"><span class="keyword">ALTER</span> <span class="keyword">TABLE</span> sys_logininfor <span class="keyword">ADD</span> <span class="keyword">COLUMN</span> `dept_id` <span class="type">BIGINT</span> <span class="keyword">DEFAULT</span> <span class="keyword">NULL</span> COMMENT <span class="string">&#x27;部门ID&#x27;</span>;</span><br><span class="line"><span class="keyword">ALTER</span> <span class="keyword">TABLE</span> sys_logininfor <span class="keyword">ADD</span> <span class="keyword">COLUMN</span> `user_id` <span class="type">BIGINT</span> <span class="keyword">DEFAULT</span> <span class="keyword">NULL</span> COMMENT <span class="string">&#x27;用户ID&#x27;</span>;</span><br></pre></td></tr></table></figure>



<h3 id="添加实体属性"><a href="#添加实体属性" class="headerlink" title="添加实体属性"></a>添加实体属性</h3><p>在<code>dkd-system</code>模块的<code>com.dkd.system.domain.SysLogininfor</code>实体类中，需要有<code>deptId</code>和<code>userId</code>这两个属性。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.dkd.system.domain;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> com.dkd.common.annotation.Excel;</span><br><span class="line"><span class="keyword">import</span> com.dkd.common.annotation.Excel.ColumnType;</span><br><span class="line"><span class="keyword">import</span> com.dkd.common.core.domain.BaseEntity;</span><br><span class="line"><span class="keyword">import</span> com.fasterxml.jackson.annotation.JsonFormat;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.Date;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 系统访问记录表 sys_logininfor</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> ruoyi</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">SysLogininfor</span> <span class="keyword">extends</span> <span class="title class_">BaseEntity</span></span><br><span class="line">&#123;</span><br><span class="line">   </span><br><span class="line">	<span class="comment">// 其他代码略.....</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 用户id</span></span><br><span class="line">    <span class="keyword">private</span> Long userId;</span><br><span class="line">    <span class="comment">// 部门id</span></span><br><span class="line">    <span class="keyword">private</span> Long deptId;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> Long <span class="title function_">getUserId</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> userId;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setUserId</span><span class="params">(Long userId)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.userId = userId;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> Long <span class="title function_">getDeptId</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> deptId;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setDeptId</span><span class="params">(Long deptId)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.deptId = deptId;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h3 id="修改映射文件"><a href="#修改映射文件" class="headerlink" title="修改映射文件"></a>修改映射文件</h3><p>在<code>dkd-system</code>模块的<code>com.dkd.system.domain.SysLogininforMapper.xml</code>映射文件中，通过动态拼接 SQL，实现数据范围的过滤</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">insert</span> <span class="attr">id</span>=<span class="string">&quot;insertLogininfor&quot;</span> <span class="attr">parameterType</span>=<span class="string">&quot;SysLogininfor&quot;</span>&gt;</span></span><br><span class="line">    insert into sys_logininfor (user_name, status, ipaddr, login_location, browser, os, msg, login_time,user_id,dept_id)</span><br><span class="line">    values (#&#123;userName&#125;, #&#123;status&#125;, #&#123;ipaddr&#125;, #&#123;loginLocation&#125;, #&#123;browser&#125;, #&#123;os&#125;, #&#123;msg&#125;, sysdate(),#&#123;userId&#125;,#&#123;deptId&#125;)</span><br><span class="line"><span class="tag">&lt;/<span class="name">insert</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">&quot;selectLogininforList&quot;</span> <span class="attr">parameterType</span>=<span class="string">&quot;SysLogininfor&quot;</span> <span class="attr">resultMap</span>=<span class="string">&quot;SysLogininforResult&quot;</span>&gt;</span></span><br><span class="line">    select l.info_id, l.user_name, l.ipaddr, l.login_location, l.browser, l.os, l.status, l.msg, l.login_time</span><br><span class="line">    , u.user_id, d.dept_id</span><br><span class="line">    from sys_logininfor l</span><br><span class="line">    left join sys_dept d on l.dept_id = d.dept_id</span><br><span class="line">    left join sys_user u on l.user_id = u.user_id</span><br><span class="line">    <span class="tag">&lt;<span class="name">where</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">if</span> <span class="attr">test</span>=<span class="string">&quot;ipaddr != null and ipaddr != &#x27;&#x27;&quot;</span>&gt;</span></span><br><span class="line">            AND ipaddr like concat(&#x27;%&#x27;, #&#123;ipaddr&#125;, &#x27;%&#x27;)</span><br><span class="line">        <span class="tag">&lt;/<span class="name">if</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">if</span> <span class="attr">test</span>=<span class="string">&quot;status != null and status != &#x27;&#x27;&quot;</span>&gt;</span></span><br><span class="line">            AND status = #&#123;status&#125;</span><br><span class="line">        <span class="tag">&lt;/<span class="name">if</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">if</span> <span class="attr">test</span>=<span class="string">&quot;userName != null and userName != &#x27;&#x27;&quot;</span>&gt;</span></span><br><span class="line">            AND user_name like concat(&#x27;%&#x27;, #&#123;userName&#125;, &#x27;%&#x27;)</span><br><span class="line">        <span class="tag">&lt;/<span class="name">if</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">if</span> <span class="attr">test</span>=<span class="string">&quot;params.beginTime != null and params.beginTime != &#x27;&#x27;&quot;</span>&gt;</span><span class="comment">&lt;!-- 开始时间检索 --&gt;</span></span><br><span class="line">            AND login_time <span class="symbol">&amp;gt;</span>= #&#123;params.beginTime&#125;</span><br><span class="line">        <span class="tag">&lt;/<span class="name">if</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">if</span> <span class="attr">test</span>=<span class="string">&quot;params.endTime != null and params.endTime != &#x27;&#x27;&quot;</span>&gt;</span><span class="comment">&lt;!-- 结束时间检索 --&gt;</span></span><br><span class="line">            AND login_time <span class="symbol">&amp;lt;</span>= #&#123;params.endTime&#125;</span><br><span class="line">        <span class="tag">&lt;/<span class="name">if</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!-- 数据范围过滤 --&gt;</span></span><br><span class="line">        $&#123;params.dataScope&#125;</span><br><span class="line">    <span class="tag">&lt;/<span class="name">where</span>&gt;</span></span><br><span class="line">    order by info_id desc</span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br></pre></td></tr></table></figure>



<h3 id="异步工厂调整"><a href="#异步工厂调整" class="headerlink" title="异步工厂调整"></a>异步工厂调整</h3><p>在<code>dkd-framework</code>模块的<code>com.dkd.framework.manager.factory.AsyncFactory</code>异步工厂创建登录日志任务时，需要有<code>deptId</code>和<code>userId</code>这两个属性。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 异步工厂（产生任务用）</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> ruoyi</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">AsyncFactory</span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 记录用户登录信息的定时任务</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> TimerTask <span class="title function_">recordLogininfor</span><span class="params">(<span class="keyword">final</span> String username, <span class="keyword">final</span> String status,</span></span><br><span class="line"><span class="params">                                             <span class="keyword">final</span> String message,</span></span><br><span class="line"><span class="params">                                             <span class="keyword">final</span> Object... args)</span>&#123;</span><br><span class="line">        <span class="comment">// 其他代码略...</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 设置登录用户id和部门id</span></span><br><span class="line">        <span class="type">SysUser</span> <span class="variable">sysUser</span> <span class="operator">=</span> SpringUtils.getBean(ISysUserService.class).selectUserByUserName(username);</span><br><span class="line">        <span class="keyword">if</span> (StringUtils.isNotNull(sysUser))</span><br><span class="line">        &#123;</span><br><span class="line">            logininfor.setUserId(sysUser.getUserId());</span><br><span class="line">            logininfor.setDeptId(sysUser.getDeptId());</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 插入数据</span></span><br><span class="line">        SpringUtils.getBean(ISysLogininforService.class).insertLogininfor(logininfor);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
  </entry>
  <entry>
    <title>注解大全</title>
    <url>/2025/01/22/%E6%B3%A8%E8%A7%A3%E5%A4%A7%E5%85%A8/</url>
    <content><![CDATA[<h1 id="Spring-注解"><a href="#Spring-注解" class="headerlink" title="Spring 注解"></a>Spring 注解</h1><h2 id="和-DI-相关的注解"><a href="#和-DI-相关的注解" class="headerlink" title="和 DI 相关的注解"></a>和 DI 相关的注解</h2><h3 id="Autowired"><a href="#Autowired" class="headerlink" title="@Autowired"></a>@Autowired</h3><p>可以在构造函数、Setter 方法或字段注入中使用 @Autowired 注解，Spring 会解析并注入依赖。<br>构造函数注入：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Car</span> &#123;</span><br><span class="line">    Engine engine;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    Car(Engine engine) &#123;</span><br><span class="line">        <span class="built_in">this</span>.engine = engine;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Setter 注入:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Car</span> &#123;</span><br><span class="line">    Engine engine;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">setEngine</span><span class="params">(Engine engine)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.engine = engine;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>字段注入：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Car</span> &#123;</span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    Engine engine;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>@Autowired 有一个名为 required 的 boolean 参数，默认值为 true。当 Spring 找不到合适的 Bean 进行注入时，它会控制 Spring 的行为。当值为 true 时，会抛出异常，反之则不会。<br>注意，如果使用构造函数注入，所有构造函数参数都是 必须的 。<br>从 Spring 4.3 开始，除非声明了至少两个构造函数，否则不需要用 @Autowired 明确注解构造函数。</p>
<span id="more"></span>

<h3 id="Bean"><a href="#Bean" class="headerlink" title="@Bean"></a>@Bean</h3><p>@Bean 用于标记工厂方法，用于实例化 Spring Bean：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Bean</span></span><br><span class="line">Engine <span class="title function_">engine</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Engine</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>生成的 Bean 的名称与工厂方法的名称相同。如果想使用不同的名称，可以使用此注解的 name 或 value 参数（value 是 name 的别名）：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Bean(&quot;engine&quot;)</span></span><br><span class="line">Engine <span class="title function_">getEngine</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Engine</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>注意，所有注解为 @Bean 的方法必须位于 @Configuration 类中。</p>
<h3 id="Qualifier"><a href="#Qualifier" class="headerlink" title="@Qualifier"></a>@Qualifier</h3><p>在模糊情况下，使用 @Qualifier 注解与 @Autowired 注解一起，提供想要使用的 Bean 的 ID 或名称。</p>
<p>以下两个 Bean 实现了相同的接口：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Bike</span> <span class="keyword">implements</span> <span class="title class_">Vehicle</span> &#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Car</span> <span class="keyword">implements</span> <span class="title class_">Vehicle</span> &#123;&#125;</span><br></pre></td></tr></table></figure>

<p>如果 Spring 需要注入一个 Vehicle Bean，最终会出现多个匹配的定义。在这种情况下，可以使用 @Qualifier 注解显式地提供 Bean 的名称。</p>
<p>构造函数注入：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Autowired</span></span><br><span class="line">Biker(<span class="meta">@Qualifier(&quot;bike&quot;)</span> Vehicle vehicle) &#123;</span><br><span class="line">    <span class="built_in">this</span>.vehicle = vehicle;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Setter 注入：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Autowired</span></span><br><span class="line"><span class="keyword">void</span> <span class="title function_">setVehicle</span><span class="params">(<span class="meta">@Qualifier(&quot;bike&quot;)</span> Vehicle vehicle)</span> &#123;</span><br><span class="line">    <span class="built_in">this</span>.vehicle = vehicle;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>或者：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Autowired</span></span><br><span class="line"><span class="meta">@Qualifier(&quot;bike&quot;)</span></span><br><span class="line"><span class="keyword">void</span> <span class="title function_">setVehicle</span><span class="params">(Vehicle vehicle)</span> &#123;</span><br><span class="line">    <span class="built_in">this</span>.vehicle = vehicle;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>字段注入：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Autowired</span></span><br><span class="line"><span class="meta">@Qualifier(&quot;bike&quot;)</span></span><br><span class="line">Vehicle vehicle;</span><br></pre></td></tr></table></figure>

<h3 id="Required"><a href="#Required" class="headerlink" title="@Required"></a>@Required</h3><p>@Required 用于 Setter 方法，以标记我们希望通过 XML 填充的依赖：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Required</span></span><br><span class="line"><span class="keyword">void</span> <span class="title function_">setColor</span><span class="params">(String color)</span> &#123;</span><br><span class="line">    <span class="built_in">this</span>.color = color;</span><br><span class="line">&#125;</span><br><span class="line">&lt;bean class=<span class="string">&quot;com.baeldung.annotations.Bike&quot;</span>&gt;</span><br><span class="line">    &lt;property name=<span class="string">&quot;color&quot;</span> value=<span class="string">&quot;green&quot;</span> /&gt;</span><br><span class="line">&lt;/bean&gt;</span><br></pre></td></tr></table></figure>

<p>否则，将抛出 BeanInitializationException 异常。</p>
<h3 id="Value"><a href="#Value" class="headerlink" title="@Value"></a>@Value</h3><p>可以使用 @Value 向 Bean 注入属性值。它与构造函数、Setter 和字段注入兼容。</p>
<p>构造函数：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Engine(<span class="meta">@Value(&quot;8&quot;)</span> <span class="type">int</span> cylinderCount) &#123;</span><br><span class="line">    <span class="built_in">this</span>.cylinderCount = cylinderCount;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Setter 方法：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Autowired</span></span><br><span class="line"><span class="keyword">void</span> <span class="title function_">setCylinderCount</span><span class="params">(<span class="meta">@Value(&quot;8&quot;)</span> <span class="type">int</span> cylinderCount)</span> &#123;</span><br><span class="line">    <span class="built_in">this</span>.cylinderCount = cylinderCount;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>或者：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Value(&quot;8&quot;)</span></span><br><span class="line"><span class="keyword">void</span> <span class="title function_">setCylinderCount</span><span class="params">(<span class="type">int</span> cylinderCount)</span> &#123;</span><br><span class="line">    <span class="built_in">this</span>.cylinderCount = cylinderCount;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>字段：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Value(&quot;8&quot;)</span></span><br><span class="line"><span class="type">int</span> cylinderCount;</span><br></pre></td></tr></table></figure>

<p>注入静态值并不实用。因此，可以在 @Value 中使用占位符字符串来注入外部资源中定义的值，例如 .properties 或 .yaml 文件中的值。</p>
<p>假设有以下 .properties 文件：</p>
<p>engine.fuelType&#x3D;petrol<br>可以用下面的方法注入 engine.fuelType 的值：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Value(&quot;$&#123;engine.fuelType&#125;&quot;)</span></span><br><span class="line">String fuelType;</span><br></pre></td></tr></table></figure>

<p>甚至可以在 SpEL 中使用 @Value。</p>
<h3 id="DependsOn"><a href="#DependsOn" class="headerlink" title="@DependsOn"></a>@DependsOn</h3><p>可以使用此注解让 Spring 在注解的 Bean 之前初始化其他 Bean。通常情况下，这种行为是自动进行的，基于 Bean 之间的显式依赖关系。</p>
<p>只有当依赖关系是隐式的，例如 JDBC 驱动加载或静态变量初始化时，才需要此注解。</p>
<p>可以在依赖类上使用 @DependsOn 来指定依赖 Bean 的名称。注解的 value 参数需要一个包含依赖 Bean 名称的数组：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@DependsOn(&quot;engine&quot;)</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Car</span> <span class="keyword">implements</span> <span class="title class_">Vehicle</span> &#123;&#125;</span><br></pre></td></tr></table></figure>

<p>或者，如果我们使用 @Bean 注解定义 Bean，那么就应该在工厂方法上使用 @DependsOn 注解：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Bean</span></span><br><span class="line"><span class="meta">@DependsOn(&quot;fuel&quot;)</span></span><br><span class="line">Engine <span class="title function_">engine</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Engine</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="Lazy"><a href="#Lazy" class="headerlink" title="@Lazy"></a>@Lazy</h3><p>当我们希望延迟初始化 Bean 时，可以使用 @Lazy 注解。默认情况下，Spring 在 Application Context 启动时会立即创建所有 Singleton（单例）Bean。</p>
<p>不过，在某些情况下，我们需要在使用时创建 Bean，而不是在应用启动时。</p>
<p>这个注解会根据具体标注位置而有不同的表现，可以用在如下位置：</p>
<p>@Bean 注解的 Bean 工厂方法，可以延迟方法调用（从而延迟 Bean 的创建）。<br>@Configuration 类，包含的 @Bean 方法都将受到影响<br>@Component 类，而该组件类不是 @Configuration 类，则该 Bean 将被延迟地初始化<br>@Autowired 构造函数、Setter 方法或字段，用于延迟地（通过代理）加载依赖本身<br>该注解有一个名为 value 的参数，默认值为 true。它可以覆盖默认行为。</p>
<p>当全局设置为懒加载（lazy）时，可以标记要立即加载的 Bean，或者在使用 @Lazy 标记的 @Configuration 类中，配置特定的 @Bean 方法以进行立即加载：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="meta">@Lazy</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">VehicleFactoryConfig</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="meta">@Lazy(false)</span>  <span class="comment">// 立即加载</span></span><br><span class="line">    Engine <span class="title function_">engine</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Engine</span>();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="Lookup"><a href="#Lookup" class="headerlink" title="@Lookup"></a>@Lookup</h3><p>使用 @Lookup 注解的方法会告诉 Spring，在调用该方法时返回该方法返回类型的实例。</p>
<h3 id="Primary"><a href="#Primary" class="headerlink" title="@Primary"></a>@Primary</h3><p>有时，需要定义多个相同类型的 Bean。在这种情况下，注入将不会成功，因为 Spring 不知道需要注入哪个 Bean。</p>
<p>上文已经介绍了处理这种情况的方法：用 @Qualifier 标记所有注入点，并指定所需的 Bean 名称。</p>
<p>可以使用 @Primary 注解来简化这种情况：将最常用的 Bean 标记为 @Primary，它将在未标记 @Qualifier 的注入点上被选中。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="meta">@Primary</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Car</span> <span class="keyword">implements</span> <span class="title class_">Vehicle</span> &#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Bike</span> <span class="keyword">implements</span> <span class="title class_">Vehicle</span> &#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Driver</span> &#123;</span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    Vehicle vehicle;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Biker</span> &#123;</span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="meta">@Qualifier(&quot;bike&quot;)</span></span><br><span class="line">    Vehicle vehicle;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在前面的示例中，Car 注解了 @Primary。因此，在 Driver 类中，Spring 注入了一个 Car Bean。当然，在 Biker Bean 中，vehicle 字段的值将是 Bike 对象，因为它注解了 @Qualifier 并且指定了 Bean 名称。</p>
<h3 id="Scope"><a href="#Scope" class="headerlink" title="@Scope"></a>@Scope</h3><p>使用 @Scope 来定义 @Component 类或 @Bean 的 Scope（作用域），它可以是 singleton、prototype、request、session、globalSession 或某些自定义作用域。</p>
<p>例如：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="meta">@Scope(&quot;prototype&quot;)</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Engine</span> &#123;&#125;</span><br></pre></td></tr></table></figure>

<h2 id="Context-配置注解"><a href="#Context-配置注解" class="headerlink" title="Context 配置注解"></a>Context 配置注解</h2><h3 id="Profile"><a href="#Profile" class="headerlink" title="@Profile"></a>@Profile</h3><p>如果我们希望 Spring 仅在特定 Profile（配置文件）处于活动状态时才启用 @Component 类或 @Bean 方法，可以使用 @Profile 对其进行标记。<br>使用注解的 value 参数配置 Profile 的名称：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="meta">@Profile(&quot;sportDay&quot;)</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Bike</span> <span class="keyword">implements</span> <span class="title class_">Vehicle</span> &#123;&#125;</span><br></pre></td></tr></table></figure>

<h3 id="Import"><a href="#Import" class="headerlink" title="@Import"></a>@Import</h3><p>可以使用该注解来使用特定的 @Configuration 类，而无需进行组件扫描。<br>通过 @Import 注解的 value 参数来提供这些类：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Import(VehiclePartSupplier.class)</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">VehicleFactoryConfig</span> &#123;&#125;</span><br></pre></td></tr></table></figure>

<h3 id="ImportResource"><a href="#ImportResource" class="headerlink" title="@ImportResource"></a>@ImportResource</h3><p>可以使用该注解导入 XML 配置。用 locations 参数或其别名 value 参数指定 XML 文件的位置：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="meta">@ImportResource(&quot;classpath:/annotations.xml&quot;)</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">VehicleFactoryConfig</span> &#123;&#125;</span><br></pre></td></tr></table></figure>

<h3 id="PropertySource"><a href="#PropertySource" class="headerlink" title="@PropertySource"></a>@PropertySource</h3><p>通过该注解，可以为应用设置定义属性文件：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="meta">@PropertySource(&quot;classpath:/annotations.properties&quot;)</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">VehicleFactoryConfig</span> &#123;&#125;</span><br><span class="line"><span class="meta">@PropertySource</span> 利用了 Java <span class="number">8</span> 的重复注解功能，这意味着可以用它多次标记一个类：</span><br><span class="line"></span><br><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="meta">@PropertySource(&quot;classpath:/annotations.properties&quot;)</span></span><br><span class="line"><span class="meta">@PropertySource(&quot;classpath:/vehicle-factory.properties&quot;)</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">VehicleFactoryConfig</span> &#123;&#125;</span><br></pre></td></tr></table></figure>

<h3 id="PropertySources"><a href="#PropertySources" class="headerlink" title="@PropertySources"></a>@PropertySources</h3><p>可以使用此注解指定多个 @PropertySource 配置：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="meta">@PropertySources(&#123; </span></span><br><span class="line"><span class="meta">    @PropertySource(&quot;classpath:/annotations.properties&quot;),</span></span><br><span class="line"><span class="meta">    @PropertySource(&quot;classpath:/vehicle-factory.properties&quot;)</span></span><br><span class="line"><span class="meta">&#125;)</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">VehicleFactoryConfig</span> &#123;&#125;</span><br></pre></td></tr></table></figure>

<p>注意，自 Java 8 以来，可以通过上述重复注解的特性来实现同样的功能。</p>
<h1 id="Spring-MVC-注解"><a href="#Spring-MVC-注解" class="headerlink" title="Spring MVC 注解"></a>Spring MVC 注解</h1><h2 id="Controller"><a href="#Controller" class="headerlink" title="@Controller"></a>@Controller</h2><p>@Controller 注解用于标识一个类为Spring MVC控制器，负责处理HTTP请求并返回视图。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Controller</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MyController</span> &#123;</span><br><span class="line">    <span class="comment">// 控制器方法定义...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="RestController"><a href="#RestController" class="headerlink" title="@RestController"></a>@RestController</h2><p>@RestController 注解是@Controller和@ResponseBody的组合，用于RESTful Web服务的控制器。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MyRestController</span> &#123;</span><br><span class="line">    <span class="comment">// 控制器中的方法将直接返回响应体。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="RequestMapping"><a href="#RequestMapping" class="headerlink" title="@RequestMapping"></a>@RequestMapping</h2><p>@RequestMapping 注解用于将HTTP请求映射到控制器的处理方法上。<br>注解属性介绍:</p>
<ul>
<li>value: 请求的URL路径。</li>
<li>method: 支持的HTTP方法。</li>
<li>params: 请求参数条件。</li>
<li>headers: 请求头条件。</li>
</ul>
<p>当value设置为”&#x2F;users&#x2F;{id}”，method为RequestMethod.GET，params为”active&#x3D;true”，headers为”Accept&#x3D;application&#x2F;json”时：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">GET /users/<span class="number">123</span>?active=<span class="literal">true</span></span><br><span class="line">Accept: application/json</span><br><span class="line"></span><br><span class="line"><span class="meta">@RequestMapping(value = &quot;/users/&#123;id&#125;&quot;, method = RequestMethod.GET, params = &quot;active=true&quot;, headers = &quot;Accept=application/json&quot;)</span></span><br><span class="line"><span class="keyword">public</span> ResponseEntity&lt;List&lt;User&gt;&gt; <span class="title function_">listActiveUsers</span><span class="params">(<span class="meta">@PathVariable</span> <span class="type">int</span> id)</span> &#123;</span><br><span class="line"><span class="comment">// 根据用户ID获取激活用户的列表。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="GetMapping"><a href="#GetMapping" class="headerlink" title="@GetMapping"></a>@GetMapping</h2><p>@GetMapping 注解用于映射GET请求到控制器的方法上。<br>注解属性介绍:</p>
<ul>
<li>value: 请求的URL路径。</li>
</ul>
<p>当value设置为”&#x2F;users&#x2F;{id}”时，请求&#x2F;users&#x2F;123将映射到方法：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">GET /users/<span class="number">123</span></span><br><span class="line"></span><br><span class="line"><span class="meta">@GetMapping(&quot;/users/&#123;id&#125;&quot;)</span></span><br><span class="line"><span class="keyword">public</span> User <span class="title function_">getUser</span><span class="params">(<span class="meta">@PathVariable</span> <span class="type">int</span> id)</span> &#123;</span><br><span class="line"><span class="comment">// 根据路径变量&#123;id&#125;获取用户信息。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="PostMapping"><a href="#PostMapping" class="headerlink" title="@PostMapping"></a>@PostMapping</h2><p>@PostMapping 注解用于映射POST请求到控制器的方法上。<br>注解属性介绍:</p>
<ul>
<li>value: 请求的URL路径。<br>当value设置为”&#x2F;users”时，带有JSON请求体的POST请求将映射到方法：</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">POST /users</span><br><span class="line">Content-Type: application/json</span><br><span class="line">&#123;</span><br><span class="line">  <span class="string">&quot;name&quot;</span>: <span class="string">&quot;John Doe&quot;</span>,</span><br><span class="line">  <span class="string">&quot;age&quot;</span>: <span class="number">30</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@PostMapping(&quot;/users&quot;)</span></span><br><span class="line"><span class="keyword">public</span> User <span class="title function_">createUser</span><span class="params">(<span class="meta">@RequestBody</span> User user)</span> &#123;</span><br><span class="line"><span class="comment">// 使用JSON请求体中的数据创建新用户。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="PutMapping"><a href="#PutMapping" class="headerlink" title="@PutMapping"></a>@PutMapping</h2><p>@PutMapping 注解用于映射HTTP PUT请求到控制器的方法上，通常用于更新资源。</p>
<p>注解属性介绍</p>
<ul>
<li>value: 请求的URL路径。<br>当value设置为”&#x2F;users&#x2F;{id}”时，请求将映射到更新用户的方法：</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"></span><br><span class="line">PUT /users/<span class="number">123</span></span><br><span class="line">Content-Type: application/json</span><br><span class="line"></span><br><span class="line">&#123;</span><br><span class="line">  <span class="string">&quot;name&quot;</span>: <span class="string">&quot;Jane Doe&quot;</span>,</span><br><span class="line">  <span class="string">&quot;age&quot;</span>: <span class="number">25</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">@PutMapping(&quot;/users/&#123;id&#125;&quot;)</span></span><br><span class="line"><span class="keyword">public</span> User <span class="title function_">updateUser</span><span class="params">(<span class="meta">@PathVariable</span> <span class="type">int</span> id, <span class="meta">@RequestBody</span> User userDetails)</span> &#123;</span><br><span class="line"><span class="comment">// 使用请求体中的数据更新指定ID的用户信息。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="DeleteMapping"><a href="#DeleteMapping" class="headerlink" title="@DeleteMapping"></a>@DeleteMapping</h2><p>@DeleteMapping 注解用于映射HTTP DELETE请求到控制器的方法上，用于删除资源。<br>注解属性介绍</p>
<ul>
<li>value: 请求的URL路径。</li>
</ul>
<p>当value设置为”&#x2F;users&#x2F;{id}”时，请求将映射到删除用户的方法：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">DELETE /users/<span class="number">123</span></span><br><span class="line"></span><br><span class="line"><span class="meta">@DeleteMapping(&quot;/users/&#123;id&#125;&quot;)</span></span><br><span class="line"><span class="keyword">public</span> ResponseEntity&lt;?&gt; deleteUser(<span class="meta">@PathVariable</span> <span class="type">int</span> id) &#123;</span><br><span class="line"><span class="comment">// 根据用户ID删除用户。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="PatchMapping"><a href="#PatchMapping" class="headerlink" title="@PatchMapping"></a>@PatchMapping</h2><p>@PatchMapping 注解用于映射HTTP PATCH请求到控制器的方法上，通常用于部分更新资源。</p>
<p>注解属性介绍</p>
<ul>
<li>value: 请求的URL路径。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"></span><br><span class="line">当value设置为<span class="string">&quot;/users/&#123;id&#125;&quot;</span>时，请求将映射到部分更新用户的方法：</span><br><span class="line"></span><br><span class="line">PATCH /users/<span class="number">123</span></span><br><span class="line">Content-Type: application/json</span><br><span class="line"></span><br><span class="line">&#123;</span><br><span class="line">  <span class="string">&quot;name&quot;</span>: <span class="string">&quot;Jane Doe&quot;</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@PatchMapping(&quot;/users/&#123;id&#125;&quot;)</span></span><br><span class="line"><span class="keyword">public</span> User <span class="title function_">patchUser</span><span class="params">(<span class="meta">@PathVariable</span> <span class="type">int</span> id, <span class="meta">@RequestBody</span> Map&lt;String, Object&gt; updates)</span> &#123;</span><br><span class="line"><span class="comment">// 使用请求体中的部分数据更新指定ID的用户信息。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="RequestParam"><a href="#RequestParam" class="headerlink" title="@RequestParam"></a>@RequestParam</h2><p>@RequestParam 注解用于将请求参数绑定到控制器方法的参数上。<br>注解属性介绍</p>
<ul>
<li>value: 请求参数的名称。</li>
<li>required: 指示参数是否必须。</li>
<li>defaultValue: 参数的默认值。</li>
</ul>
<p>当请求包含查询参数”page”和”size”时：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">GET /items?page=<span class="number">1</span>&amp;size=<span class="number">10</span></span><br><span class="line"></span><br><span class="line"><span class="meta">@GetMapping(&quot;/items&quot;)</span></span><br><span class="line"><span class="keyword">public</span> List&lt;Item&gt; <span class="title function_">listItems</span><span class="params">(</span></span><br><span class="line"><span class="params"><span class="meta">@RequestParam(value = &quot;page&quot;, defaultValue = &quot;1&quot;)</span> <span class="type">int</span> page,</span></span><br><span class="line"><span class="params"><span class="meta">@RequestParam(value = &quot;size&quot;, defaultValue = &quot;10&quot;)</span> <span class="type">int</span> size</span></span><br><span class="line"><span class="params">)</span> &#123;</span><br><span class="line"><span class="comment">// 根据请求的页码和大小返回项目列表。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="PathVariable"><a href="#PathVariable" class="headerlink" title="@PathVariable"></a>@PathVariable</h2><p>@PathVariable 注解用于将URL中的模板变量绑定到控制器方法的参数上。</p>
<p>注解属性介绍</p>
<ul>
<li>value: 模板变量的名称。</li>
<li>required: 指示变量是否必须。</li>
</ul>
<p>当URL模板包含”{id}”时，请求&#x2F;users&#x2F;123将映射到方法：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">GET /users/<span class="number">123</span></span><br><span class="line"></span><br><span class="line"><span class="meta">@GetMapping(&quot;/users/&#123;id&#125;&quot;)</span></span><br><span class="line"><span class="keyword">public</span> User <span class="title function_">getUserById</span><span class="params">(<span class="meta">@PathVariable</span> <span class="type">int</span> id)</span> &#123;</span><br><span class="line"><span class="comment">// 根据URL路径变量&#123;id&#125;获取用户信息。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="RequestBody"><a href="#RequestBody" class="headerlink" title="@RequestBody"></a>@RequestBody</h2><p>@RequestBody 注解用于将请求体绑定到控制器方法的参数上。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">POST /users</span><br><span class="line">Content-Type: application/json</span><br><span class="line">&#123;</span><br><span class="line">  <span class="string">&quot;name&quot;</span>: <span class="string">&quot;John Doe&quot;</span>,</span><br><span class="line">  <span class="string">&quot;age&quot;</span>: <span class="number">30</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@PostMapping(&quot;/users&quot;)</span></span><br><span class="line"><span class="keyword">public</span> User <span class="title function_">createUser</span><span class="params">(<span class="meta">@RequestBody</span> User user)</span> &#123;</span><br><span class="line"><span class="comment">// 使用请求体中的数据创建新用户。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="RequestHeader"><a href="#RequestHeader" class="headerlink" title="@RequestHeader"></a>@RequestHeader</h2><p>@RequestHeader 注解用于将请求头绑定到控制器方法的参数上。</p>
<p>注解属性介绍</p>
<ul>
<li>value: 请求头的名称。</li>
<li>required: 指示请求头是否必须。</li>
</ul>
<p>请求包含自定义请求头”X-Request-ID”：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">GET /users</span><br><span class="line">X-Request-ID: <span class="number">123456789</span></span><br><span class="line"> 注解业务案例</span><br><span class="line"></span><br><span class="line"><span class="meta">@GetMapping(&quot;/users&quot;)</span></span><br><span class="line"><span class="keyword">public</span> List&lt;User&gt; <span class="title function_">listUsers</span><span class="params">(<span class="meta">@RequestHeader(value = &quot;X-Request-ID&quot;, required = false)</span> String requestId)</span> &#123;</span><br><span class="line"><span class="comment">// 根据请求头&quot;X-Request-ID&quot;记录日志或进行追踪。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="CookieValue"><a href="#CookieValue" class="headerlink" title="@CookieValue"></a>@CookieValue</h2><p>@CookieValue 注解用于将请求中Cookie的值绑定到控制器方法的参数上。</p>
<p>注解属性介绍</p>
<ul>
<li>value: Cookie的名称。</li>
<li>required: 指示Cookie是否必须。</li>
</ul>
<p>请求包含名为”sessionId”的Cookie：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">GET /users</span><br><span class="line">Cookie: sessionId=abc123def456</span><br><span class="line"> 注解业务案例</span><br><span class="line"></span><br><span class="line"><span class="meta">@GetMapping(&quot;/users&quot;)</span></span><br><span class="line"><span class="keyword">public</span> List&lt;User&gt; <span class="title function_">listUsers</span><span class="params">(<span class="meta">@CookieValue(value = &quot;sessionId&quot;, required = false)</span> String sessionId)</span> &#123;</span><br><span class="line"><span class="comment">// 根据Cookie中的&quot;sessionId&quot;进行会话管理。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="ModelAttribute"><a href="#ModelAttribute" class="headerlink" title="@ModelAttribute"></a>@ModelAttribute</h2><p>@ModelAttribute 注解用于将请求参数或表单数据绑定到模型属性上。<br>注解属性介绍</p>
<ul>
<li>value: 模型属性的名称。</li>
<li>required: 指示模型属性是否必须。</li>
</ul>
<p>一个包含用户信息的POST请求：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">POST /users</span><br><span class="line">Content-Type: application/x-www-form-urlencoded</span><br><span class="line"></span><br><span class="line">name=John+Doe&amp;age=<span class="number">30</span></span><br><span class="line"> 注解业务案例</span><br><span class="line"></span><br><span class="line"><span class="meta">@PostMapping(&quot;/users&quot;)</span></span><br><span class="line"><span class="keyword">public</span> String <span class="title function_">addUser</span><span class="params">(Model model, <span class="meta">@ModelAttribute</span> User user)</span> &#123;</span><br><span class="line">model.addAttribute(<span class="string">&quot;user&quot;</span>, user);</span><br><span class="line"><span class="comment">// 将用户信息添加到模型中，以便在视图中展示。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="SessionAttribute"><a href="#SessionAttribute" class="headerlink" title="@SessionAttribute"></a>@SessionAttribute</h2><p>@SessionAttribute 注解用于将模型属性存储到HTTP会话中。<br>注解属性介绍</p>
<ul>
<li>name: 会话属性的名称。</li>
<li>value: 会话属性的值。</li>
</ul>
<p>将用户信息存储在会话中，以便跨请求访问：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">GET /user/profile</span><br><span class="line">Cookie: JSESSIONID=abc123; user=John Doe</span><br><span class="line"> 注解业务案例</span><br><span class="line"></span><br><span class="line"><span class="meta">@SessionAttribute(name = &quot;user&quot;)</span></span><br><span class="line"><span class="keyword">public</span> String <span class="title function_">showProfile</span><span class="params">(Model model)</span> &#123;</span><br><span class="line"><span class="type">String</span> <span class="variable">user</span> <span class="operator">=</span> (String) model.asMap().get(<span class="string">&quot;user&quot;</span>);</span><br><span class="line"><span class="comment">// 从会话中获取用户信息。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="RequestAttribute"><a href="#RequestAttribute" class="headerlink" title="@RequestAttribute"></a>@RequestAttribute</h2><p>@RequestAttribute 注解用于将请求属性绑定到控制器方法的参数上。<br>注解属性介绍</p>
<ul>
<li>value: 请求属性的名称。</li>
</ul>
<p>从请求转发中携带属性：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">POST /login</span><br><span class="line">...</span><br><span class="line"></span><br><span class="line">Redirect /user/profile?role=admin</span><br><span class="line"> 注解业务案例</span><br><span class="line"></span><br><span class="line"><span class="meta">@GetMapping(&quot;/user/profile&quot;)</span></span><br><span class="line"><span class="keyword">public</span> String <span class="title function_">showProfile</span><span class="params">(<span class="meta">@RequestAttribute(&quot;role&quot;)</span> String role)</span> &#123;</span><br><span class="line"><span class="comment">// 根据请求属性&quot;role&quot;展示不同的用户信息。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="ResponseStatus"><a href="#ResponseStatus" class="headerlink" title="@ResponseStatus"></a>@ResponseStatus</h2><p>@ResponseStatus 注解用于指定HTTP响应的status。<br>注解属性介绍</p>
<ul>
<li>value: 指定的HTTP状态。</li>
</ul>
<p>当资源未找到时返回404状态：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">GET /users/<span class="number">999</span></span><br><span class="line">HTTP/<span class="number">1.1</span> <span class="number">404</span> Not Found</span><br><span class="line"> 注解业务案例</span><br><span class="line"></span><br><span class="line"><span class="meta">@ControllerAdvice</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">GlobalExceptionHandler</span> &#123;</span><br><span class="line">    <span class="meta">@ExceptionHandler(ResourceNotFoundException.class)</span></span><br><span class="line">    <span class="meta">@ResponseStatus(HttpStatus.NOT_FOUND)</span></span><br><span class="line">    <span class="keyword">public</span> ResponseEntity&lt;Object&gt; <span class="title function_">handleResourceNotFound</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="comment">// 处理资源未找到的异常。</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="ExceptionHandler"><a href="#ExceptionHandler" class="headerlink" title="@ExceptionHandler"></a>@ExceptionHandler</h2><p>@ExceptionHandler 注解用于处理控制器中抛出的特定异常。<br>注解属性介绍</p>
<ul>
<li>value: 指定异常的类型。</li>
</ul>
<p>捕获并处理自定义的业务异常：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">POST /users</span><br><span class="line">...</span><br><span class="line">HTTP/<span class="number">1.1</span> <span class="number">400</span> Bad Request</span><br><span class="line"> 注解业务案例</span><br><span class="line"></span><br><span class="line"><span class="meta">@ControllerAdvice</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">GlobalExceptionHandler</span> &#123;</span><br><span class="line"><span class="meta">@ExceptionHandler(BusinessException.class)</span></span><br><span class="line"><span class="keyword">public</span> ResponseEntity&lt;Object&gt; <span class="title function_">handleBusinessException</span><span class="params">(BusinessException ex)</span> &#123;</span><br><span class="line">    <span class="comment">// 处理业务异常。</span></span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="CrossOrigin"><a href="#CrossOrigin" class="headerlink" title="@CrossOrigin"></a>@CrossOrigin</h2><p>@CrossOrigin 注解用于启用跨源请求。</p>
<p>注解属性介绍</p>
<ul>
<li>origins: 允许的来源。</li>
<li>methods: 允许的HTTP方法。</li>
<li>allowedHeaders: 允许的请求头。</li>
</ul>
<p>允许特定域名进行跨域GET请求：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">OPTIONS /api/data</span><br><span class="line">Access-Control-Request-Method: GET</span><br><span class="line">Origin: &lt;http:<span class="comment">//example.com&gt;</span></span><br><span class="line"> 注解业务案例</span><br><span class="line"></span><br><span class="line"><span class="meta">@CrossOrigin(origins = &quot;&lt;http://example.com&gt;&quot;, methods = &#123;RequestMethod.GET&#125;)</span></span><br><span class="line"><span class="meta">@GetMapping(&quot;/api/data&quot;)</span></span><br><span class="line"><span class="keyword">public</span> List&lt;Data&gt; <span class="title function_">getAllData</span><span class="params">()</span> &#123;</span><br><span class="line"><span class="comment">// 返回数据列表，允许跨域GET请求。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="MatrixVariable"><a href="#MatrixVariable" class="headerlink" title="@MatrixVariable"></a>@MatrixVariable</h2><p>@MatrixVariable 注解用于从URL的矩阵变量中获取值。</p>
<p>注解属性介绍</p>
<ul>
<li>pathVar: 指定矩阵变量所在的路径变量名称。</li>
</ul>
<p>从URL中获取矩阵变量的值：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">GET /api/users/<span class="number">123</span>;role=admin</span><br><span class="line"> 注解业务案例</span><br><span class="line"></span><br><span class="line"><span class="meta">@GetMapping(&quot;/api/users/&#123;id&#125;&quot;)</span></span><br><span class="line"><span class="keyword">public</span> User <span class="title function_">getUser</span><span class="params">(<span class="meta">@MatrixVariable(name = &quot;role&quot;, pathVar = &quot;id&quot;)</span> String role)</span> &#123;</span><br><span class="line"><span class="comment">// 根据矩阵变量&quot;role&quot;获取用户角色。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="InitBinder"><a href="#InitBinder" class="headerlink" title="@InitBinder"></a>@InitBinder</h2><p>@InitBinder 注解用于在控制器中初始化WebDataBinder，通常用于注册自定义属性编辑器。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="meta">@Controller</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MyController</span> &#123;</span><br><span class="line">    <span class="meta">@InitBinder</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">initBinder</span><span class="params">(WebDataBinder binder)</span> &#123;</span><br><span class="line">        <span class="comment">// 注册自定义属性编辑器。</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="ResponseStatus-1"><a href="#ResponseStatus-1" class="headerlink" title="@ResponseStatus"></a>@ResponseStatus</h2><p>@ResponseStatus 注解用于设置响应的HTTP状态码。<br>注解属性介绍</p>
<ul>
<li>value: 指定的HTTP状态码。</li>
</ul>
<p>当发生特定异常时，返回HTTP 503 Service Unavailable状态码。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="meta">@ResponseStatus(value = HttpStatus.SERVICE_UNAVAILABLE)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ServiceUnavailableException</span> <span class="keyword">extends</span> <span class="title class_">RuntimeException</span> &#123;</span><br><span class="line">    <span class="comment">// 自定义异常类。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="Validated"><a href="#Validated" class="headerlink" title="@Validated"></a>@Validated</h2><p>@Validated 注解用于在类或方法级别指定验证组，增强验证逻辑。<br>注解属性介绍</p>
<ul>
<li>groups: 指定验证组。</li>
</ul>
<p>使用特定的验证组对用户注册信息进行验证。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="meta">@Validated(UserValidationGroup.class)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UserRegistrationController</span> &#123;</span><br><span class="line">    <span class="comment">// 该控制器中的方法将使用UserValidationGroup验证组。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="Validated-Annotation-with-Groups"><a href="#Validated-Annotation-with-Groups" class="headerlink" title="@Validated Annotation with Groups"></a>@Validated Annotation with Groups</h2><p>@Validated 注解与groups属性结合使用，允许在方法级别指定多个验证组。<br>注解属性介绍</p>
<ul>
<li>value: 指定的验证组数组。</li>
</ul>
<p>对更新操作使用不同的验证组。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="meta">@Validated(&#123;UpdateGroup.class, AnotherUpdateGroup.class&#125;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">updateEntity</span><span class="params">(<span class="meta">@Valid</span> <span class="meta">@NotNull</span> MyEntity entity)</span> &#123;</span><br><span class="line">    <span class="comment">// 使用多个验证组对实体进行验证。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="RequestBodyAdvice"><a href="#RequestBodyAdvice" class="headerlink" title="@RequestBodyAdvice"></a>@RequestBodyAdvice</h2><p>@RequestBodyAdvice 注解用于在读取请求正文之前或之后对请求正文进行处理。<br>注解属性介绍</p>
<ul>
<li>beforeBodyRead: 指定在读取请求正文之前的处理。</li>
<li>afterBodyRead: 指定在读取请求正文之后的处理。</li>
</ul>
<p>对请求正文进行日志记录或修改。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="meta">@ControllerAdvice</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MyRequestBodyAdvice</span> &#123;</span><br><span class="line">    <span class="meta">@RequestBodyAdvice(beforeBodyRead = &quot;logBeforeRead&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">logRequestBody</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="comment">// 在读取请求正文之前进行日志记录。</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="ControllerAdvice"><a href="#ControllerAdvice" class="headerlink" title="@ControllerAdvice"></a>@ControllerAdvice</h2><p>@ControllerAdvice 注解用于定义一个类，该类可以包含多个@ExceptionHandler、@InitBinder和@ModelAttribute方法。<br>注解属性介绍</p>
<ul>
<li>basePackages: 指定要扫描的包。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="meta">@ControllerAdvice</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MyGlobalControllerAdvice</span> &#123;</span><br><span class="line">    <span class="comment">// 包含全局异常处理、属性初始化和模型属性的方法。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="MatrixParam"><a href="#MatrixParam" class="headerlink" title="@MatrixParam"></a>@MatrixParam</h2><p>@MatrixParam 注解用于从URL的矩阵变量中获取值。<br>注解属性介绍</p>
<ul>
<li>name: 矩阵变量的名称。</li>
</ul>
<p>从URL中获取矩阵变量的值：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">GET /users;role=admin/<span class="number">123</span></span><br><span class="line"> 注解业务案例</span><br><span class="line"></span><br><span class="line"><span class="meta">@GetMapping(&quot;/users/&#123;id&#125;&quot;)</span></span><br><span class="line"><span class="keyword">public</span> User <span class="title function_">getUser</span><span class="params">(<span class="meta">@MatrixParam(name = &quot;role&quot;)</span> String role)</span> &#123;</span><br><span class="line"><span class="comment">// 根据矩阵变量&quot;role&quot;获取用户角色。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="SessionAttributes"><a href="#SessionAttributes" class="headerlink" title="@SessionAttributes"></a>@SessionAttributes</h2><p>@SessionAttributes 注解用于将模型属性添加到HTTP会话中，以便在多个请求之间保持它们的值。</p>
<p>注解属性介绍</p>
<ul>
<li>names: 要存储在会话中的属性名称数组。</li>
<li>types: 要存储在会话中的属性类型。</li>
</ul>
<p>将用户信息存储在会话中，以便在用户会话期间保持状态：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">GET /user/profile</span><br><span class="line">Cookie: JSESSIONID=abc123; user=John Doe</span><br><span class="line"> 注解业务案例</span><br><span class="line"></span><br><span class="line"><span class="meta">@Controller</span></span><br><span class="line"><span class="meta">@SessionAttributes(&quot;user&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UserController</span> &#123;</span><br><span class="line"><span class="comment">// 这个控制器中的模型属性&quot;user&quot;将被存储在会话中。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="RequestAttribute-1"><a href="#RequestAttribute-1" class="headerlink" title="@RequestAttribute"></a>@RequestAttribute</h2><p>@RequestAttribute 注解用于将请求属性绑定到控制器方法的参数上。<br>注解属性介绍</p>
<ul>
<li>value: 请求属性的名称。</li>
</ul>
<p>从请求中获取属性”searchQuery”：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">GET /search?searchQuery=spring</span><br><span class="line"> 注解业务案例</span><br><span class="line"></span><br><span class="line"><span class="meta">@GetMapping(&quot;/search&quot;)</span></span><br><span class="line"><span class="keyword">public</span> String <span class="title function_">search</span><span class="params">(<span class="meta">@RequestAttribute(&quot;searchQuery&quot;)</span> String query)</span> &#123;</span><br><span class="line"><span class="comment">// 使用请求属性&quot;searchQuery&quot;执行搜索。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="ModelAttribute-1"><a href="#ModelAttribute-1" class="headerlink" title="@ModelAttribute"></a>@ModelAttribute</h2><p>@ModelAttribute 注解用于将请求参数或会话属性绑定到模型对象上。<br>注解属性介绍</p>
<ul>
<li>value: 模型对象的名称。</li>
</ul>
<p>将表单提交的参数绑定到用户对象上：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">POST /user/register</span><br><span class="line">Content-Type: application/x-www-form-urlencoded</span><br><span class="line"></span><br><span class="line">name=John+Doe&amp;email=&lt;john.doe<span class="meta">@example</span>.com&gt;</span><br><span class="line"> 注解业务案例</span><br><span class="line"></span><br><span class="line"><span class="meta">@PostMapping(&quot;/user/register&quot;)</span></span><br><span class="line"><span class="keyword">public</span> String <span class="title function_">registerUser</span><span class="params">(<span class="meta">@ModelAttribute(&quot;user&quot;)</span> User user)</span> &#123;</span><br><span class="line"><span class="comment">// 使用模型中的&quot;user&quot;对象注册新用户。</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h2 id="CookieValue-1"><a href="#CookieValue-1" class="headerlink" title="@CookieValue"></a>@CookieValue</h2><p>@CookieValue 注解用于将请求中的Cookie值绑定到控制器方法的参数上。<br>注解属性介绍</p>
<ul>
<li>value: Cookie的名称。</li>
</ul>
<p>从请求中获取名为”sessionId”的Cookie值：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">GET /user/profile</span><br><span class="line">Cookie: sessionId=abc123</span><br><span class="line"> 注解业务案例</span><br><span class="line"></span><br><span class="line"><span class="meta">@GetMapping(&quot;/user/profile&quot;)</span></span><br><span class="line"><span class="keyword">public</span> String <span class="title function_">showProfile</span><span class="params">(<span class="meta">@CookieValue(&quot;sessionId&quot;)</span> String sessionId)</span> &#123;</span><br><span class="line"><span class="comment">// 使用Cookie中的&quot;sessionId&quot;进行会话管理。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="RequestBody-1"><a href="#RequestBody-1" class="headerlink" title="@RequestBody"></a>@RequestBody</h2><p>@RequestBody 注解用于将请求正文绑定到控制器方法的参数上。</p>
<p>接收JSON格式的请求正文：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">POST /api/data</span><br><span class="line">Content-Type: application/json</span><br><span class="line"></span><br><span class="line">&#123;</span><br><span class="line">  <span class="string">&quot;key&quot;</span>: <span class="string">&quot;value&quot;</span></span><br><span class="line">&#125;</span><br><span class="line"> 注解业务案例</span><br><span class="line"></span><br><span class="line"><span class="meta">@PostMapping(&quot;/api/data&quot;)</span></span><br><span class="line"><span class="keyword">public</span> DataItem <span class="title function_">createDataItem</span><span class="params">(<span class="meta">@RequestBody</span> DataItem data)</span> &#123;</span><br><span class="line"><span class="comment">// 使用请求正文中的数据创建新的数据项。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="ResponseBody"><a href="#ResponseBody" class="headerlink" title="@ResponseBody"></a>@ResponseBody</h2><p>@ResponseBody 注解用于指示方法的返回值直接作为HTTP响应正文返回。</p>
<p>返回JSON格式的响应正文：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">GET /api/data/<span class="number">1</span></span><br><span class="line">Content-Type: application/json</span><br><span class="line"></span><br><span class="line">&#123;</span><br><span class="line">  <span class="string">&quot;id&quot;</span>: <span class="number">1</span>,</span><br><span class="line">  <span class="string">&quot;key&quot;</span>: <span class="string">&quot;value&quot;</span></span><br><span class="line">&#125;</span><br><span class="line"> 注解业务案例</span><br><span class="line"></span><br><span class="line"><span class="meta">@GetMapping(&quot;/api/data/&#123;id&#125;&quot;)</span></span><br><span class="line"><span class="meta">@ResponseBody</span></span><br><span class="line"><span class="keyword">public</span> DataItem <span class="title function_">getDataItem</span><span class="params">(<span class="meta">@PathVariable</span> <span class="type">int</span> id)</span> &#123;</span><br><span class="line"><span class="comment">// 直接将数据项作为JSON响应正文返回。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="ResponseStatus-2"><a href="#ResponseStatus-2" class="headerlink" title="@ResponseStatus"></a>@ResponseStatus</h2><p>@ResponseStatus 注解用于设置响应的HTTP状态码。<br>注解属性介绍</p>
<ul>
<li>value: 指定的HTTP状态码。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="meta">@ResponseStatus(HttpStatus.NOT_FOUND)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ResourceNotFoundException</span> <span class="keyword">extends</span> <span class="title class_">RuntimeException</span> &#123;</span><br><span class="line">    <span class="comment">// 自定义异常，用于当资源未找到时。</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h2 id="ExceptionHandler-1"><a href="#ExceptionHandler-1" class="headerlink" title="@ExceptionHandler"></a>@ExceptionHandler</h2><p>@ExceptionHandler 注解用于处理控制器抛出的特定异常。<br>注解属性介绍</p>
<ul>
<li>value: 指定异常的类型。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@ControllerAdvice</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">GlobalExceptionHandler</span> &#123;</span><br><span class="line">    <span class="meta">@ExceptionHandler(ResourceNotFoundException.class)</span></span><br><span class="line">    <span class="keyword">public</span> ResponseEntity&lt;String&gt; <span class="title function_">handleResourceNotFound</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="comment">// 处理资源未找到的异常。</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="ControllerAdvice-1"><a href="#ControllerAdvice-1" class="headerlink" title="@ControllerAdvice"></a>@ControllerAdvice</h2><p>@ControllerAdvice 注解用于定义一个类，该类可以包含多个@ExceptionHandler、@InitBinder和@ModelAttribute方法，用于全局异常处理和数据绑定。<br>注解属性介绍</p>
<ul>
<li>basePackages: 指定要扫描的包。</li>
</ul>
<p>注解业务案例</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="meta">@ControllerAdvice</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">GlobalControllerAdvice</span> &#123;</span><br><span class="line">    <span class="comment">// 包含全局异常处理、属性初始化和模型属性的方法。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="PostMapping-with-consumes-Attribute"><a href="#PostMapping-with-consumes-Attribute" class="headerlink" title="@PostMapping with consumes Attribute"></a>@PostMapping with consumes Attribute</h2><p>@PostMapping 注解与 consumes 属性结合使用，用于指定请求体的媒体类型。</p>
<p>注解属性介绍</p>
<ul>
<li>consumes: 指定请求体的媒体类型，如 application&#x2F;json。</li>
</ul>
<p>发送POST请求创建新用户，请求体为JSON格式：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">POST /users</span><br><span class="line">Content-Type: application/json</span><br><span class="line"></span><br><span class="line">&#123;</span><br><span class="line">  <span class="string">&quot;name&quot;</span>: <span class="string">&quot;John Doe&quot;</span>,</span><br><span class="line">  <span class="string">&quot;email&quot;</span>: <span class="string">&quot;&lt;john.doe@example.com&gt;&quot;</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@PostMapping(value = &quot;/users&quot;, consumes = &quot;application/json&quot;)</span></span><br><span class="line"><span class="keyword">public</span> ResponseEntity&lt;User&gt; <span class="title function_">createUser</span><span class="params">(<span class="meta">@RequestBody</span> User user)</span> &#123;</span><br><span class="line"><span class="comment">// 使用JSON请求体中的数据创建新用户。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="GetMapping-with-produces-Attribute"><a href="#GetMapping-with-produces-Attribute" class="headerlink" title="@GetMapping with produces Attribute"></a>@GetMapping with produces Attribute</h2><p>@GetMapping 注解与 produces 属性结合使用，用于指定响应的媒体类型。<br>注解属性介绍</p>
<ul>
<li>produces: 指定响应的媒体类型，如 application&#x2F;json。</li>
</ul>
<p>发送GET请求获取用户列表，期望响应为JSON格式：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">GET /users</span><br><span class="line">Accept: application/json</span><br><span class="line"></span><br><span class="line"><span class="meta">@GetMapping(value = &quot;/users&quot;, produces = &quot;application/json&quot;)</span></span><br><span class="line"><span class="keyword">public</span> ResponseEntity&lt;List&lt;User&gt;&gt; <span class="title function_">getUsers</span><span class="params">()</span> &#123;</span><br><span class="line"><span class="comment">// 以JSON格式返回用户列表。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="ExceptionHandler-with-responseBody-Attribute"><a href="#ExceptionHandler-with-responseBody-Attribute" class="headerlink" title="@ExceptionHandler with responseBody Attribute"></a>@ExceptionHandler with responseBody Attribute</h2><p>@ExceptionHandler 注解与 responseBody 属性结合使用，用于返回异常信息。<br>注解属性介绍</p>
<ul>
<li>responseBody: 指定异常处理方法的返回值是否作为响应正文。</li>
</ul>
<p>处理资源未找到的异常，并返回JSON格式的错误信息：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">GET /users/<span class="number">999</span></span><br><span class="line">HTTP/<span class="number">1.1</span> <span class="number">404</span> Not Found</span><br><span class="line">Content-Type: application/json</span><br><span class="line"></span><br><span class="line">&#123;<span class="string">&quot;timestamp&quot;</span>: <span class="string">&quot;2023-10-10T12:00:00&quot;</span>, <span class="string">&quot;status&quot;</span>: <span class="number">404</span>, <span class="string">&quot;error&quot;</span>: <span class="string">&quot;Not Found&quot;</span>&#125;</span><br><span class="line"> 注解业务案例</span><br><span class="line"></span><br><span class="line"><span class="meta">@ControllerAdvice</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">GlobalExceptionHandler</span> &#123;</span><br><span class="line"><span class="meta">@ExceptionHandler(ResourceNotFoundException.class)</span></span><br><span class="line"><span class="meta">@ResponseBody</span></span><br><span class="line"><span class="keyword">public</span> ResponseEntity&lt;Object&gt; <span class="title function_">handleResourceNotFound</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="comment">// 返回JSON格式的错误信息。</span></span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="ControllerAdvice-with-basePackages"><a href="#ControllerAdvice-with-basePackages" class="headerlink" title="@ControllerAdvice with basePackages"></a>@ControllerAdvice with basePackages</h2><p>@ControllerAdvice 注解与 basePackages 属性结合使用，用于定义全局异常处理和数据绑定的类。<br>注解属性介绍</p>
<ul>
<li>basePackages: 指定要扫描的包，这些包中的控制器将使用 @ControllerAdvice 类中定义的方法。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="meta">@ControllerAdvice(basePackages = &quot;com.example.web&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">GlobalControllerAdvice</span> &#123;</span><br><span class="line">    <span class="comment">// 包含全局异常处理、属性初始化和模型属性的方法。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="SessionAttribute-with-name"><a href="#SessionAttribute-with-name" class="headerlink" title="@SessionAttribute with name"></a>@SessionAttribute with name</h2><p>@SessionAttribute 注解与 name 属性结合使用，用于从HTTP会话中获取属性。<br>注解属性介绍</p>
<ul>
<li>name: 指定会话属性的名称。</li>
</ul>
<p>从会话中获取名为”user”的属性：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">GET /user/profile</span><br><span class="line">Cookie: JSESSIONID=abc123; user=John Doe</span><br><span class="line"> 注解业务案例</span><br><span class="line"></span><br><span class="line"><span class="meta">@GetMapping(&quot;/user/profile&quot;)</span></span><br><span class="line"><span class="keyword">public</span> String <span class="title function_">showProfile</span><span class="params">(<span class="meta">@SessionAttribute(&quot;user&quot;)</span> String user)</span> &#123;</span><br><span class="line"><span class="comment">// 使用会话中的&quot;user&quot;属性展示用户信息。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="RequestHeader-1"><a href="#RequestHeader-1" class="headerlink" title="@RequestHeader"></a>@RequestHeader</h2><p>@RequestHeader 注解用于将特定的HTTP请求头值绑定到控制器方法的参数上。<br>注解属性介绍</p>
<ul>
<li>value: 请求头的名称。</li>
<li>defaultValue: 当请求头不存在时使用的默认值。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">获取User-Agent请求头的值：</span><br><span class="line"></span><br><span class="line">GET /api/resource</span><br><span class="line">User-Agent: Mozilla/<span class="number">5.0</span> (Windows NT <span class="number">10.0</span>; Win64; x64)</span><br><span class="line"> 注解业务案例</span><br><span class="line"></span><br><span class="line"><span class="meta">@GetMapping(&quot;/api/resource&quot;)</span></span><br><span class="line"><span class="keyword">public</span> ResponseEntity&lt;?&gt; handleRequest(<span class="meta">@RequestHeader(&quot;User-Agent&quot;)</span> String userAgent) &#123;</span><br><span class="line"><span class="comment">// 根据User-Agent处理请求。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="Component"><a href="#Component" class="headerlink" title="@Component"></a>@Component</h2><p>@Component 是Spring的通用注解，用于标识一个类为Spring组件，可以被自动扫描和注册为Spring应用上下文的Bean。<br>注解属性介绍</p>
<ul>
<li>value: 指定组件的名称。<br>注解业务案例</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="meta">@Component(&quot;myComponent&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MyComponent</span> &#123;</span><br><span class="line">    <span class="comment">// 这个类会被Spring自动注册为一个Bean。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="Service"><a href="#Service" class="headerlink" title="@Service"></a>@Service</h2><p>@Service 是Spring的注解，用于标识一个类为服务层组件，通常包含业务逻辑。<br>注解业务案例</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="meta">@Service</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MyService</span> &#123;</span><br><span class="line">    <span class="comment">// 服务层组件，包含业务逻辑。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="Repository"><a href="#Repository" class="headerlink" title="@Repository"></a>@Repository</h2><p>@Repository 是Spring Data的注解，用于标识一个类为数据访问对象，通常用于数据库访问。</p>
<p>注解业务案例</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="meta">@Repository</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">MyRepository</span> <span class="keyword">extends</span> <span class="title class_">JpaRepository</span>&lt;User, Long&gt; &#123;</span><br><span class="line">    <span class="comment">// 定义数据访问方法。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="Profile-1"><a href="#Profile-1" class="headerlink" title="@Profile"></a>@Profile</h2><p>@Profile 注解用于根据激活的配置文件激活或禁用Bean。<br>注解属性介绍</p>
<ul>
<li>value: 指定配置文件的名称。</li>
</ul>
<p>注解业务案例</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="meta">@Profile(&quot;dev&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">DevConfig</span> &#123;</span><br><span class="line">    <span class="comment">// 仅在&quot;dev&quot;配置文件激活时使用。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="RequestPart"><a href="#RequestPart" class="headerlink" title="@RequestPart"></a>@RequestPart</h2><p>@RequestPart 注解用于处理multipart&#x2F;form-data请求中的部分，如文件上传。<br>注解属性介绍</p>
<ul>
<li>value: 请求部分的名称。</li>
</ul>
<p>处理包含文件的multipart请求：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">POST /api/upload</span><br><span class="line">Content-Type: multipart/form-data; boundary=----WebKitFormBoundary</span><br><span class="line"></span><br><span class="line">------WebKitFormBoundary</span><br><span class="line">Content-Disposition: form-data; name=<span class="string">&quot;file&quot;</span>; filename=<span class="string">&quot;image.png&quot;</span></span><br><span class="line">Content-Type: image/png</span><br><span class="line"></span><br><span class="line">[文件内容]</span><br><span class="line"></span><br><span class="line">------WebKitFormBoundary--</span><br><span class="line"> 注解业务案例</span><br><span class="line"></span><br><span class="line"><span class="meta">@PostMapping(&quot;/api/upload&quot;)</span></span><br><span class="line"><span class="keyword">public</span> ResponseEntity&lt;String&gt; <span class="title function_">handleFileUpload</span><span class="params">(</span></span><br><span class="line"><span class="params"><span class="meta">@RequestPart(&quot;file&quot;)</span> MultipartFile file</span></span><br><span class="line"><span class="params">)</span> &#123;</span><br><span class="line"><span class="comment">// 处理上传的文件。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="Spring-Boot-注解"><a href="#Spring-Boot-注解" class="headerlink" title="Spring Boot 注解"></a>Spring Boot 注解</h1><h2 id="ConfigurationProperties"><a href="#ConfigurationProperties" class="headerlink" title="@ConfigurationProperties"></a>@ConfigurationProperties</h2><p>@ConfigurationProperties 是 Spring Boot 提供的一个注解，主要用于将配置文件（如 application.properties 或 application.yml）中的属性值绑定到 Java 类的字段上。通过该注解，可以将一组相关的配置属性封装到一个 Java 类中，方便管理和使用。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="meta">@ConfigurationProperties(prefix = &quot;myapp&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MyAppProperties</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> port;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="attr">myapp.name</span>=<span class="string">My Application</span></span><br><span class="line"><span class="attr">myapp.port</span>=<span class="string">8080</span></span><br></pre></td></tr></table></figure>

<h2 id="EnableConfigurationProperties"><a href="#EnableConfigurationProperties" class="headerlink" title="@EnableConfigurationProperties"></a>@EnableConfigurationProperties</h2><p>@EnableConfigurationProperties 用于启用 @ConfigurationProperties 注解的类。当使用 @ConfigurationProperties 注解标记一个类后，需要使用 @EnableConfigurationProperties 注解来告知 Spring Boot 去扫描并绑定这些配置属性类。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="meta">@EnableConfigurationProperties(MyAppProperties.class)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">AppConfig</span> &#123;</span><br><span class="line">    <span class="comment">// 配置类可以包含其他配置信息</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="ConditionalOnProperty"><a href="#ConditionalOnProperty" class="headerlink" title="@ConditionalOnProperty"></a>@ConditionalOnProperty</h2><p>@ConditionalOnProperty 是一个条件注解，用于根据配置文件中的属性值来决定是否加载某个 Bean 或配置类。只有当配置文件中指定的属性满足特定条件时，被该注解标记的 Bean 或配置类才会被创建和加载。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ConditionalConfig</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="meta">@ConditionalOnProperty(name = &quot;myapp.feature.enabled&quot;, havingValue = &quot;true&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> MyFeature <span class="title function_">myFeature</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">MyFeature</span>();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MyFeature</span> &#123;</span><br><span class="line">    <span class="comment">// 自定义功能类</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="Spring-Cloud-注解"><a href="#Spring-Cloud-注解" class="headerlink" title="Spring Cloud 注解"></a>Spring Cloud 注解</h1><h1 id="Jackson"><a href="#Jackson" class="headerlink" title="Jackson"></a>Jackson</h1><h2 id="序列化注解"><a href="#序列化注解" class="headerlink" title="序列化注解"></a>序列化注解</h2><h3 id="JsonAnyGetter"><a href="#JsonAnyGetter" class="headerlink" title="@JsonAnyGetter"></a>@JsonAnyGetter</h3><p>该注解用于把可变的Map类型属性当做标准属性。<br>下例中，ExtendableBean实体有一个name属性和一组kay&#x2F;value格式的可扩展属性:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">ExtendableBean</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> String name;</span><br><span class="line">    <span class="keyword">public</span> Map&lt;String, String&gt; properties;</span><br><span class="line">    <span class="meta">@JsonAnyGetter</span></span><br><span class="line">    <span class="keyword">public</span> Map&lt;String, String&gt; <span class="title function_">getProperties</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> properties;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">ExtendableBean</span><span class="params">(String name)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.name = name;</span><br><span class="line">        properties = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;&gt;();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">add</span><span class="params">(String key, String value)</span>&#123;</span><br><span class="line">        properties.put(key, value);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>说明: name属性访问级别是public, 是为了省略get&#x2F;set方法, 简化示例<br>下面是把ExtendableBean实体序列化的过程:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">whenSerializingUsingJsonAnyGetter_thenCorrect</span><span class="params">()</span>&#123;</span><br><span class="line">        <span class="type">ExtendableBean</span> <span class="variable">bean</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ExtendableBean</span>(<span class="string">&quot;My bean&quot;</span>);</span><br><span class="line">        bean.add(<span class="string">&quot;attr1&quot;</span>, <span class="string">&quot;val1&quot;</span>);</span><br><span class="line">        bean.add(<span class="string">&quot;attr2&quot;</span>, <span class="string">&quot;val2&quot;</span>);</span><br><span class="line">        <span class="type">String</span> <span class="variable">result</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            result = <span class="keyword">new</span> <span class="title class_">ObjectMapper</span>().writeValueAsString(bean);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (JsonProcessingException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(result);</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>序列化后的结果: {“name”:”My bean”,”attr2”:”val2”,”attr1”:”val1”}</p>
<h3 id="JsonGetter"><a href="#JsonGetter" class="headerlink" title="@JsonGetter"></a>@JsonGetter</h3><p>该注解是@JsonProperty的两个作用中的一个, 用来标记一个方法是getter方法<br>下例中, 指定方法getTheName()是属性name属性的getter方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MyBean</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> id;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@JsonGetter(&quot;name&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">getTheName</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> name;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>下面是序列化过程:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">whenSerializingUsingJsonGetter_thenCorrect</span><span class="params">()</span></span><br><span class="line">  <span class="keyword">throws</span> JsonProcessingException &#123;</span><br><span class="line">    <span class="type">MyBean</span> <span class="variable">bean</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">MyBean</span>(<span class="number">1</span>, <span class="string">&quot;My bean&quot;</span>);</span><br><span class="line">    <span class="type">String</span> <span class="variable">result</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ObjectMapper</span>().writeValueAsString(bean);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="JsonPropertyOrder"><a href="#JsonPropertyOrder" class="headerlink" title="@JsonPropertyOrder"></a>@JsonPropertyOrder</h3><p>该注解可以指定实体属性序列化后的顺序</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@JsonPropertyOrder(&#123; &quot;name&quot;, &quot;id&quot; &#125;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MyBean</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> id;</span><br><span class="line">    <span class="keyword">public</span> String name;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>序列化后的结果:{ “name”:”My bean”, “id”:1}<br>该注解有一个参数alphabetic, 如果为true, 表示按字母顺序序列化,此时输出结果:{ “id”:1, “name”:”My bean”}</p>
<h3 id="JsonRawValue"><a href="#JsonRawValue" class="headerlink" title="@JsonRawValue"></a>@JsonRawValue</h3><p>该注解可以让Jackson在序列化时把属性的值原样输出<br>下面的例子中, 我们给实体属性attrs赋值一个json字符串</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">RawBean</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> String name;</span><br><span class="line">    <span class="meta">@JsonRawValue</span></span><br><span class="line">    <span class="keyword">public</span> String attrs;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">whenSerializingUsingJsonRawValue_thenCorrect</span><span class="params">()</span></span><br><span class="line">  <span class="keyword">throws</span> JsonProcessingException &#123;  </span><br><span class="line">    <span class="type">RawBean</span> <span class="variable">bean</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">RawBean</span>(<span class="string">&quot;My bean&quot;</span>, <span class="string">&quot;&#123;\&quot;attr\&quot;:false&#125;&quot;</span>);</span><br><span class="line">    <span class="type">String</span> <span class="variable">result</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ObjectMapper</span>().writeValueAsString(bean);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>输出结果是: {“name”:”Mybean”,”attrs”:{“attr”:false}}</p>
<h3 id="JsonValue"><a href="#JsonValue" class="headerlink" title="@JsonValue"></a>@JsonValue</h3><p>该注解作用于一个方法, 并且只用被注解的方法序列化整个实体对象<br>把1.1的实体例修改如下:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">ExtendableBean</span> &#123;</span><br><span class="line">   ...........</span><br><span class="line">   <span class="comment">//把注解换成JsonValue</span></span><br><span class="line">    <span class="meta">@JsonValue</span></span><br><span class="line">    <span class="keyword">public</span> Map&lt;String, String&gt; <span class="title function_">getProperties</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> properties;</span><br><span class="line">    &#125;</span><br><span class="line"> ..........</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>序列化过程不变, 则结果是: {“attr2”:”val2”,”attr1”:”val1”}<br>可见, 属性name没有被序列化</p>
<h3 id="JsonRootName"><a href="#JsonRootName" class="headerlink" title="@JsonRootName"></a>@JsonRootName</h3><p>如果wrapping是使能(enabled), 那么该注解用来指定root wrapper的名称<br>wrapping(包装)的含义是如果序列化实体User的结果是</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">&#123;</span><br><span class="line">    <span class="string">&quot;id&quot;</span>: <span class="number">1</span>,</span><br><span class="line">    <span class="string">&quot;name&quot;</span>: <span class="string">&quot;John&quot;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>那么wrapping后的效果如下:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">&#123;</span><br><span class="line">    <span class="string">&quot;User&quot;</span>: &#123;</span><br><span class="line">        <span class="string">&quot;id&quot;</span>: <span class="number">1</span>,</span><br><span class="line">        <span class="string">&quot;name&quot;</span>: <span class="string">&quot;John&quot;</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>下面看一个例子, 我们用该注解指明包装实体(wrapper entity)的包装器名称:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="meta">@JsonRootName(value = &quot;user&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UserWithRoot</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> id;</span><br><span class="line">    <span class="keyword">public</span> String name;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>包装器默认名称是实体类名, 这里就是UserWithRoot, 但是注解的value属性把包装器名称改为了user<br>序列化过程(和前面不同, 需要使能包装器)</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">whenSerializingUsingJsonRootName_thenCorrect</span><span class="params">()</span>&#123;</span><br><span class="line">        <span class="type">UserWithRoot</span> <span class="variable">user</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">UserWithRoot</span>();</span><br><span class="line">        user.id = <span class="number">1</span>;</span><br><span class="line">        user.name = <span class="string">&quot;jackma&quot;</span>;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="type">ObjectMapper</span> <span class="variable">objectMapper</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ObjectMapper</span>();</span><br><span class="line">            objectMapper.enable(SerializationFeature.WRAP_ROOT_VALUE);</span><br><span class="line">            <span class="type">String</span> <span class="variable">result</span> <span class="operator">=</span> objectMapper.writeValueAsString(user);</span><br><span class="line">            System.out.println(result);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (JsonProcessingException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>序列化的结果:{“user”:{“id”:1,”name”:”jackma”}}<br>从Jackson .2.4版本开始, 新增了一个可选参数namespace, 该属性对json没效果, 但是对xml起作用, 修改本例的实体例:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="meta">@JsonRootName(value = &quot;user&quot;, namespace = &quot;alibaba&quot;)</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">UserWithRoot</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> id;</span><br><span class="line">    <span class="keyword">public</span> String name;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>用XmlMapper序列化:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">whenSerializingUsingJsonRootName_thenCorrect</span><span class="params">()</span>&#123;</span><br><span class="line">  ..............</span><br><span class="line">            <span class="type">XmlMapper</span> <span class="variable">xmlMapper</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">XmlMapper</span>();</span><br><span class="line">            xmlMapper.enable(SerializationFeature.WRAP_ROOT_VALUE);</span><br><span class="line">  ..............</span><br></pre></td></tr></table></figure>

<p>序列化结果:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">user</span> <span class="attr">xmlns</span>=<span class="string">&quot;alibaba&quot;</span>&gt;</span></span><br><span class="line"> <span class="tag">&lt;<span class="name">id</span> <span class="attr">xmlns</span>=<span class="string">&quot;&quot;</span>&gt;</span>1<span class="tag">&lt;/<span class="name">id</span>&gt;</span></span><br><span class="line"> <span class="tag">&lt;<span class="name">name</span> <span class="attr">xmlns</span>=<span class="string">&quot;&quot;</span>&gt;</span>jackma<span class="tag">&lt;/<span class="name">name</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">user</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="JsonSerialize"><a href="#JsonSerialize" class="headerlink" title="@JsonSerialize"></a>@JsonSerialize</h3><p>该注解用于指定一个自定义序列化器(custom serializer)来序列化实体例的某属性<br>下例中, 用@JsonSerialize的参数using指明实体类属性eventDate的序列化器是CustomDateSerializer类:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Event</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> String name;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@JsonSerialize(using = CustomDateSerializer.class)</span></span><br><span class="line">    <span class="keyword">public</span> Date eventDate;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>下面是类CustomDateSerializer的定义:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">CustomDateSerializer</span> <span class="keyword">extends</span> <span class="title class_">StdSerializer</span>&lt;Date&gt; &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="type">SimpleDateFormat</span> <span class="variable">formatter</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">SimpleDateFormat</span>(<span class="string">&quot;dd-MM-yyyy hh:mm:ss&quot;</span>);</span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">CustomDateSerializer</span><span class="params">()</span> &#123; <span class="built_in">this</span>(<span class="literal">null</span>); &#125;</span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">CustomDateSerializer</span><span class="params">(Class&lt;Date&gt; t)</span> &#123; <span class="built_in">super</span>(t); &#125;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">serialize</span><span class="params">(Date value, JsonGenerator gen, SerializerProvider arg2)</span></span><br><span class="line">     <span class="keyword">throws</span> IOException, JsonProcessingException &#123;</span><br><span class="line">        gen.writeString(formatter.format(value));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>序列化:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">whenSerializingUsingJsonSerialize_thenCorrect</span><span class="params">()</span>&#123;</span><br><span class="line"> <span class="type">SimpleDateFormat</span> <span class="variable">df</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">SimpleDateFormat</span>(<span class="string">&quot;yyyy/MM/DD hh:mm:ss&quot;</span>);</span><br><span class="line"> <span class="type">String</span> <span class="variable">toParse</span> <span class="operator">=</span> <span class="string">&quot;2019/08/19 16:28:00&quot;</span>;</span><br><span class="line"> <span class="type">Date</span> <span class="variable">date</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line"> <span class="keyword">try</span> &#123;</span><br><span class="line">     date = df.parse(toParse);</span><br><span class="line"> &#125; <span class="keyword">catch</span> (ParseException e) &#123;</span><br><span class="line">     e.printStackTrace();</span><br><span class="line"> &#125;</span><br><span class="line"> <span class="type">Event</span> <span class="variable">event</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Event</span>();</span><br><span class="line"> event.name = <span class="string">&quot;party&quot;</span>;</span><br><span class="line"> event.eventDate = date;</span><br><span class="line"> <span class="keyword">try</span> &#123;</span><br><span class="line">     <span class="type">String</span> <span class="variable">result</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ObjectMapper</span>().writeValueAsString(event);</span><br><span class="line">     System.out.println(result);</span><br><span class="line"> &#125; <span class="keyword">catch</span> (JsonProcessingException e) &#123;</span><br><span class="line">     e.printStackTrace();</span><br><span class="line"> &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>序列化结果: {“name”:”party”,”eventDate”:”2019-08-19 04:28:00”}<br>而如果没有@JsonSerialize注解的序列化结果是: {“name”:”party”,”eventDate”:1566203280000}</p>
<h2 id="反序列化注解"><a href="#反序列化注解" class="headerlink" title="反序列化注解"></a>反序列化注解</h2><h3 id="JsonCreator"><a href="#JsonCreator" class="headerlink" title="@JsonCreator"></a>@JsonCreator</h3><p>该注解可以调整反序列化时构造器&#x2F;构造工厂的行为<br>当我们需要反序列化的Json字符串和目标实体类不完全匹配时, 这个注解非常有用<br>假设我们要反序列化下面的Json字符串:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">&#123;</span><br><span class="line">    <span class="string">&quot;id&quot;</span>:<span class="number">1</span>,</span><br><span class="line">    <span class="string">&quot;theName&quot;</span>:<span class="string">&quot;My bean&quot;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>但是, 我们的目标实体类并没有一个名为theName的属性. 现在, 我们不想改变实体类本身, 我们只需在数据导出时做一些控制, 方法就是在构造器中使用@JsonCreator和@JsonProperty注解:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">BeanWithCreator</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> id;</span><br><span class="line">    <span class="keyword">public</span> String name;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@JsonCreator</span></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">BeanWithCreator</span><span class="params">(</span></span><br><span class="line"><span class="params">      <span class="meta">@JsonProperty(&quot;id&quot;)</span> <span class="type">int</span> id, </span></span><br><span class="line"><span class="params">      <span class="meta">@JsonProperty(&quot;theName&quot;)</span> String name)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.id = id;</span><br><span class="line">        <span class="built_in">this</span>.name = name;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>反序列化过程:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">whenDeserializingUsingJsonCreator_thenCorrect</span><span class="params">()</span>  <span class="keyword">throws</span> IOException &#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">json</span> <span class="operator">=</span> <span class="string">&quot;&#123;\&quot;id\&quot;:1,\&quot;theName\&quot;:\&quot;My bean\&quot;&#125;&quot;</span>;</span><br><span class="line">    <span class="type">BeanWithCreator</span> <span class="variable">bean</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ObjectMapper</span>().readerFor(BeanWithCreator.class).readValue(json);</span><br><span class="line">    assertEquals(<span class="string">&quot;My bean&quot;</span>, bean.name);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="JacksonInject"><a href="#JacksonInject" class="headerlink" title="@JacksonInject"></a>@JacksonInject</h3><p>该注解指明一个属性的值是通过注入得到而不是从Json字符串反序列得到<br>下例的实体例属性id的值用注解标明是注入值:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">BeanWithInject</span> &#123;</span><br><span class="line">    <span class="meta">@JacksonInject</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> id;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> String name;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>反序列化过程:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">whenDeserializingUsingJsonInject_thenCorrect</span><span class="params">()</span> <span class="keyword">throws</span> IOException &#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">json</span> <span class="operator">=</span> <span class="string">&quot;&#123;\&quot;name\&quot;:\&quot;My bean\&quot;&#125;&quot;</span>;</span><br><span class="line">    <span class="type">InjectableValues</span> <span class="variable">inject</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">InjectableValues</span>.Std().addValue(<span class="type">int</span>.class, <span class="number">1</span>);</span><br><span class="line">    <span class="type">BeanWithInject</span> <span class="variable">bean</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ObjectMapper</span>().reader(inject).forType(BeanWithInject.class).readValue(json);</span><br><span class="line">    assertEquals(<span class="string">&quot;My bean&quot;</span>, bean.name);</span><br><span class="line">    assertEquals(<span class="number">1</span>, bean.id);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="JsonAnySetter"><a href="#JsonAnySetter" class="headerlink" title="@JsonAnySetter"></a>@JsonAnySetter</h3><p>该注解允许我们把一个可变的map属性作为标准属性, 在反序列过程中, 从Json字符串得到的属性值会加入到map属性中<br>实体例和注解:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ExtendableBean</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> String name;</span><br><span class="line">    <span class="keyword">private</span> Map&lt;String, String&gt; properties;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@JsonAnySetter</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">add</span><span class="params">(String key, String value)</span> &#123;</span><br><span class="line">        properties.put(key, value);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>准备反序列化的Json字符串:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">&#123;</span><br><span class="line">    <span class="string">&quot;name&quot;</span>:<span class="string">&quot;My bean&quot;</span>,</span><br><span class="line">    <span class="string">&quot;attr2&quot;</span>:<span class="string">&quot;val2&quot;</span>,</span><br><span class="line">    <span class="string">&quot;attr1&quot;</span>:<span class="string">&quot;val1&quot;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>反序列化过程:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">whenDeserializingUsingJsonAnySetter_thenCorrect</span><span class="params">()</span>  <span class="keyword">throws</span> IOException &#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">json</span> <span class="operator">=</span> <span class="string">&quot;&#123;\&quot;name\&quot;:\&quot;My bean\&quot;,\&quot;attr2\&quot;:\&quot;val2\&quot;,\&quot;attr1\&quot;:\&quot;val1\&quot;&#125;&quot;</span>;</span><br><span class="line">    <span class="type">ExtendableBean</span> <span class="variable">bean</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ObjectMapper</span>().readerFor(ExtendableBean.class).readValue(json);</span><br><span class="line">    assertEquals(<span class="string">&quot;My bean&quot;</span>, bean.name);</span><br><span class="line">    assertEquals(<span class="string">&quot;val2&quot;</span>, bean.getProperties().get(<span class="string">&quot;attr2&quot;</span>));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="JsonSetter"><a href="#JsonSetter" class="headerlink" title="@JsonSetter"></a>@JsonSetter</h3><p>该注解是@JsonProperty的另一个作用, 和@JsonGetter相对, 标记一个方法是setter方法<br>如果目标实体类没有和Json字符串数据完全匹配的方法时, 我们可以通过这个注解做一些调整让他们匹配<br>下例中指定方法setTheName()作为name属性的setter方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MyBean</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> id;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@JsonSetter(&quot;name&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setTheName</span><span class="params">(String name)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.name = name;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>反序列化过程:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">whenDeserializingUsingJsonSetter_thenCorrect</span><span class="params">()</span>  <span class="keyword">throws</span> IOException &#123;  </span><br><span class="line">    <span class="type">String</span> <span class="variable">json</span> <span class="operator">=</span> <span class="string">&quot;&#123;\&quot;id\&quot;:1,\&quot;name\&quot;:\&quot;My bean\&quot;&#125;&quot;</span>;</span><br><span class="line">    <span class="type">MyBean</span> <span class="variable">bean</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ObjectMapper</span>().readerFor(MyBean.class).readValue(json);</span><br><span class="line">    assertEquals(<span class="string">&quot;My bean&quot;</span>, bean.getTheName());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="JsonDeserialize"><a href="#JsonDeserialize" class="headerlink" title="@JsonDeserialize"></a>@JsonDeserialize</h3><p>该注解标明使用自定义反序列化器(custom deserializer)<br>实体类:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Event</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> String name;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@JsonDeserialize(using = CustomDateDeserializer.class)</span></span><br><span class="line">    <span class="keyword">public</span> Date eventDate;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>自定义反序列化器:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">CustomDateDeserializer</span>  <span class="keyword">extends</span> <span class="title class_">StdDeserializer</span>&lt;Date&gt; &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="type">SimpleDateFormat</span> <span class="variable">formatter</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">SimpleDateFormat</span>(<span class="string">&quot;dd-MM-yyyy hh:mm:ss&quot;</span>);</span><br><span class="line">     <span class="keyword">public</span> <span class="title function_">CustomDateDeserializer</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>(<span class="literal">null</span>);</span><br><span class="line">    &#125;  </span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">CustomDateDeserializer</span><span class="params">(Class&lt;?&gt; vc)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>(vc);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> Date <span class="title function_">deserialize</span><span class="params">(JsonParser jsonparser, DeserializationContext context)</span> <span class="keyword">throws</span> IOException &#123;</span><br><span class="line">        <span class="type">String</span> <span class="variable">date</span> <span class="operator">=</span> jsonparser.getText();</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> formatter.parse(date);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (ParseException e) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">RuntimeException</span>(e);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>反序列过程:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">whenDeserializingUsingJsonDeserialize_thenCorrect</span><span class="params">()</span>  <span class="keyword">throws</span> IOException &#123;  </span><br><span class="line">    <span class="type">String</span> <span class="variable">json</span> <span class="operator">=</span> <span class="string">&quot;&#123;&quot;</span>name<span class="string">&quot;:&quot;</span>party<span class="string">&quot;,&quot;</span>eventDate<span class="string">&quot;:&quot;</span><span class="number">20</span>-<span class="number">12</span>-<span class="number">2014</span> <span class="number">02</span>:<span class="number">30</span>:<span class="number">00</span><span class="string">&quot;&#125;&quot;</span>;</span><br><span class="line">    <span class="type">SimpleDateFormat</span> <span class="variable">df</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">SimpleDateFormat</span>(<span class="string">&quot;dd-MM-yyyy hh:mm:ss&quot;</span>);</span><br><span class="line">    <span class="type">Event</span> <span class="variable">event</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ObjectMapper</span>().readerFor(Event.class).readValue(json);</span><br><span class="line">    assertEquals(<span class="string">&quot;20-12-2014 02:30:00&quot;</span>, df.format(event.eventDate));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="JsonAlias"><a href="#JsonAlias" class="headerlink" title="@JsonAlias"></a>@JsonAlias</h3><p>该注解在反序列化过程中为属性定义一个或多个别名<br>实体类:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">AliasBean</span> &#123;</span><br><span class="line">    <span class="meta">@JsonAlias(&#123; &quot;fName&quot;, &quot;f_name&quot; &#125;)</span></span><br><span class="line">    <span class="keyword">private</span> String firstName;</span><br><span class="line">    <span class="keyword">private</span> String lastName;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Json字符串中fName, f_name或firstName的值都可以被反序列到属性firstName</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">whenDeserializingUsingJsonAlias_thenCorrect</span><span class="params">()</span> <span class="keyword">throws</span> IOException &#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">json</span> <span class="operator">=</span> <span class="string">&quot;&#123;\&quot;fName\&quot;: \&quot;John\&quot;, \&quot;lastName\&quot;: \&quot;Green\&quot;&#125;&quot;</span>;</span><br><span class="line">    <span class="type">AliasBean</span> <span class="variable">aliasBean</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ObjectMapper</span>().readerFor(AliasBean.class).readValue(json);</span><br><span class="line">    assertEquals(<span class="string">&quot;John&quot;</span>, aliasBean.getFirstName());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="属性包含注解"><a href="#属性包含注解" class="headerlink" title="属性包含注解"></a>属性包含注解</h2><h3 id="JsonIgnoreProperties"><a href="#JsonIgnoreProperties" class="headerlink" title="@JsonIgnoreProperties"></a>@JsonIgnoreProperties</h3><p>该注解是一个类级别的注解, 标记一个或多个属性被Jackson忽略<br>实体类:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="meta">@JsonIgnoreProperties(&#123; &quot;id&quot; &#125;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">BeanWithIgnore</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> id;</span><br><span class="line">    <span class="keyword">public</span> String name;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>序列化过程:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">whenSerializingUsingJsonIgnoreProperties_thenCorrect</span><span class="params">()</span>  <span class="keyword">throws</span> JsonProcessingException &#123;  </span><br><span class="line">    <span class="type">BeanWithIgnore</span> <span class="variable">bean</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">BeanWithIgnore</span>(<span class="number">1</span>, <span class="string">&quot;My bean&quot;</span>);</span><br><span class="line">    <span class="type">String</span> <span class="variable">result</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ObjectMapper</span>().writeValueAsString(bean);</span><br><span class="line">    assertThat(result, containsString(<span class="string">&quot;My bean&quot;</span>));</span><br><span class="line">    assertThat(result, not(containsString(<span class="string">&quot;id&quot;</span>)));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>参数ignoreUnknown为true时, Json字符串如果有未知的属性名, 则不会抛出异常</p>
<h3 id="JsonIgnore"><a href="#JsonIgnore" class="headerlink" title="@JsonIgnore"></a>@JsonIgnore</h3><p>该注解用于属性级别, 用于标明一个属性可以被Jackson忽略<br>实体类:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">BeanWithIgnore</span> &#123;</span><br><span class="line">    <span class="meta">@JsonIgnore</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> id;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> String name;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>序列化过程:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">whenSerializingUsingJsonIgnore_thenCorrect</span><span class="params">()</span></span><br><span class="line">  <span class="keyword">throws</span> JsonProcessingException &#123;</span><br><span class="line">  </span><br><span class="line">    <span class="type">BeanWithIgnore</span> <span class="variable">bean</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">BeanWithIgnore</span>(<span class="number">1</span>, <span class="string">&quot;My bean&quot;</span>);</span><br><span class="line"> </span><br><span class="line">    <span class="type">String</span> <span class="variable">result</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ObjectMapper</span>()</span><br><span class="line">      .writeValueAsString(bean);</span><br><span class="line">     </span><br><span class="line">    assertThat(result, containsString(<span class="string">&quot;My bean&quot;</span>));</span><br><span class="line">    assertThat(result, not(containsString(<span class="string">&quot;id&quot;</span>)));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="JsonIgnoreType"><a href="#JsonIgnoreType" class="headerlink" title="@JsonIgnoreType"></a>@JsonIgnoreType</h3><p>该注解标记类型是注解作用的类型的属性都会被忽略<br>必须作用于类, 标明以该类为类型的属性都会被Jackson忽略<br>实体类:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">User</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> id;</span><br><span class="line">    <span class="keyword">public</span> Name name;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@JsonIgnoreType</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">class</span> <span class="title class_">Name</span> &#123;</span><br><span class="line">        <span class="keyword">public</span> String firstName;</span><br><span class="line">        <span class="keyword">public</span> String lastName;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>序列化过程:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">whenSerializingUsingJsonIgnoreType_thenCorrect</span><span class="params">()</span>  <span class="keyword">throws</span> JsonProcessingException, ParseException &#123;  </span><br><span class="line">    User.<span class="type">Name</span> <span class="variable">name</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>.Name(<span class="string">&quot;John&quot;</span>, <span class="string">&quot;Doe&quot;</span>);</span><br><span class="line">    <span class="type">User</span> <span class="variable">user</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>(<span class="number">1</span>, name);</span><br><span class="line">    <span class="type">String</span> <span class="variable">result</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ObjectMapper</span>().writeValueAsString(user);</span><br><span class="line">    assertThat(result, containsString(<span class="string">&quot;1&quot;</span>));</span><br><span class="line">    assertThat(result, not(containsString(<span class="string">&quot;name&quot;</span>)));</span><br><span class="line">    assertThat(result, not(containsString(<span class="string">&quot;John&quot;</span>)));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="JsonInclude"><a href="#JsonInclude" class="headerlink" title="@JsonInclude"></a>@JsonInclude</h3><p>该注解在序列化时会排除属性值是空值（empty或null）、没有默认值的属性。<br>可作用在类和属性上<br>实体类：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="meta">@JsonInclude(Include.NON_NULL)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MyBean</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> id;</span><br><span class="line">    <span class="keyword">public</span> String name;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>序列化过程:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">whenSerializingUsingJsonInclude_thenCorrect</span><span class="params">()</span>  <span class="keyword">throws</span> JsonProcessingException &#123;  </span><br><span class="line">    <span class="type">MyBean</span> <span class="variable">bean</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">MyBean</span>(<span class="number">1</span>, <span class="literal">null</span>);</span><br><span class="line">    <span class="type">String</span> <span class="variable">result</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ObjectMapper</span>().writeValueAsString(bean);</span><br><span class="line">    assertThat(result, containsString(<span class="string">&quot;1&quot;</span>));</span><br><span class="line">    assertThat(result, not(containsString(<span class="string">&quot;name&quot;</span>)));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="JsonAutoDetect"><a href="#JsonAutoDetect" class="headerlink" title="@JsonAutoDetect"></a>@JsonAutoDetect</h3><p>该注解可以覆盖属性是否可见的默认语义, 比如对于不可见的private序列化时变成可见的<br>实体类:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="meta">@JsonAutoDetect(fieldVisibility = Visibility.ANY)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">PrivateBean</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> id;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>序列化过程:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">whenSerializingUsingJsonAutoDetect_thenCorrect</span><span class="params">()</span>  <span class="keyword">throws</span> JsonProcessingException &#123;</span><br><span class="line">    <span class="type">PrivateBean</span> <span class="variable">bean</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">PrivateBean</span>(<span class="number">1</span>, <span class="string">&quot;My bean&quot;</span>);</span><br><span class="line">    <span class="type">String</span> <span class="variable">result</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ObjectMapper</span>().writeValueAsString(bean);</span><br><span class="line">    assertThat(result, containsString(<span class="string">&quot;1&quot;</span>));</span><br><span class="line">    assertThat(result, containsString(<span class="string">&quot;My bean&quot;</span>));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="常用注解"><a href="#常用注解" class="headerlink" title="常用注解"></a>常用注解</h2><h3 id="JsonProperty"><a href="#JsonProperty" class="headerlink" title="@JsonProperty"></a>@JsonProperty</h3><p>该注解可以指定属性在Json字符串中的名字<br>下例中在非标准的setter和getter方法上使用该注解, 可以成功序列化和反序列化<br>实体类:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MyBean</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> id;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@JsonProperty(&quot;name&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setTheName</span><span class="params">(String name)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.name = name;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="meta">@JsonProperty(&quot;name&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">getTheName</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> name;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>序列化和反序列化过程:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">whenUsingJsonProperty_thenCorrect</span><span class="params">()</span>  <span class="keyword">throws</span> IOException &#123;</span><br><span class="line">    <span class="type">MyBean</span> <span class="variable">bean</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">MyBean</span>(<span class="number">1</span>, <span class="string">&quot;My bean&quot;</span>);</span><br><span class="line">    <span class="type">String</span> <span class="variable">result</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ObjectMapper</span>().writeValueAsString(bean);</span><br><span class="line">    assertThat(result, containsString(<span class="string">&quot;My bean&quot;</span>));</span><br><span class="line">    assertThat(result, containsString(<span class="string">&quot;1&quot;</span>));</span><br><span class="line"></span><br><span class="line">    <span class="type">MyBean</span> <span class="variable">resultBean</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ObjectMapper</span>().readerFor(MyBean.class).readValue(result);</span><br><span class="line">    assertEquals(<span class="string">&quot;My bean&quot;</span>, resultBean.getTheName());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="JsonFormat"><a href="#JsonFormat" class="headerlink" title="@JsonFormat"></a>@JsonFormat</h3><p>该注解指定序列化日期和时间时的格式<br>修改前面1.7的实体类:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Event</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> String name;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@JsonFormat(shape = JsonFormat.Shape.STRING, pattern = &quot;dd-MM-yyyy hh:mm:ss&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> Date eventDate;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>序列化过程:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">whenSerializingUsingJsonFormat_thenCorrect</span><span class="params">()</span> <span class="keyword">throws</span> JsonProcessingException, ParseException &#123;</span><br><span class="line">    <span class="type">SimpleDateFormat</span> <span class="variable">df</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">SimpleDateFormat</span>(<span class="string">&quot;dd-MM-yyyy hh:mm:ss&quot;</span>);</span><br><span class="line">    df.setTimeZone(TimeZone.getTimeZone(<span class="string">&quot;UTC&quot;</span>));</span><br><span class="line"></span><br><span class="line">    <span class="type">String</span> <span class="variable">toParse</span> <span class="operator">=</span> <span class="string">&quot;20-12-2014 02:30:00&quot;</span>;</span><br><span class="line">    <span class="type">Date</span> <span class="variable">date</span> <span class="operator">=</span> df.parse(toParse);</span><br><span class="line">    <span class="type">Event</span> <span class="variable">event</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Event</span>(<span class="string">&quot;party&quot;</span>, date);     </span><br><span class="line">    <span class="type">String</span> <span class="variable">result</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ObjectMapper</span>().writeValueAsString(event);     </span><br><span class="line">    assertThat(result, containsString(toParse));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="JsonUnwrapped"><a href="#JsonUnwrapped" class="headerlink" title="@JsonUnwrapped"></a>@JsonUnwrapped</h3><p>该注解指定值在序列化和反序列化时, 去除对应属性的外包装(根节点)<br>实体类:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UnwrappedUser</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> id;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@JsonUnwrapped</span></span><br><span class="line">    <span class="keyword">public</span> Name name;</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">class</span> <span class="title class_">Name</span> &#123;</span><br><span class="line">        <span class="keyword">public</span> String firstName;</span><br><span class="line">        <span class="keyword">public</span> String lastName;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>序列化过程:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">whenSerializingUsingJsonUnwrapped_thenCorrect</span><span class="params">()</span>  <span class="keyword">throws</span> JsonProcessingException, ParseException &#123;</span><br><span class="line">    UnwrappedUser.<span class="type">Name</span> <span class="variable">name</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">UnwrappedUser</span>.Name(<span class="string">&quot;John&quot;</span>, <span class="string">&quot;Doe&quot;</span>);</span><br><span class="line">    <span class="type">UnwrappedUser</span> <span class="variable">user</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">UnwrappedUser</span>(<span class="number">1</span>, name);</span><br><span class="line">    <span class="type">String</span> <span class="variable">result</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ObjectMapper</span>().writeValueAsString(user);</span><br><span class="line">    assertThat(result, containsString(<span class="string">&quot;John&quot;</span>));</span><br><span class="line">    assertThat(result, not(containsString(<span class="string">&quot;name&quot;</span>)));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>序列化结果:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">&#123;</span><br><span class="line">    <span class="string">&quot;id&quot;</span>:<span class="number">1</span>,</span><br><span class="line">    <span class="string">&quot;firstName&quot;</span>:<span class="string">&quot;John&quot;</span>,</span><br><span class="line">    <span class="string">&quot;lastName&quot;</span>:<span class="string">&quot;Doe&quot;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="JsonView"><a href="#JsonView" class="headerlink" title="@JsonView"></a>@JsonView</h3><p>该注解指明属性序列化和反序列时的视图级别(View)<br>视图类: 主要用于表明哪一级的实体类的属性会被序列化或反序列化</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Views</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">class</span> <span class="title class_">Public</span> &#123;&#125;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">class</span> <span class="title class_">Internal</span> <span class="keyword">extends</span> <span class="title class_">Public</span> &#123;&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>实体类:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">UserWithRoot</span> &#123;</span><br><span class="line">    <span class="meta">@JsonView(Views.Public.class)</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> id;</span><br><span class="line">    <span class="meta">@JsonView(Views.Public.class)</span></span><br><span class="line">    <span class="keyword">public</span> String name;</span><br><span class="line">    <span class="meta">@JsonView(Views.Internal.class)</span></span><br><span class="line">    <span class="keyword">public</span> String school;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>实例化过程:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">whenSerializingUsingJsonView_thenCorrect</span><span class="params">()</span></span><br><span class="line">  <span class="keyword">throws</span> JsonProcessingException &#123;</span><br><span class="line">    <span class="type">UserWithRoot</span> <span class="variable">user</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">UserWithRoot</span>();</span><br><span class="line">    user.id = <span class="number">1</span>;</span><br><span class="line">    user.name = <span class="string">&quot;bl&quot;</span>;</span><br><span class="line">    user.school = <span class="string">&quot;suide&quot;</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        System.out.println(<span class="keyword">new</span> <span class="title class_">ObjectMapper</span>().writerWithView(Views.Internal.class).writeValueAsString(user));</span><br><span class="line">    &#125; <span class="keyword">catch</span> (JsonProcessingException e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>本例中, school的视图级别是View.Internal类, 而序列化的映射器设定的视图显示级别是Views.Public类, 比school的类型高了一级, 所以序列化结果中没有school,<br>{“id”:1,”name”:”bl”}<br>而如果修改映射器的视图级别是Views.Internal类, 则序列化结果中包含school<br>{“id”:1,”name”:”bl”,”school”:”suide”}</p>
<h3 id="JsonManagedReference-JsonBackReference"><a href="#JsonManagedReference-JsonBackReference" class="headerlink" title="@JsonManagedReference, @JsonBackReference"></a>@JsonManagedReference, @JsonBackReference</h3><p>这两个注解配合使用, 可以解决两个不同类的属性的父子关系(parent&#x2F;child relationships)和循环引用(work around loops)<br>使用@JsonBackReference可以在序列化时阻断循环引用, 原理是忽略被注解的属性, 否则会导致异常<br>本例中, 我们用这组注解来序列化ItemWithRef实体类:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ItemWithRef</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> id;</span><br><span class="line">    <span class="keyword">public</span> String itemName;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@JsonManagedReference</span></span><br><span class="line">    <span class="keyword">public</span> UserWithRef owner;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UserWithRef</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> id;</span><br><span class="line">    <span class="keyword">public</span> String name;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@JsonBackReference</span></span><br><span class="line">    <span class="keyword">public</span> List&lt;ItemWithRef&gt; userItems;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>序列化过程:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">whenSerializingUsingJacksonReferenceAnnotation_thenCorrect</span><span class="params">()</span>  <span class="keyword">throws</span> JsonProcessingException &#123;</span><br><span class="line">    <span class="type">UserWithRef</span> <span class="variable">user</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">UserWithRef</span>(<span class="number">1</span>, <span class="string">&quot;John&quot;</span>);</span><br><span class="line">    <span class="type">ItemWithRef</span> <span class="variable">item</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ItemWithRef</span>(<span class="number">2</span>, <span class="string">&quot;book&quot;</span>, user);</span><br><span class="line">    user.addItem(item);</span><br><span class="line">    <span class="type">String</span> <span class="variable">result</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ObjectMapper</span>().writeValueAsString(item);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>序列化结果: {“id”:2,”itemName”:”book”,”owner”:{“id”:1,”name”:”John”}}<br>如果把注解对调并序列化user结果是: {“id”:1,”name”:”John”,”userItems”:[{“id”:2,”itemName”:”book”}]}</p>
<h3 id="JsonIdentityInfo"><a href="#JsonIdentityInfo" class="headerlink" title="@JsonIdentityInfo"></a>@JsonIdentityInfo</h3><p>该注解标明在序列化和反序列化一个值时, 该属性是否作为对象的唯一标识<br>该特性可以有效的解除循环引用, 和@JsonBackReference的区别是循环引用的对象的一个属性, 可以作为该对象的唯一标识被序列化, 而@JsonBackReference的循环引用对象不会二次序列化<br>两个实体类:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="meta">@JsonIdentityInfo(  generator = ObjectIdGenerators.PropertyGenerator.class,  property = &quot;id&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ItemWithIdentity</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> id;</span><br><span class="line">    <span class="keyword">public</span> String itemName;</span><br><span class="line">    <span class="keyword">public</span> UserWithIdentity owner;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UserWithIdentity</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> id;</span><br><span class="line">    <span class="keyword">public</span> String name;</span><br><span class="line">    <span class="keyword">public</span> List&lt;ItemWithIdentity&gt; userItems;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>实例化过程:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">whenSerializingUsingJsonIdentityInfo_thenCorrect</span><span class="params">()</span>  <span class="keyword">throws</span> JsonProcessingException &#123;</span><br><span class="line">    <span class="type">UserWithIdentity</span> <span class="variable">user</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">UserWithIdentity</span>(<span class="number">1</span>, <span class="string">&quot;John&quot;</span>);</span><br><span class="line">    <span class="type">ItemWithIdentity</span> <span class="variable">item</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ItemWithIdentity</span>(<span class="number">2</span>, <span class="string">&quot;book&quot;</span>, user);</span><br><span class="line">    user.addItem(item);</span><br><span class="line">    <span class="type">String</span> <span class="variable">result</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ObjectMapper</span>().writeValueAsString(item);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>序列化结果:{“id”:2,”itemName”:”book”,”owner”:{“id”:1,”name”:”John”,”userItems”:[2]}}<br>这里循环引用对象是ItemWithIdentity, 当它作为UserWithIdentity的属性时, 指定它的id属性为其唯一标识序列化到UserWithIdentity当中</p>
<h3 id="JsonFilter"><a href="#JsonFilter" class="headerlink" title="@JsonFilter"></a>@JsonFilter</h3><p>该注解可以在序列化时指定一个过滤器<br>下面为一个实体类指定一个过滤器:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="meta">@JsonFilter(&quot;myFilter&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">BeanWithFilter</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> id;</span><br><span class="line">    <span class="keyword">public</span> String name;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>定义过滤器并进行序列化</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">whenSerializingUsingJsonFilter_thenCorrect</span><span class="params">()</span>  <span class="keyword">throws</span> JsonProcessingException &#123;</span><br><span class="line">    <span class="type">BeanWithFilter</span> <span class="variable">bean</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">BeanWithFilter</span>(<span class="number">1</span>, <span class="string">&quot;My bean&quot;</span>);</span><br><span class="line">    <span class="type">FilterProvider</span> <span class="variable">filters</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">SimpleFilterProvider</span>().addFilter(<span class="string">&quot;myFilter&quot;</span>,</span><br><span class="line">           SimpleBeanPropertyFilter.filterOutAllExcept(<span class="string">&quot;name&quot;</span>));</span><br><span class="line">    <span class="type">String</span> <span class="variable">result</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ObjectMapper</span>().writer(filters).writeValueAsString(bean);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>序列化结果:{“name”:”My bean”}<br>这里添加了一个SimpleBeanPropertyFilter.filterOutAllExcept过滤器, 该过滤器的含义是除name属性外, 其他属性都被过滤掉(不序列化)</p>
<h2 id="其他注解"><a href="#其他注解" class="headerlink" title="其他注解"></a>其他注解</h2><h3 id="JsonAppend"><a href="#JsonAppend" class="headerlink" title="@JsonAppend"></a>@JsonAppend</h3><p>该注解用来给一个被序列化的对象添加一个虚拟属性. 这个功能非常有用, 尤其是当我们想直接在Json字符串中添加额外的信息时, 不再需要修改类的定义. 举例来说, 它可以很方便的在Json文档中插入bean的版本信息, 而不需要bean提供对应的属性.<br>使用@JsonAppend注解的实体类:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="meta">@JsonAppend(attrs = &#123;@JsonAppend.Attr(value = &quot;version&quot;)&#125;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">BeanWithAppend</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> id;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="comment">// constructor, getters and setters</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>序列化过程:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="type">BeanWithAppend</span> <span class="variable">bean</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">BeanWithAppend</span>(<span class="number">2</span>, <span class="string">&quot;Bean With Append Annotation&quot;</span>);</span><br><span class="line"><span class="type">ObjectWriter</span> <span class="variable">writer</span> <span class="operator">=</span> mapper.writerFor(BeanWithAppend.class).withAttribute(<span class="string">&quot;version&quot;</span>, <span class="string">&quot;1.0&quot;</span>);</span><br><span class="line"><span class="type">String</span> <span class="variable">jsonString</span> <span class="operator">=</span> writer.writeValueAsString(bean);</span><br></pre></td></tr></table></figure>

<p>序列化结果: { “id”: 2, “name”: “Bean With Append Annotation”, “version”: “1.0” }</p>
<h3 id="JsonNaming"><a href="#JsonNaming" class="headerlink" title="@JsonNaming"></a>@JsonNaming</h3><p>该注解用来在序列化时选择一个属性命名习惯来代替默认属性名. 注解参数value用来指定已有命名习惯, 或用户定义的命名习惯<br>除默认值(value&#x3D;LOWER_CAMEL_CASE, 即驼峰命名法)外, Jackson库同时提供了4种内置的属性命名习惯:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">KEBAB_CASE: 属性名单词用短线分隔连接, 比如hello-world</span><br><span class="line">LOWER_CASE: 属性名用小写字母而且没有分隔符, 比如helloworld</span><br><span class="line">SNAKE_CASE: 属性名用小写字母而且用下划线做分隔符, 比如hello_world</span><br><span class="line">UPPER_CAMEL_CASE: 属性名所有单词用大写开头而且没有分隔符, 比如HelloWorld</span><br></pre></td></tr></table></figure>

<p>下例中用SNAKE_CASE命名法, 将属性beanName名序列化为bean_name</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="meta">@JsonNaming(PropertyNamingStrategy.SnakeCaseStrategy.class)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">NamingBean</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> id;</span><br><span class="line">    <span class="keyword">private</span> String beanName;</span><br><span class="line">    <span class="comment">// constructor, getters and setters</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>序列化过程:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="type">NamingBean</span> <span class="variable">bean</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">NamingBean</span>(<span class="number">3</span>, <span class="string">&quot;Naming Bean&quot;</span>);</span><br><span class="line"><span class="type">String</span> <span class="variable">jsonString</span> <span class="operator">=</span> mapper.writeValueAsString(bean);</span><br></pre></td></tr></table></figure>

<p>序列化结果: { “id”: 3, “bean_name”: “Naming Bean” }</p>
<h3 id="JsonPropertyDescription"><a href="#JsonPropertyDescription" class="headerlink" title="@JsonPropertyDescription"></a>@JsonPropertyDescription</h3><p>Jackson的独立模块JSON Schema提供了创建Json信息表(Json schemas)来描述Java的类型信息. 信息表可用于输出我们期望的序列化Java对象, 或者在反序列化前验证Json文档(document)<br>注解@JsonPropertyDescription允许把人类可读的描述信息, 附加在要创建的Json信息表的description属性<br>实体类:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">PropertyDescriptionBean</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> id;</span><br><span class="line">    <span class="meta">@JsonPropertyDescription(&quot;This is a description of the name property&quot;)</span></span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="comment">// getters and setters</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>序列化过程: 这里生成Json信息表的同时为它附加了description属性</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="type">SchemaFactoryWrapper</span> <span class="variable">wrapper</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">SchemaFactoryWrapper</span>();</span><br><span class="line">mapper.acceptJsonFormatVisitor(PropertyDescriptionBean.class, wrapper);</span><br><span class="line"><span class="type">JsonSchema</span> <span class="variable">jsonSchema</span> <span class="operator">=</span> wrapper.finalSchema();</span><br><span class="line"><span class="type">String</span> <span class="variable">jsonString</span> <span class="operator">=</span> mapper.writeValueAsString(jsonSchema);</span><br></pre></td></tr></table></figure>

<p>序列化结果:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">&#123;</span><br><span class="line">    <span class="string">&quot;type&quot;</span>: <span class="string">&quot;object&quot;</span>,</span><br><span class="line">    <span class="string">&quot;id&quot;</span>: <span class="string">&quot;urn:jsonschema:com:baeldung:jackson:annotation:extra:PropertyDescriptionBean&quot;</span>,</span><br><span class="line">    <span class="string">&quot;properties&quot;</span>:</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="string">&quot;name&quot;</span>:</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="string">&quot;type&quot;</span>: <span class="string">&quot;string&quot;</span>,</span><br><span class="line">            <span class="string">&quot;description&quot;</span>: <span class="string">&quot;This is a description of the name property&quot;</span></span><br><span class="line">        &#125;,</span><br><span class="line"></span><br><span class="line">        <span class="string">&quot;id&quot;</span>: </span><br><span class="line">        &#123;</span><br><span class="line">            <span class="string">&quot;type&quot;</span>: <span class="string">&quot;integer&quot;</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="JsonPOJOBuilder"><a href="#JsonPOJOBuilder" class="headerlink" title="@JsonPOJOBuilder"></a>@JsonPOJOBuilder</h3><p>该注解用来配置一个builder类用于定制反序列化过程, 尤其是当Json文档中属性命名习惯和POJO类对象的属性不同<br>准备反序列化的Json字符串: { “id”: 5, “name”: “POJO Builder Bean”}<br>反序列化的目标类:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="meta">@JsonDeserialize(builder = BeanBuilder.class)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">POJOBuilderBean</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> identity;</span><br><span class="line">    <span class="keyword">private</span> String beanName;</span><br><span class="line">    <span class="comment">// constructor, getters and setters</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>注意:BeanBuilder是自定义bulider类, 参见下文.<br>可以看到, bean属性的名称和Json字符串中对应属性的名称不同. 这就是@JsonPOJOBuilder发挥作用的地方.<br>@JsonPOJOBuilder有两个参数:</p>
<p>buildMethodName: 一个无参方法, 用来在绑定Json属性和bean属性后, 创建bean的实例<br>withPrefix: 方法名前缀, 有该前缀的方法是用来匹配Json属性和bean的属性. 默认前缀是with<br>下面是BeanBuilder类定义:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="meta">@JsonPOJOBuilder(buildMethodName = &quot;createBean&quot;, withPrefix = &quot;construct&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">BeanBuilder</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> idValue;</span><br><span class="line">    <span class="keyword">private</span> String nameValue;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> BeanBuilder <span class="title function_">constructId</span><span class="params">(<span class="type">int</span> id)</span> &#123;</span><br><span class="line">        idValue = id;</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">this</span>;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">public</span> BeanBuilder <span class="title function_">constructName</span><span class="params">(String name)</span> &#123;</span><br><span class="line">        nameValue = name;</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">this</span>;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">public</span> POJOBuilderBean <span class="title function_">createBean</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">POJOBuilderBean</span>(idValue, nameValue);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面的代码中, 我们配置了注解@JsonPOJOBuilder的参数, 用createBean方法作为build方法, 用construct前缀来匹配属性名<br>反序列化过程:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="type">String</span> <span class="variable">jsonString</span> <span class="operator">=</span> <span class="string">&quot;&#123;\&quot;id\&quot;:5,\&quot;name\&quot;:\&quot;POJO Builder Bean\&quot;&#125;&quot;</span>;</span><br><span class="line"><span class="type">POJOBuilderBean</span> <span class="variable">bean</span> <span class="operator">=</span> mapper.readValue(jsonString, POJOBuilderBean.class);</span><br></pre></td></tr></table></figure>

<h3 id="JsonTypeId"><a href="#JsonTypeId" class="headerlink" title="@JsonTypeId"></a>@JsonTypeId</h3><p>该注解作用于属性, 使得该属性不再是普通属性, 其值代表bean类的类型ID(&#96;TypeId), 可以用它来描述多态时实体类对象的实际类型<br>实体类:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">TypeIdBean</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> id;</span><br><span class="line">    <span class="meta">@JsonTypeId</span></span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// constructor, getters and setters</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>序列化过程:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">mapper.enableDefaultTyping(DefaultTyping.NON_FINAL);</span><br><span class="line"><span class="type">TypeIdBean</span> <span class="variable">bean</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">TypeIdBean</span>(<span class="number">6</span>, <span class="string">&quot;Type Id Bean&quot;</span>);</span><br><span class="line"><span class="type">String</span> <span class="variable">jsonString</span> <span class="operator">=</span> mapper.writeValueAsString(bean);</span><br></pre></td></tr></table></figure>

<p>序列化结果:[“Type Id Bean”,{“id”:6}]</p>
<p>mapper.enableDefaultTyping(DefaultTyping.NON_FINAL)的作用是在序列化结果中显示实体类类型属性<br>结果是一个Json对象, 其中”Type Id Bean”是实体类ID的描述, {“id”:6}是类的属性值</p>
<h2 id="禁用Jackson注解"><a href="#禁用Jackson注解" class="headerlink" title="禁用Jackson注解"></a>禁用Jackson注解</h2><p>通过设置MapperFeature.USE_ANNOTATIONS可以禁用实体类上的Jackson注解<br>实体类:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="meta">@JsonInclude(Include.NON_NULL)</span></span><br><span class="line"><span class="meta">@JsonPropertyOrder(&#123; &quot;name&quot;, &quot;id&quot; &#125;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MyBean</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> id;</span><br><span class="line">    <span class="keyword">public</span> String name;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>序列化过程:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">whenDisablingAllAnnotations_thenAllDisabled</span><span class="params">()</span>  <span class="keyword">throws</span> IOException &#123;</span><br><span class="line">    <span class="type">MyBean</span> <span class="variable">bean</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">MyBean</span>(<span class="number">1</span>, <span class="literal">null</span>);</span><br><span class="line">    <span class="type">ObjectMapper</span> <span class="variable">mapper</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ObjectMapper</span>();</span><br><span class="line">    mapper.disable(MapperFeature.USE_ANNOTATIONS);</span><br><span class="line">    <span class="type">String</span> <span class="variable">result</span> <span class="operator">=</span> mapper.writeValueAsString(bean);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>序列化结果:{ “id”:1, “name”:null}<br>如果注释掉mapper.disable(MapperFeature.USE_ANNOTATIONS);, 则序列化结果是: {“id”:1}</p>
<h2 id="注解混合"><a href="#注解混合" class="headerlink" title="注解混合"></a>注解混合</h2><p>多个实体类的注解可以混合在一起使用<br>下例中把类MyMixInForIgnoreType的注解@@JsonIgnoreType作用到了类Item的属性User:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Item</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> id;</span><br><span class="line">    <span class="keyword">public</span> String itemName;</span><br><span class="line">    <span class="keyword">public</span> User owner;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@JsonIgnoreType</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MyMixInForIgnoreType</span> &#123;&#125;</span><br></pre></td></tr></table></figure>

<p>序列化过程:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">whenSerializingUsingMixInAnnotation_thenCorrect</span><span class="params">()</span>   <span class="keyword">throws</span> JsonProcessingException &#123;</span><br><span class="line">    <span class="type">Item</span> <span class="variable">item</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Item</span>(<span class="number">1</span>, <span class="string">&quot;book&quot;</span>, <span class="literal">null</span>);</span><br><span class="line"></span><br><span class="line">    <span class="type">String</span> <span class="variable">result</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ObjectMapper</span>().writeValueAsString(item);</span><br><span class="line">    <span class="comment">//结果: &#123;&quot;id&quot;:1,&quot;itemName&quot;:&quot;book&quot;,&quot;owner&quot;:null&#125;</span></span><br><span class="line"> </span><br><span class="line">    <span class="type">ObjectMapper</span> <span class="variable">mapper</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ObjectMapper</span>();</span><br><span class="line">    mapper.addMixIn(User.class, MyMixInForIgnoreType.class);</span><br><span class="line"> </span><br><span class="line">    result = mapper.writeValueAsString(item);</span><br><span class="line">    <span class="comment">//结果: &#123;&quot;id&quot;:1,&quot;itemName&quot;:&quot;book&quot;&#125;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>注解</tag>
      </tags>
  </entry>
</search>
